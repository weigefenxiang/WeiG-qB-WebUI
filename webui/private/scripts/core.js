(function(global){
  'use strict';
  var W=global.WeiG=global.WeiG||{}, U=W.util={};
  U.$=function(id){return document.getElementById(id);};
  U.$$=function(sel,root){return Array.prototype.slice.call((root||document).querySelectorAll(sel));};
  U.clamp=function(v,min,max){return Math.min(max,Math.max(min,v));};
  U.escapeText=function(v){return v==null?'':String(v);};
  U.sleep=function(ms){return new Promise(function(resolve){setTimeout(resolve,ms);});};
  U.formatBytes=function(value){var n=Number(value)||0;if(n<1024)return n+' B';var units=['KiB','MiB','GiB','TiB','PiB'],i=-1;do{n/=1024;i++;}while(n>=1024&&i<units.length-1);return (n>=100?n.toFixed(0):n>=10?n.toFixed(1):n.toFixed(2))+' '+units[i];};
  U.formatSpeed=function(value){return U.formatBytes(value)+'/s';};
  U.formatEta=function(value){var s=Number(value);if(!isFinite(s)||s<0||s>=8640000)return '∞';if(s===0)return '0s';var d=Math.floor(s/86400);s%=86400;var h=Math.floor(s/3600);s%=3600;var m=Math.floor(s/60);if(d)return d+'d '+h+'h';if(h)return h+'h '+m+'m';if(m)return m+'m';return Math.floor(s)+'s';};
  U.formatRatio=function(v){var n=Number(v);return isFinite(n)&&n>=0?n.toFixed(2):'—';};
  U.percent=function(v){return Math.round((Number(v)||0)*1000)/10;};
  U.form=function(obj){var p=new URLSearchParams();Object.keys(obj||{}).forEach(function(k){if(obj[k]!==undefined&&obj[k]!==null)p.append(k,String(obj[k]));});return p.toString();};
  U.debounce=function(fn,wait){var t;return function(){var a=arguments,c=this;clearTimeout(t);t=setTimeout(function(){fn.apply(c,a);},wait);};};
  U.time=function(){return new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',second:'2-digit'});};
  U.hashes=function(set){return Array.from(set).join('|');};
  U.isMobile=function(){return matchMedia('(max-width: 820px)').matches;};
  U.safeJSON=function(value,fallback){try{return JSON.parse(value);}catch(_e){return fallback;}};
  U.parseScalar=function(value){var s=String(value).trim();if(s==='true')return true;if(s==='false')return false;if(s==='null')return null;if(s!==''&&!isNaN(Number(s)))return Number(s);try{return JSON.parse(s);}catch(_e){return value;}};
  U.normalizeTracker=function(raw){var value=String(raw||'').trim();if(!value)return '';try{var url=new URL(value);var port=url.port&&url.port!==((url.protocol==='https:')?'443':(url.protocol==='http:'?'80':''))?':'+url.port:'';return url.protocol+'//'+url.hostname+port+(url.pathname||'/');}catch(_e){return value.split('?')[0].split('#')[0];}};
  U.trackerLabel=function(raw){var value=U.normalizeTracker(raw);if(!value)return '无 Tracker';try{var u=new URL(value),h=u.hostname.replace(/^tracker\./i,'');return h.split('.')[0].replace(/[-_]+/g,' ').replace(/\b\w/g,function(c){return c.toUpperCase();})||u.hostname;}catch(_e){return value;}};
  W.Config={key:'weigg-v020-settings',defaults:{theme:'dark',fontSize:'large',density:'standard',pageSize:50,refresh:2000,starfield:'subtle',motion:'system',mobileFields:['status','progress','dl','up','eta','size'],ptTrackers:[],columns:null},load:function(){var saved=U.safeJSON(localStorage.getItem(this.key),{})||{},out={};Object.keys(this.defaults).forEach(function(k){out[k]=saved[k]===undefined?W.Config.defaults[k]:saved[k];});return out;},save:function(obj){localStorage.setItem(this.key,JSON.stringify(obj));},apply:function(cfg){var h=document.documentElement;h.dataset.theme=cfg.theme==='system'?(matchMedia('(prefers-color-scheme: light)').matches?'light':'dark'):cfg.theme;h.dataset.fontSize=cfg.fontSize;h.dataset.density=cfg.density;h.dataset.starfield=cfg.starfield;h.dataset.motion=cfg.motion;}};
  W.toast=function(message,tone){var region=U.$('toast-region');if(!region)return;var el=document.createElement('div');el.className='toast';if(tone)el.dataset.tone=tone;el.textContent=message;region.appendChild(el);setTimeout(function(){el.remove();},3600);};
  W.fatal=function(error){var box=U.$('fatal'),msg=U.$('fatal-message');if(msg)msg.textContent=(error&&error.message)||String(error||'未知错误');if(box)box.classList.remove('is-hidden');};
  W.Router={route:function(){var raw=(location.hash||'#/').replace(/^#/,'');var parts=raw.split('/').filter(Boolean);return {name:parts[0]||'home',id:parts[1]||'',tab:parts[2]||''};},go:function(path){location.hash='#/'+String(path||'').replace(/^\/+/, '');},home:function(){location.hash='#/';},detail:function(hash,tab){location.hash='#/torrent/'+encodeURIComponent(hash)+(tab?'/'+tab:'');},back:function(){var r=this.route();if(r.name!=='home'&&history.length>1)history.back();else this.home();}};
  W.VirtualList=function(container,options){this.el=container;this.rowHeight=options.rowHeight||52;this.overscan=options.overscan||6;this.renderRow=options.renderRow;this.items=[];this.spacer=document.createElement('div');this.spacer.className='virtual-list__spacer';this.el.classList.add('virtual-list');this.el.textContent='';this.el.appendChild(this.spacer);var self=this;this.el.addEventListener('scroll',function(){self.render();},{passive:true});};
  W.VirtualList.prototype.setRowHeight=function(h){this.rowHeight=h||this.rowHeight;this.spacer.style.height=(this.items.length*this.rowHeight)+'px';this.render();};
  W.VirtualList.prototype.setItems=function(items,preserve){this.items=items||[];this.spacer.style.height=(this.items.length*this.rowHeight)+'px';if(!preserve)this.el.scrollTop=0;this.render();};
  W.VirtualList.prototype.render=function(){var h=this.el.clientHeight||420,top=this.el.scrollTop,start=Math.max(0,Math.floor(top/this.rowHeight)-this.overscan),end=Math.min(this.items.length,Math.ceil((top+h)/this.rowHeight)+this.overscan);this.spacer.textContent='';for(var i=start;i<end;i++){var row=this.renderRow(this.items[i],i);row.classList.add('virtual-row');row.style.height=this.rowHeight+'px';row.style.transform='translateY('+(i*this.rowHeight)+'px)';this.spacer.appendChild(row);}this.el.dataset.rendered=String(Math.max(0,end-start));};
  W.DataGrid={defaults:[{key:'name',label:'名称',width:420,min:220,sort:'name'},{key:'size',label:'大小',width:105,min:80,sort:'size'},{key:'progress',label:'进度',width:220,min:145,sort:'progress'},{key:'dlspeed',label:'下载',width:110,min:86,sort:'dlspeed'},{key:'upspeed',label:'上传',width:110,min:86,sort:'upspeed'},{key:'eta',label:'ETA',width:90,min:70,sort:'eta'},{key:'state',label:'状态',width:125,min:96,sort:'state'},{key:'ratio',label:'Ratio',width:90,min:70,sort:'ratio'},{key:'tracker',label:'Tracker',width:165,min:110,sort:'tracker'},{key:'category',label:'分类',width:130,min:90,sort:'category'}],load:function(cfg){var saved=cfg.columns;if(!Array.isArray(saved)||!saved.length)return this.defaults.slice(0,7).map(function(c){return Object.assign({},c);});var all={};this.defaults.forEach(function(c){all[c.key]=c;});return saved.filter(function(c){return all[c.key];}).map(function(c){return Object.assign({},all[c.key],c);});},save:function(cfg,cols){cfg.columns=cols.map(function(c){return {key:c.key,width:c.width};});W.Config.save(cfg);},template:function(cols){return cols.map(function(c){return Math.max(c.min||60,c.width||100)+'px';}).join(' ');},addResizeHandles:function(head,cols,onChange){U.$$('.grid-head-cell',head).forEach(function(cell,index){var handle=document.createElement('span');handle.className='col-resize';handle.setAttribute('aria-hidden','true');cell.appendChild(handle);handle.addEventListener('pointerdown',function(e){e.preventDefault();var startX=e.clientX,startW=cols[index].width||100;handle.setPointerCapture(e.pointerId);function move(ev){cols[index].width=Math.max(cols[index].min||60,startW+(ev.clientX-startX));onChange(cols,false);}function up(ev){handle.releasePointerCapture(ev.pointerId);handle.removeEventListener('pointermove',move);handle.removeEventListener('pointerup',up);onChange(cols,true);}handle.addEventListener('pointermove',move);handle.addEventListener('pointerup',up);});});}};
  document.addEventListener('mouseover',function(e){var target=e.target.closest('[data-tooltip]');if(!target||target.__tip||U.isMobile())return;var tip=document.createElement('div');tip.className='tooltip';tip.textContent=target.dataset.tooltip;document.body.appendChild(tip);target.__tip=tip;setTimeout(function(){if(!target.__tip)return;var r=target.getBoundingClientRect(),tr=tip.getBoundingClientRect();tip.style.left=Math.max(8,Math.min(innerWidth-tr.width-8,r.left+r.width/2-tr.width/2))+'px';tip.style.top=Math.max(8,r.bottom+7)+'px';tip.classList.add('is-visible');},320);});
  document.addEventListener('mouseout',function(e){var target=e.target.closest('[data-tooltip]');if(target&&target.__tip){target.__tip.remove();target.__tip=null;}});
})(window);
