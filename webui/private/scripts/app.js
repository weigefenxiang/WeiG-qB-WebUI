(function(global){
  'use strict';
  var W=global.WeiG,U=W.util,C=W.Components;
  var cfg=W.Config.load();W.Config.apply(cfg);
  var PRIVATE_TRACKER='__weig_private__',TRACKER_HOST_PREFIX='host:',TRACKER_TRACKERLESS='special:trackerless',TRACKER_ERROR='special:tracker-error',TRACKER_OTHER_ERROR='special:other-error',TRACKER_WARNING='special:warning';
  var app={client:new W.QBClient(),preferences:null,filter:'all',tracker:'',category:'',tag:'',savePath:'',page:0,pageSize:Number(cfg.pageSize)||50,pagerControl:null,torrents:[],catalog:[],catalogByHash:new Map(),catalogReady:false,catalogBusy:false,catalogError:false,catalogTask:null,hasNext:false,selection:W.Selection,search:'',sort:'added_on',reverse:true,pollTimer:null,detailHash:'',detailTab:'',busy:false,pendingLoad:null,viewport:null,detailViewport:null,detailProperties:null,detailPropertiesHash:'',columns:W.DataGrid.load(cfg),searchJob:null,searchPoll:null,pageSizeControl:null,listUiSignature:'',trackerMembership:{},trackerFacetSource:false,facets:{tracker:[],savePath:[],category:[],tag:[]},nativeCategories:{},nativeTags:[]};
  W.AppState=app;
  var statusMessages={torrent:'',transfer:''};
  function event(name,detail){try{global.dispatchEvent(new CustomEvent(name,{detail:detail||{}}));}catch(_e){}}
  function tr(key,vars){return W.I18n&&W.I18n.t?W.I18n.t(key,vars):String(key||'');}
  function emitLibraryState(reason){event('weig:library-state',{reason:reason||'render',filter:app.filter,tracker:app.tracker,savePath:app.savePath,category:app.category,tag:app.tag,search:app.search,page:app.page,pageSize:app.pageSize,sort:app.sort,reverse:app.reverse,total:totalMatching(),indexing:!app.catalogReady&&!app.catalogError,indexError:app.catalogError});}
  function emitStatusState(status){event('weig:status-state',{connection:status||'—'});}
  function renderStatus(){var node=U.$('status-message');if(node)node.textContent=statusMessages.transfer||statusMessages.torrent||'';}
  function setStatus(source,msg){if(!(source in statusMessages))return;statusMessages[source]=msg||'';renderStatus();}
  function paintText(id,next){var node=U.$(id);if(node&&node.textContent!==next)node.textContent=next;}
  function toggleLoading(on){U.$('list-loading').classList.toggle('is-hidden',!on);if(on)U.$('list-empty').classList.add('is-hidden');}
  function rowHeight(){if(U.isMobile()&&W.MobileAdaptive&&W.MobileAdaptive.mobileRowHeight)return W.MobileAdaptive.mobileRowHeight();if(U.isMobile())return cfg.density==='compact'?76:cfg.density==='comfortable'?92:84;return cfg.density==='compact'?48:cfg.density==='comfortable'?64:56;}
  function torrentScrollInteracting(){var route=W.Router&&W.Router.route?W.Router.route():{name:''},viewport=app.viewport;return !!(route.name==='home'&&viewport&&viewport.isInteracting&&viewport.isInteracting());}
  function resetTorrentScroll(){var list=U.$('torrent-list');if(!list)return;list.__weigDataViewportScrollTop=0;list.scrollTop=0;if(list.__weigTorrentDataViewport&&list.__weigTorrentDataViewport.resetScroll)list.__weigTorrentDataViewport.resetScroll();}
  function clearSelection(){if(app.selection)app.selection.clear(true);}
  function privateTrackerSupported(){return !!(W.CapabilityRegistry&&W.CapabilityRegistry.supports&&W.CapabilityRegistry.supports('privateFilter'));}
  function filterMatch(t,state){state=state||app;if(W.TorrentSemantics&&W.TorrentSemantics.matchesStatus&&!W.TorrentSemantics.matchesStatus(t,state.filter||'all',cfg.ptTrackers))return false;if(state.category&&String(t.category||'')!==state.category)return false;if(state.tag){var tags=String(t.tags||'').split(',').map(function(x){return x.trim();});if(tags.indexOf(state.tag)<0)return false;}if(state.tracker){if(state.tracker===PRIVATE_TRACKER){if(!privateTrackerSupported()||!W.TorrentSemantics||!W.TorrentSemantics.isPrivateOrPt||!W.TorrentSemantics.isPrivateOrPt(t,cfg.ptTrackers))return false;}else if(state.tracker===TRACKER_TRACKERLESS){if(Number(t.trackers_count)!==0)return false;}else if(state.tracker===TRACKER_ERROR){if(!t.has_tracker_error)return false;}else if(state.tracker===TRACKER_OTHER_ERROR){if(!t.has_other_announce_error)return false;}else if(state.tracker===TRACKER_WARNING){if(!t.has_tracker_warning)return false;}else if(app.trackerMembership&&app.trackerMembership[state.tracker]){if(!app.trackerMembership[state.tracker].has(String(t.hash||'')))return false;}else if(state.tracker.indexOf(TRACKER_HOST_PREFIX)===0){var expectedHost=state.tracker.slice(TRACKER_HOST_PREFIX.length),actualHost=W.TorrentSemantics&&W.TorrentSemantics.trackerHost?W.TorrentSemantics.trackerHost(t.tracker):'';if(actualHost!==expectedHost)return false;}else if(U.normalizeTracker(t.tracker)!==state.tracker)return false;}if(state.savePath&&String(t.save_path||'')!==state.savePath)return false;if(state.search&&String(t.name||'').toLocaleLowerCase().indexOf(String(state.search).toLocaleLowerCase())<0)return false;return true;}
  function sortCatalog(items){var key=app.sort,dir=app.reverse?-1:1;return items.slice().sort(function(a,b){var av=a[key],bv=b[key];if(key==='tracker'){av=U.normalizeTracker(a.tracker);bv=U.normalizeTracker(b.tracker);}if(typeof av==='string'||typeof bv==='string')return String(av||'').localeCompare(String(bv||''))*dir;return ((Number(av)||0)-(Number(bv)||0))*dir;});}
  function matchingCatalog(){return sortCatalog(app.catalog.filter(function(t){return filterMatch(t,app);}));}
  function totalMatching(){return app.catalogReady?app.catalog.filter(function(t){return filterMatch(t,app);}).length:null;}
  function trackerFilterFacts(){var R=W.CapabilityRegistry;return R&&R.trackerFilters?R.trackerFilters():[];}
  function trackerFilterFact(id){return trackerFilterFacts().find(function(item){return String(item.id||'')===String(id||'');})||null;}
  function trackerFactCount(id){if(id==='all')return app.catalog.length;if(id==='trackerless')return app.catalog.filter(function(t){return Number(t.trackers_count)===0;}).length;if(id==='error')return app.catalog.filter(function(t){return !!t.has_tracker_error;}).length;if(id==='otherError')return app.catalog.filter(function(t){return !!t.has_other_announce_error;}).length;if(id==='warning')return app.catalog.filter(function(t){return !!t.has_tracker_warning;}).length;return 0;}
  function trackerFactCopy(fact,count){var ref=fact&&fact.copy||{},source=String(ref.source||''),copy=W.I18n&&W.I18n.qbSourceText?W.I18n.qbSourceText(ref,source):source;if(copy.indexOf('%1')>=0)return copy.replace(/%1/g,String(count));return copy?(copy+' · '+count):String(count);}
  function defaultFacetLabel(kind){if(kind==='tracker'){var fact=trackerFilterFact('all');if(fact)return trackerFactCopy(fact,trackerFactCount('all'));}var key=kind==='tracker'?'tracker.all':kind==='savePath'?'path.all':kind==='category'?'category.all':'tag.all',fallback=kind==='tracker'?tr('tracker.all'):kind==='savePath'?tr('path.all'):kind==='category'?tr('category.all'):tr('tag.all'),value=W.t?W.t(key):'';return value&&value!==key?value:fallback;}
  function trackerSpecialOptions(){var ids={trackerless:TRACKER_TRACKERLESS,error:TRACKER_ERROR,otherError:TRACKER_OTHER_ERROR,warning:TRACKER_WARNING},out=[];trackerFilterFacts().forEach(function(fact){var id=String(fact&&fact.id||'');if(id==='all'||!ids[id])return;var count=trackerFactCount(id);out.push({value:ids[id],label:trackerFactCopy(fact,count),count:count,sourceId:id});});return out;}
  function facetOptions(kind){var items=app.facets[kind]||[],out=[{value:'',label:defaultFacetLabel(kind)}];if(kind==='tracker'){trackerSpecialOptions().forEach(function(item){out.push({value:item.value,label:item.label});});if(privateTrackerSupported())out.push({value:PRIVATE_TRACKER,label:'Private / PT'});}items.forEach(function(item){out.push({value:String(item.value),label:item.count==null?String(item.label):String(item.label)+' · '+item.count});});return out;}
  function strictTracker(raw){var value=String(raw||'').trim();if(!value||value.length>4096||/[<>\r\n\t]/.test(value)||!/^(?:https?|udp|wss?):\/\//i.test(value))return null;try{var url=new URL(value),protocol=String(url.protocol||'').toLowerCase();if(['http:','https:','udp:','ws:','wss:'].indexOf(protocol)<0||!url.hostname)return null;return{host:url.hostname.toLowerCase().replace(/^\.+|\.+$/g,''),normalized:U.normalizeTracker(value)};}catch(_e){return null;}}function trackerHost(raw){var parsed=strictTracker(raw);return parsed?parsed.host:'';}
  function trackerFacetMode(){var R=W.CapabilityRegistry;return R&&R.trackerFacetMode?R.trackerFacetMode():null;}
  function clearTrackerFacet(sourceReady){app.trackerMembership={};app.facets.tracker=[];app.trackerFacetSource=sourceReady===true;emitLibraryState('tracker-facet');}
  async function refreshTrackerFacet(){var mode=trackerFacetMode();if(mode==='none'){clearTrackerFacet(true);return true;}if(mode!=='url'&&mode!=='hostname')return false;var data=null;try{data=await app.client.getMainData(0);}catch(_e){return false;}var trackers=data&&data.trackers;if(!trackers||typeof trackers!=='object'||Array.isArray(trackers))return false;var grouped={};Object.keys(trackers).forEach(function(raw){var parsed=strictTracker(raw);if(!parsed)return;var hashes=Array.isArray(trackers[raw])?trackers[raw]:[],host=parsed.host,normalized=parsed.normalized,value=mode==='hostname'&&host?TRACKER_HOST_PREFIX+host:normalized,label=mode==='hostname'&&host?host:normalized;if(!value)return;if(!grouped[value])grouped[value]={label:label,hashes:new Set()};hashes.forEach(function(hash){grouped[value].hashes.add(String(hash));});});var membership={},facets=Object.keys(grouped).map(function(value){var item=grouped[value];membership[value]=item.hashes;return{value:value,label:item.label,count:item.hashes.size};}).sort(function(a,b){return a.label.localeCompare(b.label,undefined,{numeric:true,sensitivity:'base'});});app.trackerMembership=membership;app.facets.tracker=facets;app.trackerFacetSource=true;var syncTorrents=data.torrents&&typeof data.torrents==='object'&&!Array.isArray(data.torrents)?data.torrents:{};app.catalog.forEach(function(t){var extra=syncTorrents[t.hash];if(extra&&typeof extra==='object')Object.assign(t,extra);});emitLibraryState('tracker-facet');return true;}
  function rebuildCatalogFacets(){var paths={},categories={},tags={};app.catalog.forEach(function(t){var path=String(t.save_path||'').trim(),category=String(t.category||'').trim();if(path)paths[path]=(paths[path]||0)+1;if(category)categories[category]=(categories[category]||0)+1;String(t.tags||'').split(',').map(function(x){return x.trim();}).filter(Boolean).forEach(function(tag){tags[tag]=(tags[tag]||0)+1;});});app.facets.savePath=Object.keys(paths).sort().map(function(value){return{value:value,label:value,count:paths[value]};});app.facets.category=Object.keys(categories).sort().map(function(value){return{value:value,label:value,count:categories[value]};});app.facets.tag=Object.keys(tags).sort().map(function(value){return{value:value,label:value,count:tags[value]};});}
  function capabilitySupported(id,fallback){return W.CapabilityRegistry&&W.CapabilityRegistry.supports?W.CapabilityRegistry.supports(id):!!fallback;}
  function showCapability(kind){var id=kind==='private'?'privateFilter':kind;if(W.CapabilityRegistry&&W.CapabilityRegistry.open)W.CapabilityRegistry.open(id);}
  function hasInfoParameter(name){var R=W.CapabilityRegistry;return !!(R&&R.hasTorrentInfoParameter&&R.hasTorrentInfoParameter(name));}
  function localStatusFilter(){return !!(W.TorrentSemantics&&W.TorrentSemantics.localFilterRequired&&W.TorrentSemantics.localFilterRequired(app.filter));}
  function localTaxonomyFilter(){return !!((app.category&&!hasInfoParameter('category'))||(app.tag&&!hasInfoParameter('tag')));}
  function needsCatalogFiltering(){return !!(app.tracker||app.search||app.savePath||localStatusFilter()||localTaxonomyFilter()||localColumnSort());}
  function upstreamTorrentQuery(){return{filter:localStatusFilter()?'all':app.filter,category:app.category&&hasInfoParameter('category')?app.category:'',tag:app.tag&&hasInfoParameter('tag')?app.tag:'',sort:localColumnSort()?'added_on':app.sort,reverse:String(app.reverse),limit:app.pageSize+1,offset:app.page*app.pageSize};}
  function mergeNativeFacet(kind,values){var current={},out=[];(app.facets[kind]||[]).forEach(function(item){current[String(item.value)]=item;});(values||[]).forEach(function(value){value=String(value||'').trim();if(value&&!current[value])current[value]={value:value,label:value,count:null};});Object.keys(current).sort().forEach(function(value){out.push(current[value]);});app.facets[kind]=out;}
  function fieldRegistry(){return W.TorrentFieldRegistry||null;}
  function effectiveColumnDefinitions(){var R=fieldRegistry();return R&&R.availableColumnDefinitions?R.availableColumnDefinitions():W.DataGrid.defaults;}
  function applyEffectiveColumns(){var R=fieldRegistry();if(!R||!R.effectiveDesktopColumns)return app.columns;app.columns=R.effectiveDesktopColumns(cfg);renderHead();return app.columns;}
  function saveEffectiveColumns(cols){var R=fieldRegistry();if(R&&R.saveEffectiveDesktopColumns)app.columns=R.saveEffectiveDesktopColumns(cfg,cols);else{app.columns=cols;W.DataGrid.save(cfg,cols);}return app.columns;}
  function refreshColumnCopy(){var defs=effectiveColumnDefinitions(),byKey={};defs.forEach(function(def){byKey[def.key]=def;});app.columns=(app.columns||[]).map(function(column){var def=byKey[column.key]||column;return Object.assign({},def,{width:column.width||def.width,visible:column.visible!==false});});if(!app.columns.length&&defs.length)app.columns=defs.slice(0,7).map(function(def){return Object.assign({},def);});renderHead();renderList();if(W.ColumnConfigurator&&W.ColumnConfigurator.refreshActive)W.ColumnConfigurator.refreshActive();}
  function columnSortToken(column){return String(column&&column.sort||column&&column.localSort||'');}
  function localColumnSort(){var defs=effectiveColumnDefinitions(),column=defs.find(function(item){return columnSortToken(item)===app.sort;});return !!(column&&column.localSort&&!column.sort);}
  async function setSort(sort,dir){var key=String(sort||'').trim();if(!key)return false;app.sort=key;app.reverse=String(dir||'asc').toLowerCase()==='desc';app.page=0;clearSelection();resetTorrentScroll();renderHead();emitLibraryState('sort');if(await ensureCatalogForLocalView())return true;loadPage(false);return true;}
  function renderHead(){var head=U.$('torrent-table-head'),template=W.DataGrid.template(app.columns);head.style.gridTemplateColumns=template;W.DataGridHeader.render(head,app.columns,{surfaceId:'torrent-main',label:function(c){var token=columnSortToken(c);return c.label+(token&&app.sort===token?(app.reverse?' ↓':' ↑'):'');},onClick:function(c){var token=columnSortToken(c);if(!token)return;if(app.sort===token)setSort(token,app.reverse?'asc':'desc');else setSort(token,'asc');}});W.DataGrid.addResizeHandles(head,app.columns,function(cols,save){app.columns=cols;var t=W.DataGrid.template(cols);head.style.gridTemplateColumns=t;if(app.viewport)app.viewport.render();if(save)saveEffectiveColumns(cols);});W.DataGridHeader.refresh(head,app.columns);}
  function renderPageLabel(host,total){host=host||U.$('page-label');if(!host)return;var state=document.createElement('span');host.textContent='';state.className='pager-index-state';if(total==null){var mark=document.createElement('span');if(app.catalogError){mark.className='pager-index-error';mark.textContent='!';mark.setAttribute('aria-label',tr('library.totalUnavailable'));}else{mark.className='pager-index-spinner';mark.setAttribute('aria-hidden','true');}state.appendChild(mark);}var copy=document.createElement('span');copy.className='pager-index-copy';var page=app.page+1,pages=total==null?null:Math.max(1,Math.ceil(total/app.pageSize));copy.textContent=U.isMobile()?(pages==null?String(page):page+' / '+pages):(total==null?tr('library.pageCurrent',{page:page,size:app.pageSize}):tr('library.page',{page:page,pages:pages,size:app.pageSize}));state.appendChild(copy);host.appendChild(state);}
  function installPagerControl(){if(app.pagerControl)return app.pagerControl;var prev=U.$('prev-btn'),next=U.$('next-btn'),label=U.$('page-label'),host=prev&&prev.closest?prev.closest('.pager'):null,nav=prev&&prev.closest?prev.closest('.pager__nav'):null;if(!prev||!next||!label||!host)return null;app.pagerControl=C.pagerControl({host:host,nav:nav,prev:prev,next:next,label:label,onPrev:function(){if(app.page>0){app.page--;resetTorrentScroll();loadPage(false);}},onNext:function(){app.page++;resetTorrentScroll();loadPage(false);}});return app.pagerControl;}
  function torrentRowKey(t,index){return t&&t.hash?String(t.hash):index;}
  function torrentVisualKey(key){return key==='status'?'state':key==='dl'?'dlspeed':key==='up'?'upspeed':String(key||'');}function torrentVisualValues(t,mobile){var columns=mobile?(Array.isArray(cfg.mobileFields)?cfg.mobileFields:[]).map(function(key){return{key:torrentVisualKey(key),dataProperties:[torrentVisualKey(key)]};}):(app.columns||[]);return columns.map(function(column){var key=String(column&&column.key||''),props=Array.isArray(column&&column.dataProperties)&&column.dataProperties.length?column.dataProperties.map(String):[torrentVisualKey(key)],values=props.map(function(prop){return t&&t[prop];});if(key==='progress')values=values.concat([t&&t.state,t&&t.dlspeed,t&&t.upspeed]);return[key,values];});}function torrentRowSignature(t){var locale=W.I18n&&W.I18n.getLocale?W.I18n.getLocale():'en',isMobile=U.isMobile(),mobile=isMobile?'mobile':'desktop',ptTrackers=Array.isArray(cfg.ptTrackers)?cfg.ptTrackers:[],privacy=[t&&t.private,t&&t.is_private,t&&t.isPrivate,t&&t.has_metadata,t&&t.tracker];try{return JSON.stringify([mobile,locale,cfg.timezone||'',ptTrackers,t&&t.name,t&&t.state,t&&t.progress,privacy,torrentVisualValues(t,isMobile)]);}catch(_e){return[mobile,locale,String(t&&t.hash||''),String(t&&t.name||''),String(t&&t.state||''),String(t&&t.progress||'')].join('|');}}
  function finalizeListUi(){var list=U.$('torrent-list'),items=app.torrents,empty=items.length===0&&!app.busy;U.$('list-empty').classList.toggle('is-hidden',!empty);var panel=list.closest('.torrent-panel');if(panel)panel.classList.toggle('is-empty',empty);var total=totalMatching(),locale=W.I18n&&W.I18n.getLocale?W.I18n.getLocale():'en',uiSignature=JSON.stringify([locale,empty,total,app.page,app.pageSize,app.hasNext,app.catalogReady,app.catalogError]);if(app.listUiSignature!==uiSignature){app.listUiSignature=uiSignature;var pager=app.pagerControl||installPagerControl();if(pager)pager.update({prevDisabled:app.page===0||app.busy,nextDisabled:app.busy||(total!=null?(app.page+1)*app.pageSize>=total:!app.hasNext),renderLabel:function(host){renderPageLabel(host,total);}});paintText('status-torrents','Torrent '+(total==null?'…':total));}app.selection.apply();app.selection.sync();}
  function renderList(){var list=U.$('torrent-list'),items=app.torrents,handlers={open:openDetail,menu:function(hash){app.selection.selectOnly(hash);app.selection.openActions();}},mobile=U.isMobile(),schemaKey=(mobile?'mobile:':'desktop:')+(mobile?(Array.isArray(cfg.mobileFields)?cfg.mobileFields.join(','):''):(app.columns||[]).map(function(column){return column.key;}).join(',')),createRow=mobile?function(t){return C.mobileTorrentCard(t,app.selection.has(t.hash),handlers,cfg.mobileFields);}:function(t){return C.torrentRow(t,app.selection.has(t.hash),handlers,app.columns,W.DataGrid.template(app.columns));},updateRow=mobile?function(row,t){return C.updateMobileTorrentCard(row,t,app.selection.has(t.hash),handlers,cfg.mobileFields);}:function(row,t){return C.updateTorrentRow(row,t,app.selection.has(t.hash),handlers,app.columns,'var(--weig-grid-template)');},nextHeight=rowHeight(),nextOverscan=mobile?2:3;if(!app.viewport||app.viewport.el!==list||app.viewport.__weigRowSchemaKey!==schemaKey){if(app.viewport&&app.viewport.destroy)app.viewport.destroy();app.viewport=new W.DataViewport(list,{rowHeight:nextHeight,overscan:nextOverscan,renderRow:createRow,createRow:createRow,updateRow:updateRow,releaseRow:function(row){delete row.dataset.hash;row.classList.remove('is-selected','selection-range-anchor');},itemKey:torrentRowKey,itemSignature:torrentRowSignature,staticHead:U.$('torrent-table-head')});app.viewport.__weigRowSchemaKey=schemaKey;list.__weigTorrentDataViewport=app.viewport;}else{app.viewport.renderRow=createRow;app.viewport.createRow=createRow;app.viewport.updateRow=updateRow;app.viewport.recycler=true;app.viewport.itemKey=torrentRowKey;app.viewport.itemSignature=torrentRowSignature;app.viewport.overscan=nextOverscan;if(app.viewport.rowHeight!==nextHeight)app.viewport.setRowHeight(nextHeight);}app.viewport.setItems(items);app.viewport.commitWhenIdle(finalizeListUi);}
  async function pageFromCatalog(){var matches=matchingCatalog(),slice=matches.slice(app.page*app.pageSize,(app.page+1)*app.pageSize);app.hasNext=(app.page+1)*app.pageSize<matches.length;if(!slice.length)return [];var hashes=slice.map(function(t){return t.hash;}).join('|');var live=await app.client.getTorrents({hashes:hashes});var map={};(live||[]).forEach(function(t){map[t.hash]=t;});return slice.map(function(t){return Object.assign({},t,map[t.hash]||{});});}
  async function loadPage(silent){if(silent&&torrentScrollInteracting())return false;if(app.busy){if(!silent)app.pendingLoad=false;return;}var deferredByScroll=false;app.busy=true;if(!silent)toggleLoading(true);try{var data;if(app.catalogReady&&needsCatalogFiltering())data=await pageFromCatalog();else{var raw=await app.client.getTorrents(upstreamTorrentQuery());raw=Array.isArray(raw)?raw:[];app.hasNext=raw.length>app.pageSize;data=raw.slice(0,app.pageSize);}if(silent&&torrentScrollInteracting()){deferredByScroll=true;return false;}if(app.pendingLoad!==null)return;app.torrents=Array.isArray(data)?data:[];app.torrents.forEach(function(t){var existing=app.catalogByHash.get(t.hash);if(existing)Object.assign(existing,t);});setStatus('torrent','');return true;}catch(e){setStatus('torrent',tr('library.refreshFailedStatus'));if(!silent)W.toast(tr('library.refreshFailedPrefix')+e.message,'error',{title:tr('library.refreshFailedTitle')});if(e.status===403)location.reload();return false;}finally{var pending=app.pendingLoad;app.pendingLoad=null;app.busy=false;if(!silent)toggleLoading(false);if(!deferredByScroll)renderList();if(pending!==null)loadPage(pending);}}
  async function loadTransfer(){if(torrentScrollInteracting())return false;try{var info=await app.client.getTransferInfo();if(torrentScrollInteracting())return false;var downloadSpeed=info&&info.dl_info_speed||0,uploadSpeed=info&&info.up_info_speed||0,status=info&&info.connection_status||'connected';if(W.TransferRuntime&&W.TransferRuntime.ingest)W.TransferRuntime.ingest(info);paintText('status-dl',U.formatSpeed(downloadSpeed));paintText('status-up',U.formatSpeed(uploadSpeed));setStatus('transfer','');emitStatusState(status);return true;}catch(_e){if(torrentScrollInteracting())return false;setStatus('transfer',tr('transfer.statusUnavailable'));emitStatusState('error');return false;}}
  async function buildCatalog(){if(app.catalogTask)return app.catalogTask;app.catalogTask=(async function(){app.catalogBusy=true;app.catalogError=false;app.catalogReady=false;U.$('catalog-state').textContent='';renderList();try{var all=[],offset=0,batch=200;while(true){var part=await app.client.getTorrents({sort:'added_on',reverse:'true',limit:batch,offset:offset});part=Array.isArray(part)?part:[];all=all.concat(part);if(part.length<batch)break;offset+=batch;await U.sleep(0);}app.catalog=all;app.catalogByHash=new Map(all.filter(function(t){return t&&t.hash;}).map(function(t){return[String(t.hash),t];}));app.catalogReady=true;rebuildCatalogFacets();await refreshTrackerFacet();emitLibraryState('catalog-ready');if(needsCatalogFiltering())await loadPage(false);else renderList();return all;}catch(e){app.catalogError=true;app.catalogReady=false;U.$('catalog-state').textContent='';renderList();emitLibraryState('catalog-error');return null;}finally{app.catalogBusy=false;app.catalogTask=null;}})();return app.catalogTask;}
  async function ensureCatalogForLocalView(){if(needsCatalogFiltering()&&!app.catalogReady){await buildCatalog();return true;}return false;}
  async function setSavePath(value){app.savePath=String(value||'');app.page=0;clearSelection();resetTorrentScroll();emitLibraryState('savepath-filter');if(await ensureCatalogForLocalView())return;loadPage(false);}
  async function loadTaxonomy(){if(app.catalogReady){rebuildCatalogFacets();await refreshTrackerFacet();}app.nativeCategories={};app.nativeTags=[];if(capabilitySupported('categories',app.client.capabilities.categories)){try{var cats=await app.client.categories();app.nativeCategories=cats&&typeof cats==='object'&&!Array.isArray(cats)?cats:{};mergeNativeFacet('category',Object.keys(app.nativeCategories));}catch(_e){}}if(capabilitySupported('tags',app.client.capabilities.tags)){try{var tags=await app.client.tags();app.nativeTags=Array.isArray(tags)?tags.slice():[];mergeNativeFacet('tag',app.nativeTags);}catch(_e){}}emitLibraryState('taxonomy');}
  function schedulePoll(){clearTimeout(app.pollTimer);var delay=document.hidden?10000:(Number(cfg.refresh)||2000);app.pollTimer=setTimeout(async function(){var r=W.Router.route();if(r.name==='home'&&torrentScrollInteracting()){schedulePoll();return;}if(r.name==='home')await loadPage(true);if(r.name==='home'&&torrentScrollInteracting()){schedulePoll();return;}await loadTransfer();schedulePoll();},delay);}
  function applyRuntimeConfig(key,value){if(!(key in cfg))return false;cfg[key]=value;if(key==='pageSize'){cfg.pageSize=Number(value)||50;app.pageSize=cfg.pageSize;app.page=0;resetTorrentScroll();if(app.pageSizeControl&&app.pageSizeControl.setValue)app.pageSizeControl.setValue(String(app.pageSize));emitLibraryState('page-size');loadPage(false);}else if(key==='refresh'){cfg.refresh=Number(value)||2000;schedulePoll();}else if(key==='density'){renderList();}else if(key==='ptTrackers'){app.page=0;if(app.catalogReady&&needsCatalogFiltering())loadPage(false);else renderList();}return true;}
  async function setFilter(filter){filter=W.TorrentSemantics&&W.TorrentSemantics.canonicalFilter?W.TorrentSemantics.canonicalFilter(filter):String(filter||'all');if(filter==='private')return false;if(W.TorrentSemantics&&W.TorrentSemantics.isSupportedFilter&&!W.TorrentSemantics.isSupportedFilter(filter))return false;app.filter=filter;app.page=0;clearSelection();resetTorrentScroll();U.$$('#filter-nav .nav-item').forEach(function(el){el.classList.toggle('is-active',el.dataset.filter===filter);});closeDrawer();emitLibraryState('filter');if(await ensureCatalogForLocalView())return true;loadPage(false);return true;}
  async function setTax(type,value){var capability=type==='tag'?'tagFacet':'categoryFacet';if(!capabilitySupported(capability,true)){showCapability(capability);return false;}app[type]=String(value||'');app.page=0;clearSelection();resetTorrentScroll();emitLibraryState('taxonomy-filter');if(await ensureCatalogForLocalView())return true;loadPage(false);return true;}
  async function setTracker(value){value=String(value||'');if(value===PRIVATE_TRACKER&&!privateTrackerSupported()){showCapability('private');return false;}app.tracker=value;app.page=0;clearSelection();resetTorrentScroll();emitLibraryState('tracker-filter');if(await ensureCatalogForLocalView())return true;loadPage(false);return true;}
  var LibraryController={state:function(){return{filter:app.filter,tracker:app.tracker,savePath:app.savePath,category:app.category,tag:app.tag,search:app.search,page:app.page,pageSize:app.pageSize,sort:app.sort,reverse:app.reverse};},total:totalMatching,facetOptions:facetOptions,matchesTorrent:function(t,state){return filterMatch(t,state||app);},setFilter:setFilter,setTracker:setTracker,setSavePath:setSavePath,setCategory:function(value){return setTax('category',value);},setTag:function(value){return setTax('tag',value);},setSort:setSort,applyRuntimeConfig:applyRuntimeConfig,showCapability:showCapability};
  W.LibraryController=LibraryController;
  function setView(name){if(name!=='torrent'&&app.detailViewport){if(app.detailViewport.destroy)app.detailViewport.destroy();app.detailViewport=null;}['list','detail','settings','search','rss','logs'].forEach(function(v){U.$(v+'-view').classList.toggle('is-active',(name==='home'&&v==='list')||(name==='torrent'&&v==='detail')||(name===v));});var active=name==='home'||name==='torrent'?'':name;U.$$('#app-nav [data-route],#mobile-bottom-nav [data-route]').forEach(function(btn){btn.classList.toggle('is-active',(btn.dataset.route||'')===active);});var shell=U.$('app');if(shell)shell.classList.toggle('is-tool-route',name!=='home');U.$('back-btn').classList.toggle('is-hidden',name==='home');closeDrawer();event('weig:route-state',{name:name,scope:name==='home'?'library':'tool'});}
  function detailUi(){var R=W.CapabilityRegistry;return R&&typeof R.torrentDetailUi==='function'?R.torrentDetailUi():null;}
  function detailTabKeys(){var ui=detailUi(),tabs=ui&&ui.tabs||{},raw=Array.isArray(ui&&ui.tabOrder)?ui.tabOrder:Object.keys(tabs);return raw.map(String).filter(function(key,index,list){return tabs[key]&&list.indexOf(key)===index;});}
  function defaultDetailTab(){var keys=detailTabKeys();return keys.length?keys[0]:'';}
  function detailTableSurface(tab){var ui=detailUi(),tables=ui&&ui.tables,key=String(tab||'');return tables&&Array.isArray(tables[key])&&tables[key].length?key:'';}
  async function route(){var r=W.Router.route();if(r.name==='torrent'&&r.id){if(W.CapabilityRegistry&&W.CapabilityRegistry.ensure)await W.CapabilityRegistry.ensure('detail');setView('torrent');var nextDetailHash=decodeURIComponent(r.id);if(app.detailHash!==nextDetailHash){app.detailHash=nextDetailHash;app.detailProperties=null;app.detailPropertiesHash='';}var tabs=detailTabKeys(),requested=String(r.tab||''),tab=requested&&tabs.indexOf(requested)>=0?requested:(tabs[0]||'');if(tab&&requested!==tab){W.Router.detail(app.detailHash,tab);return;}app.detailTab=tab;await loadDetailHeader();await loadDetailTab(app.detailTab);}else if(r.name==='settings'){setView('settings');if(W.SettingsRenderer&&W.SettingsRenderer.open)await W.SettingsRenderer.open();}else if(r.name==='search'){if(!capabilitySupported('search',app.client.capabilities.search)){showCapability('search');W.Router.home();return;}setView('search');}else if(r.name==='rss'){if(!capabilitySupported('rss',app.client.capabilities.rss)){showCapability('rss');W.Router.home();return;}setView('rss');await loadRSS();}else if(r.name==='logs'){if(!capabilitySupported('logs',app.client.capabilities.logs)){showCapability('logs');W.Router.home();return;}setView('logs');if(W.Logs&&W.Logs.activate)W.Logs.activate();}else{setView('home');if(!app.torrents.length)loadPage(false);}}
  function showUnsupported(id,msg){var el=U.$(id);el.innerHTML='';var p=document.createElement('p');p.className='text-description';p.textContent=msg;el.appendChild(p);}
  async function openDetail(hash){var tabs=detailTabKeys(),tab=app.detailTab&&tabs.indexOf(app.detailTab)>=0?app.detailTab:defaultDetailTab();W.Router.detail(hash,tab);}
  async function torrentFor(hash){var t=app.catalogByHash.get(String(hash||''))||app.torrents.find(function(x){return x.hash===hash;});if(!t){try{var a=await app.client.getTorrents({hashes:hash,limit:1});t=a&&a[0];}catch(_e){}}return t||{hash:hash};}
  function hasDetailField(surface,name){var R=W.CapabilityRegistry;return !!(R&&R.hasTorrentDetailField&&R.hasTorrentDetailField(surface,name));}
  function detailValue(surface,item,name,fallback){if(!hasDetailField(surface,name))return fallback;var value=item&&item[name];return value===undefined||value===null?fallback:value;}
  function detailControl(name){var ui=detailUi(),controls=ui&&ui.controls;return controls&&controls[String(name||'')]||null;}
  function detailOptionLabel(controlName,option){var ref=option&&option.translation||null,fallback=String(ref&&ref.source||option&&option.value||'');return W.QbUiEvidence&&typeof W.QbUiEvidence.text==='function'?W.QbUiEvidence.text('detail.control.'+String(controlName||'')+'.'+String(option&&option.value||''),ref,fallback):fallback;}
  function detailEndpointForAction(action){var R=W.CapabilityRegistry,desc=R&&typeof R.actionDescriptor==='function'?R.actionDescriptor(action):null;if(!desc||!desc.endpoint)return'';var controller=String(action||'').split(':')[0].replace(/controller\.h$/,'');return controller?controller+'/'+String(desc.endpoint):String(desc.endpoint);}
  function derivedFileMenu(){var out=[],renameAction='torrentscontroller.h:renameFileAction',renameEndpoint=detailEndpointForAction(renameAction);if(renameEndpoint&&hasDetailField('files','name')&&writeActionSupported(renameAction))out.push({id:'__weig_file_rename',label:tr('action.renameEllipsis'),sourceAction:renameAction,endpoint:renameEndpoint,availability:{minSelection:1,maxSelection:1},fileKinds:['file'],parameterBindings:{oldPath:'name'},parameterPrompts:{newPath:{mode:'basename-preserve-parent',from:'oldPath'}}});var renameFolderAction='torrentscontroller.h:renameFolderAction',renameFolderEndpoint=detailEndpointForAction(renameFolderAction);if(renameFolderEndpoint&&writeActionSupported(renameFolderAction))out.push({id:'__weig_folder_rename',label:tr('action.renameEllipsis'),sourceAction:renameFolderAction,endpoint:renameFolderEndpoint,availability:{minSelection:1,maxSelection:1},fileKinds:['folder'],parameterBindings:{oldPath:'name'},parameterPrompts:{newPath:{mode:'basename-preserve-parent',from:'oldPath'}}});var priorityAction='torrentscontroller.h:filePrioAction',priorityEndpoint=detailEndpointForAction(priorityAction),control=detailControl('filePriority'),options=Array.isArray(control&&control.options)?control.options:[];if(priorityEndpoint&&writeActionSupported(priorityAction))options.filter(function(option){return Number(option&&option.value)>=0;}).forEach(function(option){out.push({id:'__weig_file_priority_'+String(option.value),label:detailOptionLabel('filePriority',option),sourceAction:priorityAction,endpoint:priorityEndpoint,availability:{minSelection:1,maxSelection:1},fileKinds:['file'],parameterValues:{priority:String(option.value)},requiresNonSeed:true});});out.push({id:'copy-file-path',label:tr('detail.copyFilePath'),localAction:'copy-file-path',availability:{minSelection:1,maxSelection:1},fileKinds:['file']},{id:'copy-containing-folder',label:tr('detail.copyContainingFolderPath'),localAction:'copy-containing-folder',availability:{minSelection:1,maxSelection:1},fileKinds:['file']});return out;}
  function detailMenu(surface){var ui=detailUi(),menus=ui&&ui.contextMenus,items=menus&&menus[String(surface||'')],out=Array.isArray(items)?items.slice():[];if(surface==='files')out=out.concat(derivedFileMenu());return out;}
  function detailMenuLabel(surface,item){var explicit=String(item&&item.label||'');if(explicit)return explicit;var ref=item&&item.translation||null,fallback=String(ref&&ref.source||item&&item.id||'');return W.QbUiEvidence&&typeof W.QbUiEvidence.text==='function'?W.QbUiEvidence.text('detail.menu.'+String(surface||'')+'.'+String(item&&item.id||''),ref,fallback):fallback;}
  function detailMenuAvailable(item,selectionCount,rowValue,rowItem){var availability=item&&item.availability||{},count=Number(selectionCount)||0;if(availability.minSelection!=null&&count<Number(availability.minSelection))return false;if(availability.maxSelection!=null&&count>Number(availability.maxSelection))return false;var kinds=Array.isArray(item&&item.fileKinds)?item.fileKinds:[];if(kinds.length){var kind=String(rowItem&&rowItem.__weigFileKind||'file');if(kinds.indexOf(kind)<0)return false;}var value=String(rowValue||''),prefixes=Array.isArray(availability.excludedPrefixes)?availability.excludedPrefixes:[];if(value&&prefixes.some(function(prefix){return value.startsWith(String(prefix));}))return false;if(item&&item.requiresNonSeed&&rowItem&&rowItem.is_seed===true)return false;return true;}
  function detailColumnValue(surface,item,columnKey,fallback){var columns=W.QbUiEvidence&&W.QbUiEvidence.detailColumns?W.QbUiEvidence.detailColumns(surface):[],column=columns.find(function(candidate){return candidate&&candidate.key===columnKey;}),keys=Array.isArray(column&&column.dataProperties)?column.dataProperties.map(String):[];if(keys.indexOf(columnKey)>=0&&item&&Object.prototype.hasOwnProperty.call(item,columnKey))return item[columnKey];var scalar=keys.filter(function(key){return key!=='endpoints'&&item&&Object.prototype.hasOwnProperty.call(item,key);});return scalar.length===1?item[scalar[0]]:fallback;}
  function writeActionSupported(action){var R=W.CapabilityRegistry;return !!(action&&R&&R.supportsWriteAction&&R.supportsWriteAction(action));}
  function detailActionDescriptor(item){var R=W.CapabilityRegistry,action=item&&item.sourceAction,endpoint=String(item&&item.endpoint||'');if(!action||!endpoint||!writeActionSupported(action)||!R||typeof R.actionDescriptor!=='function')return null;var desc=R.actionDescriptor(action);if(!desc)return null;var expected=String(desc.endpoint||'');if(expected&&endpoint.split('/').pop()!==expected)return null;return desc;}
  function detailLocalAction(surface,item){if(!item||item.sourceAction||item.endpoint)return'';if(item.localAction)return String(item.localAction);var id=String(item.id||'').toLowerCase();return(surface==='trackers'||surface==='peers')&&id.indexOf('copy')===0?'copy-row-address':'';}
  function detailRowAddress(surface,item,index){if(surface==='trackers'){if(item&&item.__weigRowAddress)return String(item.__weigRowAddress);return String(detailColumnValue(surface,item,'url','')||'');}if(surface==='peers'){var ip=detailColumnValue(surface,item,'ip',item&&item.__key||''),port=detailColumnValue(surface,item,'port','');return ip&&port?String(ip)+':'+String(port):String(ip||'');}if(surface==='files'){var fileId=hasDetailField('files','index')?detailValue('files',item,'index',index):index;return String(fileId==null?'':fileId);}return'';}
  function detailRowParameter(surface,item,index,name,allowRow){name=String(name||'');if(name==='hash'||name==='hashes')return{found:true,value:app.detailHash};if(!allowRow||!item)return{found:false};if(Object.prototype.hasOwnProperty.call(item,name))return{found:true,value:item[name]};if(surface==='trackers'){var url=detailColumnValue(surface,item,'url',''),tier=detailColumnValue(surface,item,'tier',null);if(name==='url'||name==='origUrl'||name==='urls')return url?{found:true,value:url}:{found:false};if(name==='tier'&&tier!==null&&tier!==undefined&&tier!=='')return{found:true,value:tier};}if(surface==='peers'&&name==='peers'){var address=detailRowAddress(surface,item,index);return address?{found:true,value:address}:{found:false};}if(surface==='files'&&(name==='id'||name==='ids')){var id=detailRowAddress(surface,item,index);return id!==''?{found:true,value:id}:{found:false};}return{found:false};}
  function detailBoundParameter(surface,item,index,name,allowRow,menuItem){var values=menuItem&&menuItem.parameterValues||{},bindings=menuItem&&menuItem.parameterBindings||{};if(Object.prototype.hasOwnProperty.call(values,name))return{found:true,value:values[name]};if(allowRow&&item&&Object.prototype.hasOwnProperty.call(bindings,name)){var source=String(bindings[name]||'');if(source==='rowAddress'){var address=detailRowAddress(surface,item,index);return address?{found:true,value:address}:{found:false};}if(Object.prototype.hasOwnProperty.call(item,source))return{found:true,value:item[source]};var projected=detailColumnValue(surface,item,source,undefined);if(projected!==undefined&&projected!==null)return{found:true,value:projected};}return detailRowParameter(surface,item,index,name,allowRow);}
  function detailBasename(value){value=String(value==null?'':value);var at=Math.max(value.lastIndexOf('/'),value.lastIndexOf('\\'));return at>=0?value.slice(at+1):value;}
  function detailPromptDefault(surface,item,index,name,menuItem,form){var rules=menuItem&&menuItem.parameterPrompts||{},rule=rules[name];if(rule&&rule.mode==='basename-preserve-parent'&&form&&Object.prototype.hasOwnProperty.call(form,rule.from))return detailBasename(form[rule.from]);var direct=detailBoundParameter(surface,item,index,name,true,menuItem);if(direct.found)return direct.value;var match=String(name||'').match(/^new(.+)$/);if(match){var base=match[1].replace(/^./,function(ch){return ch.toLowerCase();}),prior=detailBoundParameter(surface,item,index,base,true,menuItem);if(prior.found)return prior.value;}return'';}
  function detailNormalizePrompt(name,value){value=String(value==null?'':value);if(name==='peers'&&/[\r\n]/.test(value))return value.trim().split(/[\r\n]+/).filter(Boolean).join('|');return value;}
  function detailApplyPromptRule(menuItem,name,value,form){var rules=menuItem&&menuItem.parameterPrompts||{},rule=rules[name];if(!rule||rule.mode!=='basename-preserve-parent')return value;var old=String(form&&form[rule.from]||''),at=Math.max(old.lastIndexOf('/'),old.lastIndexOf('\\'));return(at>=0?old.slice(0,at+1):'')+String(value||'').trim();}
  function detailPromptValue(title,name,value,options){return new Promise(function(resolve,reject){var dialog=U.$('prompt-dialog'),input=U.$('prompt-value'),allowed=Array.isArray(options)&&options.length?options.map(String):[];U.$('prompt-title').textContent=title;U.$('prompt-label').textContent=String(name||tr('dialog.action.value'))+(allowed.length?' ('+allowed.join(' / ')+')':'');input.value=value==null?'':String(value);W.DialogRuntime.open(dialog,{draggable:true,focus:input});setTimeout(function(){input.focus();input.select();},20);var settled=false;function cleanup(){dialog.removeEventListener('close',onClose);U.$('prompt-close').onclick=U.$('prompt-cancel').onclick=function(){W.DialogRuntime.close(dialog);};}function finish(ok,result){if(settled)return;settled=true;cleanup();W.DialogRuntime.close(dialog);if(ok)resolve(result);else{var error=new Error(tr('common.actionCancelled'));error.code='WEIG_ACTION_CANCELLED';reject(error);}}function onClose(){if(!settled){settled=true;cleanup();var error=new Error(tr('common.actionCancelled'));error.code='WEIG_ACTION_CANCELLED';reject(error);}}dialog.addEventListener('close',onClose);U.$('prompt-confirm').onclick=function(){var result=String(input.value||'');if(allowed.length&&allowed.indexOf(result)<0){W.toast(tr('detail.sourceValuePromptPrefix')+allowed.join(', '),'warning',{title:tr('common.invalidValue')});return;}finish(true,result);};U.$('prompt-close').onclick=U.$('prompt-cancel').onclick=function(){finish(false);};});}
  async function detailActionForm(surface,item,index,menuItem,desc){var form={},params=Array.isArray(desc.parameters)?desc.parameters.map(String):[],required=Array.isArray(desc.required)?desc.required.map(String):[],optional=Array.isArray(desc.optional)?desc.optional.map(String):[],options=desc.parameterOptions||{},availability=menuItem&&menuItem.availability||{},selectionBound=!!(item&&Number(availability.minSelection)>=1),rowScoped=!!(selectionBound&&(availability.maxSelection!=null||(Array.isArray(availability.excludedPrefixes)&&availability.excludedPrefixes.length))),promptNames=[];params.forEach(function(name){var isOptional=optional.indexOf(name)>=0,allowRow=isOptional?rowScoped:selectionBound,found=detailBoundParameter(surface,item,index,name,allowRow,menuItem);if(found.found)form[name]=found.value;else if(required.indexOf(name)>=0||Object.prototype.hasOwnProperty.call(options,name)||/^new[A-Z_]/.test(name))promptNames.push(name);});for(var i=0;i<promptNames.length;i++){var name=promptNames[i],raw=await detailPromptValue(detailMenuLabel(surface,menuItem),name,detailPromptDefault(surface,item,index,name,menuItem,form),options[name]);raw=detailNormalizePrompt(name,raw);if(required.indexOf(name)>=0&&!raw)throw new Error(tr('detail.requiredParameterEmptyPrefix')+name);form[name]=detailApplyPromptRule(menuItem,name,raw,form);}for(var j=0;j<required.length;j++)if(!Object.prototype.hasOwnProperty.call(form,required[j]))throw new Error(tr('detail.unsafeParameterPrefix')+required[j]);return form;}
  async function executeDetailAction(surface,item,index,menuItem){var desc=detailActionDescriptor(menuItem);if(!desc)throw new Error(tr('detail.actionDescriptorUnavailable'));var form=await detailActionForm(surface,item,index,menuItem,desc);await app.client.request(String(menuItem.endpoint),{method:'POST',form:form,type:'void'});}
  async function detailProperties(){if(app.detailProperties&&app.detailPropertiesHash===app.detailHash)return app.detailProperties;var p=await app.client.properties(app.detailHash);if(!p||typeof p!=='object'||Array.isArray(p))throw new Error(tr('detail.propertiesUnexpected'));app.detailProperties=p;app.detailPropertiesHash=app.detailHash;return p;}
  async function executeDetailLocalAction(kind,rowValue,rowItem){var value=rowValue;if(kind==='copy-file-path'||kind==='copy-containing-folder'){var properties=await detailProperties(),path=W.QbPath&&W.QbPath.resolveFile?W.QbPath.resolveFile(properties,rowItem):'';value=kind==='copy-containing-folder'&&W.QbPath&&W.QbPath.dirname?W.QbPath.dirname(path):path;}if((kind!=='copy-row-address'&&kind!=='copy-file-path'&&kind!=='copy-containing-folder')||!value)throw new Error(tr('detail.localExecutorUnavailable'));if(!global.navigator||!navigator.clipboard||typeof navigator.clipboard.writeText!=='function')throw new Error(tr('common.clipboardUnavailable'));await navigator.clipboard.writeText(String(value));W.toast(tr('common.copiedMessage'),'success',{title:tr('common.copiedTitle')});}
  function detailActionItems(surface,item,index,selectionCount){var rowValue=detailRowAddress(surface,item,index);return detailMenu(surface).map(function(menuItem){if(!detailMenuAvailable(menuItem,selectionCount,rowValue,item))return null;var localAction=detailLocalAction(surface,menuItem),desc=detailActionDescriptor(menuItem);if(!localAction&&!desc)return null;return{key:menuItem.id,label:detailMenuLabel(surface,menuItem),onSelect:async function(){try{if(localAction){await executeDetailLocalAction(localAction,rowValue,item);return;}await executeDetailAction(surface,item,index,menuItem);await renderDetailTable(U.$('detail-content'),surface);}catch(e){if(e&&e.code==='WEIG_ACTION_CANCELLED')return;W.toast(e.message,'error',{title:tr('detail.actionFailedTitle')});}}};}).filter(Boolean);}
  function renderDetailUnsupported(root){root.textContent='';var p=document.createElement('p');p.className='text-description';p.textContent=tr('detail.surfaceUnavailable');root.appendChild(p);}
  function requireDetailArray(items,label){if(!Array.isArray(items))throw new Error(label+tr('detail.unexpectedResponseSuffix'));return items;}
  async function loadDetailHeader(){var t=await torrentFor(app.detailHash),fallback=tr('detail.eyebrow');if(!t.name){U.$('detail-title').textContent=fallback;return;}U.$('detail-title').textContent=t.name||fallback;var s=C.state(t.state),bar=U.$('detail-progress-bar'),track=bar&&bar.closest('.progress-track'),host=bar&&bar.closest('.detail-progress'),visual=C.paintProgress(track,bar,t),state=U.$('detail-state');state.textContent=s[0];state.dataset.tone=visual.tone;state.dataset.active=visual.active?'true':'false';if(host){host.dataset.progressState=visual.state;host.dataset.progressTone=visual.tone;host.dataset.progressActive=visual.active?'true':'false';}U.$('detail-progress-text').textContent=C.progressText(t);}
  function activateTab(tab){U.$$('.tab').forEach(function(el){var active=el.dataset.tab===tab;el.classList.toggle('is-active',active);el.setAttribute('aria-selected',active?'true':'false');});app.detailTab=tab;}
  async function loadDetailTab(tab){var root=U.$('detail-content'),tabs=detailTabKeys(),surface=detailTableSurface(tab);if(!tab||tabs.indexOf(tab)<0){activateTab('');renderDetailUnsupported(root);event('weig:detail-content',{tab:'',surface:''});return;}activateTab(tab);root.textContent='';var loading=document.createElement('div');loading.className='loading-state';loading.innerHTML='<div class="spinner"></div>';var loadingCopy=document.createElement('span');loadingCopy.textContent=tr('detail.loading');loading.appendChild(loadingCopy);root.appendChild(loading);try{if(tab==='overview')await renderOverview(root);else if(surface)await renderDetailTable(root,surface);else renderDetailUnsupported(root);}catch(e){root.textContent='';var p=document.createElement('p');p.className='text-description';p.textContent=tr('detail.loadFailed',{error:e.message});root.appendChild(p);}finally{event('weig:detail-content',{tab:tab,surface:surface||''});}}
  async function renderOverview(root){var p=await detailProperties();if(!W.QbUiEvidence||typeof W.QbUiEvidence.renderGeneral!=='function'||!W.QbUiEvidence.renderGeneral(root,p,app.detailHash))throw new Error(tr('detail.generalUnavailable'));}
  function makeDataViewport(root,items,rowH,render,extra){if(app.detailViewport&&app.detailViewport.destroy)app.detailViewport.destroy();app.detailViewport=null;root.textContent='';var note=document.createElement('p');note.className='section-note';note.textContent=tr('detail.virtualSummary',{count:items.length});var box=document.createElement('div');root.append(note,box);var options=Object.assign({rowHeight:rowH,overscan:6,renderRow:render},extra||{});app.detailViewport=(W.UiSystem&&W.UiSystem.createDetailDataViewport)?W.UiSystem.createDetailDataViewport(box,options):new W.DataViewport(box,options);app.detailViewport.setItems(items);return box;}
  async function refreshDetailFiles(){var latest=requireDetailArray(await app.client.files(app.detailHash),'files API');if(app.detailViewport&&app.detailViewport.setItems)app.detailViewport.setItems(latest,true);return latest;}
  async function writeDetailFilePriority(ids,priority){ids=Array.from(new Set((ids||[]).map(Number).filter(Number.isFinite)));if(!ids.length)return false;try{for(var i=0;i<ids.length;i++)await app.client.setFilePriority(app.detailHash,ids[i],priority);await refreshDetailFiles();W.toast(tr('file.priorityUpdatedMessage'),'success',{title:tr('file.priorityUpdatedTitle')});return true;}catch(e){try{await refreshDetailFiles();}catch(_refreshError){}W.toast(e.message,'error',{title:tr('file.priorityFailedTitle')});throw e;}}
  function detailFilePriorityCell(surface){if(surface!=='files')return null;var control=detailControl('filePriority'),sourceOptions=Array.isArray(control&&control.options)?control.options:[],priorityOptions=sourceOptions.map(function(option){return{value:String(option.value),label:detailOptionLabel('filePriority',option)};}),canWrite=writeActionSupported('torrentscontroller.h:filePrioAction');return function(ctx){if(ctx.column.key!=='priority'||!canWrite||!priorityOptions.length||!hasDetailField('files','priority'))return false;var current=String(detailValue('files',ctx.item,'priority',''));if(!priorityOptions.some(function(option){return option.value===current;}))return false;ctx.cell.__weigDetailItem=ctx.item;ctx.cell.__weigDetailIndex=ctx.index;var ctl=ctx.cell.__weigFilePriorityControl;if(!ctl){var fileName=detailValue('files',ctx.item,'name','File');ctl=C.selectControl({value:current,options:priorityOptions,ariaLabel:fileName+' priority',onChange:function(value){var item=ctx.cell.__weigDetailItem,index=ctx.cell.__weigDetailIndex,fileId=hasDetailField('files','index')?detailValue('files',item,'index',index):index;writeDetailFilePriority([fileId],value).catch(function(){});}});ctx.cell.textContent='';ctx.cell.appendChild(ctl);ctx.cell.__weigFilePriorityControl=ctl;}else if(ctl.setValue)ctl.setValue(current);return true;};}
  async function renderDetailTable(root,surface){surface=detailTableSurface(surface);if(!surface||typeof app.client[surface]!=='function'){renderDetailUnsupported(root);return;}if(surface==='files')await detailProperties();var items=requireDetailArray(await app.client[surface](app.detailHash),surface+' API'),cellRenderer=detailFilePriorityCell(surface);makeDataViewport(root,items,54,function(item,index){var row=document.createElement('div');row.textContent=detailRowAddress(surface,item,index)||String(index+1);return row;},{detailCellRenderer:cellRenderer,detailFilePriorityWriter:surface==='files'?writeDetailFilePriority:null,detailContextItems:function(item,_surface,index,selectionCount){var count=selectionCount==null?(item?1:0):selectionCount;return detailActionItems(surface,item,index,count);}});}
  function promptAction(title,label,value,cb){U.$('prompt-title').textContent=title;U.$('prompt-label').textContent=label;U.$('prompt-value').value=value||'';W.DialogRuntime.open(U.$('prompt-dialog'),{draggable:true,focus:U.$('prompt-value')});setTimeout(function(){U.$('prompt-value').focus();},20);U.$('prompt-confirm').onclick=async function(){var v=U.$('prompt-value').value,notice=W.toast(tr('app.operationSubmitting'),'info',{title:tr('app.processingTitle'),duration:0});try{await cb(v);W.DialogRuntime.close(U.$('prompt-dialog'));notice.update(tr('app.operationCompleteMessage'),'success',{title:tr('app.operationCompleteTitle')});}catch(e){notice.update(e.message,'error',{title:tr('app.operationFailedTitle')});}};}
  function addDescriptor(){var R=W.CapabilityRegistry;return R&&R.sourceActionDescriptor?R.sourceActionDescriptor('torrentscontroller.h:addAction'):null;}function addOwnedText(key){var I=W.I18n,value=I&&I.qbText?I.qbText(key,''):'';return String(value||'');}function paintAddSourceCopy(){var title=U.$('add-dialog-title'),urls=U.$('add-urls-label'),files=U.$('add-files-label'),submit=U.$('add-submit'),value;if(title&&(value=addOwnedText('add.title')))title.textContent=value;if(urls&&(value=addOwnedText('add.links')))urls.textContent=value;if(files&&(value=addOwnedText('add.files')))files.textContent=value;if(submit&&(value=addOwnedText('add.submit')))submit.textContent=value;}
  function addText(source,fallback,context){var I=W.I18n;return I&&I.qbSourceText?I.qbSourceText({context:context||'AddNewTorrentDialog',source:source},fallback||source):String(fallback||source||'');}
  function addParamSet(){var d=addDescriptor();return new Set(d&&Array.isArray(d.parameters)?d.parameters:[]);}
  function addGroup(root,title,key){var g=document.createElement('fieldset');g.className='add-option-group';g.dataset.addGroup=key||'';var l=document.createElement('legend');l.textContent=title;g.appendChild(l);root.appendChild(g);return g;}
  function addInput(group,id,label,value,options){options=options||{};var wrap=document.createElement('label');wrap.className='field add-option-field';var cap=document.createElement('span');cap.textContent=label;var input=document.createElement('input');input.id=id;input.className='field-input';input.type=options.type||'text';if(options.inputMode)input.inputMode=options.inputMode;if(options.step!=null)input.step=String(options.step);if(options.min!=null)input.min=String(options.min);if(options.placeholder)input.placeholder=options.placeholder;if(options.list)input.setAttribute('list',options.list);input.value=value==null?'':String(value);wrap.append(cap,input);group.appendChild(wrap);return input;}
  function addCheck(group,id,label,checked){var wrap=document.createElement('label');wrap.className='add-option-check';var input=document.createElement('input');input.id=id;input.type='checkbox';input.checked=!!checked;var copy=document.createElement('span');copy.textContent=label;wrap.append(input,copy);group.appendChild(wrap);return input;}
  function addSelect(group,id,label,value,options,onChange){var wrap=document.createElement('label');wrap.className='field add-option-field';var cap=document.createElement('span');cap.textContent=label;var select=C.selectControl({id:id,value:String(value==null?'':value),options:options||[],ariaLabel:label,onChange:function(next){if(typeof onChange==='function')onChange(next);}});wrap.append(cap,select);group.appendChild(wrap);return select;}
  function addList(id,values){var list=document.createElement('datalist');list.id=id;(values||[]).forEach(function(value){var opt=document.createElement('option');opt.value=String(value);list.appendChild(opt);});return list;}
  function addCategoryInfo(name){var map=app.nativeCategories||{},item=map[String(name||'')];return item&&typeof item==='object'?item:null;}
  function addDefaultSavePath(category,auto){var prefs=app.preferences||{},base=String(prefs.save_path||''),item=addCategoryInfo(category);if(!auto||!category||!item)return base;if(String(item.savePath||'').trim())return String(item.savePath);return base?(base.replace(/[\\/]+$/,'')+'/'+category):base;}
  function addDefaultDownloadPath(category,auto){var prefs=app.preferences||{},base=String(prefs.temp_path||''),item=addCategoryInfo(category);if(!auto||!category||!item)return base;if(item.downloadPath===false)return'';if(typeof item.downloadPath==='string'&&item.downloadPath.trim())return item.downloadPath;return base?(base.replace(/[\\/]+$/,'')+'/'+category):base;}
  function addDefaultDownloadPathEnabled(category,auto){var prefs=app.preferences||{},item=addCategoryInfo(category);if(!auto||!category||!item||item.downloadPath==null)return !!prefs.temp_path_enabled;return item.downloadPath!==false;}
  function updateAddPaths(){var auto=U.$('add-auto-tmm'),category=U.$('add-category'),save=U.$('save-path'),use=U.$('add-use-download-path'),download=U.$('add-download-path'),isAuto=!!(auto&&((auto.getValue&&auto.getValue())||auto.value)==='true'),name=category?category.value.trim():'';if(save){if(isAuto||!save.dataset.userEdited)save.value=addDefaultSavePath(name,isAuto);save.disabled=isAuto;}if(use){use.disabled=isAuto;if(isAuto)use.checked=addDefaultDownloadPathEnabled(name,true);}if(download){if(isAuto||!download.dataset.userEdited)download.value=use&&use.checked?addDefaultDownloadPath(name,isAuto):'';download.disabled=isAuto||!!(use&&!use.checked);}}
  function renderAddOptions(){var root=U.$('add-options');if(!root)return null;root.textContent='';var params=addParamSet(),prefs=app.preferences||{},has=function(name){return params.has(name);},saveGroup=addGroup(root,addText('Save at','Save at'),'save'),settingsGroup=addGroup(root,addText('Torrent settings','Torrent settings'),'settings'),startGroup=addGroup(root,addText('Start torrent','Start torrent'),'start'),limitsGroup=addGroup(root,addText('Torrent Upload/Download Ratio Limiting','Limits','UpDownRatioDialog'),'limits');
    var auto=null;if(has('autoTMM'))auto=addSelect(saveGroup,'add-auto-tmm',addText('Torrent Management Mode:','Torrent Management Mode'),prefs.auto_tmm_enabled?'true':'false',[{value:'false',label:addText('Manual','Manual')},{value:'true',label:addText('Automatic','Automatic')}],updateAddPaths);
    var save=null;if(has('savepath')){save=addInput(saveGroup,'save-path',addText('Save files to location:','Save files to location'),String(prefs.save_path||''));save.addEventListener('input',function(){save.dataset.userEdited='1';});}
    var use=null,download=null;if(has('useDownloadPath'))use=addCheck(saveGroup,'add-use-download-path',addText('Use another path for incomplete torrent','Use another path for incomplete torrent'),!!prefs.temp_path_enabled);if(has('downloadPath')){download=addInput(saveGroup,'add-download-path',addText('Save path:','Save path'),String(prefs.temp_path||''));download.addEventListener('input',function(){download.dataset.userEdited='1';});}
    if(has('rename'))addInput(settingsGroup,'add-rename',addText('Rename torrent','Rename torrent'),'');
    if(has('category')){var catList=addList('add-category-list',Object.keys(app.nativeCategories||{}));settingsGroup.appendChild(catList);var cat=addInput(settingsGroup,'add-category',addText('Category:','Category'),'',{list:'add-category-list'});cat.addEventListener('input',updateAddPaths);}
    if(has('tags')){var tagsList=addList('add-tags-list',app.nativeTags||[]);settingsGroup.appendChild(tagsList);addInput(settingsGroup,'add-tags',addText('Tags:','Tags'),'',{list:'add-tags-list',placeholder:(app.nativeTags||[]).join(', ')});}
    if(has('cookie'))addInput(settingsGroup,'add-cookie','Cookie','');
    var stoppedKey=has('stopped')?'stopped':(has('paused')?'paused':'');if(stoppedKey){var addStopped=Object.prototype.hasOwnProperty.call(prefs,'add_stopped_enabled')?!!prefs.add_stopped_enabled:!!prefs.start_paused_enabled;var start=addCheck(startGroup,'add-start',addText('Start torrent','Start torrent'),!addStopped);start.dataset.addStoppedKey=stoppedKey;}
    if(has('forced'))addCheck(startGroup,'add-forced',tr('torrent.forceStart'),false);
    if(has('addToTopOfQueue'))addCheck(startGroup,'add-queue-top',addText('Add to top of queue','Add to top of queue'),!!prefs.add_to_top_of_queue);
    if(has('stopCondition'))addSelect(startGroup,'add-stop-condition',addText('Stop condition:','Stop condition'),prefs.torrent_stop_condition||'None',[{value:'None',label:addText('None','None')},{value:'MetadataReceived',label:addText('Metadata received','Metadata received')},{value:'FilesChecked',label:addText('Files checked','Files checked')}]);
    if(has('skip_checking'))addCheck(startGroup,'add-skip-check',addText('Skip hash check','Skip hash check'),false);
    if(has('sequentialDownload'))addCheck(startGroup,'add-sequential',addText('Download in sequential order','Download in sequential order'),false);
    if(has('firstLastPiecePrio'))addCheck(startGroup,'add-first-last',addText('Download first and last pieces first','Download first and last pieces first'),false);
    if(has('contentLayout')||has('root_folder'))addSelect(startGroup,'add-content-layout',addText('Content layout:','Content layout'),prefs.torrent_content_layout||'Original',[{value:'Original',label:addText('Original','Original')},{value:'Subfolder',label:addText('Create subfolder','Create subfolder')},{value:'NoSubfolder',label:addText("Don't create subfolder","Don't create subfolder")}]);
    var kib=addText('KiB/s','KiB/s');if(has('dlLimit'))addInput(limitsGroup,'add-dl-limit',addText('Limit download rate','Limit download rate'),'',{type:'number',inputMode:'decimal',min:0,step:1,placeholder:kib});if(has('upLimit'))addInput(limitsGroup,'add-up-limit',addText('Limit upload rate','Limit upload rate'),'',{type:'number',inputMode:'decimal',min:0,step:1,placeholder:kib});
    if(has('ratioLimit'))addInput(limitsGroup,'add-ratio-limit',addText('ratio','Ratio','UpDownRatioDialog'),'',{type:'number',inputMode:'decimal',min:0,step:.01});
    if(has('seedingTimeLimit'))addInput(limitsGroup,'add-seeding-time',addText('total minutes','Seeding time (min)','UpDownRatioDialog'),'',{type:'number',inputMode:'numeric',min:0,step:1});
    if(has('inactiveSeedingTimeLimit'))addInput(limitsGroup,'add-inactive-seeding-time',addText('inactive minutes','Inactive seeding time (min)','UpDownRatioDialog'),'',{type:'number',inputMode:'numeric',min:0,step:1});
    if(has('shareLimitAction'))addSelect(limitsGroup,'add-share-action',addText('Action when the limit is reached','Action when the limit is reached','UpDownRatioDialog'),'Default',[{value:'Default',label:addText('Default','Default','UpDownRatioDialog')},{value:'Stop',label:addText('Stop torrent','Stop torrent','UpDownRatioDialog')},{value:'Remove',label:addText('Remove torrent','Remove torrent','UpDownRatioDialog')},{value:'RemoveWithContent',label:addText('Remove torrent and its content','Remove torrent and its content','UpDownRatioDialog')},{value:'EnableSuperSeeding',label:addText('Enable super seeding for torrent','Enable super seeding for torrent','UpDownRatioDialog')}]);
    [saveGroup,settingsGroup,startGroup,limitsGroup].forEach(function(group){if(!group.querySelector('input,select'))group.remove();});if(use)use.addEventListener('change',updateAddPaths);updateAddPaths();return params;}
  function addControlValue(id){var node=U.$(id);if(!node)return'';var value=node.getValue?node.getValue():node.value;return String(value==null?'':value).trim();}
  function addControlChecked(id){var node=U.$(id);return !!(node&&node.checked);}
  function collectAddOptions(params){var extra={},put=function(name,value){if(params.has(name)&&value!==''&&value!=null)extra[name]=value;};put('autoTMM',addControlValue('add-auto-tmm'));put('rename',addControlValue('add-rename'));put('category',addControlValue('add-category'));put('tags',addControlValue('add-tags'));put('cookie',addControlValue('add-cookie'));var start=U.$('add-start');if(start&&start.dataset.addStoppedKey)put(start.dataset.addStoppedKey,!start.checked);if(U.$('add-forced'))put('forced',addControlChecked('add-forced'));if(U.$('add-queue-top'))put('addToTopOfQueue',addControlChecked('add-queue-top'));put('stopCondition',addControlValue('add-stop-condition'));if(U.$('add-skip-check'))put('skip_checking',addControlChecked('add-skip-check'));if(U.$('add-sequential'))put('sequentialDownload',addControlChecked('add-sequential'));if(U.$('add-first-last'))put('firstLastPiecePrio',addControlChecked('add-first-last'));var layout=addControlValue('add-content-layout');if(layout&&params.has('contentLayout'))put('contentLayout',layout);else if(layout&&params.has('root_folder')&&layout!=='Original')put('root_folder',layout==='Subfolder');var use=U.$('add-use-download-path');if(use&&!use.disabled){put('useDownloadPath',use.checked);put('downloadPath',use.checked?addControlValue('add-download-path'):'');}var dl=addControlValue('add-dl-limit'),up=addControlValue('add-up-limit');if(dl!=='')put('dlLimit',Math.max(0,Number(dl)||0)*1024);if(up!=='')put('upLimit',Math.max(0,Number(up)||0)*1024);put('ratioLimit',addControlValue('add-ratio-limit'));put('seedingTimeLimit',addControlValue('add-seeding-time'));put('inactiveSeedingTimeLimit',addControlValue('add-inactive-seeding-time'));put('shareLimitAction',addControlValue('add-share-action'));return extra;}
  async function openAdd(){if(W.I18n&&W.I18n.ready)await W.I18n.ready();U.$('torrent-urls').value='';U.$('torrent-files').value='';paintAddSourceCopy();renderAddOptions();W.DialogRuntime.open(U.$('add-dialog'),{draggable:true,focus:U.$('torrent-urls')});setTimeout(function(){U.$('torrent-urls').focus();},50);}
  async function submitAdd(){var urls=U.$('torrent-urls').value,files=U.$('torrent-files').files;if(!urls.trim()&&!files.length){W.toast(tr('torrent.sourceRequiredMessage'),'warning',{title:tr('torrent.sourceRequiredTitle')});return;}var params=addParamSet(),save=U.$('save-path'),btn=U.$('add-submit'),notice=W.toast(tr('torrent.submitting'),'info',{title:tr('torrent.addingTitle'),duration:0});btn.disabled=true;btn.textContent=tr('torrent.addingButton');try{var result=await app.client.add(urls,files,save&&!save.disabled?save.value:'',collectAddOptions(params));if(result&&String(result).trim()!=='Ok.')throw new Error(String(result));W.DialogRuntime.close(U.$('add-dialog'));notice.update(tr('torrent.addedMessage'),'success',{title:tr('torrent.added')});app.page=0;await buildCatalog();await loadPage(false);}catch(e){notice.update(tr('torrent.addFailedPrefix')+e.message,'error',{title:tr('torrent.addFailedTitle')});}finally{btn.disabled=false;btn.textContent=tr('header.add');}}
  function closeDrawer(){if(W.HeaderUtilities&&W.HeaderUtilities.setDrawer){W.HeaderUtilities.setDrawer(false);return;}U.$('sidebar').classList.remove('is-open');U.$('drawer-scrim').classList.remove('is-open');}
  function installPageSizeControl(){if(app.pageSizeControl)return app.pageSizeControl;var native=U.$('page-size'),host=U.$('page-size-control');if(!host&&native){host=document.createElement('span');host.id='page-size-control';native.replaceWith(host);}if(!host)return null;app.pageSizeControl=C.selectControl({id:'page-size',value:String(app.pageSize),options:[20,50,100,200].map(function(n){return{value:String(n),label:String(n)};}),ariaLabel:tr('library.torrentsPerPage'),onChange:function(value){applyRuntimeConfig('pageSize',value);W.Config.save(cfg);}});host.appendChild(app.pageSizeControl);return app.pageSizeControl;}
  function mainColumnConfiguratorProvider(){return{title:function(){return W.I18n&&W.I18n.t?W.I18n.t('columns.title'):'Columns';},reorderable:false,columns:function(){var R=fieldRegistry(),defs=R&&R.sourceOrderedColumnDefinitions?R.sourceOrderedColumnDefinitions():effectiveColumnDefinitions(),active={};app.columns.forEach(function(column,index){active[column.key]={column:column,index:index};});return defs.map(function(def){var hit=active[def.key];return Object.assign({},def,hit?hit.column:{},{visible:!!hit});});},commit:function(next){var defs=effectiveColumnDefinitions(),byKey={};defs.forEach(function(def){byKey[def.key]=def;});var chosen=(next||[]).filter(function(column){return column.visible!==false&&byKey[column.key];}).map(function(column){return Object.assign({},byKey[column.key],{width:column.width||byKey[column.key].width});});if(!chosen.length&&defs.length)chosen=[Object.assign({},defs[0])];saveEffectiveColumns(chosen);renderHead();renderList();},reset:function(){var R=fieldRegistry();if(R&&R.resetDesktopColumns)app.columns=R.resetDesktopColumns(cfg);else{app.columns=W.DataGrid.defaults.slice(0,7).map(function(c){return Object.assign({},c);});W.DataGrid.save(cfg,app.columns);}renderHead();renderList();}};}
  function installMainColumnConfigurator(){if(W.ColumnConfigurator)W.ColumnConfigurator.register('torrent-main',mainColumnConfiguratorProvider());}
  function openColumns(){if(W.ColumnConfigurator)W.ColumnConfigurator.open('torrent-main');}
  async function loadRSS(){if(!capabilitySupported('rss',app.client.capabilities.rss)){showCapability('rss');return false;}if(!W.RSSWorkspace||typeof W.RSSWorkspace.load!=='function')throw new Error(tr('rss.workspaceUnavailable'));return W.RSSWorkspace.load();}
  async function addRSS(){var url=U.$('rss-url').value.trim();if(!url)return;if(!W.RSSWorkspace||typeof W.RSSWorkspace.addFeed!=='function')throw new Error(tr('rss.workspaceUnavailable'));var notice=W.toast(tr('rss.addingMessage'),'info',{title:tr('rss.addingTitle'),duration:0});try{await W.RSSWorkspace.addFeed(url,'');U.$('rss-url').value='';notice.update(tr('rss.addedMessage'),'success',{title:tr('rss.addedTitle')});}catch(e){notice.update(e.message,'error',{title:tr('rss.addFailedTitle')});}}
  async function startSearch(){if(!capabilitySupported('search',app.client.capabilities.search)){showCapability('search');return;}var q=U.$('engine-query').value.trim();if(!q)return;try{var res=await app.client.searchStart(q,'enabled','all');app.searchJob=res&&res.id!=null?res.id:res;pollSearch();}catch(e){W.toast(tr('search.startFailedPrefix')+e.message,'error',{title:tr('search.startFailedTitle')});}}
  async function pollSearch(){clearTimeout(app.searchPoll);if(app.searchJob==null)return;try{var data=await app.client.searchResults(app.searchJob,100,0),results=data&&data.results||[];var root=U.$('search-results');root.textContent='';results.forEach(function(x){var row=document.createElement('div');row.className='tool-row';var a=document.createElement('span');a.textContent=x.fileName||x.fileUrl||'—';var b=document.createElement('span');b.textContent=U.formatBytes(x.fileSize);var c=document.createElement('span');c.textContent=tr('search.seeds',{count:x.nbSeeders==null?'—':x.nbSeeders});row.append(a,b,c);root.appendChild(row);});var st=await app.client.searchStatus(app.searchJob),status=Array.isArray(st)&&st[0]&&st[0].status;if(status==='Running')app.searchPoll=setTimeout(pollSearch,1200);}catch(e){W.toast(tr('search.readFailedPrefix')+e.message,'error',{title:tr('search.readFailedTitle')});}}
  async function stopSearch(){if(app.searchJob==null)return;try{await app.client.searchStop(app.searchJob);app.searchJob=null;clearTimeout(app.searchPoll);}catch(_e){}}
  function bind(){if(W.UiSystem&&W.UiSystem.installRSSControls)W.UiSystem.installRSSControls();installPageSizeControl();installPagerControl();installMainColumnConfigurator();renderHead();U.$$('#app-nav [data-route],#mobile-bottom-nav [data-route],.sidebar [data-route]').forEach(function(b){b.addEventListener('click',function(){var path=b.dataset.route||'';if(path)W.Router.go(path);else W.Router.home();});});U.$('resume-btn').onclick=function(){app.selection.dispatch('resume').catch(function(e){W.toast(e.message,'error',{title:tr('torrent.commandFailedTitle')});});};U.$('pause-btn').onclick=function(){app.selection.dispatch('pause').catch(function(e){W.toast(e.message,'error',{title:tr('torrent.commandFailedTitle')});});};U.$('more-actions-btn').onclick=app.selection.openActions;U.$('delete-btn').onclick=app.selection.openDelete;U.$('add-btn').onclick=openAdd;U.$('empty-add-btn').onclick=openAdd;U.$('add-submit').onclick=submitAdd;U.$('back-btn').onclick=function(){W.Router.back();};U.$('search-input').addEventListener('input',U.debounce(async function(e){var next=e.target.value.trim();if(next!==app.search){app.search=next;app.page=0;clearSelection();resetTorrentScroll();emitLibraryState('search');}if(await ensureCatalogForLocalView())return;if(app.catalogReady)loadPage(false);else renderList();},180));U.$('actions-close').onclick=function(){W.DialogRuntime.close(U.$('actions-dialog'));};U.$('prompt-close').onclick=U.$('prompt-cancel').onclick=function(){W.DialogRuntime.close(U.$('prompt-dialog'));};U.$('columns-btn').onclick=openColumns;U.$('rss-add-btn').onclick=addRSS;U.$('rss-refresh-btn').onclick=loadRSS;U.$('engine-search-btn').onclick=startSearch;U.$('engine-stop-btn').onclick=stopSearch;U.$('fatal-back').onclick=function(){W.Router.back();};U.$('fatal-home').onclick=function(){W.Router.home();U.$('fatal').classList.add('is-hidden');};global.addEventListener('hashchange',route);document.addEventListener('visibilitychange',schedulePoll);global.addEventListener('resize',U.debounce(function(){renderList();},120));global.addEventListener('weig:qbcopychange',refreshColumnCopy);global.addEventListener('weig:languagechange',refreshColumnCopy);document.addEventListener('keydown',function(e){if(e.key==='Escape'){var open=document.querySelector('dialog[open]');if(open)return;if(W.Router.route().name!=='home')W.Router.back();else{app.selection.clear();renderList();}}if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='f'){e.preventDefault();if(U.isMobile()&&W.HeaderUtilities&&W.HeaderUtilities.setSearchOpen)W.HeaderUtilities.setSearchOpen(true);else U.$('search-input').focus();}if(e.key==='Delete'&&app.selection.count())app.selection.openDelete();});}
  function applyCapabilityUI(){if(W.CapabilityRegistry&&W.CapabilityRegistry.sync)W.CapabilityRegistry.sync();if(W.TorrentFilterView&&W.TorrentFilterView.render)W.TorrentFilterView.render();emitLibraryState('capabilities');}
  async function init(){try{bind();if(W.SessionController&&W.SessionController.ready&&!await W.SessionController.ready())return;app.selection.onCommandComplete=async function(kind){if(['delete','rename','location','category','tags'].indexOf(kind)>=0)await buildCatalog();else await loadPage(false);};await app.client.detect();if(W.SpatialRuntime&&W.SpatialRuntime.capabilitiesReady)await W.SpatialRuntime.capabilitiesReady();if(W.CapabilityRegistry&&W.CapabilityRegistry.bind)await W.CapabilityRegistry.bind(app.client);app.preferences=await app.client.getPreferences();if(W.SettingsState)W.SettingsState.prefs=app.preferences;if(app.preferences&&app.preferences.locale!=null&&W.I18n&&W.I18n.applyLocale)W.I18n.applyLocale(app.preferences.locale);if(W.I18n&&W.I18n.ready)await W.I18n.ready();applyEffectiveColumns();U.$('qb-version').textContent=app.client.qbVersion;U.$('api-version').textContent=app.client.webApiVersion;U.$('compat-mode').textContent=app.client.capabilities.certified?tr('app.compatible'):tr('app.compatMode');applyCapabilityUI();await Promise.all([loadPage(false),loadTransfer(),loadTaxonomy()]);await route();schedulePoll();setTimeout(buildCatalog,500);}catch(e){console.error(e);W.fatal(e);}}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})(window);
