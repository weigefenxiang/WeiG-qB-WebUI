import fs from 'node:fs';import path from 'node:path';import vm from 'node:vm';import {fileURLToPath} from 'node:url';import {readSettingsRuntime,packSettingsRuntime} from '../tools/qb-compact-runtime.mjs';
const here=path.dirname(fileURLToPath(import.meta.url)),root=path.resolve(here,'..'),read=rel=>fs.readFileSync(path.join(root,rel),'utf8'),assert=(ok,msg)=>{if(!ok)throw new Error(msg);};
const runtime=readSettingsRuntime(),manifest=runtime.manifest,compat=runtime.settingsData;assert(manifest.schemaVersion===3&&manifest.source==='qb-upstream-preferences-native-surface-runtime'&&manifest.encoding==='deflate-raw-base64-json','Settings runtime manifest must own the bounded source-native payload');assert(runtime.raw.length===311540&&manifest.payload.sha256==='2784279c685ffc3df3cd4bb5bcdcb3838e50940ccdc2cb83d32e7a3cf04b326f','Settings payload must remain byte-identical to accepted #1856 evidence');assert(compat.schemaVersion===2&&compat.source==='qb-upstream-preferences-native-surface-compact'&&compat.releases.length===65,'decoded Settings payload must remain source-native schema v2 for all 65 releases');
const repacked=packSettingsRuntime(compat,manifest.catalogIdentity);assert(repacked.manifest.payload.sha256===manifest.payload.sha256&&repacked.manifest.payload.compressedSha256===manifest.payload.compressedSha256&&repacked.manifest.payload.base64===manifest.payload.base64,'Settings runtime packer must deterministically reproduce the accepted bounded payload');
const schemaSource=read('webui/private/scripts/settings-schema.js');for(const retired of ['SURFACES=','SECTION_ORDER=','SECTION_TITLES=','FAMILY_RULES=','function add(','function layout('])assert(!schemaSource.includes(retired),'retired manual qB Settings owner returned: '+retired);
let ensureCalls=0,writeAction=true;const W={CapabilityRegistry:{ensure:async name=>{ensureCalls++;assert(name==='settings','unexpected domain '+name);return compat;},supportsWriteAction:name=>writeAction&&name==='appcontroller.h:setPreferencesAction'}};
const sandbox={window:{WeiG:W}};sandbox.window.window=sandbox.window;vm.runInNewContext(schemaSource,{window:sandbox.window,console},{filename:'settings-schema.js'});const S=sandbox.window.WeiG.SettingsSchema;await S.loadCompatibility();await S.loadCompatibility();assert(ensureCalls===1,'Settings source-native domain must lazy-load once');
const latestSha='0b63c3d17373f6132ea211c9dcd4241284ccdfaf';S.bindRelease({qbVersion:'5.2.3',sourceSha:latestSha,certified:true,fallback:false});assert(JSON.stringify(S.nativeSurfaces())===JSON.stringify(['behavior','downloads','connection','speed','bittorrent','rss','webui','advanced']),'latest native Options tabs/order must come from exact source');
const prefs={locale:'en-US',dl_limit:1024,scheduler_enabled:true,schedule_from_hour:8,alternative_webui_enabled:false,alternative_webui_path:'/config/webui'};assert(S.toDisplay('dl_limit',1024)===1&&S.toRaw('dl_limit',1)===1024,'source-proven 1024 scale projection drifted');assert(S.isWritable('locale','en-US',prefs,prefs),'locale direct-string write proof must stay writable');assert(S.isWritable('dl_limit',2048,prefs,prefs),'source-proven numeric inverse must stay writable');assert(!S.isWritable('schedule_from_hour',8,prefs,prefs),'source READ_ONLY schedule field must fail closed');assert(!S.isWritable('alternative_webui_path','/tmp',prefs,prefs),'upstream dependency gate must block disabled alternate WebUI path');const enabled={...prefs,alternative_webui_enabled:true};assert(S.isWritable('alternative_webui_path','/tmp',enabled,{...enabled,alternative_webui_path:'/tmp'}),'source-proven path write must unlock only after dependency proof');
const safe=S.writableDraft({locale:'zh_CN',dl_limit:2048,schedule_from_hour:9},prefs);assert(safe.locale==='zh_CN'&&safe.dl_limit===2048&&!('schedule_from_hour' in safe),'writableDraft must filter every key against current prefs + source projection');
S.bindRelease({qbVersion:'4.1.0',sourceSha:'e9ed621178af841114a045887c5ce1e6b50cb366',certified:true,fallback:false});assert(S.toDisplay('proxy_type',2)==='socks5','historical switch-map display projection drifted');assert(!S.isWritable('proxy_type',2,{proxy_type:2},{proxy_type:2}),'historical read-only switch-map must never invent an inverse write');
S.bindRelease({qbVersion:'5.2.3',sourceSha:latestSha,certified:false,fallback:false});assert(!S.isWritable('locale','en-US',{locale:'en-US'},{locale:'en-US'}),'non-certified release must fail closed');S.bindRelease({qbVersion:'5.2.3',sourceSha:'deadbeef',certified:true,fallback:false});assert(S.nativeSurfaces().length===0,'source SHA mismatch must expose no native Settings surface');writeAction=false;S.bindRelease({qbVersion:'5.2.3',sourceSha:latestSha,certified:true,fallback:false});assert(!S.isWritable('locale','en-US',{locale:'en-US'},{locale:'en-US'}),'missing setPreferences source action must block writes');
const settings=read('webui/private/scripts/settings.js'),index=read('webui/private/index.html');for(const retired of ['QB_TAB_FALLBACKS','function listeningPortRow(','function scheduleRangeRow(','S.layout&&S.layout','S.sectionOrder'])assert(!settings.includes(retired),'retired renderer owner returned: '+retired);assert(settings.includes('S.group(tab,prefs)')&&settings.includes('S.writableDraft(controller.draft,controller.prefs||{})'),'renderer/save path must consume source-native SettingsSchema with current Preferences');assert(settings.includes('S.isWritable(key,value,prefs,state)'),'draft mutation must enforce current-key/dependency write safety');assert(index.includes('id="settings-qb-tabs"')&&!/data-settings-tab="downloads"/.test(index),'index must not statically own native qB Options tabs');
console.log('Settings source-native runtime contract passed: #1856 payload is deterministic and byte-identified, exact tabs/sections/projections drive the UI, manual compounds are retired, and writes fail closed on identity/action/setter/type/key/dependency/projection gaps.');
