import assert from 'node:assert/strict';
import fs from 'node:fs';
const read=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');
const selection=read('webui/private/scripts/selection.js');
const capabilities=read('webui/private/scripts/capabilities.js');
const version=read('VERSION').trim();
assert.equal(version,'1.0.15');
assert.match(selection,/data\.actionInteraction=item\.interaction\|\|'close'/);
assert.match(selection,/item\.interaction==='stay'/);
assert.match(selection,/refreshActionGroup\(dialog,grid,groupKind\)/);
assert.match(selection,/kind:'category:'\+category[\s\S]*?interaction:'stay'/);
assert.match(selection,/kind:'category-reset'[\s\S]*?interaction:'stay'/);
assert.match(selection,/kind:'tag:'\+tag[\s\S]*?interaction:'stay'/);
assert.match(selection,/kind:'tag-remove-all'[\s\S]*?interaction:'stay'/);
assert.match(capabilities,/var TORRENT_ACTION_CHOICES=/);
assert.match(capabilities,/function torrentActionSurface\(\)/);
assert.match(capabilities,/function torrentSourceActionInventory\(\)/);
assert.match(capabilities,/torrentActionSurface:torrentActionSurface/);
assert.match(selection,/sourceAction:source&&source\.sourceAction\|\|''/);
console.log('A25 B1 contract passed: taxonomy actions stay open and action UI consumes source-accounted descriptors.');
