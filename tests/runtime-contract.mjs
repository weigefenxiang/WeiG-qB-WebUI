import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const here=path.dirname(fileURLToPath(import.meta.url)),root=path.resolve(here,'..'),runtimeBase=path.join(root,'webui/private');
function assert(ok,msg){if(!ok)throw new Error(msg);}
function text(p){return fs.readFileSync(path.join(root,p),'utf8');}
function walk(abs,rel=''){return fs.readdirSync(abs,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(abs,e.name),path.join(rel,e.name)):[path.join(rel,e.name).replaceAll('\\','/')]);}
const runtimeFiles=walk(runtimeBase),versioned=runtimeFiles.filter(p=>/(?:^|\/)(?:v\d{3}|[^/]+-v\d{3})(?:\.[^/]+)$/.test(p));
assert(versioned.length===0,`Versioned runtime assets remain: ${versioned.join(', ')}`);
const required=['css/app.css','css/base.css','css/spatial.css','css/transfer.css','css/controls.css','css/layout.css','css/theme.css','css/ui.css','css/polish.css','css/logs.css','css/brand.css','css/header.css','css/session.css','css/settings.css','scripts/i18n.js','scripts/core.js','scripts/i18n-time.js','scripts/i18n-alternative-webui.js','scripts/i18n-interface.js','scripts/i18n-transfer.js','scripts/torrent-semantics.js','scripts/settings-schema.js','scripts/settings-translations.js','scripts/qb-client.js','scripts/components.js','scripts/floating.js','scripts/time.js','scripts/advanced-settings.js','scripts/brand.js','scripts/session.js','scripts/settings.js','scripts/navigation.js','scripts/spatial.js','scripts/transfer.js','scripts/responsive.js','scripts/selection.js','scripts/layout.js','scripts/ui.js','scripts/polish.js','scripts/app.js','scripts/ux.js','scripts/header.js','scripts/logs.js'];required.forEach(p=>assert(fs.existsSync(path.join(runtimeBase,p)),`Missing semantic runtime asset ${p}`));
const index=text('webui/private/index.html');assert(!/(?:src|href)=["'][^"']*(?:\/v\d{3}|-v\d{3}\.)/i.test(index),'Private index references a versioned runtime asset');for(const name of ['settings.js','settings.css','brand.js','brand.css','session.js','session.css','header.js','header.css'])assert(index.includes(name),`Private index missing ${name}`);
const components=text('webui/private/scripts/components.js');assert(!components.includes('loadRuntimeLayers'),'Components must not own a hidden runtime loader');assert(!/(?:scripts|css)\/(?:v\d{3}|[^'"/]+-v\d{3})\./.test(components),'Components still references versioned runtime assets');
const settings=text('webui/private/scripts/settings.js'),settingsCss=text('webui/private/css/settings.css'),ux=text('webui/private/scripts/ux.js');assert(settings.includes("className='setting-row'")&&settings.includes("className='settings-grid'"),'Canonical SettingRow/SettingsGrid owner missing');assert(!/settings-row--canonical|setting-row-grid|settings-grid-canonical|settings-section--rows/.test(settings),'Legacy Settings owner recreated in settings.js');assert(settingsCss.includes('grid-template-columns:repeat(2,minmax(0,1fr))')&&settingsCss.includes('@media(max-width:1180px)'),'Responsive SettingsGrid contract missing');assert(settingsCss.includes('justify-content:flex-end')&&settingsCss.includes('text-align:left'),'SettingRow alignment contract missing');assert(ux.includes('MutationObserver')&&ux.includes('SettingsRenderer.open')&&ux.includes('canonicalSettingsLost'),'Initial/deep-link Settings ownership reclaim missing');
const nonSettingsCss=['base.css','spatial.css','transfer.css','controls.css','layout.css','theme.css','ui.css','polish.css'];for(const file of nonSettingsCss){const css=text('webui/private/css/'+file);assert(!/\.(?:settings|setting)-|\.about-/i.test(css),`${file} reintroduces a Settings/About layout owner`);}
const brand=text('webui/private/scripts/brand.js');assert(brand.includes('brand-mark-home')&&brand.includes('brand-name-home'),'Header brand targets are not separated');assert(brand.includes('createIdentity')&&brand.includes('AmbientMark'),'Shared About/Header BrandMark owner missing');
const advanced=text('webui/private/scripts/advanced-settings.js');assert(advanced.includes('AdvancedSettingsV036=W.AdvancedSettings'),'Semantic AdvancedSettings must suppress removed legacy loader');
const transfer=text('webui/private/scripts/transfer.js');assert(transfer.includes('weigg:transfer')&&!transfer.includes('transfer-dock'),'Transfer service must not create a second transfer UI owner');
const session=text('webui/private/scripts/session.js');assert(session.includes('auth/logout')&&session.includes('probeSession'),'Verified logout contract missing');assert(session.indexOf('client.logout()')<session.indexOf('client.probeSession()'),'Logout must precede protected session probe');assert(session.includes('pageshow')&&session.includes('weigg.logoutGuard'),'BFCache/logout guard missing');const reentry=session.slice(session.indexOf('async function verifyReentry'),session.indexOf('function onPageShow'));assert(reentry.includes('AUTH-BFCACHE-FAIL-CLOSED'),'Fail-closed BFCache marker missing');assert(!reentry.slice(reentry.indexOf('catch')).includes('unlock()'),'Guarded BFCache/network failure must never unlock the private shell');
const header=text('webui/private/scripts/header.js');assert(header.includes('github-link')&&header.includes('blog-link')&&header.includes('logout-btn'),'Header utility actions incomplete');
console.log(`Semantic runtime contract passed: ${runtimeFiles.length} runtime files, 0 versioned asset filenames, single Settings CSS owner, fail-closed SessionGate.`);
