#!/usr/bin/env node
import crypto from 'node:crypto';
import fs from 'node:fs';
import path from 'node:path';
import {spawnSync} from 'node:child_process';
import {fileURLToPath} from 'node:url';
import {packCatalog} from './qb-webui-catalog.mjs';
const here=path.dirname(fileURLToPath(import.meta.url)),projectRoot=path.resolve(here,'..');
function arg(name,fallback=''){const prefix=`--${name}=`;const hit=process.argv.find(value=>value.startsWith(prefix));return hit?hit.slice(prefix.length):fallback;}
function required(name){const value=arg(name);if(!value)throw new Error(`Missing --${name}=...`);return value;}
function assert(ok,message){if(!ok)throw new Error(message);}
function replaceBuildSha(root,sha){const textExt=new Set(['.html','.js','.css','.json']);for(const entry of fs.readdirSync(root,{withFileTypes:true})){const file=path.join(root,entry.name);if(entry.isDirectory()){replaceBuildSha(file,sha);continue;}if(!entry.isFile())continue;if(!textExt.has(path.extname(entry.name))&&entry.name!=='GIT_SHA')continue;const source=fs.readFileSync(file,'utf8');if(source.includes('__WEIGG_GIT_SHA__'))fs.writeFileSync(file,source.replaceAll('__WEIGG_GIT_SHA__',sha),'utf8');}}
function sha256(file){return crypto.createHash('sha256').update(fs.readFileSync(file)).digest('hex');}
function walkFiles(root){const out=[];for(const entry of fs.readdirSync(root,{withFileTypes:true})){const file=path.join(root,entry.name);if(entry.isDirectory())out.push(...walkFiles(file));else if(entry.isFile())out.push(file);}return out;}
function runtimeSizeReport(root){let coreRuntimeBytes=0,releaseProfileBytes=0,translationBytes=0,redundantBytes=0,totalUncompressedBytes=0,maxFileBytes=0,maxFile='';let fileCount=0,releaseProfileFiles=0,translationFiles=0;for(const file of walkFiles(root)){const rel=path.relative(root,file).replaceAll('\\','/'),bytes=fs.statSync(file).size;fileCount+=1;totalUncompressedBytes+=bytes;if(bytes>maxFileBytes){maxFileBytes=bytes;maxFile=rel;}if(rel.startsWith('private/data/qb-release-profiles/')){releaseProfileBytes+=bytes;releaseProfileFiles+=1;}else if(rel==='private/data/qb-settings-native.txt'||/^translations\/webui_.+\.qm$/i.test(rel)){translationBytes+=bytes;translationFiles+=1;}else if(rel.startsWith('private/data/qb-settings/'))redundantBytes+=bytes;else coreRuntimeBytes+=bytes;}return{fileCount,coreRuntimeBytes,releaseProfileBytes,releaseProfileFiles,translationBytes,translationFiles,redundantBytes,totalUncompressedBytes,maxFileBytes,maxFile};}
export function buildWebuiDist({webuiRoot,catalogPath,outDir,sha,version}){
  webuiRoot=path.resolve(webuiRoot);catalogPath=path.resolve(catalogPath);outDir=path.resolve(outDir);assert(/^[0-9a-f]{40}$/i.test(sha),'Distribution requires an exact 40-character Git SHA.');assert(fs.existsSync(path.join(webuiRoot,'public/index.html'))&&fs.existsSync(path.join(webuiRoot,'private/index.html')),'Distribution source is not a valid WebUI root.');const sourceVersion=fs.readFileSync(path.join(webuiRoot,'VERSION'),'utf8').trim();assert(sourceVersion===version,`Distribution VERSION mismatch: ${sourceVersion} != ${version}`);
  fs.rmSync(outDir,{recursive:true,force:true});fs.mkdirSync(outDir,{recursive:true});const stage=path.join(outDir,'.stage'),root=path.join(stage,'WeiG-qB-WebUI');fs.mkdirSync(stage,{recursive:true});fs.cpSync(webuiRoot,root,{recursive:true,force:true});
  const packed=packCatalog(catalogPath,path.join(root,'private/data/qb-releases.json'));replaceBuildSha(root,sha);fs.writeFileSync(path.join(root,'GIT_SHA'),`${sha}\n`,'utf8');
  const runtimeCatalog=JSON.parse(fs.readFileSync(path.join(root,'private/data/qb-releases.json'),'utf8'));assert(Array.isArray(runtimeCatalog)&&runtimeCatalog.length>0,'Materialized distribution catalog is empty.');assert(fs.existsSync(path.join(root,'private/data/qb-settings-native.txt')),'Materialized distribution is missing compact qB-owned runtime IR.');assert(!fs.existsSync(path.join(root,'private/data/qb-settings')),'Materialized distribution retained retired qb-settings shards.');const translations=path.join(root,'translations'),qms=fs.existsSync(translations)?fs.readdirSync(translations).filter(name=>/^webui_.+\.qm$/i.test(name)):[];assert(qms.length>0,'Materialized distribution is missing minimal official qB WebUI QM assets.');
  const sizeReport=runtimeSizeReport(root);assert(sizeReport.redundantBytes===0,'Retired Settings translation assets remain in runtime payload.');assert(sizeReport.maxFileBytes<packed.maxStaticBytes,`Runtime file ${sizeReport.maxFile} exceeds qB static-file limit.`);assert(sizeReport.maxFileBytes<packed.projectMaxSingleFileBytes,`Runtime file ${sizeReport.maxFile} exceeds project single-file target.`);
  const zipPath=path.join(outDir,'WeiG-qB-WebUI.zip'),zipped=spawnSync('zip',['-qr',zipPath,'WeiG-qB-WebUI'],{cwd:stage,stdio:'inherit'});if(zipped.status!==0)throw new Error(`zip failed with status ${zipped.status}`);const digest=sha256(zipPath),zipBytes=fs.statSync(zipPath).size;sizeReport.zipBytes=zipBytes;fs.writeFileSync(path.join(outDir,'SHA256SUMS'),`${digest}  WeiG-qB-WebUI.zip\n`,'utf8');fs.writeFileSync(path.join(outDir,'GIT_SHA'),`${sha}\n`,'utf8');fs.writeFileSync(path.join(outDir,'VERSION'),`${version}\n`,'utf8');
  const linuxInstallerSource=path.join(projectRoot,'installers/install.sh'),linuxInstallerTarget=path.join(outDir,'install.sh'),windowsInstallerSource=path.join(projectRoot,'installers/install.ps1'),windowsInstallerTarget=path.join(outDir,'install.ps1');assert(fs.existsSync(linuxInstallerSource),'Canonical Linux installer is missing.');assert(fs.existsSync(windowsInstallerSource),'Canonical Windows installer is missing.');fs.copyFileSync(linuxInstallerSource,linuxInstallerTarget);fs.chmodSync(linuxInstallerTarget,0o755);fs.copyFileSync(windowsInstallerSource,windowsInstallerTarget);
  const manifest={schemaVersion:3,kind:'materialized-webui-dist',gitSha:sha,version,profiles:runtimeCatalog.length,qmAssets:qms.length,ownedCopyRegistry:'private/data/qb-settings-native.txt',legacySettingsAssets:false,packedCatalogBytes:packed.packedBytes,ownedCopyBytes:packed.ownedCopyBytes,bindingSetCount:packed.bindingSetCount,bridgeSetCount:packed.bridgeSetCount,nativeLocaleRoutes:packed.nativeLocaleRoutes,bridgeLocaleRoutes:packed.bridgeLocaleRoutes,sizeReport,linuxInstaller:'install.sh',windowsInstaller:'install.ps1'};fs.writeFileSync(path.join(outDir,'manifest.json'),JSON.stringify(manifest,null,2)+'\n','utf8');fs.rmSync(stage,{recursive:true,force:true});return{zipPath,digest,profiles:runtimeCatalog.length,qmAssets:qms.length,sizeReport,linuxInstaller:linuxInstallerTarget,windowsInstaller:windowsInstallerTarget};
}
const isMain=process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url));if(isMain){try{const result=buildWebuiDist({webuiRoot:path.resolve(projectRoot,required('webui-root')),catalogPath:path.resolve(projectRoot,required('catalog')),outDir:path.resolve(projectRoot,required('out')),sha:required('sha'),version:required('version')});console.log(`Built materialized WebUI distribution: ${result.profiles} profiles, ${result.qmAssets} minimal QM assets, ${result.sizeReport.totalUncompressedBytes} bytes uncompressed, ${result.sizeReport.zipBytes} bytes ZIP, sha256 ${result.digest}`);}catch(error){console.error(error?.stack||error);process.exit(1);}}
