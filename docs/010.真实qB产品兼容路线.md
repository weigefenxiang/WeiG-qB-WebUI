# 010. 真实 qB 产品兼容路线

Status: **P0 product roadmap / Phase A DONE / Phase B DONE / Phase C DONE / Phase D DONE / Phase E APPROVED / IN PROGRESS**

> `webui/**` 是唯一正式产品。目标不是把 Simulator 做得更像 qB，而是让同一份 WeiG WebUI 在 qBittorrent 4.1.0 起的所有已验证 official stable、当前 latest stable，以及未来新 stable / new major 上自动适配并保持正确产品语义。

## 1. 长期产品目标

同一份：

```text
webui/**
```

应能够：

```text
安装到 qB 4.1.0 起任意受支持 official stable
-> 自动识别真实 release / WebAPI / source capabilities
-> 自动选择 native / normalized / adapter / composition / derived
-> read-only / unavailable only when evidence requires
-> UI 只消费 canonical WeiG semantics
-> 不要求用户选择 qB4/qB5/qB6 专用包
-> 不要求手工选择 WebAPI compatibility mode
```

正式兼容链：

```text
qB 4.1.0
-> all verified 4.x stable
-> all verified 5.x stable
-> current stable
-> future stable
-> qB 6.x / 7.x / future x.x.x
```

## 2. 优先级

当前工程优先级固定为：

```text
P0 当前 latest official stable
P0 未来 official stable / future major
P1 qB 4.1.0 起所有历史 verified stable
P2 representative real-qB / release hardening
P3 supporting simulator fidelity
```

核心思想：不为了极少使用的老版本占用当前/未来版本适配资源。

### qB4.0.x / WebAPI v1

qB4.0.x 只保留 **FUTURE OPTIONAL** 架构位：

```text
Legacy WebAPI v1 Adapter
-> canonical WeiG semantics
```

当前不实现、不进 CI、不进 full-stable matrix、不占 Phase。若以后明确批准支持，只能通过独立集中 Adapter 接入，禁止把 v1/v2 判断散落进 UI、Settings、Torrent workspace。

## 3. Authority / 兼容架构原则

Authority：

```text
docs/001 + docs/010   product goal / support scope / phase priority
docs/002              current compatibility facts
DESIGN.md             Current Owner / UI / design system
docs/003              module architecture
docs/006              release / CI / validation tiers
docs/009              Virtual qB auxiliary tooling only
```

实际行为最终以当前 exact-SHA canonical runtime + upstream/source/real-qB evidence 为准。

长期结构：

```text
qB upstream official stable source
          |
          v
Source Extractors / generated stable facts
          |
          v
W.ReleaseProfile
          |
          +--> W.TorrentSemantics
          +--> W.CapabilityRegistry
          +--> W.SettingsSchema
          +--> W.QBClient
          |
          v
Canonical WeiG semantics
          |
          v
UI / presentation
```

业务层不得保存 patch/major version table。优先判断：

```text
source action exists?
source field exists?
source filter exists?
required parameters exist?
Preference getter/setter/type provenance?
reliable old-API composition available?
reliable local derivation available?
```

只有无法通过 source/API facts 证明的极少数历史异常，才允许进入集中 exception owner。

## 4. Product capability 状态

长期统一为：

```text
NATIVE
NORMALIZED
DERIVED
READ_ONLY
UNAVAILABLE
UNKNOWN
```

解析优先级：

```text
NATIVE
-> NORMALIZED / ADAPTER
-> reliable old-API composition
-> reliable DERIVED
-> READ_ONLY
-> UNAVAILABLE
```

future `UNKNOWN` 只对无法证明的具体 capability fail closed，尤其危险写操作；future version 本身不能因为 major/version number 被自动拒绝，也不能因为版本更高就自动继承 current latest semantics。

## 5. Read / Write 必须分离

禁止：

```text
tags = true / false
```

应该拆成：

```text
tags.read
tags.list/facet
tags.filter
tags.assign
tags.create
tags.delete
```

同理：

```text
Category read/filter != Category mutation
Tracker read/add/remove/edit/reannounce 彼此独立
Settings getter-only -> read-only
a getter + trustworthy setter/type -> editable
```

Phase B 与 Phase C 已证明 read/write 分离是长期兼容模型，而不是旧版本特例补丁。

## 6. 性能规则

兼容不能以暴力请求换功能。

禁止：

```text
N items -> N extra compatibility requests
high-cost compatibility polling
presentation caller self-probe
second business state / owner
```

优先复用 canonical bulk data，例如 `torrents/info` catalog，再供 Filters、Facets、Search/local projection、Columns、derived capabilities 多处消费。

## 7. 阶段总览

```text
Phase A — Source Facts Expansion                              DONE
Phase B — qB4.1+ Product Compatibility Core                   DONE
Phase C — Full Stable Product Compatibility Matrix           DONE
Phase D — Incremental Future Stable Admission / Last Known Good DONE
Phase E — Torrent Fields / Columns provenance                 APPROVED / IN PROGRESS
Phase F — Settings + Search/RSS/Logs/Details                  PLANNED
Phase G — Real-qB / Release Hardening                         PLANNED
Legacy WebAPI v1 Adapter                                      FUTURE OPTIONAL
```

Phase E approved start base：

```text
d300b52475a4b3f2349bbfe148400596e1911ee9
VERSION before product implementation: 0.3.58
```

每个阶段完成后必须 STOP、复盘、更新 canonical docs，再进入下一阶段。

## 8. Phase A checkpoint — DONE

```text
base: 4178c5a4302c347367ae04d0dd71fe54909d2ffa
implementation: c3d184d1f39164467d9fde2cfabdfa22a315fae5
post-A docs / Phase-B base: 1de58781dd9179507b2c170a7657d6681c898201
VERSION: 0.3.57
main: untouched
```

Phase A 建立 machine-readable facts：stable tags、qB/WebAPI/source SHA、actions/parameters、Torrent request/response fields、states/filters、Properties/Tracker/File/WebSeed fields、Preference getter/setter/type provenance、release evolution。

Parser 不理解 upstream 新形态时 fail closed，而不是猜测。

## 9. Phase B checkpoint — DONE

最终 candidate：

```text
SHA: 84b86956924d79b1086bb10a915e45599f9d6685
VERSION: 0.3.58
branch: dev
main: untouched
```

核心产品变化：

```text
native -> normalized -> source-proven derived filters
Category / Tags read/facet 与 taxonomy/write 分离
Reannounce / Remove Tracker exact source action gate
unsupported dangerous action fail closed before HTTP
bulk catalog reuse; no N-to-N request amplification
```

最终 smoke、all-stable source audit、Linux/Windows browser candidate、release packaging、full upstream evolution audit 均 PASS。

## 10. Phase C — Full Stable Product Compatibility Matrix — DONE

最终 checkpoint：

```text
base: 8a3a3eec3af567967621fa3ca28ecc9a241adba4
candidate: b14926b8d67b5919aa2c281762ed97c5cee46c21
VERSION: 0.3.58 unchanged
branch: dev
main: untouched
```

Phase C 把每个 official stable `>=4.1.0` 从 source/structure target 升级为 formal product target。

正式矩阵执行：

```text
W.ReleaseProfile
W.CapabilityRegistry
W.TorrentSemantics
W.SettingsSchema
W.QBClient
```

验证 Filters、Facets、Torrent Actions、当前 17 columns prerequisites、Settings Preference provenance、Search/RSS/Logs gating。

实际结果：

```text
65 / 65 official stable profiles PASS
qB 4.1.0 -> 5.2.3
50 qB 4.x + 15 qB 5.x
36 releases use at least one reliable local filter derivation
11 releases expose Tags read/facet before native taxonomy
latest 5.2.3 resolved Torrent actions: 20
latest 5.2.3 writable source-proven Preferences: 214 / 223
```

Phase C 没有修改 `webui/**`、tools 或 VERSION。

## 11. Phase D — Incremental Future Stable Admission / Last Known Good — DONE

Phase D 的目标不是为 qB6/qB7 预写 version table，而是保护已验证 historical facts，并让 future stable/new major 自动进入一条 fail-closed 的增量 admission 链。

Checkpoint：

```text
phase start base:        566eb1dd3ac9cd52f5b78e059a4e5a80c564685c
validated implementation: 8d2f9d162af9357328879206d71ab82714916ccc
VERSION:                 0.3.58 unchanged
branch:                  dev
main:                    untouched
```

### 11.1 Frozen Last Known Good

```text
tests/fixtures/qb-release-catalog.lkg.json
65 profiles
qB 4.1.0 -> 5.2.3
SHA-256 8b91742ac8eee276b7994c84445c681913430f220325a10afc8c931c89645ddb
```

该 catalog 是单一完整文件。它来自 Phase C validated evidence；seed 只有在 GitHub runner 重建字节级 SHA-256 完全一致并通过 product matrix 后才接纳。

旧 stable profile 默认是 Frozen Historical Facts。Future admission 不允许重写、覆盖或“顺便刷新”旧 profiles。

### 11.2 Tag-first discovery

Future Stable Watch 首先只比较官方 stable tag 名称：

```text
verify LKG
-> git ls-remote release-* tag names
-> stableAdmissionDelta
```

无新 stable：

```text
immediate exit
no upstream source checkout
no frozen-history product replay
no source extraction
no repository write
```

有新 stable 才进入：

```text
upstream source checkout
-> source-extract only newly discovered stable profiles
-> assert frozen prefix unchanged
-> diff from previous admitted stable
-> source audit support floor + new refs
-> focused product gate:
   support floor + previous admitted stable + new profiles
-> repository contracts / architecture guard
-> upstream + product capability diff
-> prepare next manifest
-> fresh exact dev race check
-> safe fast-forward catalog + manifest only
```

### 11.3 Admission failure

以下任一出现：

```text
parser failure
unknown API/response semantics
unknown or unsafe write semantics
product owner gap
product regression
history drift / frozen profile mutation
repository contract failure
```

必须：

```text
PENDING_REVIEW
candidate not admitted
Last Known Good unchanged
formal release baseline unaffected
```

禁止为了“最新版必须自动支持”放宽 parser、猜 response/write semantics、改旧 profile 或硬把 CI 变绿。

### 11.4 Historical product regression

只有通用 formal product compatibility code / matrix contract 变化时才跑历史 frozen product regression，例如：

```text
ReleaseProfile
QBClient
TorrentSemantics
CapabilityRegistry
SettingsSchema
full-stable product matrix / architecture guard
```

该 regression 直接消费 frozen profiles，不重新 source-extract old stable。仅新增 verified stable profile或 LKG/manifest/admission-only 变化不触发全部 historical product replay。

### 11.5 Heavy source/evolution audit

Stable discovery、source parser、source extraction、evolution ledger/tooling 变化触发：

```text
Full qB stable WebAPI evolution audit
```

`tools/qb-stable-admission.mjs` 已纳入该 trigger。普通 docs/UI/CSS/图片/翻译/VERSION-only/installer/test-only 不触发；heavy audit 不重复 npm test。

### 11.6 Architecture / proof guard

`tests/compat-architecture-contract.mjs` 在 exact checkout 上递归读取 `webui/private/scripts/**` 全部 `.js` 完整内容，阻止新增 scattered qB version-if；集中版本知识保留在 canonical resolver/adapter/exception owner。

Repo-wide ownership/absence/proof 固定要求：

```text
target exact SHA
+ complete tree identity
+ complete blob/file contents for semantic inspection
```

Code search/snippet 只可 discovery；旧 SHA、截断结果、局部片段和推测不能作为 repo-wide final proof。

### 11.7 Phase D evidence

Exact implementation `8d2f9d162af9357328879206d71ab82714916ccc`：

```text
smoke / npm test / syntax                         PASS
Phase D automation / admission contracts          PASS
Full qB stable WebAPI evolution audit             PASS
  Generate all supported stable source catalog    PASS
  Audit every official stable >=4.1.0             PASS
  WebAPI chronology/classification                PASS
```

Operational no-new-stable path was exercised at exact SHA：

```text
72fa7d4d1a7a138835e2a70bd5d963e150fb416b
Future Stable Watch                               PASS
Frozen 65-profile product regression              PASS
```

Evidence identity remains exact-SHA scoped；后续 SHA 不自动继承旧 runtime evidence。

### 11.8 Phase D final implementation scope

从 `566eb1dd3ac9cd52f5b78e059a4e5a80c564685c` 到 `8d2f9d162af9357328879206d71ab82714916ccc` 的 final changed paths：

```text
.github/workflows/frozen-stable-compat.yml
.github/workflows/stable-watch.yml
.github/workflows/upstream-compat.yml
package.json

tests/compat-architecture-contract.mjs
tests/fixtures/qb-release-catalog.lkg.json
tests/phase-d-automation-contract.mjs
tests/qb-product-capability-diff-contract.mjs
tests/qb-stable-admission-contract.mjs
tests/upstream-workflow-contract.mjs

tools/data/qb-stable-lkg.json
tools/qb-product-capability-diff.mjs
tools/qb-release-catalog.mjs
tools/qb-stable-admission.mjs
```

Phase D 没有 `webui/**` 产品变更，因此 VERSION 保持 `0.3.58`。

## 12. Phase E — Torrent Fields / Columns provenance — APPROVED / IN PROGRESS

Phase E approved start base：

```text
d300b52475a4b3f2349bbfe148400596e1911ee9
VERSION before product implementation: 0.3.58
```

Phase E 不是修现有 stable blocker。Phase C 已证明当前正式 17 个 Torrent column source prerequisites 在 frozen 65 stable 全部存在；Phase E 要把这件事升级为 formal runtime provenance owner，并为 future stable/new major 建立 fail-closed field semantics。

启动前 exact-SHA 复盘发现当前 field metadata 有三处重叠：

```text
ui.js         -> FIELDS[17] + W.TorrentFieldRegistry + mobile field preference
core.js       -> W.DataGrid.defaults overlap
components.js -> common field renderer branches
```

禁止增加第四套 field table。目标是 atomic ownership convergence：

```text
W.ReleaseProfile exact torrentInfoFields
-> W.TorrentFieldRegistry = single semantic field/provenance owner
-> NATIVE / NORMALIZED / DERIVED / UNAVAILABLE / UNKNOWN
-> W.DataGrid = layout/persistence only
-> W.Components = DOM/special renderer only
-> Desktop/Mobile Columns + Sort = callers of same effective registry
```

必要时允许 `READ_ONLY`，但只在有实际 read/write 语义时使用，不为凑枚举而添加。

### 12.1 Current 17 fields

首批不扩列，只 formalize 当前产品字段：

```text
name
size
progress
dlspeed
upspeed
eta
state
ratio
tracker
category
tags
num_seeds
num_leechs
save_path
added_on
completion_on
priority
```

对 frozen 65 stable，正式 expectation 是全部由 runtime owner 解析为 `NATIVE`。Full Stable PRODUCT Compatibility Matrix 必须实际加载 field owner 证明这一点，而不再只做 `profile.torrentInfoFields.includes(field)`。

### 12.2 Provenance contract

```text
NATIVE
  exact certified profile exposes canonical source field

NORMALIZED
  trustworthy historical/source alias maps centrally to canonical field

DERIVED
  reliably computed from already available canonical bulk data
  no extra per-torrent request / no new polling

UNAVAILABLE
  exact certified profile proves no reliable product path

UNKNOWN
  future/unadmitted profile cannot prove this specific field
  fail closed for that field only
```

Synthetic contracts 可验证 `NORMALIZED / DERIVED / UNAVAILABLE / UNKNOWN` 的通用行为，但不能把 synthetic facts 记作真实 qB release provenance。

### 12.3 User preference / effective availability

用户保存的 Columns/Mobile field preference 与当前 release 的 effective availability 分离。某 field 暂时 `UNAVAILABLE/UNKNOWN` 时不得从 saved preference 永久删除；future capability 恢复后可重新生效。

Desktop/Mobile 共享同一 semantic registry/state，只允许 responsive presentation 差异。

### 12.4 Sort boundary

Torrent field presence 本身不证明 qB server-side sort semantics。Phase E 不因为 field `NATIVE` 就猜 server sort support；sort key/provenance 必须独立可靠证明。若需要 local sort，只能复用已有 canonical bulk catalog，不能增加请求放大。

### 12.5 Owner cutover / DESIGN

`DESIGN.md` 是 Current Owner authority。Planned stable-responsibility file `webui/private/scripts/torrent-fields.js` 在正式 runtime cutover 前不得提前登记为新 Current Owner。

正式切换必须同一个 atomic product change 完成：

```text
new canonical field/provenance owner
-> switch all runtime callers
-> retire superseded ui.js/core.js field metadata ownership
-> keep DataGrid/Components in presentation responsibilities only
-> update DESIGN.md Current Owner
-> update contracts
-> bump VERSION to 0.3.59
```

禁止 old/new 双 active、runtime alias bridge、第二套 field state 或 post-render repair。

### 12.6 Phase E validation

正式 implementation 至少需要：

```text
smoke / npm test / syntax
exact direct TorrentFieldRegistry provenance contract
frozen LKG identity
architecture guard
65-profile frozen PRODUCT regression using runtime field owner
relevant Desktop/Mobile browser evidence
Linux candidate browser
Windows candidate browser
exact-SHA candidate package
```

只有 source parser/catalog/evolution tooling 真发生变化时才跑 Full qB stable WebAPI evolution audit；runtime owner/test/docs/version-only 不额外触发 heavy audit。

## 13. Phase F — Settings + Other Surfaces — PLANNED

Settings 当前 exact Preference provenance / fail-closed 架构较强，不推倒重写。

后续统一接入更细产品矩阵：

```text
Settings
Search
RSS
Logs
Torrent Details
Files / priorities
Peers
Trackers / WebSeeds
Categories / Tags
Metadata / Torrent Creator
Transfer / global / alternate limits
```

## 14. Phase G — Real-qB / Release Hardening — PLANNED

最终仍需要 representative real qB binary/server evidence。

验证层级：

```text
Level 1  upstream source/evolution facts                  DONE
Level 2  product compatibility contracts                 DONE
Level 3  Virtual qB / real Chrome representative matrix  PASS on Linux/Windows
Level 4  full/frozen stable product matrix               DONE / 65 stable
Level 5  representative real qB binary/server            PLANNED
Level 6  promotion-time frozen release evidence          PLANNED
```

Simulator PASS 永远不单独等价于真实 qB binary PASS。

## 15. Definition of Done

长期最终 DoD：

```text
same webui/** package
-> qB >= 4.1.0 supported official stable
-> auto-detect exact release/source capability
-> native/normalized/adapter/composition/derived selected automatically
-> no manual generation/WebAPI mode selection
-> no upper-major rejection by version number
-> read/write capability split
-> no guaranteed 404/501 user action
-> no N-to-N compatibility request amplification
-> every admitted stable passes required product admission
-> new stable automatically enters tag-first validation
-> historical verified profiles remain protected LKG
-> unavoidable/unknown gaps explicit and fail-safe
```

Phase E DoD：

```text
one runtime TorrentFieldRegistry semantic owner
current 17 fields have exact per-release provenance
65 frozen stable resolve current 17 fields through runtime owner
NORMALIZED/DERIVED/UNAVAILABLE/UNKNOWN rules directly tested
Columns/Desktop/Mobile consume effective field availability
saved preference survives temporary unavailability
no duplicated field ownership / no N-to-N compatibility requests
VERSION 0.3.59 for product implementation
relevant exact-SHA browser/candidate evidence PASS
```

## 16. 阶段纪律

每完成一个大阶段：

```text
STOP
-> fresh-read dev
-> before/after SHA
-> exact final changed paths
-> product/architecture changes
-> exact-SHA tests / source / browser evidence
-> known risks
-> update relevant canonical docs
-> list next-phase scope
-> only then continue
```

Phase E 已经用户批准并进入 IN PROGRESS；本 docs-start checkpoint 只建立 approved scope/base/DoD，不提前修改 Current Owner。Phase E 实现完成后必须再次 STOP，更新 canonical docs 后再进入 Phase F。`main` 只有用户明确批准发布时才允许修改。
