# 010. 真实 qB 产品兼容路线

Status: **P0 product roadmap / Phase A–F DONE / Phase G PLANNED / STOP**

> `webui/**` 是唯一正式产品。目标不是把 Simulator 做得更像 qB，而是让同一份 WeiG WebUI 在 qBittorrent 4.1.0 起的全部 verified official stable、当前 latest stable，以及未来 stable / new major 上自动适配并保持正确产品语义。

## 1. 长期目标

```text
qB 4.1.0
-> all verified 4.x official stable
-> all verified 5.x official stable
-> current latest official stable
-> future official stable
-> qB6.x / qB7.x / future x.x.x
```

能力解析：

```text
NATIVE
-> NORMALIZED / ADAPTER
-> reliable old-API composition
-> reliable DERIVED
-> READ_ONLY
-> UNAVAILABLE
```

future `UNKNOWN` 只关闭无法证明的具体 capability；尤其危险写操作 fail closed。禁止手动 compatibility mode、按 major 整体拒绝或自动继承 current latest semantics。

## 2. 优先级

```text
P0 current latest official stable
P0 future official stable / future major
P1 every verified stable >= qB 4.1.0
P2 representative real-qB / release hardening
P3 simulator fidelity
```

qB4.0.x / WebAPI v1：

```text
Legacy WebAPI v1 Adapter = FUTURE OPTIONAL
```

当前不实现、不进 CI/full-stable matrix、不占 Phase F/Phase G。

## 3. Authority / architecture

```text
docs/001 + docs/010   goal/scope/phase priority
docs/002              current facts
DESIGN.md             Current Owner/UI/design system
docs/003              architecture
docs/006              validation/promotion
canonical runtime + upstream/source/real-qB evidence = final behavior truth
```

长期结构：

```text
qB official source
-> generated exact stable facts
-> W.ReleaseProfile
   + W.CapabilityRegistry
   + W.TorrentSemantics
   + W.TorrentFieldRegistry
   + W.SettingsSchema
   + W.QBClient
-> canonical WeiG semantics
-> UI/presentation
```

业务层不维护 patch-version table作为最终 truth。

## 4. Phase overview

```text
Phase A — Source Facts Expansion                                DONE
Phase B — qB4.1+ Product Compatibility Core                     DONE
Phase C — Full Stable Product Compatibility Matrix             DONE
Phase D — Incremental Future Stable Admission / Last Known Good DONE
Phase E — Torrent Fields / Columns provenance                   DONE
Phase F — Settings + Tool/Detail Surface Provenance             DONE
Phase G — Real-qB / Release Hardening                           PLANNED / STOP
Legacy WebAPI v1 Adapter                                        FUTURE OPTIONAL
main promotion                                                  NOT STARTED
```

Phase F 已完成 canonical docs closure；**Phase G 不自动开始。**

## 5. A–F checkpoint summary

```text
Phase A implementation:
c3d184d1f39164467d9fde2cfabdfa22a315fae5
VERSION 0.3.57

Phase B candidate:
84b86956924d79b1086bb10a915e45599f9d6685
VERSION 0.3.58

Phase C candidate:
b14926b8d67b5919aa2c281762ed97c5cee46c21
65/65 stable product matrix PASS
VERSION 0.3.58

Phase D validated implementation:
8d2f9d162af9357328879206d71ab82714916ccc
Frozen LKG 65 profiles
VERSION 0.3.58

Phase E docs-start base:
97170de50dfd79c953cd93f64f6b5807147436eb

Phase E final validated implementation:
6443cc35f570e8f1ed7a7e078aa15efa1618ce25
VERSION 0.3.59

Phase E docs closure / Phase F reviewed base:
ca9ffaf221f3a18c4b9c9dbd7f5281e89009d6b9

Phase F final validated candidate:
7ae5493ab409af81abe25e579245160c26bfc7e3
VERSION 0.3.60
CI run 34125875685
Virtual qB Pages Source run 34125875723
```

Phase F exact candidate evidence：

```text
smoke PASS
Generate exact stable source catalog PASS
Audit every official stable >=4.1.0 PASS
Full Stable PRODUCT Matrix PASS
Linux Hosted Chrome PASS
Windows Hosted Chrome PASS
exact-SHA reusable release candidate package PASS
Virtual qB Pages Source PASS
```

Frozen LKG：

```text
tests/fixtures/qb-release-catalog.lkg.json
qB 4.1.0 -> 5.2.3
65 profiles
SHA-256:
8b91742ac8eee276b7994c84445c681913430f220325a10afc8c931c89645ddb
```

保持一个完整文件，不分片、不重写 verified history。

## 6. Phase F mission — DONE

Approved base：

```text
ca9ffaf221f3a18c4b9c9dbd7f5281e89009d6b9
```

Final validated candidate：

```text
7ae5493ab409af81abe25e579245160c26bfc7e3
VERSION 0.3.60
main untouched
```

已覆盖：

```text
Settings
Search
RSS
Logs
Torrent Details
Files / Peers / Trackers / WebSeeds
Categories / Tags
Metadata / Torrent Creator
```

从：

```text
version/endpoint assumptions
```

迁移到：

```text
exact source actions
+ action parameters
+ response-field facts
+ Preference getter/setter/type provenance
-> read/write/action/field-aware canonical product semantics
```

## 7. Phase F source-truth findings — IMPLEMENTED

### 7.1 RSS correction

qB upstream `release-4.1.0/src/webui/api/rsscontroller.h` 已声明 core RSS actions：

```text
addFolder
addFeed
removeItem
moveItem
items
setRule
renameRule
removeRule
rules
```

Frozen source catalog 同样证明 core RSS 从 qB 4.1.0 起存在，65/65。

此前把 RSS 最低能力绑到 `WebAPI >=2.1.0` 会错误隐藏 qB 4.1.0–4.1.2；Phase F 已修正式产品，并同步修 Virtual qB/Pages stale expectation。

### 7.2 Search / Logs

```text
Search core source actions:
qB 4.1.4 -> 5.2.3

Logs main/peers:
qB 4.1.0 -> 5.2.3
```

Final capability 已绑定 exact actions，而不是只使用 WebAPI/qB version threshold。

### 7.3 Settings

Settings schema 保留 Preference-level getter/setter/type provenance；Phase F 已补 endpoint-level：

```text
preferences read action
setPreferences write action
+
per-Preference writable descriptor
```

形成两层 fail-closed，没有重写 Settings。

### 7.4 Details

Source facts覆盖：

```text
properties                 65/65
files                      65/65
filePrio                   65/65
trackers                   65/65
addTrackers                65/65
webseeds                   65/65
torrentPeers               65/65
removeTrackers             60/65 from 4.1.5
editTracker                60/65 from 4.1.5
banPeers                   54/65 from 4.2.0
```

Catalog 包含 Properties/Tracker/File/WebSeed response fields 与 action parameters；Phase F 没有修改 source parser。

`editTracker` parameter evolution 已按 exact source parameters处理：旧 stable 使用 `origUrl/newUrl`，5.2.x 使用 `url` required 且 `newUrl/tier` optional。Runtime 不再按 version猜 form。

Unsupported/error 与 real empty 已分离，不再用 fake-empty compatibility。

## 8. Phase F work packages — COMPLETED

### F1 — Surface provenance substrate

```text
exact action presence
exact action required/optional parameters
exact response fields
certified exact profile => NATIVE/UNAVAILABLE
future unadmitted => UNKNOWN
unknown dangerous write => no HTTP
```

扩展现有 `W.ReleaseProfile/W.CapabilityRegistry/W.QBClient`，没有新造通用 duplicate owner。

### F2 — Settings endpoint provenance

```text
read endpoint gate
write endpoint gate
per-Preference writable gate
save-time defense in depth
```

保留现有 schema/render/UI owner。

### F3 — Search / RSS / Logs

```text
route != every child action
read != write/job mutation
RSS qB4.1.0 historical correction
Search exact source start qB4.1.4
Logs exact source start qB4.1.0
```

### F4 — Details / taxonomy / creator

```text
Properties / Files / Trackers / Peers / WebSeeds reads
File priority / tracker write / peer ban independent
Categories/Tags read/list/write independent
Torrent Creator add/status/delete/file independent
response-field-aware rendering
```

已删除 fake-empty兼容：

```text
unsupported/unknown != real empty != runtime error
```

### F5 — Admission / validation

```text
product capability diff
frozen 65 runtime matrix
synthetic future-major UNKNOWN/no-HTTP
Linux browser
Windows browser
Virtual qB/Pages alignment
exact [candidate] package
```

全部 required gates PASS。

## 9. Phase F non-goals — PRESERVED

```text
no Settings rewrite
no large UI redesign
no manual compatibility mode
no qB4.0.x/WebAPI v1
no N->N compatibility requests
no extra high-cost polling
no caller self-probe
no second business state
no duplicate capability owner
no Phase G real-qB hardening
```

第一版没有修改 source parser/discovery/evolution。

## 10. Phase F VERSION — COMPLETE

```text
0.3.59 -> 0.3.60
```

已同步四处 version identity：

```text
VERSION
webui/VERSION
package.json.version
package-lock.json root/package version
```

## 11. Phase F validation / DoD — SATISFIED

```text
all touched surfaces source-proven
read/write/action/parameter/field separation
unknown dangerous write fails before HTTP
qB4.1.0 RSS corrected
stale simulator/test corrected rather than product weakened
unsupported/error not rendered as fake empty
65/65 frozen product matrix PASS
future-major UNKNOWN/no-HTTP contracts PASS
Linux browser PASS
Windows browser PASS
final exact source catalog/all-stable audit PASS
exact-SHA candidate package PASS
canonical docs closure recorded
STOP before Phase G
```

Final candidate：`7ae5493ab409af81abe25e579245160c26bfc7e3`。

Runtime compatibility owner changes 已触发 frozen historical product regression。Source parser/discovery/evolution 不变，因此没有人为跑独立 heavy evolution workflow；final candidate 仍生成 exact catalog并执行 all-stable source/product audit，结果 PASS。

## 12. Product diff / future stable admission

Future admission继续：

```text
verify frozen LKG
-> tag-only comparison
-> no new stable: immediate exit
-> new stable only source extraction
-> preserve frozen prefix
-> source/product gates
-> formal product capability diff
-> PENDING_REVIEW on failure
-> safe fast-forward admission only
```

Phase F granular capabilities 已进入 formal product compatibility contract；禁止维护第二份 roadmap table。

## 13. Phase G — PLANNED / NOT STARTED

```text
representative real-qB binary/server validation
promotion-time frozen evidence
operator/live hardening
```

Virtual qB、source audit、browser candidate都不能代替这一层。

**当前 STOP。只有新的用户 checkpoint / 明确批准后才开始 Phase G。**

## 14. Main / release boundary

未经明确授权：

```text
no main write
no promotion
no release
no tag
no force
```

## 15. Git / proof discipline

```text
fresh-read dev exact HEAD
-> candidate only parent = fresh HEAD
-> compare expected paths
-> validate
-> fresh-read dev before update
-> if moved: discard/rebuild
-> force:false only
```

Repo-wide proof只认 target exact SHA + complete relevant tree/blob/file。Search/snippet 只能 discovery。每个大 Phase 完成后更新 canonical docs并 STOP。
