# 014. AI 协作与分支纪律

Status: **single current handoff authority**

长期硬规则先读 `docs/015.开发硬规则（长期版）.md`。本文件是唯一 mutable current-work / TODO / handoff authority；默认只读 STOP 前。Git history / Actions保存过程，不新增第二套 STATUS/HANDOFF/ROADMAP。

## 1. 当前项目状态

~~~text
working branch: dev only
handoff exact HEAD: fresh-read dev before work
validated product SHA: a75b54954d413686eaf0728d83d03d1ff03b7cce
main: DO NOT MOVE
[candidate]: DO NOT CREATE / DO NOT USE
Product VERSION owners: 0.3.130 / 0.3.130 / 0.3.130 / 0.3.130
latest published Release/tag: v0.3.61
qB support floor: 4.1.0
latest admitted stable: 5.2.3
Frozen admitted stable profiles: 65
current phase: A3 IMPLEMENTED — WAITING HUMAN REVIEW
human review: A FAIL / A1 FAIL / A2 FAIL / A3 fourth review NOT STARTED
~~~

A3产品代码的自动闭环已经完成。**停止继续修改产品代码，不进入B；等待用户第四轮真实 qB 人工复核。**

### 已完成的 A3 产品收口

- canonical Frozen Settings/common catalog identity已materialize并通过runtime fail-closed identity检查；
- source-native `displayFormat/source-action/dynamicOptions/writeOnly` interaction metadata已由generic decoder接入；
- WebUI Password write-only draft与最终save `omitEmpty`语义分离；
- dynamic Select不再被single-row shortcut降级，继续走canonical Select；
- `W.QBClient.settingsDynamicOptions()` 已成为source-proven runtime dynamic options的generic transport owner；
- RSS source-action、long-copy intrinsic layout、Scheduler zero-padding、official qB source/context zh_CN均进入同一正式 owner链。

没有为具体Preference key建立第二套Settings owner；main未移动，未创建/使用 `[candidate]`。

## 2. 当前验证结果

validated product SHA `a75b54954d413686eaf0728d83d03d1ff03b7cce`：

- CI #1962 / 35446437757 = **SUCCESS**；
- smoke = **SUCCESS**；
- Linux semantic UI **9/9 SUCCESS**：theme / torrent-workspace / sidebar-capability / adaptive-ui / runtime / torrent-field-provenance / settings-fidelity / feature-parity / feedback；
- `settings-fidelity` 明确PASS：real pointer Select、source-action RSS、write-only Password、dynamic network options、zero-padded Scheduler、layout geometry、verified save/reread、official zh_CN、qB 5.2.3 exhaustive graph audit；
- Virtual qB Pages Source #1050 / 35446437763 = **SUCCESS**。

Source/admission等被ordinary request classification明确SKIP的昂贵job不计作本轮PASS证据；本阶段依赖的 ordinary smoke/UI/Pages均为真实SUCCESS。**SKIPPED != PASS**。

## 3. 当前最大 blocker

1. 唯一当前 blocker 是 **A3 第四轮真实 qB 人工复核尚未开始**；自动门禁已经闭环。
2. 产品代码保持冻结，先由用户按第三轮同类 Options 场景真实操作复核。
3. 若人工复核 FAIL：只复现并修真实 generic root cause，然后对新的product exact SHA重跑 final ordinary CI/browser/Pages；旧SHA证据不继承。
4. 若用户明确 PASS：A3进入人工通过态，再运行 required admitted-version A full validation。
5. required admitted-version A full validation也通过后，A才可标记 VERIFIED DONE；此前不得开始B。

## 4. 其它未完成项

~~~text
A3  fourth real-qB human review -> required admitted-version A full validation
B   Torrent Status / Sidebar facets
C   qB-owned copy exact source/context + official translation
D   RSS final closure：feeds/articles/read-state/census/common identity/actions
E   Torrent Detail final inventory/copy/action audit
F   integrated compact-runtime/common identity/census/mixed-stale rejection closure
~~~

A–F全部完成且final dev SHA冻结后，才考虑 `docs/011` Real qB Full Frozen Matrix。当前禁止Candidate/main/tag/Release。

## 5. 分支纪律

~~~text
dev only
main DO NOT MOVE
[candidate] DO NOT CREATE / DO NOT USE
no tag / Release
no force push
~~~

## 6. 一键复制给下一位 AI

~~~text
按顺序完整阅读：015 → 014（仅STOP前）→ 002/005。读完后 fresh-read dev/main exact HEAD；A3 final ordinary CI/browser/Pages 已绿，停止代码修改，等第四轮人工复核，仅dev；SKIPPED != PASS。
~~~

## 7. 当前停止条件

**A3 产品自动实现与验证已完成；现在必须停止继续修改产品代码。** 当前尚未“整个A阶段完成”，因为第四轮真实 qB 人工复核与其后的 required admitted-version A full validation尚未执行。不得进入B。

==================================================
STOP — COMPLETED ARCHIVE BELOW
AI 默认不要继续读取。
只有排查 regression / historical decision / 用户明确要求时才读取。
==================================================

## ✅ 已完成归档

### Foundation — RSS source-driven rule fidelity
RSS rule source semantics、historical/modern tri-state/default-null、unknown-field preservation与 `rss-compat.json` lazy runtime foundation已建立；D整体尚未 VERIFIED DONE。

### Foundation — Preferences source/admission
Source-native extractor、keyed compact IR、dynamic native tabs、independent tabs/preferences/bindings census、catalog identity与 Preference-level value projection 已建立；formal browser runtime cutover已完成实现，仍需final exact-SHA automated validation与人工复核。

### ✅ Validation infrastructure — bounded Preferences source gate
历史 `6e834a92... / CI #1847 / 35136160556` 的约6小时取消事故已通过 profiling/root-cause修复；legacy dynamic-regex catastrophic backtracking被替换为锚定线性解析；正式 gate改为 base + 16-way shards + strict aggregate。

### ✅ Source write-proof follow-up
`4c89db25...` 首次提交 direct-string write proof后，CI #1855因 regression fixture变量 typo失败；随后 `9456fac7...` 修正 fixture，CI #1856 与 Pages Source #944 SUCCESS。该 exact SHA 重新证明65 releases、16/16 shards、65/65 census，并产出311540-byte source-native Preferences compact；这只关闭 source/admission evidence，不代表 A 产品完成。
