# 014. AI 协作与分支纪律

Status: **single current handoff authority**

长期硬规则以 `docs/015.开发硬规则（长期版）.md` 为最高约束。本文件是唯一 mutable current-work / handoff / TODO authority。**默认只读到 `STOP — COMPLETED ARCHIVE BELOW`；除非排查 regression、历史决策或用户明确要求，否则不要读取完成归档。** Git history / Actions / artifacts 保存详细历史，不新增平行 STATUS、TODO、HANDOFF、ROADMAP 或 FINAL-PLAN。

## 1. 当前项目状态

```text
branch: dev only
reviewed dev SHA: f6c96c2da620320929140a081e79aac70ba27bc1
reviewed main SHA: ba8ee49886fad159e76014514b5a0a4ba803bc29
main: DO NOT MOVE
[candidate]: DO NOT CREATE / DO NOT USE
Product VERSION: 0.3.107
latest published Release/tag: v0.3.61
qB support floor: 4.1.0 official stable
latest admitted stable: 5.2.3
Frozen admitted stable profiles: 65
current plan: IN PROGRESS — A–F Native Surface Projection
current primary product blocker:
  A formal Settings runtime has not atomically cut over to source-native Preferences facts
current validation blocker:
  NONE — Preferences exact-source admission is bounded, sharded and closed on current source/tooling checkpoint
latest source/tooling evidence:
  CI #1853 / run 35326167117 SUCCESS on f6c96c2da620320929140a081e79aac70ba27bc1
```

本文件写入后 dev HEAD 会变化；上面的 SHA 是本次状态复核起点，不是永久 current HEAD。任何实际开发/写入前必须 fresh-read dev/main 与相关 Actions。

### 状态定义

```text
🔴 BLOCKED
🟡 IN_PROGRESS
🟠 IMPLEMENTED — WAITING HUMAN REVIEW
🔵 HUMAN REVIEWED — WAITING FULL VERSION VALIDATION
⚪ PLANNED / NOT STARTED
✅ VERIFIED DONE
```

只有“正式实现 + applicable focused/browser/source evidence + 用户明确人工复核 PASS + 该功能要求的 admitted-version full validation PASS + docs同步”全部满足，才能标记 ✅ VERIFIED DONE。AI 不得自行把自动化测试 PASS 当作“人工复核 PASS”。

## 2. 接手读取规则

按顺序完整阅读：

```text
1. docs/015.开发硬规则（长期版）.md
2. docs/014.AI协作与分支纪律.md —— 默认只读 STOP 之前
3. 014 当前任务明确引用到的专项 docs / DESIGN 对应章节
```

不要再默认完整重读所有已完成领域文档。比如当前只处理 A Settings 时，优先读取 `002 / 003 Settings部分 / 005 / DESIGN Settings部分` 与相关代码；Torrent Detail、installer、release-grade real-qB 等无关内容不必整份重读。

读完后 fresh-read dev/main exact HEAD、VERSION owners、latest Release/tag、当前 exact-SHA Actions/checks 与当前任务相关代码。旧聊天、旧 SHA、旧 Actions、014 完成归档都只能作为历史线索。

## 3. 当前最大 blocker

### 🟡 Product blocker — A Settings formal runtime atomic cutover

Preferences extraction / source admission 的独立 validation blocker 已在 source/tooling checkpoint `f6c96c2da620320929140a081e79aac70ba27bc1` 闭环；不要再把它放在当前任务前面。最新 CI **#1853 / 35326167117 = SUCCESS**，65-release exact source、16-shard aggregate、independent census 与 compact contract均已闭环。

当前真正未完成的是正式产品 owner 切换：

```text
source-native Preferences IR
-> formal settings-compat runtime shape
-> W.CapabilityRegistry
-> W.SettingsSchema
-> W.SettingsRenderer
-> projection.safeWrite in browser write path
-> retire superseded manual qB-owned Settings truth
```

默认下一步：

```text
1. fresh-read current Settings runtime / CapabilityRegistry / settings-compat / renderer
2. 设计 source-native compact runtime shape与exact identity binding
3. 同一coherent change修改webui/** canonical owner与caller
4. 接入projection.safeWrite与read/display conversion
5. 同批退休SURFACES / SECTION_ORDER / SECTION_TITLES / META / FAMILY_RULES / superseded add() truth
6. 只在实际暴露新的source gap时回补最小tools/tests
7. focused Settings/browser evidence
8. 产品实现完成后进入人工复核状态；不能因source gate已PASS就把A标记完成
```

Product First继续生效：除非 A cutover暴露新的 exact source blocker，否则不要继续扩 Preferences tooling、重复 profile旧性能事故或再次运行无必要的全版本 source gate。

## 4. 🟡 正在进行

### A — Options / Preferences

状态：**🟡 IN_PROGRESS**

最终目标：

```text
exact qB Preferences source
+ exact app/preferences descriptors
+ native tabs/sections/order/control/options/unit/dependencies/copy
+ source raw <-> UI projection
+ independent tabs/preferences/bindings census
∩ runtime GET /api/v2/app/preferences
-> bounded compact Settings contract
-> W.CapabilityRegistry
-> W.SettingsSchema
-> W.SettingsRenderer
-> canonical qB Options UI
```

Dangerous write 同时要求：

```text
certified exact/equivalent release
AND setPreferences action proven
AND Preference setter/type proven
AND projection.safeWrite == true
AND Preference exists in current runtime app/preferences
```

当前正式产品仍未完成的事实：

```text
settings-schema.js 仍拥有 SURFACES / SECTION_ORDER / SECTION_TITLES / META / FAMILY_RULES / many add()
settings.js 仍有 Listening port + Random 与 Schedule From/To compound logic
CapabilityRegistry Settings domain仍消费 legacy-shaped runtime contract
source-native Preferences IR尚未成为 formal browser runtime owner
```

当前执行优先级是 **PRODUCT FIRST**。Preferences source validation blocker已解除，默认直接继续 formal `webui/**` atomic cutover。正式 cutover需要同一 coherent change完成新 owner生效、caller切换、safeWrite接入、superseded manual qB-owned truth退休、tests/docs同步；禁止 source-native + legacy 双 active。只有实际产品切换暴露新的 exact source gap时，才允许回补最小 tools/tests。

A 状态迁移：

```text
🟡 IN_PROGRESS
-> 产品实现 + focused/browser PASS
-> 🟠 IMPLEMENTED — WAITING HUMAN REVIEW
-> 用户明确人工复核 PASS
-> 🔵 HUMAN REVIEWED — WAITING FULL VERSION VALIDATION
-> A领域 admitted-stable full validation PASS
-> ✅ VERIFIED DONE
```

## 5. 🟠 已实现，等待人工复核

当前：**NONE**。

只有真正完成当前功能实现与适用 focused/browser/source验证后，才能把项目从“正在进行”移动到这里。自动测试不能替代用户人工复核。

## 6. 🔵 人工复核通过，等待全版本验证

当前：**NONE**。

只有用户明确说明实际功能/界面复核通过后才能进入这里。随后运行该功能要求的 admitted-version full validation；全版本验证未闭环时不得归档为完成。

## 7. 其它未完成项

### ⚪ B — Torrent Status / Sidebar facets
source-derived native status/filter/facet inventory/order/semantics -> generic Library state/projection -> WeiG Path last；退休固定 qB facet owner。

### ⚪ C — qB-owned copy
所有正式 qB-owned runtime text绑定 exact `{source,context}` + exact-release official translation；official target缺失只回 upstream English；WeiG-owned copy保持独立。

### ⚪ D — RSS final closure
保留已完成的 source-driven rule fidelity；继续补 feeds/articles/read-state/Downloader independent census、copy route、common identity 与 action completeness。不得回退 smartFilter / stopped / tri-state/default-null / unknown-field preservation。

### ⚪ E — Torrent Detail audit
保留成熟 `CapabilityRegistry.torrentDetailUi -> QbUiEvidence -> generic Detail runtime`；只补 inventory/copy/action真实缺口，不建立第二 renderer/state/polling/column/scroll owner。

### ⚪ F — integrated source/runtime closure
RSS common identity/compiler integration、全部 runtime/distribution domain identity agreement、mixed/stale bytes fail closed、remaining census/coverage closure、最终 integrated admitted-stable offline/Virtual matrix。

A–F 全部完成、final dev SHA冻结后，才考虑 `docs/011` 的 Real qB Full Frozen Matrix。当前禁止 Candidate/main/tag/Release。

## 8. CI / Validation Health

当前 source/tooling checkpoint：

```text
exact SHA: f6c96c2da620320929140a081e79aac70ba27bc1
CI: #1853 / 35326167117
overall: SUCCESS
wall clock: 2026-09-18 08:47:26Z -> 08:51:25Z (~3m59s)
Virtual qB Pages Source: #941 SUCCESS
base catalog + RSS source/compact/full contract: SUCCESS
Preferences matrix: 16/16 exact-SHA shard artifacts present
aggregate: expected 65 / executed 65 / missing 0 / duplicate 0 / unexpected 0
independent Preferences census: 65/65 complete
compact Preferences IR: 65 releases, 308791 bytes
Frozen releaseSetSha256: 67abde3a4649d1142d798d5d3c14eb53b1f1eaf7fba4cf14d5cdebeb03161888
```

Workflow 配置 `max-parallel: 16`；GitHub hosted runner调度并不保证16个同时获得runner，本次观察到至少12个 shard并行运行，但最终16个 shard artifact全部存在。抽样 shard 中真实 Preferences extraction约37–63ms/release；当前较大成本已转为checkout/hydration，其中一个 partial-clone shard的direct object hydration失败后 bounded refetch约18秒，但完整 gate仍在3–5分钟预算内，因此当前不作为 blocker。若未来重新超过预算，再以日志证据优化，不提前扩工具。

统一时间预算：

```text
< 3 min      正常
3–5 min      记录耗时，判断是否天然可分片
> 5 min      必须输出 progress/timing；可独立任务优先 matrix/shard
> 10 min     release/locale/fixture 等天然可分片全量任务原则上不得继续单 runner
> 30 min     无明确合理原因和持续可观察进度 => 验证设计异常，停止并查根因
```

默认并发策略：

```text
naturally independent full-version work
-> bounded shards
-> max-parallel: 16
-> 65 releases通常约16 shards × 4–5 releases
-> per-shard artifact
-> one aggregate
```

Aggregate 必须来自同一 exact SHA + 同一 Frozen expected set/catalog identity，并证明：

```text
executed == expected
missing == []
duplicate == []
unexpected == []
FAIL == 0
BLOCKED == 0
```

不可安全分片的工作保留单 owner，但必须提供阶段进度和耗时。长任务优先 instrumentation/profile root cause，禁止用 blind timeout/rerun 掩盖问题。

`SKIPPED != PASS`。旧 SHA、cancelled/superseded run、其它 SHA 的 shard都不能证明 current exact SHA。

## 9. 分支 / VERSION / 写入纪律

```text
dev only
main DO NOT MOVE
[candidate] DO NOT CREATE / DO NOT USE
no tag / Release without explicit user approval
no force push
```

每个 mutation batch前 fresh-read dev；构造 final commit 后、update ref前再次 fresh-read dev做 race check。docs/tests/tooling/workflow-only变化不 bump Product VERSION；正式 `webui/**` 产品 bytes变化时才同步所有 version owner。

一个 coherent feature先完成计划内 code/docs/tests/fixture/tooling变更，再对 final feature SHA做适用验证；禁止“改一个文件 -> 等一次CI -> 再改一个文件 -> 再等一次CI”。

## 10. 一键复制给下一位 AI

```text
接手项目：weigefenxiang/WeiG-qB-WebUI。

按顺序完整阅读：
1. docs/015.开发硬规则（长期版）.md
2. docs/014.AI协作与分支纪律.md，仅阅读 STOP — COMPLETED ARCHIVE BELOW 之前
3. 只阅读014当前任务引用到的专项docs与DESIGN对应章节

读完后 fresh-read dev/main exact HEAD、四个VERSION owner、latest Release/tag、current exact-SHA Actions以及当前任务相关代码。不要把旧聊天、旧SHA、旧Actions或014完成归档当current truth。

只在dev工作；main不动；不要创建/使用[candidate]；不要tag/Release。Product First：tools/tests只解除具体product blocker或证明即将进入runtime的contract。

当前A为IN_PROGRESS。Preferences extraction/source admission validation blocker已经在 f6c96c2d / CI #1853闭环，不要重复做旧profiling或继续扩source tooling。

现在直接做A Settings formal webui atomic cutover：source-native compact runtime -> CapabilityRegistry -> SettingsSchema -> SettingsRenderer -> projection.safeWrite，并在同一coherent change退休superseded manual qB-owned truth。A实现+focused/browser通过后只标记 IMPLEMENTED_WAITING_HUMAN_REVIEW；只有用户明确人工复核PASS才能进入 HUMAN_REVIEWED_WAITING_FULL_VALIDATION；A领域admitted-stable full validation也PASS且docs同步后才标记 VERIFIED_DONE，并把A移动到014的STOP之后。

之后依序B -> C -> D -> E -> F。每个功能都遵循相同状态迁移。A–F全部VERIFIED DONE、final dev SHA冻结后，才运行最终 ordinary CI/browser/Pages 与需要的release-grade real-qB evidence。Candidate/main/tag/Release仍需用户之后明确授权。
```

## 11. 当前停止条件

当前不是 COMPLETED。只有 A–F 各自达到 ✅ VERIFIED DONE、superseded owners退休、阶段required evidence闭环、final ordinary CI/browser/Pages闭环，并在最终需要时完成 `docs/011` real-qB evidence，才能把项目状态改为 COMPLETED / CURRENT TODO: NONE。

==================================================
STOP — COMPLETED ARCHIVE BELOW
AI 默认不要继续读取。
只有排查 regression / historical decision / 用户明确要求时才读取。
==================================================

## ✅ 已完成归档

### Foundation — RSS source-driven rule fidelity
- smartFilter source-driven；
- qB 4.6+/5.x使用 `torrentParams.stopped`；
- historical/modern tri-state/default-null语义保留；
- 编辑 existing rule保留 unknown fields；
- `rss-compat.json` 已是正式 lazy runtime domain。
- 注意：这只是 D 的已完成 foundation，不代表 D 整体 VERIFIED DONE。

### Foundation — Preferences source/admission
- Preferences source-native extractor + keyed compact IR 已存在；
- 固定8-tab whitelist已退休；
- independent tabs/preferences/bindings census foundation已存在；
- Preference-level `identity/scale/switch-map/unproven + safeWrite` 已存在；
- common Frozen catalog identity foundation已存在。
- 注意：A formal runtime cutover仍未完成，因此 A 不是 VERIFIED DONE。

### ✅ Validation infrastructure — Preferences extraction / 65-release source admission
- 历史失败：`6e834a92...` / CI #1847 / 35136160556 的 Preferences extraction约6小时后被取消；
- bounded profiling把热点定位到 qB 4.6.7 `Speed -> alt_dl_limit -> extractQbPreferenceValueProjection()`；
- 根因是 legacy value-projection动态正则在 `$('alt_dl_limit_value').getProperty('value').toInt() * 1024` 路径发生灾难性回溯；
- parser改为锚定、线性的 modern/legacy token + numeric factor解析，并增加 qB 4.x variable-backed rate-limit regression contract；
- focused修复证据：qB 4.6.7完整 Preferences extraction约162ms，原 `alt_dl_limit` projection恢复为 `scale:1024, safeWrite:true`；
- 正式 gate改为“一次base catalog/RSS -> 16-way Preferences shards -> strict aggregate -> compact -> independent census/full contract”；
- final source/tooling checkpoint：`f6c96c2da620320929140a081e79aac70ba27bc1`，CI #1853 / 35326167117 SUCCESS；
- 16/16 shard artifacts存在；最终65 releases、census 65/65 complete、compact 308791 bytes，Preferences source/census/compact共享 Frozen `releaseSetSha256=67abde3a4649d1142d798d5d3c14eb53b1f1eaf7fba4cf14d5cdebeb03161888`；
- 这是非产品 validation blocker的闭环，不代表 A 功能实现、用户人工复核或 A VERIFIED DONE。

### Foundation — Torrent Detail generic chain
- `CapabilityRegistry.torrentDetailUi() -> QbUiEvidence -> generic Detail runtime` 已建立；
- E 后续只做 completeness/copy/action audit与真实缺口，不重写第二套 Detail系统。
