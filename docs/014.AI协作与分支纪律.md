# 014. AI 协作与分支纪律

Status: **single current handoff authority**

长期硬规则以 `docs/015.开发硬规则（长期版）.md` 为最高约束；current facts见 `docs/002`，architecture owner见 `docs/003` + `DESIGN.md`，Torrent workspace/detail见 `docs/008`，发布见 `docs/006`，安装/runtime distribution见 `docs/007`。

本文件是唯一 mutable handoff / 当前未完成事项 authority。历史过程由 Git history / Actions / artifacts保存，不新增 patch-version“复盘/下一 AI 交接”文档。

## 1. 接手顺序

先完整阅读：

```text
docs/015.开发硬规则（长期版）.md
this file
docs/001.项目总方案.md
docs/002.兼容与实现状态.md
docs/003.项目架构.md
docs/005.统一交互与设置系统.md
docs/006.发布与晋级流程.md
docs/007.安装升级与手动部署.md
docs/008.Torrent工作区与状态所有权.md
docs/009.Virtual-qB-Lab.md
docs/010.真实qB产品兼容路线.md
docs/011.全版本真实qB验证规则.md
DESIGN.md
```

然后 fresh-read：`refs/heads/dev`、`refs/heads/main`、`VERSION`、latest Release/tag、current dev exact-SHA Actions。本文任何 SHA 都只是 checkpoint，不能当永久 HEAD；docs-only closure HEAD 也必须以 fresh-read 为准，不在本文件制造自指式“最终 HEAD”。

## 2. 当前产品状态

截至本 checkpoint：

```text
repository/product VERSION: 0.3.89
latest published Release/tag: v0.3.61
qB support floor: 4.1.0 official stable
latest admitted stable: 5.2.3
Frozen stable profiles: 65
0.3.89 product implementation checkpoint: 17a25074133d638f6e06bf0d8918986248106c95
0.3.89 validated product/test checkpoint: aedf659f51f5a518038864e3058aec486f64bb3d
```

0.3.89 当前方案已经完成的产品内容：

```text
Detail hero row 1: Detail identity left + torrent state right
Detail hero row 2: torrent title left + truthful progress right
Tracker / Peers / HTTP Sources / Content header and body share one left-aligned grid definition
Content uses native-like folder/file hierarchy without re-sorting qB response order
Content preserves exact source file index for writes
Content sticky parent stays inside the same viewport
Content collapse/re-expand restores a stable child anchor / bounded offset fallback
General secondary values use native-like parenthetical semantics
General whole-value URL comment opens safely in a new tab
General duplicate Progress / Availability presentation is omitted without deleting qB truth
Torrent more/actions add source-proven Super Seeding, queue increase/decrease, Copy name/hash/magnet and Export .torrent
Desktop sidebar keeps the existing realtime transfer card in the bottom/high-priority region on short-height layouts
Unproven desktop-only Open / Open containing folder is not fabricated as a WebAPI action
```

现有 owner 仍然是 source/profile-first：`W.ReleaseProfile` 提供 exact facts/action descriptors，`W.QbUiEvidence` 做 source projection，generic Detail runtime消费 descriptor，`W.SharedColumns` / `W.ColumnInteraction` / `W.VirtualList` 管 shared table projection/interaction，`W.Selection` 管 torrent-level action presentation，`W.TransferRuntime` 是 realtime transfer history owner，`W.QBClient` 是唯一 qB transport boundary。

## 3. 上一轮结果与验证证据

产品实现提交：

```text
17a25074133d638f6e06bf0d8918986248106c95
fix: complete Torrent Detail native interaction parity
```

首轮 CI #1709 与后续 CI #1710 都停在 `tests/compat-matrix.mjs` 对 canonical `docs/002` 措辞的旧 literal regex。实际产品/current-plan/runtime/source-provenance gates已经通过；失败不是产品回归。只同步 stale gate，没有恢复旧 owner、放宽 source/write safety，也没有修改产品 bytes：

```text
d283e9ef65c2b041c96c3e0164ec001e16242a7e
test: accept canonical qB 4.0.x scope wording

aedf659f51f5a518038864e3058aec486f64bb3d
test: accept canonical browser evidence wording
```

最终 validated product/test checkpoint：

```text
CI #1711 / run 34940278003: SUCCESS
Virtual qB Pages Source #799 / run 34940277997: SUCCESS
Virtual qB Pages #775 / run 34940290661: SUCCESS
Pages build: SUCCESS
Pages deploy: SUCCESS
18 hosted verification lanes: SUCCESS
```

Pages #775 对 exact `aedf659f...` 做了真实 materialization，而不是只跑 focused tests：

```text
Frozen profiles: 65
minimal QM assets: 66
uncompressed distribution bytes: 19,144,699
WeiG-qB-WebUI.zip bytes: 3,114,753
ZIP SHA256: d350e120cb860dce505b0908b375374ce07cabe2cd5a650cc4beee499a971823
exact Pages identity: PASS
downloads/dev/GIT_SHA + VERSION + SHA256SUMS + WeiG-qB-WebUI.zip: present and checked
manifest / owned-copy / QM / single-file budget checks: PASS
```

因此 0.3.89 product implementation、stale test alignment、ordinary CI、Pages materialization/build/deploy/live verification均已完成。Canonical current-facts / Torrent ownership / handoff已同步到 0.3.89；docs-only closure commit的具体 HEAD必须 fresh-read，不在本文硬编码成永久 truth。

## 4. 当前未完成事项判定

**当前 0.3.89 已批准方案已经全部完成并通过必要验证；当前没有已批准的产品开发 TODO，也没有剩余的 docs/handoff closure TODO。**

完成 canonical docs 阅读与 fresh-read 后，如果 current `dev` 没有新的 deterministic failure，应立即停止主动开发，保持在 `dev`，等待用户提出下一方案。

在用户提出下一方案之前，不得自行从以下事项中挑选工作：

```text
qb-release-profiles Batch B dedupe/rearchitecture
promotion/main/tag/Release
manual Current Stable Locale Matrix
Real qB Full Frozen Matrix
unrelated UI redesign/beautification
support-floor change
“顺手”优化 runtime/package/translation
已完成 Torrent Detail / Files tree / General / torrent actions / sidebar / Settings / Peer flags / Locale / runtime convergence 的二次重构
```

如果 fresh-read 发现新的 deterministic failure、损坏的 docs authority 或其他提交引入的实际回归，先判断是否属于用户当前明确批准 scope；不属于时先报告，不自动扩 scope。

## 5. 当前必须保持的硬边界

```text
normal development branch: dev
main: 不移动，除非用户明确授权
[candidate]: 不创建、不使用
promotion/tag/Release: 不自动进入
manual full matrices: 不自动触发
```

Product patch `VERSION` 只在正式 `webui/**` 产品内容变化时递增。Docs/tests-only closure产生新 SHA，但不再次 bump VERSION。

Exact-SHA evidence不能跨提交偷用：产品 implementation SHA 与后续 test/docs closure SHA必须分层记录；如果 classifier证明 docs-only change Pages-irrelevant，可以合法复用已验证的 materialized product payload，但不能把旧 SHA 的 ordinary CI当成新 SHA CI。

## 6. 累计复盘：需要避免的坑

### 6.1 用户现场失败与实际 UI 问题优先于“旧 gate 绿了”

用户指出的真实交互问题必须先验证 runtime/页面行为，再看 contract 是否 stale。旧 test 与已确认 source/runtime truth冲突时更新 test，不恢复错误实现。

### 6.2 Files hierarchy只能是现有 shared table / VirtualList 的 projection

Folder row、sticky parent、expand/collapse和anchor restore都不得形成第二 data/scroll owner。必须保持 qB files response顺序和exact source file index；folder aggregation是只读 presentation，不能给folder凭空增加file mutation capability。

### 6.3 Sticky parent / sticky header不得用 repair architecture

Sticky parent与header都只能在现有 viewport/stacking体系内实现。禁止第二 scroll container、scrollLeft同步、MutationObserver、monkey patch或post-render repair。

### 6.4 General native-like presentation不能变成第二 schema

Parenthetical secondary values、URL link与Progress/Availability omission都只是同一 source-generated `propertyLayout` 的 presentation。Source order、field binding与qB-owned copy不能复制到第二手写 registry。

### 6.5 Source-proven local action与transport action要分开

Local Copy使用 browser clipboard；qB mutation/export进入 `W.QBClient`。没有 source/WebAPI provenance的桌面客户端 action（例如当前未证明的 Open/Open containing folder）不得因为截图里存在就伪造 Alternative WebUI endpoint。

### 6.6 Torrent-level菜单不能退化为固定版本菜单

Super Seeding、queue actions、export等必须由 exact/equivalent release source/capability决定是否出现。WeiG可以保持双列presentation，但不能让presentation拥有第二 action truth。

### 6.7 Transfer sidebar只能改变布局优先级

短高度桌面可把 realtime transfer card保留在底部高优先级区域，但数据继续由 existing `transfer/info` + `W.TransferRuntime` 提供；不得增加第二 request/timer/history owner。

### 6.8 Exact Pages/materialization验证不能省略

Focused contracts全绿不等于 materialized hosted runtime正确。涉及 `webui/**` 的正式产品 change必须看 exact Pages build/deploy/live verification和真实distribution identity/size/manifest；0.3.89 已完成这一闭环。

### 6.9 不要为 stale contract改坏正确 runtime

0.3.89 的 CI #1709/#1710 都是 compatibility docs literal regex落后于 canonical wording。正确做法是确认产品/runtime truth后同步 test；禁止为过 gate倒退兼容文档或改变产品 owner。

### 6.10 Closure authority不能自指

`docs/014` 可以记录已经稳定验证的 product/test checkpoints，但不要在尚未验证的 docs-only提交里写“本提交 SHA = 最终 closure SHA”。Current docs HEAD必须由接手者 fresh-read；否则每次更新 closure都会立刻把自己写旧。

## 7. 一键复制给下一位 AI

```text
接手 WeiG-qB-WebUI 前，先不要改代码。

第一步：完整阅读 canonical docs：
1. docs/015.开发硬规则（长期版）.md
2. docs/014.AI协作与分支纪律.md
3. docs/001.项目总方案.md
4. docs/002.兼容与实现状态.md
5. docs/003.项目架构.md
6. docs/005.统一交互与设置系统.md
7. docs/006.发布与晋级流程.md
8. docs/007.安装升级与手动部署.md
9. docs/008.Torrent工作区与状态所有权.md
10. docs/009.Virtual-qB-Lab.md
11. docs/010.真实qB产品兼容路线.md
12. docs/011.全版本真实qB验证规则.md
13. DESIGN.md

第二步：fresh-read refs/heads/dev、refs/heads/main、VERSION、latest Release/tag、current dev exact-SHA Actions。任何旧对话或本文 SHA 都只能当 checkpoint。

当前产品 VERSION 是 0.3.89，latest published Release/tag 仍是 v0.3.61。0.3.89 当前方案已经完成并通过 ordinary CI、Pages Source、Pages exact materialization/build/deploy 和 18 hosted live verification lanes。

已完成，不要重新实现：两行 Detail hero、shared Detail 左对齐、Files hierarchy/sticky parent/anchor restore、exact source file index、General parenthetical secondary values/URL/duplicate-field omission、source-proven Torrent more actions、desktop sidebar transfer priority、现有 Detail source actions、Settings、Peer flags、Locale/runtime convergence。

完成上述 canonical docs 阅读与 fresh-read 后，如果 current dev 没有新的 deterministic failure，就停止主动开发，保持在 dev，等待用户提出下一方案。

未经用户明确授权：不要 push/merge main；不要创建或使用 [candidate]；不要 promotion/tag/Release；不要跑 manual full matrices；不要进入 qb-release-profiles Batch B。
```
