# 014. AI 协作与分支纪律

Status: **single current handoff authority**

先读 `docs/015.开发硬规则（长期版）.md`。本文件是唯一 mutable current-work / TODO / handoff authority；默认只读 STOP 前。A1–A3 历史已迁移到 `docs/016.Options历史方案归档.md`，**默认不要读取**；A4 详细 bounded plan 见 `docs/017.A4-Settings复合交互收口.md`。

## 1. 当前项目状态

~~~text
working branch: dev only
exact dev HEAD: fresh-read before every mutation
main: DO NOT MOVE
[candidate]: DO NOT CREATE / DO NOT USE
Product VERSION: 0.3.131 after A4 P0-23A runtime-owner batch
last automated-validated product: A3 / 0.3.130
current phase: A4 ROOT-CAUSE CLOSURE — P0-23A IMPLEMENTED / VALIDATION IN PROGRESS
human review: A/A1/A2/A3 all failed; fourth real-qB review opened A4
~~~

A3 ordinary CI/browser/Pages 自动证据仍作为历史 regression evidence保留，但**第四轮真实 qB Options 人工复核 FAIL 优先于“自动已绿”的完成判断**。当前禁止进入B，也禁止为了保住旧PASS去改test/隐藏真实failure。

## 2. 第四轮人工 FAIL：A4 输入

本轮截图/真实 qB 现象：

1. Downloads「运行外部程序」与 Connection「IP 过滤」出现文字被挤成竖排、section/control文案重复、组合控件布局失真。
2. Connection「I2P (Experimental) (requires libtorrent >= 2.0)」未按预期中文呈现。
3. Speed「计划备用速度限制的启用时间」标题重复；from/to语义丢失，时间拆成裸数字而不是 `08:00 -> 20:00`。
4. Downloads「Torrent 内容布局 / Torrent 停止条件」与 BitTorrent「加密模式」真实 qB 中无法修改。
5. Downloads「自动从此处添加 torrent」下面的子项集合/结构与原生不一致，需要按source-native grouped options重建呈现。
6. Web UI「用户名」后缺 source-proven Password 输入框。
7. 多个 Select/多选菜单展开后长文字被截断，需 shared overlay 统一完整展示且不越界。

这些现象不是七八个独立 key patch。A4 按四个 family追根：native group / compound layout fidelity；shared Select + writable/draft/save fidelity；credential/time/watch-folder compound semantics；exact qB-owned locale + shared overlay readability。

## 3. 已确认的 repo/source 事实

- source catalog 已有 Run external program、IP Filtering、Scheduler 的 section/preference grouping；
- source graph / compact evidence已有 `webui_password_text -> web_ui_password`；
- qB copy evidence包含 I2P 的 `OptionsDialog` source ref；
- current renderer存在 generic `compoundRow()` / fixed Settings row grid；当前CSS允许copy在窄列 `overflow-wrap:anywhere`，需用真实DOM证明是否造成逐字挤压；
- shared Select option当前使用单行 `nowrap + ellipsis`，这与第四轮“展开文字显示不全”直接冲突；
- A3 fixture/browser PASS 不能证明 real-qB runtime的全部视觉/交互 fidelity。

A4 P0-23A 已完成 root-cause audit：compound 重复/窄列来自 canonical `compoundRow()` 的 outer-copy + inline 投影；write-only Password 的 source presence 被错误绑定到 write-safety；shared Select 展开项由 `nowrap + ellipsis` 主动截断；Scheduler 已有 pair label + `zero-pad-2` source facts，可由 graph shape 泛化为 time-range。qB 5.2.3 官方 `webui_zh_CN.ts` 已确认 `I2P (Experimental) -> I2P（实验性）`。watch-folder 静态 source table 目前未进入 Control Graph，仍是下一 bounded source/compact gap。

P0-23A 只修改 canonical runtime owner + browser contracts，不建立 Preference-key / 中文字符串 / qB 5.2.x CSS 特判；watch-folder 未伪修。

## 4. 当前最大 blocker

1. 验证 P0-23A focused `browser-settings-fidelity` + **final ordinary CI/browser/Pages**；**SKIPPED != PASS**。
2. 继续补 watch-folder：从 exact source table / dynamic population facts 扩 generic Control Graph/compact metadata，再由同一 SettingsRenderer 投影，禁止 renderer 猜测。
3. 若真实 qB 仍出现 Select 不可写，沿 exact release identity / runtime key / `SettingsSchema.isWritable()` 抓真实 trace；fixture PASS 不能替代真实 runtime。
4. 自动证据闭合后做**第五轮真实 qB人工复核**。
5. 人工PASS后才运行 required admitted-version A full validation；该验证也PASS后 A才 VERIFIED DONE。

## 5. 其它未完成项

~~~text
A4 implementation + automated evidence
-> fifth real-qB human review
-> required admitted-version A full validation
-> A VERIFIED DONE
-> B Torrent Status / Sidebar facets
-> C qB-owned copy
-> D RSS final closure
-> E Torrent Detail final audit
-> F common compact-runtime / identity / census closure
-> freeze final dev
-> docs/011 release-grade Real qB Full Frozen Matrix
~~~

若后续阶段发现依赖缺口，可以做 bounded prerequisite fix，但不得另建第二 roadmap，也不得跳过当前阶段完成条件。

## 6. 分支纪律

~~~text
dev only
main DO NOT MOVE
[candidate] DO NOT CREATE / DO NOT USE
no tag / Release
no force push
fresh-read dev before every mutation
~~~

## 7. 一键复制给下一位 AI

~~~text
按顺序完整阅读：015→014(STOP前)→017→002/005；读完后 fresh-read dev/main exact HEAD，继续A4，仅dev。
~~~

## 8. 当前停止/继续条件

当前 **NOT COMPLETED**，但不是等待状态：下一位 AI 应继续 A4 root-cause audit与实现。只有 A4 自动验证 + 第五轮真实 qB人工PASS + required admitted-version A full validation全部通过后，才结束 A 并进入B。

==================================================
STOP — COMPLETED ARCHIVE BELOW
AI 默认不要继续读取。
A1–A3 历史方案已迁移到 docs/016.Options历史方案归档.md。
只有 regression / historical decision / 用户明确要求时才读取 016。
==================================================
