# 014. AI 协作与分支纪律

Status: **single current handoff authority**

长期硬规则以 `docs/015.开发硬规则（长期版）.md` 为最高约束。本文件是唯一 mutable current-work / handoff / TODO authority。**默认只读到 `STOP — COMPLETED ARCHIVE BELOW`；除非排查 regression、历史决策或用户明确要求，否则不要读取完成归档。** Git history / Actions / artifacts 保存详细历史，不新增平行 STATUS、TODO、HANDOFF、ROADMAP 或 FINAL-PLAN。

## 1. 当前项目状态

本轮从 `209d36e2...` 的 #1867 unit-complete source evidence完成 A final runtime promotion，并在 validated product SHA `8fc3175dcc1a3fc43279c59801a18e46d4c899cd` 完成适用自动验证。本节现在记录的是 **A 已实现、等待用户人工复核** 的 current handoff；自动化PASS不得冒充人工复核。

```text
working branch: dev only
validated product SHA: 8fc3175dcc1a3fc43279c59801a18e46d4c899cd
reviewed main: ba8ee49886fad159e76014514b5a0a4ba803bc29
main: DO NOT MOVE
[candidate]: DO NOT CREATE / DO NOT USE
next Product VERSION owners: 0.3.109 / 0.3.109 / 0.3.109 / 0.3.109
latest published Release/tag: v0.3.61
qB support floor: 4.1.0 official stable
latest admitted stable: 5.2.3
Frozen admitted stable profiles: 65
current plan: IN PROGRESS — A–F Native Surface Projection
current source/admission blocker: NONE on 209d36e2 — CI #1867 SUCCESS
current human blocker:
  USER HUMAN REVIEW REQUIRED; do not enter B until explicit PASS
```

最新 automated product evidence：validated product SHA `8fc3175...` 的 **CI #1869 / 35355486177 = SUCCESS**、**Virtual qB Pages Source #957 / 35355486052 = SUCCESS**、**Virtual qB Pages #849 / 35355502565 = SUCCESS**；18/18 browser verify lanes全部SUCCESS。`verify services` 已覆盖auth/services/protocol/drawer并通过 English → `zh_CN` save → app/preferences reread → automatic reload → reopen persistence。Source/admission evidence仍固定为 `209d36e2...` / CI #1867，65-release base + 16/16 Preferences shards SUCCESS。

A formal implementation已经删除legacy qB Settings semantic owners、接入unit-complete bounded source-native runtime、generic renderer与完整write fail-closed gate，并通过exact-SHA自动验证。当前必须停止继续B，等待用户实际界面/保存复核。

### 状态定义

```text
🔴 BLOCKED
🟡 IN_PROGRESS
🟠 IMPLEMENTED — WAITING HUMAN REVIEW
🔵 HUMAN REVIEWED — WAITING FULL VERSION VALIDATION
⚪ PLANNED / NOT STARTED
✅ VERIFIED DONE
```

只有“正式实现 + applicable focused/browser/source evidence + 用户明确人工复核 PASS + 该功能要求的 admitted-version full validation PASS + docs同步”全部满足，才能标记 ✅ VERIFIED DONE。自动化 PASS 不得冒充用户人工复核。

## 2. 接手读取规则

按顺序完整阅读：

```text
1. docs/015.开发硬规则（长期版）.md
2. docs/014.AI协作与分支纪律.md —— 默认只读 STOP 之前
3. 014 当前任务明确引用到的专项 docs / DESIGN 对应章节
```

读完后 fresh-read dev/main exact HEAD、四个 VERSION owner、latest Release/tag、current exact-SHA Actions/checks、当前任务相关代码与需要消费的 artifact。旧聊天、旧 SHA、旧 Actions、014 完成归档只作为历史线索。

**用户无需再次粘贴长背景。** 若用户只发“继续当前方案”“继续检查并完成”或等价简短续做指令，直接按本文件最前方活动项继续，不要先让用户复述项目。

## 3. 当前最大 blocker

### 🟠 Human review blocker — A manual product review

Source/admission输入固定为：

```text
checkpoint: 209d36e2d1b6e3a7cd7e791e9e1e82ee6cfb0bab
CI: #1867 / 35346821058 SUCCESS
Virtual qB Pages Source: #955 / 35346821091 SUCCESS
Preferences shards: 16/16 SUCCESS
strict aggregate: 65 expected / 65 executed / missing 0 / duplicate 0 / unexpected 0
independent Preferences census: 65/65 complete
Frozen releaseSetSha256:
  67abde3a4649d1142d798d5d3c14eb53b1f1eaf7fba4cf14d5cdebeb03161888
source-native Preferences compact:
  311581 bytes
  sha256 4693b9a538d243c6ea4665ebc86809f1d704cc7524677718cf8132764eaafbba
```

当前 implementation已经把这份exact IR作为唯一formal Settings semantics输入。剩余 blocker不是继续扩 parser/census，而是：

```text
validated product SHA 8fc3175...
-> user manually reviews Options/Preferences UI + save/reopen behavior
-> user explicitly reports PASS
-> A moves to 🔵 HUMAN REVIEWED — WAITING FULL VERSION VALIDATION
-> run A-required admitted-version full validation
-> only after PASS + docs sync may A become ✅ VERIFIED DONE
```

自动化PASS不得冒充用户人工复核。A进入🟠后必须停止继续B，等待用户实际点开/保存/查看Options确认。

## 4. 🟡 正在进行

当前：**NONE**。
## 5. 🟠 已实现，等待人工复核

### A — Options / Preferences

状态：**🟠 IMPLEMENTED — WAITING HUMAN REVIEW**

Validated product:

```text
Product VERSION: 0.3.109
validated product SHA: 8fc3175dcc1a3fc43279c59801a18e46d4c899cd
CI: #1869 / 35355486177 SUCCESS
Virtual qB Pages Source: #957 / 35355486052 SUCCESS
Virtual qB Pages: #849 / 35355502565 SUCCESS
browser verify: 18/18 SUCCESS
source/admission input: 209d36e2... / CI #1867 SUCCESS
Settings IR: 311581 bytes
Settings IR sha256: 4693b9a538d243c6ea4665ebc86809f1d704cc7524677718cf8132764eaafbba
```

人工复核只需检查实际产品体验，不重跑自动证据：

1. 打开 dev WebUI 的 Options / Settings，确认 qB native tabs、section/order、控件值正常，没有空白/错位/错误禁用。
2. 在 Advanced 检查 `.torrent file size limit` 显示 `MiB`、socket receive buffer显示 `KiB`。
3. 把 Language 从 English 切到 `zh_CN`，保存；确认自动重载后仍是中文，重新打开Settings仍显示 `zh_CN`。
4. 修改一个明确可写的安全数值Preference（例如 download limit），保存并重新打开，确认值持久化。
5. 确认没有旧synthetic `schedule_range` / Listening Random compound重新出现，也没有明显browser error。

**在用户明确回复人工复核PASS之前，AI必须停在这里，不进入B。**
## 6. 🔵 人工复核通过，等待全版本验证

当前：**NONE**。

只有用户明确说明实际功能/界面复核通过后才能进入这里；随后只运行该功能定义要求的 admitted-version full validation。全版本验证未闭环时不得归档。

## 7. 其它未完成项

### ⚪ B — Torrent Status / Sidebar facets
source-derived native status/filter/facet inventory/order/semantics -> generic Library state/projection -> WeiG Path last；退休固定 qB facet owner。

### ⚪ C — qB-owned copy
所有正式 qB-owned runtime text绑定 exact `{source,context}` + exact-release official translation；official target缺失只回 upstream English；WeiG-owned copy保持独立。

### ⚪ D — RSS final closure
保留已完成的 source-driven rule fidelity；继续补 feeds/articles/read-state/Downloader independent census、copy route、common identity 与 action completeness。

### ⚪ E — Torrent Detail audit
保留成熟 `CapabilityRegistry.torrentDetailUi -> QbUiEvidence -> generic Detail runtime`；只补 inventory/copy/action真实缺口，不建立第二 renderer/state/polling/column/scroll owner。

### ⚪ F — integrated source/runtime closure
RSS common identity/compiler integration、全部 runtime/distribution domain identity agreement、mixed/stale bytes fail closed、remaining census/coverage closure、最终 integrated admitted-stable offline/Virtual matrix。

顺序默认：**A -> B -> C -> D -> E -> F**。一个功能达到 ✅ VERIFIED DONE 后，才从本活动区移到 STOP 后简短归档；详细过程留 Git/Actions，不把014越写越长。

A–F 全部完成、final dev SHA冻结后，才考虑 `docs/011` 的 Real qB Full Frozen Matrix。当前禁止 Candidate/main/tag/Release。

## 8. CI / Validation Health

当前 validation policy：

```text
< 3 min      正常
3–5 min      记录耗时，判断是否天然可分片
> 5 min      必须输出 progress/timing；可独立任务优先 matrix/shard
> 10 min     天然可分片全量任务原则上不得继续单 runner
> 30 min     无合理原因和持续进度 => 停止并查根因/换验证设计
```

默认 independent full-version work：

```text
bounded shards
-> max-parallel: 16
-> 65 releases通常约16 shards × 4–5 releases
-> per-shard artifact
-> strict same-SHA aggregate
```

#1867 从 push 到完成约4m32s，16个 Preferences shard全部 SUCCESS，说明当前 source gate 已回到预算内。未来若某任务再次超过3–5分钟，先判断能否安全16-way分片；若不能分片则要求阶段进度与耗时。超过30分钟不得“再等等”，必须定位 parser/checkout/hydration/browser/runtime根因或选择更合适的验证方案。

Aggregate 必须来自同一 exact SHA + 同一 Frozen expected set/catalog identity，并证明：

```text
executed == expected
missing == []
duplicate == []
unexpected == []
FAIL == 0
BLOCKED == 0
```

`SKIPPED != PASS`。旧 SHA、cancelled/superseded run、其它 SHA shard都不能证明 current exact SHA。

## 9. 分支 / VERSION / 写入纪律

```text
dev only
main DO NOT MOVE
[candidate] DO NOT CREATE / DO NOT USE
no tag / Release without explicit user approval
no force push
```

每个 mutation batch前 fresh-read dev；构造 final commit 后、update ref前再次 fresh-read dev做 race check。多文件 coherent change优先一个 tree/commit。

每完成一个功能都必须同步 docs：
1. 未完成/进行中状态在014前部；
2. 实现+自动验证后移动到🟠；
3. 用户明确人工复核后移动到🔵；
4. required全版本验证+docs同步后才能✅；
5. ✅后从前部删除并在STOP后留短归档。

## 10. 一键复制给下一位 AI

用户最短可以只发：

```text
继续当前方案。
```

下一位 AI 收到这句后，直接执行下面规则，不要求用户重复背景：

```text
接手 weigefenxiang/WeiG-qB-WebUI。
按顺序完整阅读：
1. docs/015.开发硬规则（长期版）.md
2. docs/014.AI协作与分支纪律.md，仅到 STOP 之前
3. 仅阅读014当前任务引用到的专项docs/DESIGN章节

读完后 fresh-read dev/main exact HEAD、四个VERSION owner、latest Release/tag、current exact-SHA Actions与当前任务代码/artifact。

只在dev工作；main不动；不要创建/使用[candidate]；不要tag/Release。
从014最前方第一个 IN_PROGRESS/BLOCKED 项继续，不重做STOP后的完成史。
当前停在 A `🟠 IMPLEMENTED — WAITING HUMAN REVIEW`；用户未明确人工复核PASS前，不进入B。
优先修真实产品问题，再做必要验证；同一coherent feature完成后再验证。
自动验证PASS后只标 IMPLEMENTED_WAITING_HUMAN_REVIEW；用户明确人工复核PASS后才进入 HUMAN_REVIEWED_WAITING_FULL_VALIDATION；required全版本验证PASS且docs同步后才 VERIFIED_DONE并移到STOP之后。
之后依序B -> C -> D -> E -> F。
A–F全部VERIFIED DONE、final dev SHA冻结后，才做 final ordinary CI/browser/Pages 与需要的release-grade real-qB evidence。
```

## 11. 当前停止条件

当前不是 COMPLETED。只有 A–F 各自达到 ✅ VERIFIED DONE、superseded owners退休、阶段 required evidence闭环、final ordinary CI/browser/Pages闭环，并在最终需要时完成 `docs/011` real-qB evidence，才能把项目状态改为 COMPLETED / CURRENT TODO: NONE。

==================================================
STOP — COMPLETED ARCHIVE BELOW
AI 默认不要继续读取。
只有排查 regression / historical decision / 用户明确要求时才读取。
==================================================

## ✅ 已完成归档

### Foundation — RSS source-driven rule fidelity
RSS rule source semantics、historical/modern tri-state/default-null、unknown-field preservation与 `rss-compat.json` lazy runtime foundation已建立；D整体尚未 VERIFIED DONE。

### Foundation — Preferences source/admission
Source-native extractor、keyed compact IR、dynamic native tabs、independent tabs/preferences/bindings census、catalog identity与 Preference-level value projection 已建立；formal browser runtime cutover已完成实现，仍需final exact-SHA automated validation与人工复核。

### ✅ Validation infrastructure — bounded Preferences source gate
历史 `6e834a92... / CI #1847 / 35136160556` 的约6小时取消事故已通过 profiling/root-cause修复；legacy dynamic-regex catastrophic backtracking被替换为锚定线性解析；正式 gate改为 base + 16-way shards + strict aggregate。

### ✅ Source write-proof follow-up
`4c89db25...` 首次提交 direct-string write proof后，CI #1855因 regression fixture变量 typo失败；随后 `9456fac7...` 修正 fixture，CI #1856 与 Pages Source #944 SUCCESS。该 exact SHA 重新证明65 releases、16/16 shards、65/65 census，并产出311540-byte source-native Preferences compact；这只关闭 source/admission evidence，不代表 A 产品完成。
