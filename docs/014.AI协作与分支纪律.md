# 014. AI 协作与分支纪律

Status: **single current handoff authority**

长期硬规则先读 `docs/015.开发硬规则（长期版）.md`。本文件是唯一 mutable current-work / TODO / handoff authority；默认只读 STOP 前。Git history / Actions保存过程，不新增第二套 STATUS/HANDOFF/ROADMAP。

## 1. 当前项目状态

~~~text
working branch: dev only
handoff code HEAD: 9d02e252327403b21fb1a699589496c3f8d4cad9
main: DO NOT MOVE
[candidate]: DO NOT CREATE / DO NOT USE
Product VERSION owners: 0.3.125 / 0.3.125 / 0.3.125 / 0.3.125
latest published Release/tag: v0.3.61
qB support floor: 4.1.0
latest admitted stable: 5.2.3
Frozen admitted stable profiles: 65
current phase: A3 IN_PROGRESS
human review: A FAIL / A1 FAIL / A2 FAIL / A3 fourth review NOT STARTED
~~~

A3本轮已经把**处理过的代码全部推到dev**，当前不是完成态。

### 已推到 dev 的 A3 内容

- `74fcae4c...`：5.2.3 exact source证明 Select identity、RSS singleton getter、bounded `source-action`、network `dynamicOptions`、WebUI Password `writeOnly`、Scheduler `zero-pad-2`、RSS disabled source/context。
- `4f1b12e7...`：Product 0.3.125；正式 Settings/RSS consumer接入 `displayFormat/source-action/dynamicOptions/writeOnly`，generic long-copy layout，canonical settings/rss runtime bytes，focused contracts，并把真实 `settings-fidelity` 加进 UI matrix。
- `a47158cc...`：更新 accepted Settings runtime source contract。
- `160473f1...`：新增 Frozen identity rebind/materializer尝试与相关contract/workflow。

shared Select没有建立第二套owner；dynamic options走 `W.QBClient`；RSS helper复用 `W.RSSRules`；write-only secret仍走同一Settings draft/save owner。

## 2. 当前验证结果

### 已确认有效

- Virtual qB Pages Source：`4f1b12e...`、`a47158c...`、`160473f...`、`9d02e252...` 均 SUCCESS；当前 Pages run #35443468625 / #1041 = **SUCCESS**。
- source-side A3 facts不是当前首要blocker。
- branch正式 VERSION仍是 **0.3.125**；materializer临时生成0.3.126，尚未push，不能把0.3.126当仓库现状。
- `9d02e252...` CI #35443468637 / #1953 = **FAIL**；普通 smoke仍看见未materialize的旧0.3.125 identity，这是并行提交前的预期失败。
- 同一run的materializer已证明新方案通过 `current-plan`、RSS、Preferences source/compact/inventory/composite/runtime contracts，且没有再出现 mixed identity / action timeline回退；最终只被 `docs-authority-contract` 的014标题结构挡住。

**不要**通过放宽 `sameCatalogIdentity`、忽略 `sourceCatalogSha256`、删除assertion或用旧Frozen catalog全量重编译当前compact facts制造绿灯。Frozen identity只绑定已逐条验证的release/source identity；现有source facts保持原owner。

## 3. 当前最大 blocker

1. 修复 `docs/014` STOP前结构，使single-handoff authority重新满足 `docs-authority-contract`；这是当前materializer唯一已确认失败。
2. 重新触发Settings materializer，让 canonical `9f783354...` Settings + capabilities 写入dev并由完整 `npm test` 证明。
3. materialized产品commit后跑新增 `settings-fidelity` browser gate，至少验证：
   - Downloads/BitTorrent真实pointer Select与draft dependency
   - RSS source-action + disabled feedback
   - WebUI write-only Password：空显示、非空POST、保存后清空、GET仍无secret
   - Advanced network dynamic options
   - Scheduler 08:00/20:05 zero-padding
   - I2P official zh_CN
   - long-copy row geometry无重叠
4. final ordinary CI/browser/Pages 必须在同一final exact SHA闭环；**SKIPPED != PASS**。
5. exact-SHA ordinary CI + UI + Pages全部绿后，A3才可改成 **🟠 IMPLEMENTED — WAITING HUMAN REVIEW**。
6. 然后停止代码修改，交用户做A3/第四轮真实qB人工复核。
7. 用户明确PASS后 -> 🔵 -> required admitted-version A full validation -> ✅；A完成前不得开始B。

## 4. 其它未完成项

~~~text
A3  Frozen Settings/common catalog identity -> smoke/UI/Pages -> human review
B   Torrent Status / Sidebar facets
C   qB-owned copy exact source/context + official translation
D   RSS final closure：feeds/articles/read-state/census/common identity/actions
E   Torrent Detail final inventory/copy/action audit
F   integrated compact-runtime/common identity/census/mixed-stale rejection closure
~~~

A–F全部完成且final dev SHA冻结后，才考虑 `docs/011` Real qB Full Frozen Matrix。当前禁止Candidate/main/tag/Release。

## 5. 分支纪律

~~~text
dev only
main DO NOT MOVE
[candidate] DO NOT CREATE / DO NOT USE
no tag / Release
no force push
~~~

## 6. 一键复制给下一位 AI

~~~text
按顺序完整阅读：015 → 014（仅STOP前）→ 002/005。读完后 fresh-read dev/main exact HEAD；继续A3，仅dev，先闭环Settings Frozen identity，再跑final ordinary CI/browser/Pages；SKIPPED != PASS。
~~~

## 7. 当前停止条件

当前 **NOT COMPLETED**。A3 exact-SHA CI/UI/Pages尚未闭环，也尚未进行第四轮人工复核；不得进入B。

==================================================
STOP — COMPLETED ARCHIVE BELOW
AI 默认不要继续读取。
只有排查 regression / historical decision / 用户明确要求时才读取。
==================================================

## ✅ 已完成归档

### Foundation — RSS source-driven rule fidelity
RSS rule source semantics、historical/modern tri-state/default-null、unknown-field preservation与 `rss-compat.json` lazy runtime foundation已建立；D整体尚未 VERIFIED DONE。

### Foundation — Preferences source/admission
Source-native extractor、keyed compact IR、dynamic native tabs、independent tabs/preferences/bindings census、catalog identity与 Preference-level value projection 已建立；formal browser runtime cutover已完成实现，仍需final exact-SHA automated validation与人工复核。

### ✅ Validation infrastructure — bounded Preferences source gate
历史 `6e834a92... / CI #1847 / 35136160556` 的约6小时取消事故已通过 profiling/root-cause修复；legacy dynamic-regex catastrophic backtracking被替换为锚定线性解析；正式 gate改为 base + 16-way shards + strict aggregate。

### ✅ Source write-proof follow-up
`4c89db25...` 首次提交 direct-string write proof后，CI #1855因 regression fixture变量 typo失败；随后 `9456fac7...` 修正 fixture，CI #1856 与 Pages Source #944 SUCCESS。该 exact SHA 重新证明65 releases、16/16 shards、65/65 census，并产出311540-byte source-native Preferences compact；这只关闭 source/admission evidence，不代表 A 产品完成。
