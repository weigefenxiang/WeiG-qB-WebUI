# 014. AI 协作与分支纪律

Status: **single current handoff authority**

长期硬规则以 `docs/015.开发硬规则（长期版）.md` 为最高约束。本文件是唯一 mutable handoff / 当前进度 / 未完成事项 authority；历史过程由 Git history / Actions / artifacts 保存。不要新增重复 `HANDOFF.md`、版本复盘流水账或第二套 roadmap。

**当前工作状态：COMPLETED / WAITING_FOR_USER_DIRECTION。**

```text
COMPLETED
WAITING_FOR_USER_DIRECTION
NO KNOWN CURRENT DETERMINISTIC FAILURE
CURRENT TODO: NONE
```

当前用户明确要求只在 `dev` 完成当前方案，并明确禁止修改/合并 `main`、禁止创建或使用 `[candidate]`。0.3.103 当前批准方案已经完成实现并取得 required ordinary CI 与适用 Pages evidence；在用户给出新的明确方案前，停止继续修改产品。

## 1. 接手顺序：先读 canonical docs，再碰代码

下一位 AI 不要只读 014 或旧聊天摘要。先完整阅读当前 canonical docs 与 `DESIGN.md`，建议顺序：

```text
1. docs/015.开发硬规则（长期版）.md
2. docs/014.AI协作与分支纪律.md
3. docs/001.项目总方案.md
4. docs/002.兼容与实现状态.md
5. docs/003.项目架构.md
6. docs/004.UI与缓存契约.md
7. docs/005.统一交互与设置系统.md
8. docs/006.发布与晋级流程.md
9. docs/007.安装升级与手动部署.md
10. docs/008.Torrent工作区与状态所有权.md
11. docs/009.Virtual-qB-Lab.md
12. docs/010.真实qB产品兼容路线.md
13. docs/011.全版本真实qB验证规则.md
14. docs/webapi-evolution-ledger.md
15. DESIGN.md
```

完成全部 canonical docs 与 DESIGN.md 阅读并 fresh-read repository truth 后，再根据当前 exact HEAD 与 required-lane evidence 决定是否存在新的用户批准工作；若没有新的明确方案，必须停止，不得自行找新任务。

fresh-read 至少包括：

```text
refs/heads/dev
refs/heads/main
VERSION
webui/VERSION
package.json / package-lock.json version
latest published Release/tag
current dev exact-SHA Actions
```

任何本文记录的 SHA/run 都只是 checkpoint，不能替代 fresh repository truth。若 `dev` 已变化，先确认变化来源、diff 与新 evidence，禁止从旧 SHA 直接推断 current 状态。

## 2. 当前产品、分支与版本事实

本次完成判定对应的 product validation checkpoint：

```text
normal development branch: dev
Product VERSION: 0.3.103
0.3.103 feature commit: e17bc9e482f8739c3c9e06c2610deb3334590e66
final product validation SHA: 6aaabe393e154a373a0a2e176b85823d99cc2cb4
main: ba8ee49886fad159e76014514b5a0a4ba803bc29
latest published Release/tag: v0.3.61
qB support floor: 4.1.0 official stable
latest admitted stable: 5.2.3
Frozen stable profiles: 65
```

0.3.103 的实际产品范围：

- Torrent 左侧 facet 按 qB-native ownership/order 投影：Category → Tags → Trackers，再接 WeiG Save Path；每个组标题与 `All ...` 零筛选选项分离。
- `tools/qb-owned-ui-source.mjs` 从 exact upstream `TransferListFiltersWidget` 提取 `Status / Categories / Tags / Trackers` source/context，避免手写替代 qB-owned copy。
- 新增 RSS Downloader rule runtime owner；`setRule / renameRule / removeRule` 通过 source-gated `W.QBClient` transport，并保留 qB 返回的完整 rule object 后再编辑，避免重建不完整 schema。
- RSS rule 模块只在 RSS route lazy-load，不新增第二全局 owner。
- Product VERSION 从 0.3.102 bump 到 0.3.103；根 `VERSION`、`webui/VERSION`、`package.json`、`package-lock.json` 已同步为 0.3.103。

`main` 未移动。本轮没有创建/使用 `[candidate]`，没有 force push，没有 tag/Release/promotion。

## 3. 本轮发现并关闭的真实问题

### 3.1 Version owner 漏同步

首个 feature exact SHA `e17bc9e...` 的 ordinary CI #1786 暴露 `VERSION=0.3.103`、`package.json=0.3.103`，但 `webui/VERSION=0.3.102`。这是实际 version-owner 漏同步，已修正并由后续 runtime contract 验证一致。

### 3.2 Canonical docs authority 漂移

后续 smoke 暴露 `docs/014` 丢失 canonical copyable handoff block，以及 `docs/002` 未明确把 mutable TODO authority 路由到 014。两处均已修复；`docs-authority-contract.mjs` 在最终 product SHA 的 ordinary smoke 中通过。

### 3.3 Spatial 越权版本分支

`compat-architecture-contract` 发现 `spatial.js` 仍直接读取 `client.qbVersion` 决定 compatibility lifecycle。该 legacy presentation version branch 已移除；Spatial 只预加载 registry，正式 bind 继续由 App owner 在 `client.detect()` 后执行，且对应 frozen legacy allowlist 例外已退休。

### 3.4 Sidebar 原生 title 泄漏

Hosted Chrome qB5 sidebar visual gate 发现 Tracker / Save Path facet Select 会恢复两个非 canonical 原生 `title`。根因是 facet control 对无 capability ID 的项仍启用 `nativeTitle`。现统一为 `nativeTitle:false`，保留 group title 与 `aria-label`；最终 Linux semantic UI regression 已通过。

### 3.5 Pages preferences-anchor 单次 bootstrap 超时

最终 product SHA 的 Pages 首次 `preferences-anchor` verify 在 preference 断言开始前等待 `#login-form` 60 秒超时；同 exact SHA 单独重跑后 SUCCESS，且其余 Pages live verify shards 在首次尝试即通过。当前没有可复现 deterministic product failure，因此记录为单次 live bootstrap/transient failure，不通过放宽断言、改 FAIL 为 SKIP 或延长测试语义来掩盖。

## 4. 当前未完成事项判定

当前批准方案：**COMPLETED。**

```text
CURRENT TODO: NONE
```

没有尚未完成的当前产品项、验证项或 docs 收口项。不得把历史未来方向、顺手优化、Release promotion、Frozen Matrix、main 合并当作 current TODO。下一步只等待用户新的明确指令。

## 5. Evidence 与完成判定

最终 product bytes 对应 SHA：`6aaabe393e154a373a0a2e176b85823d99cc2cb4`。

- ordinary CI run `35051832835`（#1791）：**SUCCESS**。Smoke、semantic runtime/qB baselines/JS syntax、installer contracts，以及 Linux semantic UI regression 均通过。
- Virtual qB Pages run `35051840334`（#813，最终 attempt 2）：**SUCCESS**。同一 product SHA 的 Pages materialization/build/deploy/live 链完成；build、deploy、core、services、mobile-layout、locale-bootstrap、release-profile 与 preferences shards 均最终 SUCCESS。
- 与当前 ordinary lane 不适用的 candidate/release/full-stable heavy jobs 由 classifier 明确 SKIPPED；`SKIPPED != PASS`，不把它们写成成功验证，也不自行触发 release-grade matrix。

证据规则继续有效：

- 新 commit 的 PASS 只证明对应 exact SHA；旧 SHA 绿灯不能自动证明新产品 SHA。
- docs-only 收尾 commit 若 classifier 明确跳过与产品无关的重型 lane，可以引用父 product exact SHA 的已完成 product evidence，但必须明确两者身份，不得把 docs-only SHA 冒充 product-validation SHA。
- Pages build/deploy 不能单独替代 live acceptance；反之 live 页面也不能替代 source/build identity proof。
- Simulator/Virtual qB evidence 不能替代要求的 real-qB evidence；不要自行扩大为 Frozen Matrix。

## 6. Protected owner 边界

继续保护以下 canonical ownership：

- `webui/**` 是唯一正式产品。
- `W.QBClient`：transport + detected identity；write 必须 source-gated/fail closed。
- `W.CapabilityRegistry`：runtime compatibility/current release facts。
- `W.SettingsSchema + settings-compat.json`：Settings Preference semantics 与 layout/write proof。
- `app/preferences.locale`：persisted language truth；`W.I18n` 仅 presentation owner。
- `W.VirtualList`：主 Torrent / Detail virtualization 与 scroll interaction owner。
- Torrent sidebar 的 qB-owned Status/Category/Tags/Trackers copy 来自 exact upstream source/context；Save Path 仍是 WeiG extension，不伪装成 qB native group。
- RSS rules 只有一个 runtime owner，复用 canonical transport/capability；禁止平行 polling/state owner。
- Tests 跟随 current semantic DOM；禁止为了 stale selector 复活退休 DOM。
- MutationObserver repair、monkey patch repair、duplicate polling/state/scroll/language owner 继续禁止。

## 7. 分支、版本与发布硬边界

```text
normal development: dev
main: 不移动，除非用户明确授权
[candidate]: 当前用户明确禁止创建或使用
force push: 禁止
Product VERSION: 0.3.103；只有后续正式 webui/** 产品内容再次变化才继续 bump
promotion / main / tag / Release: 当前不做
Frozen Matrix / release-grade candidate gate: 当前不做
```

任何未来 write batch 前 fresh-read `dev`；更新 ref 前再次 race-check；只允许 safe fast-forward / normal dev commit，不得 force。

## 8. 历史 checkpoint 与 current truth 的关系

0.3.102 的 locale bootstrap、compound Settings row、sidebar readiness 等旧问题已经在其历史 exact-SHA evidence 中闭环，不是 current TODO。0.3.103 本轮中出现的 version owner、docs authority、Spatial compatibility branch、sidebar native title 与一次性 Pages bootstrap 超时，也都已经按上文证据闭环。

Git history 是版本历史档案；本文件只保留当前接手所必需的 checkpoint。未来若 `dev` 变化，以 fresh repository truth 为准。

## 9. 一键复制给下一位 AI

```text
接手 weigefenxiang/WeiG-qB-WebUI。当前 0.3.103 批准方案已经 COMPLETED，只在用户给出新的明确方案后才继续 dev 开发。不要修改/合并 main，不要创建或使用 [candidate]。

先完整阅读 canonical docs 与 DESIGN.md；完成全部文档阅读并 fresh-read repository truth 后，再确认 dev/main、VERSION/webui/VERSION/package versions、latest Release/tag、current dev exact-SHA Actions。

当前已完成：qB-native Torrent sidebar facet projection + exact source-owned copy、RSS Downloader rules 的 source-gated write/runtime owner、RSS route lazy-load，以及本轮暴露的 version owner/docs authority/Spatial version branch/sidebar title 问题。不要重做已经闭环的功能，不要恢复旧 DOM/旧 owner。

最终 product validation checkpoint 为 6aaabe393e154a373a0a2e176b85823d99cc2cb4：ordinary CI #1791 SUCCESS；Virtual qB Pages #813 最终 SUCCESS。SKIPPED != PASS。若 fresh-read 后没有新的用户批准方案或新的 deterministic required-lane failure，立即停止，不要自行寻找优化任务。
```

## 10. 停止条件

fresh-read 后若同时满足：

```text
Product VERSION = 0.3.103 且所有 version owner 一致
main 仍未被本轮移动
没有 current deterministic required-lane failure
当前 0.3.103 product bytes 的 ordinary CI required evidence 已闭环
适用 Pages materialization/build/deploy/live evidence 已闭环，或对应 lane 被 canonical classifier 明确判定不适用
current TODO = NONE
```

则状态保持：

```text
COMPLETED
WAITING_FOR_USER_DIRECTION
NO KNOWN CURRENT DETERMINISTIC FAILURE
CURRENT TODO: NONE
```

并明确告诉用户：**当前批准方案已经全部完成并验证通过。** 不再“顺便优化”，不碰 `main`，不创建 `[candidate]`。
