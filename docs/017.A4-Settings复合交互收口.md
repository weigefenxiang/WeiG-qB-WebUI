# 017. A4 Settings 复合交互收口

Status: **bounded A4 design / acceptance scope — NOT handoff authority**

> 先读 `docs/014` STOP 前，再读本文件。`docs/014` 拥有 mutable TODO / exact-SHA / next action；本文件只定义 A4 的问题族、owner边界、实施顺序与验收。A1–A3 历史在 `docs/016`，默认不要读。

## 1. A4 触发条件

第四轮真实 qB Options 人工复核FAIL。A3自动证据曾全绿，但真实产品仍出现 compound/group layout、Select editability、credential/time grouping、locale与overlay readability问题。因此 A4 的目标不是增加更多source tooling，而是把已有 source truth正确投影为真实 qB 可用的正式产品。

## 2. 本轮人工 failure inventory

| # | Surface | 真实现象 | A4 family |
|---|---|---|---|
| 1 | Downloads / Run external program | 长文案被挤成逐字竖排；section/control文案重复；两个运行外部程序语义混乱 | R1 compound/group layout |
| 2 | Connection / I2P | `I2P (Experimental) (requires libtorrent >= 2.0)` 未正确中文呈现 | R4 exact locale closure |
| 3 | Connection / IP Filtering | 与 external-program 同类竖排/重复/组合布局问题 | R1 compound/group layout |
| 4 | Speed / Scheduler | 标题重复；from/to语义丢失；显示为裸 `8 : 0 -> 20 : 0` 而非 `08:00 -> 20:00` | R1 + R3 compound semantics |
| 5 | Downloads / content layout + stop condition；BitTorrent / encryption | 显示当前值但真实 qB 中不可修改 | R2 Select/editability |
| 6 | Downloads / automatically add torrents from | 子项集合/顺序/分组与原生不一致；应按group/table/stacked family表达 | R1 + R3 grouped semantics |
| 7 | Web UI / Authentication | 只有用户名，缺 source-proven Password输入 | R3 credential family |
| 8 | Shared Select menus | 展开后长文字显示不全 | R2 shared overlay readability |

## 3. Repo/source 复盘：已确认事实 vs 待证明假设

### 3.1 已确认事实

- current source catalog已有 `Run external program on torrent completion`、`IP Filtering`、Scheduler section/preference grouping；
- compact/source evidence包含 `webui_password_text -> web_ui_password`；
- qB copy evidence包含 I2P 的 `OptionsDialog` source ref；
- current `W.SettingsRenderer` 已有 generic `compoundRow()`，但仍以通用row/copy/control slot投影结构；
- current Settings CSS主row采用两列grid，copy允许 `overflow-wrap:anywhere`；在copy列极窄时具备逐字断开的可能性；
- current shared Select option使用 `white-space:nowrap; overflow:hidden; text-overflow:ellipsis`，这与本轮“长菜单文字显示不全”直接冲突；
- A3 fixture/browser自动PASS与本轮real-qB人工FAIL并存。

### 3.2 待证明，不得直接当根因

- external-program / IP Filtering 的竖排是否由grid列宽、compound row结构、重复local label或fieldset嵌套共同造成；
- content-layout / stop-condition / encryption不可编辑究竟是 writable gate、projection/current-key、control path还是实际pointer callback问题；
- Password丢失发生在 compact decode、writeOnly intersection、row/group renderer还是real-qB runtime state；
- I2P英文fallback是官方zh_CN本来不存在，还是 copy registry / locale tag / resolvedRefs lookup未命中；
- watch-folder 子项不对是source graph缺少native grouping metadata，还是renderer把已有group摊平。

任何一项在真实trace证明前都不得按猜测写key patch。

## 4. Root-cause families

### R1 — Native group / compound layout fidelity

目标：source-native fieldset/row/legend/control/content层级在正式UI保持可理解关系。需要时新增通用family/layout primitive，例如：

- `stacked compound`：长标题/说明独占一行，control cluster另起一行；
- `inline compound`：多个短控件按source顺序成组；
- `time range`：from HH:MM -> to HH:MM + day selector；
- `group/table`：watch-folder等多子项保持同一group；
- `credential`：username/password + related gates。

这些名字是设计方向，不是要求固定API。最终owner必须仍是同一Control Graph / SettingsSchema / SettingsRenderer。

### R2 — Canonical Select / editability / overlay

同一个 `W.Components.selectControl()` 同时负责：真实pointer/keyboard交互、selected value、draft callback、focus/a11y与floating menu。A4不能建立Settings专用第二Select。

检查链：

~~~text
exact source options
-> compact option facts
-> Preference descriptor/projection
-> current runtime key / write gate
-> graph control semantic
-> canonical Select
-> draft mutation
-> setPreferences
-> verified reread
~~~

Overlay必须viewport-aware；长option需完整可辨识，可采用shared max-width / wrap / vertical scroll策略，但不能用单项CSS patch。

### R3 — Compound control semantics

Source存在多个controls并不自动形成正确UI family。A4需证明time range、credential、watch-folder等组合关系来自source structure；若当前compact contract缺少必要relation，扩充generic metadata并由全部版本source admission约束。

WebUI Password仍遵守write-only安全规则：不预填、不缓存旧secret、非空才POST、保存后清空，不伪造GET reread；但真实Options中必须出现source-proven输入。

### R4 — Exact qB-owned locale closure

所有qB-owned section/control/option/note只由exact `{source,context}` + exact-release official translation解析。Source ref已存在时，A4优先查 runtime registry/locale resolution，不建立WeiG手写中文数据库。

## 5. 实施顺序

### Step 0 — Freeze evidence / fresh-read

- fresh-read当前dev/main/Actions；
- 保留用户第四轮截图作为real-qB acceptance evidence；
- 不读取016历史，除非新trace指向已关闭regression；
- 不先改代码。

### Step 1 — Family-level reproduction

在真实 qB 或能等价复现真实DOM/API的环境中逐family抓：DOM tree、`data-native-*`、computed width、control disabled/readOnly、Select options、SettingsSchema writable result、draft前后值、locale lookup result。

先让test能失败，不接受只用programmatic value/change的假交互。

### Step 2 — Source/compact/schema audit

对照 exact qB Preferences source、`settings-runtime-materialized`、`W.SettingsSchema.controlGraph()`，确认group/order/labels/options/writeOnly/displayFormat/dynamicOptions是否已经存在。只在事实确实缺失时改source/compact contract。

### Step 3 — Canonical owner fix

按定位结果只改一个owner：

- structural semantics缺失 -> source/compiler/SettingsSchema generic family；
- DOM grouping/layout错误 -> SettingsRenderer + settings.css shared family；
- Select行为/overlay错误 -> W.Components + shared controls/ui CSS；
- write/read transport错误 -> W.QBClient / Settings write gate；
- translation resolution错误 -> qB copy artifact / W.I18n exact route。

禁止Preference key、当前中文字符串、截图坐标、qB 5.2.x专用CSS hack。

### Step 4 — Focused regression contracts

至少覆盖：

- compound external-program / IP Filtering不竖排不重复；
- Scheduler `08:00 -> 20:00` + day selector；
- content-layout / stop-condition / encryption真实pointer可选择并save/reread；
- watch-folder group结构/顺序；
- username + write-only Password；
- I2P exact official locale；
- Select长选项完整可辨识且不越viewport；
- Light/Dark + representative desktop/mobile geometry。

### Step 5 — Evidence closure

~~~text
focused tests
-> ordinary smoke
-> affected semantic UI / runtime
-> Pages
-> fifth real-qB human review
~~~

第五轮人工FAIL则继续A4，只修新证明的generic root。人工PASS后进入required admitted-version A full validation。

## 6. A4 完成条件

A4本身只有同时满足以下条件才可标记IMPLEMENTED / HUMAN PASS：

1. 本轮8类现象全部由generic owner解决，无key-specific patch；
2. focused browser能真实复现并证明交互，不靠直接set/change；
3. ordinary CI/browser/Pages在同一产品exact SHA闭环；
4. 第五轮真实 qB人工复核明确PASS。

随后运行 required admitted-version A full validation；该验证也PASS后，**A VERIFIED DONE**。

## 7. A 之后的后续方案

默认继续顺序：

~~~text
B  Torrent Status / Sidebar native facets
C  qB-owned copy exact source/context + official translation
D  RSS feeds/articles/read-state/Downloader final closure
E  Torrent Detail inventory/copy/action final audit
F  integrated compact-runtime/common identity/census/mixed-stale rejection
-> freeze final dev exact SHA
-> docs/011 Real qB Full Frozen Matrix
~~~

每一阶段继续遵守 product-first、single owner、exact source、focused-first、human/real-qB evidence where applicable。禁止为了提前进入后续阶段跳过 A 的 full validation。

## 8. VERSION / branch

- 只允许 `dev`；main/candidate/tag/Release仍禁止；
- docs/tests-only变化不 bump Product VERSION；
- A4第一次正式 `webui/**` 产品变化时，从当前 Product VERSION按项目规则递增最后一段，并同commit同步全部version owner；
- 每个mutation batch前后执行SAFE-REF。

## 9. P0-23A runtime-owner 实施记录

首批正式 A4 产品变化使用 Product VERSION **0.3.131**，范围严格限定为已由 exact source / current graph 证明的 generic owner：

- R1：`gated-sentinel` 及其它多交互 compound row 统一 full-span；compound 多交互项改为 local source labels，隐藏重复 outer copy，避免 Run external program / IP Filtering 长标签被固定 copy 列挤压；
- R3：由 `inline-multi-control + 4 controls + pair source labels + zero-pad-2` 泛化识别 time-range，保留 From/To 与 `HH:MM` 语义；write-only control 的**存在性**与**可写安全性**解耦，Password source-proven 时必须显示，写入仍走原有 safeWrite/omitEmpty 规则；
- R2：继续使用唯一 `W.Components.selectControl()`；floating menu 宽度仍受 viewport 约束，但 long option 允许换行完整显示，trigger 本身仍可 ellipsis；
- R4：不新增手写中文；focused browser 改为验证 visible DOM 的 exact official qB translation，而不只调用 resolver；
- browser gate 同时新增真实 pointer -> draft -> `setPreferences` -> reread、compound geometry、credential order、time range、long overlay viewport/readability 证据。

**明确未在本批伪修：** `Automatically add torrents from:` 的 source table / dynamic rows。当前 compiler 只把 formRow/tr 中 controls 与 list/note content 进入 Control Graph，静态 watched-folder table 结构没有进入 compact runtime；该项必须在下一 bounded source/compact batch 补 generic table/group facts 后再渲染。

## 10. P0-23B source-native structured table

第二批 A4 产品变化使用 Product VERSION **0.3.132**，目标是补齐上一批明确保留的 watch-folder source gap，并修正 `control-helper` compact 回归。

- source compiler 从通用 `settings[key] = helper()` 写入、helper 对 source `<table>` 的读取、fieldset/thead QBT_TR、动态 `<option>` 以及 object getter/setter descriptor 推导 `structured.kind=keyed-map`；renderer 不包含 `scan_dirs` / watched-folder key 特判；
- 65 个 admitted stable release 的 16 个 Preferences shard、compact 编译与 independent census 均通过。5.2.3 的 `scan_dirs` source fact 为 `watched_folders_tab` / structured / object→object EXACT / identity safeWrite；旧版本只有在同等 source proof 存在时才开放写入，否则继续 fail-closed；
- canonical `settings-runtime-materialized.json` 由 source admission 生成，60834 bytes，SHA-256 `e4aa4059ddd9bb232ca3a336168b0a3968e8e0fbe816488aa59840a414d3e4d9`，作为 0.3.132 committed Settings runtime；
- runtime 使用 generic structured keyed-map editor：source 两列表头、source mode labels、已有 object rows + 尾部空白 row、shared Select、custom override、object draft / `setPreferences` / reread；未证明的其它 structured value 仍保留 read-only JSON fallback；
- P0-23A 的 `interactive>1` full-span 规则误伤 `control-helper`。P0-23B 恢复为 generic `interactive>2`，并按 graph template 决定 local labels，因此 Listening Port + Random 保持 compact，而 gated-sentinel / time-range / true multi-control 继续 full-span。

本节记录的是 implemented state，不代表 A4 已完结；仍需同一 0.3.132 exact HEAD 的 ordinary CI/browser/Pages、第五轮真实 qB人工复核，以及人工 PASS 后的 admitted-version A full validation。

## 11. P0-23C compound span correction

0.3.132 自动验证证明 source/runtime 物化本身成立：ordinary smoke、Pages、16/16 Preferences source shards、65-release native source admission/independent census、committed Settings runtime compare，以及除两项外的 semantic UI 均通过。失败只集中在 `runtime` 与 `settings-fidelity`，且两者都在同一 Downloads / Run external program geometry 断言处失败。

根因不是 watch-folder 或 Select writeback，而是 P0-23B 为恢复 Listening Port `control-helper` compact layout，把 `graphRowSpan()` 从 `interactive>1` 简化为 `interactive>2`。这会同时把 source template 为 `inline-multi-control`、但只有两个交互控件的 external-program 行错误降为半宽。

P0-23C 的 generic 修正：

- `control-helper` 不强制 full-span，继续保持 Listening Port + Random 的 compact desktop packing；
- `inline-multi-control`、`inline-multi-helper`、`gated-sentinel` 按 source template 明确 full-span；
- textarea / structured / content / time-range 的既有 full-span 规则不变；
- renderer 仍禁止 `listen_port`、`scan_dirs`、external-program preference key 等特判；
- Product VERSION 递增到 **0.3.133**，需在同一 exact HEAD 重新跑 focused `browser-runtime` / `browser-settings-fidelity`、ordinary CI/browser/Pages。

本节仅记录 implementation/evidence state。第五轮真实 qB 人工复核及其后的 required admitted-version A full validation 仍是 A4/A 的完成门槛。
