# 003. 项目架构

Status: **current architecture / Current Owner boundaries**


> **A3 当前交接事实（dev 160473f13c5c）**：Product VERSION **0.3.125**；A3 source facts 与 product consumers 已推入 dev，main 未移动、未创建/使用 `[candidate]`。当前 exact-SHA CI #35442179347 与 Virtual qB Pages #35442186315 均 FAIL；首要 blocker 是 Settings runtime 与 canonical Frozen catalog 的 `sourceCatalogSha256` identity/materialization 未一致。状态仍为 **🟡 A3 IN_PROGRESS**，mutable TODO/下一 AI 交接只看 `docs/014` STOP 前。

> **2026-09-19 A3交接同步**：本文件已按当前 A–F 方案复核。mutable current status / blocker / TODO / next-AI handoff 只维护在 `docs/014.AI协作与分支纪律.md` 的 `STOP — COMPLETED ARCHIVE BELOW` 之前；本文件只维护本领域 authority/contract。具体 exact SHA、Actions 与运行结果在实际开发前必须 fresh-read，不以本注记代替 repository truth。

`webui/**` 是唯一正式产品。浏览器 runtime 的 release-profile layer 已退休；source catalog是离线 source truth / admission / validation input，不是 runtime owner。

## 1. 总体分层

```text
qB WebAPI
-> W.QBClient                    # transport + detected qB/WebAPI identity
-> W.CapabilityRegistry          # runtime release resolution + lazy domain loader
   <- capabilities.json          # control plane only
   <- torrent-compat.json
   <- detail-compat.json
   <- settings-compat.json
   <- rss-compat.json
   <- source-actions.json
-> canonical semantic owners
-> UI / presentation

current qB-owned copy runtime
   qb-settings-native.txt + minimal official QM
   -> W.I18n

qB official source / Frozen admitted release catalog
-> semantic source extractors
-> independent inventory/census
-> common catalog identity
-> compact domain compilers
-> exact-release compact contracts + qB-owned copy evidence
-> formal browser consumes only bounded/lazy runtime artifacts
```

`capabilities.json` 只负责 release identity、high-level gates 与 `compatFiles` index，不承载所有 UI/version history。领域事实各自归对应 compact contract；full 65-release catalog只存在于 tools/CI/simulator/admission层。

### 1.1 Product-first architecture discipline

Source/tooling层存在的目的，是让正式 `webui/**` canonical owner可以安全、可证明地运行；它本身不是交付目标。开发顺序默认是：

```text
formal product blocker
-> webui owner/caller cutover
-> only if blocked: patch exact source/tooling/test gap
-> return immediately to product cutover
```

已经具备的 source foundation不能长期停在 evidence层。只要足以支撑当前 product owner切换，就应优先完成 `webui/**` integration/retirement，而不是继续横向扩 parser/census/test surface。新增 infrastructure必须有明确 product consumer或具体 validation responsibility。

## 2. Current Owner map

```text
HTTP transport / detected identity         W.QBClient
Runtime release/capability/source facts    W.CapabilityRegistry
Settings current runtime semantics         W.SettingsSchema + settings-compat.json
Settings structural presentation            W.SettingsRenderer + generic Control Graph templates
Preferences source/structural admission     tools/qb-preferences-* (offline exact-source evidence)
RSS Downloader native surface              rss-compat.json -> RSSRules generic manifest consumer
Persisted language truth                   qB app/preferences.locale
qB-owned/WeiG copy                         W.I18n
Torrent status/filter semantics            W.TorrentSemantics
Torrent field/column provenance            W.TorrentFieldRegistry
Selection/torrent actions                  W.Selection / ActionRegistry
Main/detail virtual projection             W.VirtualList
Detail source schema/descriptors           W.CapabilityRegistry
Detail source projection                   W.QbUiEvidence
Detail generic dispatch                    app.js descriptor consumer
Detail user columns                        W.SharedColumns
Detail resize/reorder lifecycle            W.ColumnInteraction
Transfer runtime/history                   W.TransferRuntime
Responsive placement                       W.MobileAdaptive / W.SpatialRuntime
```

一个 semantic purpose只能有一个 Current Owner。A cutover后，source-native Preferences compact只通过 `settings-compat.json -> W.CapabilityRegistry -> W.SettingsSchema` 成为正式runtime truth；旧手工Settings schema/layout owner已退休。禁止 runtime alias、repair layer、第二 polling/state/scroll/layout/language owner。

## 3. Compact domain contract

正式 runtime domain当前包括：

```text
capabilities.json
  releases / WebAPI milestones / high-level features / compatFiles

torrent-compat.json
  Torrent filters/states/fields/columns
  B target: native sidebar facet inventory/order/semantics

detail-compat.json
  Torrent Detail tabs/layout/tables/menus/control facts

settings-compat.json
  bounded exact-source Preferences transport envelope
  A2 target: tabs/fieldsets/rows/clusters/controls/auxiliary gates/actions/note-help-list-hint/adornments/behavior predicates/source refs + full copy closure
  plus descriptors + value projection; display adornment is not the same contract as numeric scale

rss-compat.json
  exact RSS Downloader native-surface fields/copy/path/control semantics

source-actions.json
  exact action existence + required/optional parameter provenance

qB-owned copy artifact
  exact {source, context} -> official locale route
```

`tools/qb-catalog-identity.mjs` 现在是 common Frozen catalog identity helper。`tools/qb-compact-runtime.mjs` 已把同一 identity注入 capabilities/torrent/detail/actions/settings outputs；Preferences keyed native IR也携带该 identity。**这只是 foundation，不是 F 完成**：RSS尚未纳入同一 compiler/identity closure，browser/distribution也尚未对全部 domains执行 mixed/stale identity拒绝。

最终所有 domain与 qB-owned copy必须从同一个 Frozen admitted catalog编译并携带可比较的 catalog/release-set identity。若任一 domain停在不同 release set，CI/runtime contract必须 fail closed，不能混合“capabilities已更新、settings/rss仍是旧 catalog”的 bytes。

## 4. Compatibility / writes

`W.CapabilityRegistry` 从 compact source facts解析 Torrent filters/info fields/table columns/Detail UI/RSS UI/source actions。UI key不是 API proof；unknown upstream shape fail closed。Dangerous write必须具备 certified identity + exact action/parameter provenance，并由 `W.QBClient` transport；不能用 patch/major version猜 endpoint。

Settings write additionally要求 current runtime key、exact Preference setter/type agreement与source raw/UI `projection`共同成立。`identity / scale / switch-map / unproven` + `safeWrite`直接来自exact source；其中 exact writable string control若 source明确 direct identity setter，可附加 `writeIdentity:true`。只有 certified identity + source action + current key + descriptor + `projection.safeWrite` + dependencies + raw type 全部成立才允许进入HTTP；未知路径 read-only/fail closed。


## 5. Settings source-to-runtime boundary — A2 contract

A-base与A1已经建立唯一Settings runtime owner、安全write gate与第一代Control Graph；A2不建立第二套owner，而是把A1从“controls represented”提升为 **source content + interaction + copy + presentation fidelity**。

```text
exact qB Preferences HTML + inline source behavior
+ exact app/preferences/setPreferences descriptors
+ exact upstream C++ receiver declarations
  ├─ independent Preference inventory/census
  ├─ value projection / setter provenance
  ├─ typed receiver proof
  └─ A2 full-fidelity graph parser
       tab / fieldset / nested fieldset
       row / cluster / source order
       preference + auxiliary controls
       helper/source actions
       note / help / list / hint copy nodes
       option / local label / adornment
       bounded behavior predicates
       boolean / sentinel / presence gate projections
       |
       v
     independent structural/content/behavior accounting
       source node == represented/reviewed
       runtime-relevant unknown predicate == 0
       duplicate ownership == 0
       |
       v
     compact exact-release Control Graph IR
       | bounded transport + Frozen identity + raw payload hash
       v
settings-compat.json
  -> W.CapabilityRegistry lazy decode / exact-release identity gate
  -> W.SettingsSchema graph adapter ∩ current GET app/preferences
  -> W.SettingsRenderer
       exact source structure
       generic responsive compact row packing
       canonical expandable textarea editing same draft
  -> existing writeSafe()
  -> W.QBClient app/setPreferences + verified reread
```

Build-time parser只能把已证明source behavior降级为小型可审计IR，不能执行arbitrary upstream JS。特别地，**unknown behavior不得同时被计为“complete”并在runtime静默disabled**；无法解析的runtime-relevant behavior必须阻断admission/acceptance或成为明确reviewed exclusion。

Typed getter proof必须按receiver/source declaration通用推导。除 `session->` / `pref->` / local-member外，类似 `app()->isFileLoggerEnabled()` 也只能通过exact upstream interface/header declaration证明return type；禁止按Preference key猜boolean/string。

Gate/value projection至少允许source-proven的：

- boolean truthy/falsy；
- equals/notEquals + allOf/anyOf；
- numeric sentinel gate；
- empty-string / presence gate；
- parent fieldset/nested gate。

这些是IR family，不是Downloads/Proxy专用分支。

qB-owned graph copy也属于A2闭环：所有runtime正式显示的 `{source,context}`（tab/fieldset/control/option/helper/adornment/note/list/local-label）都必须进入现有exact qB copy dependency closure。官方目标locale存在翻译时不得因bundle漏ref而回退English；不存在官方翻译时才安全回upstream source。A2不提前建立C阶段的第二copy数据库。

Presentation可重新排版但不得改变语义。Desktop可以把相邻短row按generic fit规则成对/同一视觉行利用空间；textarea、structured/table、note/list、长copy与source-required full-width保持整行；Mobile只是同一graph自适应堆叠。Expanded textarea Dialog只操作同一个Settings draft，不成为第二持久状态或save owner。

Display adornment与value projection仍分属不同source fact；Product Identity仍沿A1既有单一路径，不回退weigg-install version authority。

## 6. Torrent Sidebar / Detail / VirtualList

B目标链：

```text
exact upstream native status/filter/facet inventory
-> torrent compact contract
-> W.CapabilityRegistry / W.TorrentSemantics
-> generic facet state + generic projection
-> append WeiG Path extension last
```

当前 `spatial.js` 固定 category/tag/tracker/savePath是未完成状态，不是长期架构。未来 qB增加同类 native facet时，应由 source admission进入 manifest；UI不能再加第 N 个 hard-coded button/branch。

E/Detail继续固定为：

```text
W.CapabilityRegistry.torrentDetailUi()
-> W.QbUiEvidence
-> generic app Detail runtime
-> W.SharedColumns / W.ColumnInteraction / existing W.VirtualList
-> W.QBClient
```

General只消费 source-generated layout；Files/Trackers/Peers/Web Seeds共用 generic renderer/action chain；禁止 per-surface第二 schema、第二 scroll owner或 endpoint-name业务分支。

`W.VirtualList` 是 Torrent主列表和需要虚拟投影的 Detail surface 的 canonical virtual/scroll owner。原生横/纵向滚动或 scrollbar drag期间，网络 polling可以继续，但新 snapshot不得替换正在交互的 DOM；只保留 latest pending snapshot，bounded idle后一次提交。禁止第二轮 polling、scroll repair或 MutationObserver同步。

## 7. Completeness / anti-omission architecture

仅有 semantic extractor不足以证明“没有漏”。每个准备声明阶段完成的 source-driven domain必须有独立 inventory/census路径：

```text
raw exact upstream source
  ├─ semantic extractor -> compact product facts
  └─ independent inventory/census -> upstream exposed inventory

inventory = mapped + explicit reviewed exclusions
unaccounted = 0
duplicate = 0
```

Preferences字段级source layer已检查tabs/preferences/bindings；A1又建立control/helper/adornment/behavior structural accounting，但A2人工复核证明旧complete定义仍会漏纯copy/input-button并允许unknown behavior假绿。A2必须把source-owned actions、note/help/list/hint、copy refs与runtime-relevant predicate resolution纳入独立accounting；B/C/D/E/F仍需各自census/coverage closure。

Inventory应尽量避免复用 semantic extractor的同一 parser assumption。若 upstream改成未知语法，正确结果是 admission fail/PENDING_REVIEW，而不是 extractor返回少几个元素后仍然绿色。

## 8. Build/runtime boundary

65 stable source catalog、full translation recovery、upstream source parser属于 tools/CI/simulator。raw `webui/` 只携带浏览器真正消费的 compact contracts/copy/QM，因此可直接作为 Alternative WebUI root。Historical `main=0.3.71` simulator preview可在 `branch==='main'` 的 simulator-only build路径临时物化旧 profile runtime；不得回流到 dev formal product。

历史上用于 connector传输的 `.weigg-bootstrap/**` 和一次性 `dev-bootstrap-*.yml` 不属于任何正式 architecture owner；它们必须在最终产品 tree中消失，不能被后续 AI当成 runtime/build subsystem继续维护。

## 9. Validation

Source audit、phase-end 65-release offline/Virtual matrix、browser/Pages、real-qB、Candidate/Release是不同 evidence层。阶段内部只跑与当前 product change直接相关的 focused/change-point；一个阶段完整收口后最多一次该领域的65-release offline/Virtual full matrix。最终 real-qB heavy matrix只属于全部 A–F完成、final dev exact SHA冻结后的 release-grade gate。

`SKIPPED != PASS`；exact-SHA evidence不可跨提交偷用。Workflow内部 source-contract PASS不能替代该产品 checkpoint 的 applicable UI/browser 与 exact-SHA Pages materialization/build/deploy/live evidence。


## A3 product-consumer architecture delta（2026-09-19）

A3没有建立第二Settings系统。正式链保持 `settings-compat Control Graph -> W.SettingsSchema -> W.SettingsRenderer -> W.Components -> W.QBClient`。Schema/renderer现可消费 `displayFormat/source-action/dynamicOptions/writeOnly`；dynamic option transport归 `W.QBClient.settingsDynamicOptions()`，RSS Downloader仍归 `W.RSSRules`，write-only secret仍走同一Settings draft/save事务。当前架构 blocker是 Settings compact runtime identity必须与canonical Frozen catalog一致；runtime对mixed-generation bytes fail closed是正确行为。
