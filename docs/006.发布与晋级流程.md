# 006. 发布与晋级流程

Status: **current product validation / promotion / release contract**

本文件以 `webui/**` 正式产品兼容为中心。Source audit、Full Stable Product Matrix、Virtual qB/browser、real-qB evidence 职责不同，不能互相替代。

## 1. 当前阶段 / VERSION

```text
Phase A — DONE
Phase B — DONE
Phase C — DONE
Phase D — DONE
Phase E — DONE
Phase F — Settings + Tool/Detail Surface Provenance — DONE
Phase G — Real-qB / Release Hardening — PLANNED / STOP
qB4.0.x / WebAPI v1 — FUTURE OPTIONAL
```

Phase F final validated candidate：

```text
7ae5493ab409af81abe25e579245160c26bfc7e3
VERSION 0.3.60
CI run 34125875685
Virtual qB Pages Source run 34125875723
main untouched
```

只有正式 `webui/**` 产品内容变化才 bump patch VERSION。

Phase F 第一次正式产品改动已完成：

```text
0.3.59 -> 0.3.60
```

同一 final tree 已同步：

```text
VERSION
webui/VERSION
package.json.version
package-lock.json root/package version
```

Docs/tests/tools/workflow/Simulator-only 变化不 bump 产品 VERSION。

Exact Git SHA 是 runtime/cache/test/Pages/LIVE/candidate/artifact identity；任何新 SHA 不自动继承旧 SHA 的 candidate/LIVE evidence。

## 2. Validation tiers

### Tier 1 — normal dev / docs / tests / tooling-neutral

普通 `dev` push：

```text
smoke
-> npm test
-> JavaScript / installer / script syntax
```

Docs-only phase closure 只应触发 normal smoke；不应人为重跑 candidate/browser/full upstream audit，除非 closure 改了 gate/product code。

### Tier 2 — general product/UI compatibility change

正式 `webui/**` compatibility 修改至少：

```text
smoke
-> direct product contracts
-> frozen LKG product regression when formal compatibility owner changes
-> relevant browser / Virtual qB evidence
```

Frozen regression：

```text
verify frozen LKG identity
-> architecture guard
-> Full Stable PRODUCT Compatibility Matrix on 65 frozen profiles
```

它不重新 checkout/source-extract verified historical qB。

### Tier 2.5 — future stable incremental admission

```text
verify frozen LKG
-> lightweight official stable tag comparison
-> no new stable: immediate exit
```

有新 stable 才：

```text
checkout upstream
-> source-extract only new stable profiles onto frozen prefix
-> reject historical mutation
-> source audit floor + new refs
-> focused product gate
-> repository contracts
-> upstream + product diff
-> PASS
-> fresh dev race check
-> safe fast-forward catalog + manifest only
```

失败：

```text
PENDING_REVIEW
candidate rejected
LKG unchanged
```

### Tier 3 — source/discovery/evolution changes

独立 heavy `Full qB stable WebAPI evolution audit` 只用于真正改变：

```text
stable tag discovery
source parsers
source-derived actions/parameters/fields/filters/preferences
evolution tooling / ledger
```

明确不因 runtime compatibility owner、ordinary UI、docs、tests、VERSION-only、installer自动触发。

Phase F 使用现有 catalog facts 完成，没有修改 parser/discovery/evolution，因此没有人为触发独立 Tier 3。Final candidate 仍重新生成 exact stable source catalog 并执行 all-stable source/product audit。

## 3. Frozen Historical Facts

```text
tests/fixtures/qb-release-catalog.lkg.json
65 profiles
qB 4.1.0 -> 5.2.3
SHA-256:
8b91742ac8eee276b7994c84445c681913430f220325a10afc8c931c89645ddb
```

禁止因为新 stable 重解析/重写 verified historical profiles；Frozen catalog 保持一个完整文件。

## 4. Formal product matrix

当前执行：

```text
W.ReleaseProfile
W.CapabilityRegistry
W.TorrentSemantics
W.TorrentFieldRegistry
W.SettingsSchema
W.QBClient
```

Phase F 已把 matrix 从粗粒度 Search/RSS/Logs boolean 升级为：

```text
surface read provenance
surface write/action provenance
required/optional action parameters
Settings endpoint + Preference writable provenance
Details response-field provenance where user-visible
future UNKNOWN/no-HTTP behavior
```

Final candidate 的 65-profile Full Stable PRODUCT Matrix PASS。

## 5. Phase F product validation contract — SATISFIED

### 5.1 Settings

已证明：

```text
read endpoint source action exists
write endpoint source action exists
per-Preference setter/type agreement exists
```

写入条件：

```text
endpoint writable
AND
preference writable
```

Unknown endpoint、getter-only、missing setter、type mismatch、unproven structured editor => no unsafe payload。

### 5.2 RSS historical correction

Exact upstream qB 4.1.0 已有 core RSS actions；Phase F candidate 已回归证明：

```text
qB 4.1.0 RSS core route/read/actions = source-proven
```

旧 Virtual qB/Pages stale expectation 已按 source truth 修正，没有为了旧 test 保留错误产品隐藏。

### 5.3 Search / Logs

```text
Search core: source-proven from qB 4.1.4
Logs main/peers: source-proven from qB 4.1.0
```

Final product gate读取 exact source actions，不再只验证 WebAPI/qB version threshold。

### 5.4 Details / action parameters

已分别验证：

```text
Properties / Files / Trackers / Peers / WebSeeds reads
File priority / Tracker mutations / Peer Ban writes
response fields used by UI
action parameter form generation
```

代表性 parameter evolution：`editTracker` old vs 5.2.x contract；QBClient 按 exact action parameters 生成 form，unknown contract HTTP 前 fail closed。

Unsupported/error 不再 catch 成 fake empty list。

## 6. Phase F direct contracts — SATISFIED

已覆盖：

```text
exact historical profile -> exact capability/action/parameter result
future-major exact-known source facts -> same semantics
future-major missing fact -> UNKNOWN / no HTTP for dangerous write
unsupported exact stable -> UNAVAILABLE / no HTTP
response-field absence -> UI does not invent data
real empty response -> real empty state
HTTP error -> explicit error state
```

没有通过删除断言、降低覆盖、改 baseline 为错误值来修红灯；candidate 中发现的 browser failures 均按完整日志定位为 stale fixture，并在不削弱产品 gate 的前提下修正。

## 7. Browser / Pages contract — PASS

Final candidate relevant browser：

```text
Linux Hosted Chrome PASS
Windows Hosted Chrome PASS
minimum qB4 / qB5 representative semantic matrix PASS
Settings source-proven read/write behavior PASS
Search / RSS / Logs source capability flows PASS
Details / fields / actions / empty-vs-error semantics PASS
```

Virtual qB/Pages：

```text
same formal webui/**
-> historical simulated backend
-> real browser user flow
```

Pages Source run `34125875723` PASS，绑定 exact source SHA `7ae5493ab409af81abe25e579245160c26bfc7e3`。

Pages evidence 是 auxiliary evidence，不替代 Phase G real-qB binary/server validation。

## 8. Final Phase F candidate — PASS

Exact candidate：

```text
SHA:
7ae5493ab409af81abe25e579245160c26bfc7e3

CI run:
34125875685
```

Required gates：

```text
smoke PASS
Generate exact stable source catalog PASS
Audit every official stable >=4.1.0 PASS
Full Stable PRODUCT Matrix PASS
Linux browser candidate PASS
Windows browser candidate PASS
exact-SHA release candidate package PASS
```

Candidate package 复用同一 exact generated catalog并写入 exact SHA，没有在 release candidate job 中另构建不同产品。

## 9. Phase F completion checkpoint — SATISFIED

```text
all approved scope implemented or explicitly documented non-goal
65 frozen product matrix green
future UNKNOWN/no-HTTP contracts green
relevant Linux/Windows browser green
Pages auxiliary acceptance aligned with source truth
overall candidate green
canonical docs updated with exact base/final SHA/evidence/risks
STOP before Phase G
```

Phase F 完成后的 docs-only closure 不重复重型 candidate；只需普通 smoke，除非 docs closure 同时修改 gate/product code。

## 10. Cross-platform exact proof

Git identity/hash proof：

```text
exact checkout/index
-> canonical tracked path/blob identity
-> canonical Git blob bytes
```

不能用 Windows checkout CRLF-transformed working-tree bytes证明 Git blob identity。

Repo-wide semantic claim 仍要求 target exact SHA + complete relevant tree/blob/file；code search/snippet 只用于 discovery。

## 11. Representative real-qB validation — PHASE G ONLY

属于 Phase G：

```text
minimum supported historical qB4
modern qB4
qB5 generation
latest official stable
```

Phase F 的 Virtual qB/browser/source audit 不能冒充 real-qB binary/server validation。

当前状态：

```text
Phase G = PLANNED / NOT STARTED
STOP until explicit user checkpoint / approval
```

## 12. Promote dev to main

未经用户明确授权：

```text
no main write
no promotion
no release
no tag
```

授权后仍需 exact validated dev SHA、required evidence、fresh main/dev checks、safe fast-forward only。

## 13. SAFE-REF

每次正式写入：

```text
fresh-read dev exact HEAD
-> candidate only parent = that SHA
-> compare exact expected paths
-> validate
-> fresh-read dev immediately before ref update
-> if moved: candidate invalid, rebuild
-> force:false safe fast-forward
-> validate final exact SHA
```

## 14. Red gate discipline

红灯分类：

```text
real product bug
upstream/source misunderstanding
owner/caller migration gap
stale implementation-shape test
stale browser/simulator fixture
cross-platform proof bug
profile/evidence problem
source parser problem
CI trigger problem
```

完整失败日志优先。Correct upstream/source/canonical runtime truth > stale fixture/test。禁止为了变绿回退正确产品行为、放宽正确 gate、恢复旧 owner或加 compat hack。
