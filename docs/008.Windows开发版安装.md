# Windows 开发版安装与排障

本文只用于测试 `dev` 分支。稳定版用户继续使用正式 GitHub Release。

## 1. Dev bootstrap authority

Windows Dev 推荐始终从 Virtual qB Pages 获取与当前 materialized distribution 同步发布的 installer：

```powershell
Invoke-WebRequest https://weigefenxiang.github.io/WeiG-qB-WebUI/downloads/dev/install.ps1 -OutFile .\weigg-install-dev.ps1
powershell -ExecutionPolicy Bypass -File .\weigg-install-dev.ps1 -dev -configure
```

指定目录：

```powershell
powershell -ExecutionPolicy Bypass -File .\weigg-install-dev.ps1 -dev -configure -o "D:\软件\下载工具\qbittorrent\WeiG-qB-WebUI"
```

历史旧 installer 的 `-dev` 曾直接下载 GitHub raw source archive，导致 runtime catalog/profile/QM/Settings translation materialization 缺失。当前 installer 已修为 Pages materialized distribution，但**不要复用旧 Release/缓存脚本中的 Dev 实现**。

## 2. Pages-relevant SHA / materialized 规则

Pages payload 自身仍保持严格 exact-SHA identity：

```text
Pages downloads/dev/GIT_SHA
== downloaded package GIT_SHA
```

`current dev HEAD` 可以只因为明确的 Pages-irrelevant commit 暂时领先 Pages SHA，例如：

```text
docs/**
*.md
LICENSE
CI / promotion / release-only workflow
```

此时 Dev installer 会通过 GitHub compare 校验：

```text
Pages published SHA ... current dev HEAD
```

只有中间所有变更都属于明确的 Pages-irrelevant 集合时，才允许继续复用最后一个已验证 materialized payload。任何未知路径、`webui/**`、Virtual qB / simulator、installer、tool、runtime data 或其它 Pages-relevant 变化都会 fail closed，要求新的 Pages build；禁止 raw-source fallback。

materialized payload 至少要有：

```text
private/data/qb-releases.json
private/data/qb-release-profiles/
private/data/qb-settings-native.txt
translations/webui_*.qm
```

如果存在尚未发布的 Pages-relevant 变化，必须：

```text
wait for exact Pages build and retry
```

正式 candidate / promotion / release 的 final dev exact SHA 仍需要同 SHA evidence；若 final freeze 最后只包含 docs-only commit，可显式 `workflow_dispatch` Virtual qB Pages 为该 final exact SHA 建发布证据。日常 docs push 不应因此自动 rebuild/deploy Pages。

## 3. 为什么 raw source 不够

Dev runtime 依赖构建期 materialized：

```text
compact release index
exact/inherited release profile shards
source-proven Settings facts
native QBT_TR registry
official minimal webui_*.qm
exact translation bridge shards
```

缺失时真实 qB 可能表现为：

```text
This API action is not source-proven for the current qBittorrent release.
This qBittorrent version returned no settings in this group.
qB-owned Settings labels unexpectedly fall back to English.
```

遇到这些错误先核对 GIT_SHA/materialized files，不要放宽 source guard。

## 4. Windows `-configure` 已完成的安全链

2026-09-11 事故暴露了 Windows PowerShell 5.1 whole-file transcode 风险。当前 installer 已完成以下 fail-closed hardening：

```text
qBittorrent running -> refuse
non-empty qBittorrent_new.ini/recovery file -> refuse
zero/obviously invalid config -> refuse
multiple discovered configs -> refuse to guess
exactly one [Preferences] required
managed WebUI keys must be unique and inside [Preferences]
strict encoding decode + byte round-trip
byte-identical safety backup
temp write -> reread/invariant verify
encoding/BOM preserve verification
target unchanged recheck
atomic replace
post-replace verification
safe rollback when mutation itself replaced the target
```

支持保留：

```text
UTF-8 BOM
UTF-8 no BOM
UTF-16 LE BOM
UTF-16 BE BOM
```

不要重新引入：

```powershell
Get-Content $cfg -Raw
Set-Content -Encoding UTF8 $cfg
```

## 5. Config discovery

标准 profile 会自动发现常见路径；若存在 multiple candidates、portable/custom profile 或你明确知道配置文件位置，使用：

```powershell
powershell -ExecutionPolicy Bypass -File .\weigg-install-dev.ps1 -dev -configure -qbconfig "D:\path\qBittorrent.ini"
```

多个候选时脚本必须拒绝猜测。

人工运行前仍建议完全退出 qBittorrent；如果没退出，当前 installer 会直接 fail closed。

## 6. 安装后快速验证

```powershell
$root="D:\软件\下载工具\qbittorrent\WeiG-qB-WebUI"
Get-Content "$root\GIT_SHA"
Get-Item "$root\private\data\qb-releases.json" | Select-Object Length
(Get-ChildItem "$root\private\data\qb-release-profiles" -Filter '*.json').Count
Test-Path "$root\private\data\qb-settings-native.txt"
Test-Path "$root\translations\webui_zh_CN.qm"
```

应满足：

```text
installed GIT_SHA == Pages published materialized GIT_SHA
current dev HEAD == installed GIT_SHA
OR
all commits after installed GIT_SHA are verified Pages-irrelevant
qb-releases.json 是 compact index
profile shard directory exists
qb-settings-native.txt exists
webui_zh_CN.qm exists
```

## 7. 当前 CI 事实

旧 release-candidate baseline：

```text
SHA: ba8ee49886fad159e76014514b5a0a4ba803bc29
CI #1349 / 34661850911: PASS
Virtual qB Pages #548 / 34661856893: PASS
qB 4.6.7 candidate acceptance: PASS
qB 5.2.3 candidate acceptance: PASS
Windows PS5.1 config safety contract: PASS
```

本轮 Pages-trigger 修复已经在新的 dev SHA 上重新建立 normal CI / Pages evidence；docs-only follow-up 会继续推进 `dev`，但按设计不再自动重建 Virtual qB Pages。不要把任意旧 SHA evidence 当成 future release 的 final exact-SHA evidence。

## 8. 分支纪律

Windows 排障、installer 修复、docs 修复全部只进入 `dev`。

```text
不要直接改 main
不要为了“快速验证”在 main 修 installer
不要把 main 当 Dev 工作分支
```

main 只能接收经过正式 promotion gate 的 final dev exact SHA fast-forward。详细见：

```text
docs/006.发布与晋级流程.md
docs/014.AI协作与分支纪律.md
```

## 9. 事故复盘

编码事故、恢复过程、为什么必须 fail closed，见：

```text
docs/013.Windows配置编码事故复盘.md
```
