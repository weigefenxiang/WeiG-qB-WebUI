# 002. 兼容与实现状态

Status: **current compatibility / implementation / evidence facts**

本文件只记录当前仍有效的产品兼容事实、已实现能力和验证边界；历史过程由 Git history / Actions / artifacts 保存。Current HEAD 与 Actions 必须 fresh-read。**当前未完成事项只维护在 `docs/014.AI协作与分支纪律.md`**。

## 1. 当前产品状态

```text
repository/product VERSION: 0.3.101
current product commit: d3f1b2ae40b05098ed9bd832a6a6f9bac51e54c5
0.3.101 final-tree bootstrap validation: run 35035651389 SUCCESS
external-validation checkpoint before docs consolidation: aecfab6a531bdcf5145e0ecf29b225da3a957f99
Virtual qB Pages Source run 35036992446: SUCCESS
ordinary CI run 35036992447: FAILURE
Virtual qB Pages run 35037003377: FAILURE
latest published Release/tag: v0.3.61
qB support floor: 4.1.0 official stable
latest admitted stable: 5.2.3
Frozen stable profiles: 65
```

`d3f1b2ae...` 是最近一次真正改变正式产品 bytes 的 0.3.101 product commit；后续 `aecfab6...` 是只改 `docs/014` 的 docs-only `[ui]` validation checkpoint，产品 bytes 与 `d3f1b2ae...` 相同。不要把 docs-only checkpoint 当作新的产品实现，也不要把 bootstrap 内部 PASS 冒充 external exact-head evidence。

0.3.101 bootstrap run `35035651389` 在移除一次性 bootstrap payload/workflow 后的最终工作树上通过 repository contracts、syntax/installer/live checks 与其执行范围内的 Chrome browser runtime。随后 external checkpoint 暴露尚未闭环的 deterministic failures：ordinary CI 的 Linux semantic UI regression 在 `tests/browser-feedback.mjs` 等待 `listen_port` input visible 超时；Virtual qB Pages 的 `locale-bootstrap` 得到 `en` 而非预期 `zh_CN`，`preferences-anchor` 等待 Settings surface 超时。详细日志语义、待办顺序和用户暂停状态只在 `docs/014` 维护。

兼容优先级继续面向 **future official stable / future major**，不得靠 hard-coded major 拒绝未来版本。qB 4.0.x **不属于当前实现/CI 范围**，未来可选；正式支持底线保持 qB 4.1.0 official stable。

## 2. Runtime compatibility truth

当前浏览器 compatibility owner 为 `W.CapabilityRegistry`。它消费 checked-in compact contracts，并结合 `W.QBClient` 检测的 qB/WebAPI identity 解析 exact/equivalent/bounded inherited/fallback 状态。`W.QBClient` 只拥有 transport 与运行实例版本检测，不重新拥有 capability/version policy。

运行时 compact contracts 是 **source-derived / 上游源码生成** 的兼容真值。正式 runtime 不再包含旧 release-profile owner、`qb-releases.json` 或 per-release profile shards；qB official source、65-release catalog、完整 translation/source evidence 只留在 tools/tests/CI/simulator，用来生成或审计 compact contracts。

## 3. Runtime assets

raw `webui/` 现在直接携带：

```text
private/data/capabilities.json
private/data/torrent-compat.json
private/data/detail-compat.json
private/data/settings-compat.json
private/data/source-actions.json
private/data/qb-settings-native.txt
translations/webui_*.qm
VERSION
```

必须不存在：`private/scripts/release-profile.js`、`private/data/qb-releases.json`、`private/data/qb-release-profiles/`、旧 Settings translation shards 与 full recovery runtime bodies。

历史上为解决 connector 大文件传输限制曾短暂使用 `.weigg-bootstrap/**` 分块 payload 与一次性 `dev-bootstrap-*.yml`；它们不是 runtime asset，也不是 product source。最终 product commit 已删除这些临时文件，后续不得重新引入或把 dangling Git blobs 当待上传源码。

## 4. Settings / Locale

Preference current value/presence 只来自 `app/preferences`。`W.SettingsSchema + settings-compat.json` 拥有 Preference semantic/type/setter descriptor；certified write gate 来自当前 compatibility identity；`W.QBClient.setPreferences()` 后必须 `getPreferences()` verification reread。唯一 persisted language truth 仍是 `app/preferences.locale`。

qB-owned copy 走 `W.I18n` + `qb-settings-native.txt` + source-proven official QM/bridge；`4.5.0 -> 4.6.4` Alternative WebUI translation hard gap保持 exact bridge，不因 QM 存在而伪装 native。

Settings compound controls（例如 Listen port + Random、schedule From/To）是**单个 canonical SettingRow 内的组合控件**；组合控件不自动意味着顶层 SettingsGrid full-span。顶层 row 仍按 source/layout contract 占标准 grid cell，除非独立 layout owner 明确证明需要 full-span。

Locale bootstrap 验证必须同时尊重 browser locale、qB persisted `app/preferences.locale` 与 W.I18n presentation 边界；只改显示语言、只改 localStorage 或只让 DOM 看起来是中文都不能替代 qB locale truth。

## 5. Torrent / Detail protected state

`W.TorrentSemantics`、`W.TorrentFieldRegistry`、`W.Selection/ActionRegistry`、`W.QbUiEvidence`、`W.SharedColumns`、`W.ColumnInteraction`、`W.VirtualList` 等既有唯一 owner 不变。Files 必须保持 qB response order 与 exact source file index；synthetic folder != qB source file；file/folder Rename provenance独立；Tracker 使用 exact URL；Peer flags 本地 source-pinned；Copy browser-local；transport mutation只经 QBClient；checked Tag removal必须 exact `removeTagsAction`；unknown dangerous write零 HTTP。

0.3.100/0.3.101 已把 Torrent/Detail 的滚动期数据提交纪律收口到 canonical VirtualList：原生横/纵向滚动或 scrollbar drag 期间允许 polling 继续取得数据，但不得用新 snapshot 替换当前 DOM；只保留 latest pending snapshot，交互 idle 后一次提交。Detail Files 展开/收起以用户点击 folder row 的 viewport Y 为 anchor，不能恢复 stale child anchor。

## 6. Evidence boundary

**Linux/Windows browser evidence** 证明真实浏览器产品行为；**Full qB stable WebAPI evolution audit** 证明 all-stable 上游源码/API 演进。Ordinary CI、Virtual qB、Pages/browser、real-qB 与 release-grade matrix各自证明不同层级，不能互相冒充；`SKIPPED != PASS`，旧 SHA PASS 不自动证明新 SHA。

Pages `build` / `deploy` SUCCESS 也不等于 Pages 全链路 PASS；任何 required live verify lane FAILURE 都表示该 exact checkpoint 的 Pages evidence 未闭环。Promotion/main/tag/Release仍需用户明确授权。
