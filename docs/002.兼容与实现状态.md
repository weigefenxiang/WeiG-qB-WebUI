# 002. 兼容与实现状态

Status: **current compatibility / implementation / evidence facts**


> **A3 当前交接事实（dev 160473f13c5c）**：Product VERSION **0.3.125**；A3 source facts 与 product consumers 已推入 dev，main 未移动、未创建/使用 `[candidate]`。当前 exact-SHA CI #35442179347 与 Virtual qB Pages #35442186315 均 FAIL；首要 blocker 是 Settings runtime 与 canonical Frozen catalog 的 `sourceCatalogSha256` identity/materialization 未一致。状态仍为 **🟡 A3 IN_PROGRESS**，mutable TODO/下一 AI 交接只看 `docs/014` STOP 前。

> **2026-09-19 A3交接同步**：本文件已按当前 A–F 方案复核。mutable current status / blocker / TODO / next-AI handoff 只维护在 `docs/014.AI协作与分支纪律.md` 的 `STOP — COMPLETED ARCHIVE BELOW` 之前；本文件只维护本领域 authority/contract。具体 exact SHA、Actions 与运行结果在实际开发前必须 fresh-read，不以本注记代替 repository truth。

本文件只记录当前仍有效的产品兼容事实、已实现能力和验证边界；历史过程由 Git history / Actions / artifacts 保存。Current HEAD 与 Actions 必须 fresh-read。**当前未完成事项只维护在 `docs/014.AI协作与分支纪律.md`**；本文件不建立第二套 TODO / handoff authority。

兼容政策：正式支持从 **qB 4.1.0 official stable** 开始；qB **4.0.x 不属于当前**实现/CI scope，未来可选的 legacy adapter 需另行确认。未来 stable / new major 继续以 source-derived（上游源码生成）事实自动适配，UNKNOWN dangerous writes fail closed。日常 **Linux/Windows browser** evidence 与 **Full qB Release compatibility audit / all-stable** source audit 是不同证据层，二者不得互相替代。


## 1. 当前产品状态

当前 A 阶段为 **🟡 A3 IN_PROGRESS**。Product VERSION **0.3.125** 已推到 dev；A3 已完成 source facts -> product consumer 接线，包括 Scheduler `zero-pad-2`、RSS bounded `source-action`、WebUI Password source-proven `writeOnly`、Advanced network `dynamicOptions`、RSS Downloader disabled exact source/context copy、generic long-copy layout约束，以及focused/browser contracts。

~~~text
working branch: dev
handoff code HEAD: 160473f13c5c9fe0a14a863a8fd19399d6a70a66
main: do not move
[candidate]: prohibited
product VERSION: 0.3.125
latest published Release/tag: v0.3.61
qB support floor: 4.1.0
latest admitted stable: 5.2.3
Frozen admitted stable profiles: 65
~~~

当前不能宣告 A3 implemented/verified：`160473f...` 上 CI #35442179347 FAIL，Virtual qB Pages #35442186315 FAIL。已定位的第一 blocker 是 committed Settings runtime 的 `catalogIdentity.sourceCatalogSha256=b2b859...` 与 current canonical Frozen catalog identity `9f7833...` 不一致；`capabilities.js` 因此正确 fail closed 为 `Invalid Settings runtime manifest`。新增的 rebind/materializer workflow 已进入 dev，但临时生成 0.3.126 后测试失败，未产生新dev commit，所以仓库 VERSION仍是0.3.125。

Virtual qB Pages Source 在 `4f1b12e...`、`a47158c...`、`160473f...` 都 SUCCESS，说明source-side evidence仍有效；它不能替代产品 smoke/browser/Pages。下一位AI先修canonical Frozen identity/materialization，再跑新增 `settings-fidelity` browser gate；全部exact-SHA自动证据绿后才进入第四轮人工复核。

### 1.1 当前执行优先级：root-cause-first product closure

A3不是重新扩一套 Preferences tooling，也不是继续做“结构完整度”工程；当前优先级是把第三轮人工 FAIL 变成**可稳定自动复现的真实交互失败**，证明断点后再修 canonical product owner：

~~~text
PRIMARY
  A3 real-interaction trace / acceptance
  -> real Select pointer/keyboard path
  -> same Settings draft + live dependency/action
  -> dynamic/write-only control semantics
  -> generic layout/time/locale presentation
  -> formal webui/** owner fix

SUPPORTING ONLY WHEN PROVEN NECESSARY
  tools/**
  source parser / compact IR / census
  materializer / simulator / CI
~~~

tests/tools 工作必须直接绑定本轮已确认人工 failure 或其generic regression contract。禁止为了“让framework更完整”而先扩tooling，也禁止因为A2自动证据已经很强就跳过真实复现直接改产品。Product-first在A3的含义是：**先证明真实产品断点，再修那个唯一owner。**

## 2. qB-owned / WeiG-owned 边界

```text
qB-owned
  Status / native Torrent filters/facets
  Options / Preferences
  RSS feeds/articles/Downloader/rules
  Torrent Detail
  qB actions / fields / menus
  qB official source/context copy

WeiG-owned
  Path facet
  Tracker Private / PT extension
  WeiG WebUI settings
  About
  WeiG presentation/layout/interaction extensions
```

qB-owned 的存在性、顺序、语义由 exact qB source 决定；当前值由 WebAPI 决定；文案由 exact `{source, context}` + official qB translation 决定。WeiG-owned 使用 WeiG schema/i18n/UI。两层不得互相冒充 owner。

## 3. Runtime compatibility truth

浏览器总控制面是 `W.CapabilityRegistry + webui/private/data/capabilities.json`，但 `capabilities.json` 不是所有版本事实的大杂烩。完整 admitted release source truth属于 build/CI Frozen catalog/LKG；browser只按 domain lazy-load compact facts：

```text
capabilities.json      release identity / high-level gates / compatFiles
torrent-compat.json    Torrent filters/status/fields/columns; B still needs native facet manifest
detail-compat.json     Torrent Detail source projection
settings-compat.json   bounded exact-source Settings runtime envelope; lazy decode/identity gate
rss-compat.json        RSS Downloader native surface/rule facts
source-actions.json    exact action/parameter provenance
qB-owned copy artifact source/context + official locale route
```

`capabilities.json.compatFiles` 当前包含 torrent/detail/actions/settings/rss。正式 runtime不得重新加载65-release full catalog。

危险 write继续 fine-grained fail closed：certified exact/equivalent identity、source action/endpoint、required parameter、payload semantics缺一不可；Settings进一步要求 current runtime key、exact Preference setter/type、`projection.safeWrite` 与 dependency gate同时成立。UNKNOWN/future不允许用 major/nearest-version猜写能力。



## 4. A / A3 — Options / Preferences 当前事实

### A-base/A1/A2 是 foundation；A2第三轮人工复核FAIL，A3正在进行

A-base建立单一 Settings Current Owner、bounded source-native runtime与dangerous-write fail-closed gate；A1建立generic exact-source Control Graph；A2在0.3.124进一步完成 full-fidelity source content/action/predicate census、presence projection、typed `app()->` receiver proof、compact generic renderer、expanded textarea、qB-owned copy closure以及focused/exhaustive automated Options audit。#1940/#1941 自动证据仍有效，但第三轮真实qB Options人工复核已确认它们不能证明最终可用性。

A3已确认的真实缺口：

- Downloads的 `Torrent content layout` / `Torrent stop condition` options可见但真实用户无法选择；
- Downloads external-program 与 Behavior IP-filter 长文案/说明出现重叠；
- Speed Scheduler分钟显示 `0` 而不是时间语义的 `00`；
- BitTorrent `Encryption mode` 与做种限制后的 `Then` Select无法真实选择；
- RSS auto-download开关无法勾选，Edit auto downloading rules source action/dialog与禁用反馈缺失；
- Web UI username旁缺失 Password source control/label，需验证API-omitted/write-only control建模；
- Advanced的Network interface / optional IP address需要正确的可选项来源；I2P quantity/length等source-owned zh_CN仍发生英文fallback。

A3不把上述现象直接等同于根因。当前必须逐项trace：

~~~text
exact qB source/API
-> compact Settings facts / option or action identity
-> SettingsSchema intersection
-> W.SettingsRenderer / W.Components.selectControl
-> real pointer/keyboard event
-> same Settings draft
-> predicate/action re-evaluation
-> setPreferences+reread OR exact source action/dialog
-> layout / exact source-context locale presentation
~~~

优先根因家族是：canonical Select真实事件链、live dependency重算、static-vs-runtime dynamic options、API不返回current value的write-only/敏感控件、source helper/action/dialog、generic intrinsic layout、time-composite formatter、exact locale/context closure。**这些是调查方向，不是已证实根因。**

A3 validation必须先让browser acceptance能以真实用户交互稳定复现人工失败，再按已证明的generic owner修复；不得 programmatic set/change 测绿后宣称Select可用，也不得新增 Preference-key 特判。完整当前计划只维护在 `docs/014` active区。

## 5. B / E — Torrent Sidebar 与 Detail 当前事实

Status filter semantics已走 `W.CapabilityRegistry + W.TorrentSemantics`；Torrent Detail已有成熟的 `torrentDetailUi() -> detail-compat.json -> W.QbUiEvidence -> generic Detail runtime` 链，E不另造第二套 Detail系统。

B仍未完成：`spatial.js` 目前固定声明 category/tag/tracker/savePath四个 facet，LibraryController也仍按固定 state/setter知道这些维度。目标是 source-derived native facet inventory/order/semantics -> generic state/projection；所有 qB-native facets按 exact source顺序出现，WeiG-owned Path永远最后。`All`只是 no-filter option，不应成为额外持久 facet标题。

E剩余工作是 source/copy/action completeness audit并补真正缺口，不能因为A/B/C/F重构而复制 renderer、polling、columns或 Detail state owner。

## 6. C — qB-owned copy 当前事实

`W.I18n` 已存在 exact-release official translation主链，`qb-settings-native.txt` 仍是当前 Settings-focused runtime copy registry；但 Settings外层、RSS、Sidebar等 surface仍存在手写英文/中文 fallback。

目标规则：qB-owned copy只以 exact upstream `{source, context}` 为身份；目标 locale从 exact release official translation facts解析；官方目标翻译不存在时只安全回退 upstream English source，不维护 WeiG英文→中文第二数据库。WeiG-owned Path、Private/PT、WeiG WebUI、About等继续 WeiG i18n。

若将 `qb-settings-native.txt` 泛化为 domain-neutral qB-owned copy artifact，必须原子替换文件、loader、callers、tests与 distribution contract；禁止新旧两个 copy registry并存。

## 7. D — RSS 当前事实

D的真实 rule-fidelity bug已修入当前产品：

- qB 4.3.0+ `smartFilter` 由 source manifest渲染/写回。
- qB 4.6+/5.x 使用 `torrentParams.stopped`，不再错误依赖 `torrentParams.paused`。
- historical `addPaused/createSubfolder` 与 modern `stopped/content_layout` 的 default/null语义由 source options保留，避免 checkbox破坏 null/default。
- `rss.js` 以 source-returned完整 rule object作为 draft base，只更新 manifest映射字段，未知字段保留。
- `rss-compat.json` 已成为正式 lazy-load runtime domain。
- native-surface source contract已经覆盖 all admitted stable；当前 latest exact SHA的最终 run状态仍需 fresh-read Actions确认。

D仍不能宣告整体完成：还需要 C/F 的 qB-owned official copy、independent RSS inventory/census、common catalog identity integration，以及 feeds/articles/read-state/Downloader actions final audit。不得恢复手写 rule-field owner。

## 8. F — Source admission / compact runtime 当前事实

已完成的 F-foundation：

```text
fixed Settings native-tab whitelist retired
common catalogIdentity helper exists
capabilities/torrent/detail/actions/settings compact compiler outputs share catalogIdentity
Preferences independent census exists and is wired into full-stable source contract
Preferences keyed native IR carries the same Frozen catalog identity
Preferences full source admission now uses one base + 16 bounded shards + strict exact-SHA aggregate
```

仍未完成：

```text
A2 0.3.124 full-fidelity Options automation已建立 #1940/#1941 exact-SHA证据，但第三轮真实人工复核FAIL；当前必须先完成A3并由用户真实复核PASS，之后才能做 required admitted-version A full validation
rss-compat generation is not yet unified into the same compact-runtime/common identity closure
runtime/distribution does not yet reject every mixed/stale domain set by common identity
independent census is not yet universal for B/C/D/E/F domains
```

最终 F要求所有 compact domains与 qB-owned copy都来自同一个 Frozen catalog并携带/验证同一 identity；browser只加载 bounded current-domain facts，不加载 full history。

## 9. 验证边界

阶段内部只跑与当前 product change直接相关的 unit/source/focused change-point tests；不要把“继续增加测试数量”当成阶段进度。阶段完整收口后最多一次 admitted-stable offline/Virtual full matrix。阶段 65 expected set必须直接来自 Frozen catalog，并校验 `executed==expected`、missing/duplicate/unexpected为空。

Source-driven domain completion必须满足：

```text
upstream inventory
= mapped facts + explicit reviewed exclusions
unaccounted = 0
duplicates = 0
```

Preferences字段级source census、formal Settings唯一browser owner、A2 full-fidelity graph/copy/materialization与automated browser evidence均已建立。第三轮真实人工复核仍暴露真实interaction/presentation缺口，因此当前缺的是A3 root-cause closure、A3真实人工复核与其后的 required full-version validation；全部闭环前 A 不得完成。A–F全部完成并冻结 final dev exact SHA后，才按 `docs/011` 运行一次昂贵 real-qB Full Frozen Matrix。若 real-qB后又产生新的最终产品 commit，则旧 SHA evidence不能证明新 SHA。
