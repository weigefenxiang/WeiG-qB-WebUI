# 002. qBittorrent 兼容与实现状态

更新时间：2026-08-31  
当前产品版本：**0.3.1**

## 1. 目标范围

- Compatibility floor: **qBittorrent 4.1.9**
- 4.x target: **4.1.9 → 4.6.x**
- 5.x target: **5.0.x → 当前稳定 5.2.x**
- Future: 未验证新版本进入 capability-based compatibility mode。

## 2. Release-blocking 实机环境

```text
Lab A
qBittorrent 4.1.9.1
WebAPI 2.2.1
container: qbittorrent
host config root: /root/qbittorrent/config

Lab B
qBittorrent 5.2.0
WebAPI 2.15.1 (live detected)
container: q3
host config root: /root/qbittorrent3/config
```

两端都属于 Release Blocking。自动 fixture、静态 CI、源码分析都不能替代真实实例回归。

## 3. 当前测试状态

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| CI / smoke / JS syntax | PASS | v0.3.1 当前兼容补丁 CI 已通过 |
| 4.1.9.1 fixture / WebAPI 2.2.1 | PASS | 自动兼容夹具 |
| 4.6.x fixture / WebAPI 2.8.3 | PASS | 自动兼容夹具 |
| 5.2.0 fixture / WebAPI 2.14.x contract | PASS | 自动兼容夹具，覆盖 2.13 tracker 参数与 2.14 structured add contract |
| Lab A 登录协议 | PASS | `Ok.` + HTTP 200 已实测 |
| Lab A v0.3.0 scroll | **FAIL / 已定位** | 每次约 3s polling 时 viewport 向上移动 |
| Lab A v0.3.1 scroll | **待实机复测** | teardown scroll-event guard 已加入 |
| Lab B 5.2.0 targeted compatibility | **PASS** | q3 实测：qB 5.2.0 / WebAPI 2.15.1；Login/Add/Start/Stop/EditTracker/Preferences/Transfer 通过 |
| Lab B 5.2.0 full UI regression | **部分完成** | 仍需 Private/PT、Tags、Settings save、Files/Peers、Search/RSS/Logs、Scroll/Empty State 等完整 UI 回归 |

## 4. Lab A 认证问题结论

4.1.9.1 已通过直接 WebAPI 登录验证：

```text
Ok.
HTTP 200
```

因此：

- `/api/v2/auth/login` 请求格式正常；
- 反向代理路径正常；
- qB 4.1.9 WebUI authentication contract 正常；
- 之前失败的 `Fails.` 来自旧 `Password_ha1` 与输入密码不匹配；
- 早期一次手工恢复脚本还曾错误匹配双反斜杠键；真实配置键必须按单反斜杠处理：

```text
WebUI\Username
WebUI\Password_ha1
```

普通 WeiG installer 不修改 WebUI username/password。

## 5. v0.3.1 Scroll teardown hotfix

Lab A 实机反馈确认：底部 `Refresh HH:MM:SS` 每约 3 秒更新时，Torrent 列表也发生向上位移。时间文字本身不是滚动原因；它只是和主轮询同频，因此暴露了问题。

根因：

```text
polling
→ app renderList()
→ 旧列表 container 被清空
→ 浏览器产生瞬时 programmatic scrollTop=0
→ 旧 VirtualList scroll handler 仍可能收到这次 scroll event
→ __weiggVirtualScrollTop 被覆盖成 0/更小值
→ 新 VirtualList 恢复了错误位置
```

v0.3.1 修复：旧 scroll handler 在自己的 spacer 已从 active container 脱离时直接忽略 scroll event：

```text
!spacer.isConnected
或
spacer.parentNode !== container
→ 不更新 remembered scroll state
```

真实用户滚动仍正常更新 `__weiggVirtualScrollTop`。

同时隐藏底部 `Refresh HH:MM:SS`。DOM hook 暂时保留，以避免旧 controller 的 `last-refresh` 写入产生 null 错误；它不再作为产品 UI 展示。

## 6. v0.3 Stable Virtual UI

`VirtualList`：

- 在 container 上记录 `__weiggVirtualScrollTop`；
- 新 VirtualList 接管同一 container 时恢复用户滚动位置；
- 替换旧 scroll handler，避免轮询重建 listener leak；
- `setItems()` 默认 preserve viewport；
- 位置超过新数据最大高度时做 clamp；
- 暴露 `resetScroll()` 给 deliberate context change。

以下用户主动上下文变化才 reset：

```text
Filter / Tracker / Save Path / Category / Tag
Prev / Next page
Page size
Torrent search
```

普通 polling / manual Refresh 应保持当前位置。

## 7. Compact Empty State

统一处理所有 0 Torrent 结果：

```text
Private / PT
Error
Downloading
Seeding
Completed
Paused
Active
Stalled
Tracker
Save Path
Category
Tag
Torrent search
```

确认 0 条后：

- 隐藏空 DataGrid body；
- 隐藏 Pager；
- Empty State 压缩为约 180–240px；
- Filter Shelf、Stats、全局状态仍保留；
- Mobile 使用同一语义的紧凑布局。

## 8. Transfer Control Dock

Desktop 状态栏：

```text
↓ DL | ↑ UL | ALT | Connection | Torrent count | Transfer
```

`Refresh HH:MM:SS` 从 v0.3.1 起不再展示。

点击 ↓ / ↑ 可修改全局限速；ALT 切换备用限速；Transfer 打开 Session + Graph；Connection 继续打开 qB/WebAPI/Compatibility 详情。

### 跨版本 API

```text
GET  transfer/downloadLimit
POST transfer/setDownloadLimit
GET  transfer/uploadLimit
POST transfer/setUploadLimit
GET  transfer/speedLimitsMode
POST transfer/toggleSpeedLimitsMode
GET  transfer/info
GET  sync/maindata
```

`QBClient` 已封装：

```text
getGlobalDownloadLimit
setGlobalDownloadLimit
getGlobalUploadLimit
setGlobalUploadLimit
getAltSpeedMode
toggleAltSpeedMode
getMainData
```

UI 不直接 fetch `/api/v2/*`。

## 9. Session Stats / Transfer Graph

已实现：

- 订阅现有 `getTransferInfo()`；
- 不新增第二个高频 transfer timer；
- ring buffer 最大 900 samples；
- 1 / 5 / 15 min；
- Session downloaded / uploaded；
- current Global DL / UL limit；
- DHT / Peer connections；
- `sync/maindata.server_state.free_space_on_disk` 可用时显示 free space；
- Canvas 原生绘图。

## 10. 4.x / 5.x 自动兼容矩阵

### 4.1.9.1 / WebAPI 2.2.1

```text
major=4
resume → torrents/resume
pause  → torrents/pause
stopped filter → paused
globalSpeedLimits = true
altSpeedLimits = true
mainData = true
tags = false
privateFlag = false
cookies = false
torrentCreator = false
```

### 4.6.x / WebAPI 2.8.3

```text
tags = true
tagFilter = true
privateFlag = false
globalSpeedLimits = true
cookies = false
```

### 5.2.0 / WebAPI 2.14+ contract fixture

```text
major=5
resume semantic → torrents/start
pause semantic  → torrents/stop
paused filter   → stopped
tags = true
privateFlag = true
cookies = true
torrentCreator capability foundation = true
trackerEditUrl = true at WebAPI >= 2.13.0
structuredTorrentAdd = true at WebAPI >= 2.14.0
globalSpeedLimits = true
```

`tests/compat-v030.mjs` 使用合成 API-contract fixture 验证 2.13/2.14 的 breaking changes。真实 q3 已检测到 **WebAPI 2.15.1**，并通过对应 targeted live test。

## 11. Lab B 5.2.0 实机兼容结果

2026-08-31 在 `q3`（`linuxserver/qbittorrent:5.2.0`）执行 `tests/live-qb52.sh`：

```text
qBittorrent: v5.2.0
WebAPI:      2.15.1
Login:       HTTP 204
Bad login:   HTTP 401
Torrent list HTTP 200
Add:         HTTP 200 + structured JSON
Stop:        HTTP 200
stopped filter: PASS
Start:       HTTP 200
EditTracker: HTTP 204
Preferences: HTTP 200
transfer/info: HTTP 200
transfer/downloadLimit: HTTP 200
transfer/uploadLimit: HTTP 200
transfer/speedLimitsMode: HTTP 200
```

Structured add 实际响应包含：

```text
added_torrent_ids
failure_count=0
pending_count=0
success_count=1
```

因此以下 5.2 compatibility hotfix 已获得真实实例验证：

- 登录同时接受旧版 `200 Ok.` 与 5.2 `204 No Content`；
- `401 Unauthorized` 映射为用户名/密码错误；
- `torrents/add` 同时兼容旧 `Ok.` 与现代 structured JSON / 202 contract；
- WebAPI >= 2.13 的 `editTracker` 使用 `url`，旧版继续使用 `origUrl`；
- 5.x 使用 `start` / `stop` 与 `stopped` filter。

这表示 **5.2.0 API compatibility targeted gate 已 PASS**，但不等价于整个 Lab B UI 回归全部完成。

## 12. 功能 Capability 状态

| 能力 | 4.1.9.1 | Mature 4.x | 5.2.0 target |
| --- | ---: | ---: | ---: |
| Torrent Start/Stop semantic bridge | ✓ | ✓ | ✓ live |
| Global DL/UL limits | ✓ | ✓ | ✓ API live |
| Alternative speed mode | ✓ | ✓ | ✓ API live |
| Transfer/session stats | ✓ | ✓ | ✓ endpoint live |
| Search | API gate | ✓ | ✓ |
| RSS | API gate | ✓ | ✓ |
| Logs | × at old API | ✓ | ✓ |
| Tags | × | ✓ | ✓ |
| Exact Private flag | × | × | ✓ |
| Tracker edit | ✓ at 2.2+ | ✓ | ✓ live at WebAPI 2.15.1 |
| Cookie API foundation | × | version gate | ✓ at supported WebAPI |
| Torrent Creator foundation | × | × | modern capability gate |

## 13. VueTorrent 差距审查

WeiG v0.3 已补：

```text
Session stats
Transfer graph
Global speed controls
Alternative speed mode
rendered-row Ctrl/Cmd/Shift selection
```

已建立兼容层基础但尚未做完整 UI：

```text
Torrent Creator
Cookie Manager
```

现代功能仍必须 Capability gate，不能破坏 4.1.9 floor。

## 14. 仍需真实回归

### Lab A — 4.1.9.1

v0.3.1 第一优先级：

```text
1. 主列表滚到中间，等待至少 15s
2. 确认多个 polling 周期完全不向上移动
3. 滚到底部再等待至少 15s
4. 手工点击 Refresh，也不改变 viewport
5. 切换筛选/分页时应主动回顶部
6. 底部不再显示 Refresh 时间
7. Compact Empty State
8. Global DL/UL / ALT / Transfer Graph
```

### Lab B — 5.2.0

已完成 targeted API compatibility：

```text
✓ Login 204
✓ bad credentials 401
✓ torrents/info
✓ structured torrents/add
✓ start/stop
✓ stopped filter
✓ editTracker url parameter
✓ app/preferences read
✓ transfer/info
✓ global DL/UL read endpoints
✓ ALT mode read endpoint
```

完整 UI release regression 仍需：

```text
1. exact Private/PT
2. Tags
3. Global DL/UL UI write + restore
4. ALT UI toggle + restore
5. Transfer Graph
6. Settings save + restore
7. Files / Tracker / Peers UI
8. Search / RSS / Logs
9. Scroll stability / Empty State
10. Desktop / Mobile / i18n interaction pass
```

## 15. Release 判定

```text
CI PASS
+
4.1.9.1 live regression PASS
+
5.2.0 full live regression PASS
+
Desktop / Mobile / i18n / performance PASS
=
允许稳定版发布
```

任何 fixture PASS 或 targeted API PASS 都不能替代对应 full live regression。
