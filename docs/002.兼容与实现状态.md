# 002. qBittorrent 兼容与实现状态

更新时间：2026-08-31  
当前产品版本：**0.2.2**

## 目标范围

- Compatibility floor: **qBittorrent 4.1.9**
- 4.x target: **4.1.9 → 4.6.x**
- 5.x target: **5.0.x → 当前稳定 5.2.x**
- Future: 未验证的新版本进入 capability-based compatibility mode。

## 实机回归环境

```text
Lab A
qBittorrent 4.1.9.1
WebAPI 2.2.1
container: qbittorrent
host config root: /root/qbittorrent/config

Lab B
qBittorrent 5.2.0
container: q3
host config root: /root/qbittorrent3/config
```

两端都属于 Release Blocking。未在真实实例上验证过的 patch 版本只能标记 compatibility target，不能宣称 certified。

## 已实现兼容桥

### Start / Stop

```text
qB 4.x
resume / pause

qB 5.x
start / stop
```

`QBClient` 选择首选 endpoint；必要时对 404/405 使用兼容 fallback。

### Paused / Stopped filter

```text
qB 4.x: paused
qB 5.x: stopped
```

UI 使用统一语义，兼容层负责转换。

### Version parsing

- qB 应用版本允许 `v5.2.3` 等前缀；先 normalize 再取 major。
- WebAPI 使用点分整数比较，禁止 `parseFloat()`。

```text
2.11.3 > 2.8.0
```

### Historical exceptions

qB 4.3.3 存在 WebAPI version reporting 历史异常，因此 Capability 不能只依赖报告版本；已保留已知例外和 endpoint fallback 入口。

### Legacy filters / capabilities

老 4.x 不支持的筛选、Tags、Private flag 或新操作按 Capability 隐藏，不向旧实例暴露已知不可用控件。

## v0.2.2 UI/运行架构状态

已实现：

- Topbar 应用级导航；
- Desktop Sidebar 只保留 Torrent 状态筛选；
- Tracker / Save Path / Category / Tags → Filter Shelf + searchable Popover；
- Connection 详细信息 → Status Dock Popover；
- English canonical + zh-CN 维护翻译 + English fallback；
- Settings metadata/schema + human-readable label/description；
- Settings Title → Description → Control；
- Desktop Settings 两列 / Mobile 单列；
- Nebula Spatial 六级 Surface；
- Search/Input 默认、Hover、Focus 视觉区分；
- 20 / 50 / 100 / 200 server page sizes；
- VirtualList / DataGrid；
- Tracker credential normalization；
- Mobile Torrent Card / Bottom Navigation / Action Sheet；
- Search / RSS / Logs capability-aware；
- Linux / Windows installer、backup、rollback。

## 性能实现

Torrent 主列表：

```text
pageSize = 20 / 50 / 100 / 200
limit = pageSize
offset = page * pageSize
```

DOM：

```text
Data page/cache
↓
Virtual Window
↓
Viewport + overscan rows
```

当前页 200 条数据不代表 200 个挂载 Torrent Row。

Tracker / SavePath / PT 全库索引按有限批次构建，只生成 Data Catalog，不生成隐藏 DOM。

## i18n 状态

正式维护：

```text
English
简体中文
```

其他 locale overlay 可以存在，但缺失词条必须 English fallback。

qBittorrent 专有术语优先参考官方 WebUI translations；WeiG 独有术语由本项目维护。

## 静态 CI 当前验证

- 必需运行文件存在；
- JavaScript syntax；
- v0.2.2 Spatial CSS/Controller 存在；
- Filter Shelf / Connection Dock / Settings vertical layout tokens；
- English canonical markup/i18n；
- Settings schema/translations；
- 20/50/100/200 page size；
- VirtualList / DataGrid；
- 4.x / 5.x start-stop compatibility tokens；
- Reduced Motion；
- Runtime UI 不依赖外部 CDN；
- Installer multi-instance safety。

## 仍需实机继续验证

- 4.1.9.1：Filter Shelf 点击/筛选、Settings 保存、老 API 特性、移动端；
- 5.2.0：start/stop、stopped filter、Private flag、Tags、新 Preference；
- 不同高度 Desktop：确认 Sidebar 无嵌套滚动条；
- 320/375/390/430/768px：Filter Popover、Settings 单列、Bottom Navigation；
- 1000+ / 5000+ Torrent：Catalog、分页、Virtual DOM、Tracker facet 性能。

## 发布原则

静态 CI 成功 ≠ 全版本 certified。

```text
4.1.9.1 live PASS
+
5.2.0 live PASS
+
CI PASS
+
UI / i18n / mobile / performance PASS
=
允许稳定版发布
```
