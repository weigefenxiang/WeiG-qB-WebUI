# 002. 兼容与实现状态

Status: **current compatibility / implementation / evidence facts**

本文件只记录当前仍有效的产品兼容事实、已实现能力和验证边界；历史过程由 Git history / Actions / artifacts保存。Current HEAD与Actions必须 fresh-read，本文的 SHA只能作 checkpoint。

当前未完成事项只维护在 `docs/014.AI协作与分支纪律.md`；本文件不复制 mutable TODO / handoff 状态。

## 1. 当前产品状态

截至 2026-09-15：

```text
repository/product VERSION: 0.3.87
latest published Release: v0.3.61
main: stable promoted baseline
dev: active; every write must fresh-read refs/heads/dev
0.3.87 final implementation checkpoint: 8af8a172580922f8ac4f256da34d85807dc5ef92
qB support floor: 4.1.0 official stable
latest admitted stable: 5.2.3
Frozen stable profiles: 65
current-stable locale surface: 61
```

`VERSION = 0.3.87` 不代表 `v0.3.87` 已发布。qB 4.0.x/WebAPI v1不是当前正式实现/CI scope；仅作为未来可选研究。

0.3.87 当前方案已经在 final implementation SHA `8af8a172580922f8ac4f256da34d85807dc5ef92` 建立 ordinary CI、Virtual qB Pages Source、materialization、deploy 与 live verification evidence。后续 docs-only SHA 必须 fresh-read current dev 并按 classifier/Pages representability 规则判断适用 evidence，不能把旧 implementation SHA 自动当作新 SHA 的 release-grade证明。

兼容策略优先未来 stable / new-major compatibility：不得按 major 号硬编码拒绝未来版本，仍以 source-derived capability/evidence fail closed。验证证据明确区分 Linux/Windows browser smoke 与 all-stable upstream source audit；前者证明浏览器产品路径，后者证明完整上游 stable truth。

## 2. Source / release profile

正式 compatibility truth仍由上游源码生成：

```text
exact qB official source / official TS
-> per-release source facts
-> deterministic merge/recovery evidence
-> certified Settings/source LKG
-> Frozen/admitted release facts
-> bounded runtime release profile + compiled runtime copy IR
-> canonical product owners
```

Release profile继续提供 exact torrent columns、Torrent Detail UI/action facts、Preference getter/setter/type provenance、action/parameter surface、translator behavior/locale facts等。Resolution保持：

```text
EXACT -> EQUIVALENT -> bounded INHERITED -> FALLBACK
```

Future/unknown dangerous writes fail closed；field/action/Preference presence不得按 patch version猜。

## 3. Settings 当前实现

Current value/presence owner已经是真实 qB WebAPI：

```text
W.QBClient.getPreferences()
-> app/preferences actual keys + values
-> W.SettingsSchema semantic grouping/control
-> W.ReleaseProfile exact setter/type/action provenance
-> W.SettingsRenderer
```

Write仍要求 source-proven writable/type + `setPreferences` action，并在 POST 后 `getPreferences()` verification reread；unknown returned Preference没有 exact setter/type proof时只读。

0.3.87 已把 source-proven native Settings topology 收口到一个 canonical tab reconciler：Downloads / Connection / Speed / BitTorrent / RSS / Web UI / Advanced / About 等来自 exact Settings/source facts的 surface由同一个 owner组织和暴露，不再用独立 Speed/单项补丁 owner修复缺 tab。Settings当前值或当前 key存在性仍只来自真实 `app/preferences`，不依赖 historical runtime Settings translation shard。

## 4. Locale / qB-owned copy / runtime slimming

唯一 persisted language truth仍是：

```text
qB app/preferences.locale
```

qB-owned wording统一走一个 source-derived runtime IR：

```text
exact source/context + certified official translation evidence
-> deduplicated refs/bindings/bridge sets
-> private/data/qb-settings-native.txt
-> W.I18n.qbSetting/qbText/loadQbOwnedText
```

Native-capable active-root family只生成 **WeiG实际引用 refs** 的 minimal `webui_<locale>.qm`；完整 deterministic recovery union仍是 build-time evidence，不进入 runtime。

Translator hard boundary保持：

```text
4.1.0 -> 4.1.3   qApp/application translator
4.1.4            dedicated strict
4.1.5 -> 4.4.5   dedicated native
4.5.0 -> 4.6.4   Alternative WebUI document translation disabled
4.6.5+            dedicated native restored
```

所以 `4.5.0 -> 4.6.4` 的 11 个 stable release继续 exact compact official-TS bridge；不能因 QM存在就伪装 native。

已退休 end-user runtime assets/loaders：

```text
private/data/qb-settings/
old full qb-settings-native.txt registry BODY/FORMAT
W.I18n loadSettingsData/loadNativeSettingsData dual path
release-profile settingsTranslationPath/settingsNativeLocales/settingsTranslationLocales duplicates
full/unfiltered recovery QM runtime payload
```

`qb-release-profiles` 仍保留 exact source/action/parameter/field/Preference provenance；其 dedupe是独立后续 Batch B，不属于当前已批准方案，且当前没有用户授权继续该工作。

## 5. Main Torrent table / list

Exact/certified release主表链不变：

```text
W.ReleaseProfile.torrentTableColumns
-> W.TorrentFieldRegistry
-> W.DataGrid / Columns UI / row renderer
```

Source schema拥有 native key/order/defaultVisible/defaultWidth/dataProperties；用户 visibility/order/width是 override。主列表 `#torrent-list` / `W.VirtualList` 是唯一 horizontal scroll/virtual projection owner；横向滚动期间 polling继续但 repaint coalesce，到 bounded idle只 flush最新 snapshot一次。

## 6. Torrent Detail

Current chain：

```text
exact qB properties/detail/menu/controller source
-> Detail source parsers
-> W.ReleaseProfile.torrentDetailUi + actionDescriptor
-> W.QbUiEvidence
-> generic app Detail runtime
-> W.SharedColumns / W.ColumnInteraction
-> W.QBClient
```

已完成并保持：source-driven tab order、General source layout、generic table renderer、Tracker exact URL/source projection、Peer source-driven country projection、source-driven context actions、shared Columns first-toolbar placement、header/body同 definition与single horizontal viewport。永久 inline Tracker Edit/Delete与Peer `×` mutation affordance已退休。

0.3.87 对 Detail 的当前收口为：

```text
General -> exact source-generated field/group layout + one local scroll owner
Files / Trackers / Peers / HTTP Sources -> shared table toolbar/layout owner
Peers country_code -> source-pinned qB-owned local images/flags/<iso>.svg
raw/unmaterialized source -> safe ISO-code fallback, no external flag CDN runtime dependency
```

qB 4.1.0、4.6.7、5.2.3 的原生 Peer country rendering均沿本地 `images/flags/<iso>.svg` 约定；materialization由现有 release catalog owner从 pinned qB source生成本地资产/CSS，而不是把现代 CDN sprite行为写成 compatibility truth。

Detail mutation继续：

```text
source context menu
-> source availability
-> actionDescriptor
-> endpoint agreement
-> required/optional parameter/options
-> generic binder
-> QBClient.request
```

无法证明 required parameter/endpoint/source action时 HTTP前fail closed。

## 7. Transfer / capability / shared owner

Transfer继续使用 existing `transfer/info` + `W.TransferRuntime`；没有第二 polling owner。Read/write/action/field/parameter capability继续分离证明；dangerous `UNKNOWN` 必须 transport前阻断。

一个 semantic purpose只有一个 Current Owner；禁止 runtime alias、monkey patch、MutationObserver repair、post-render repair、第二业务状态或重复 polling。

## 8. Installer / Universal Portable / distribution

Stable installer消费 immutable Release + checksum/identity；Dev installer只消费 Pages exact-SHA materialized distribution，禁止 raw-source fallback。

Universal Portable要求继续成立：

```text
one materialized WeiG-qB-WebUI folder
+ offline
+ copy to supported qB host
+ set Alternative WebUI path
= works
```

当前 materialized runtime保留：

```text
qb-releases.json
qb-release-profiles/
qb-settings-native.txt   # compact qB-owned runtime IR; compatibility filename
minimal translations/webui_*.qm
images/flags/*.svg       # source-pinned local qB-owned Peer flags
css/qb-peer-flags.css    # materialized local bindings
VERSION / GIT_SHA
```

并拒绝旧 `qb-settings/` 与旧 native-registry body/dual-loader 回流；`qb-settings-native.txt` 只保留为 compact IR 的兼容文件名。Distribution manifest输出 core/profile/translation/redundant/total/ZIP/max-file size breakdown；单文件继续低于 qB 10 MiB hard limit，并以项目 `<5 MiB` target fail closed。

以下是 0.3.86 runtime-convergence checkpoint `a05b25d2e3034183dedf42521e0ed65a7862a954` 的历史 package facts，仅作为已验证 slimming 基线，不冒充 0.3.87 当前包体数字：

```text
profiles: 65
minimal QM assets: 66
qb-settings-native.txt: 3,829,474 B
legacy Settings assets: false
redundant bytes: 0
total uncompressed: 17,250,820 B
distribution ZIP: 2,455,131 B
ZIP sha256: 199b8fde97263ab623808f9dbf66335d434dfd2a3349bf9d020c6721ae95fb3b
```

0.3.87 final implementation SHA 已重新完成真实 Pages materialization并成功部署；由于本轮增加本地 qB flag assets，若需要审计当前精确 bytes/ZIP，应以该 exact SHA 的 Actions/artifact/manifest为准，而不是复用上述 0.3.86数字。

Windows qB config mutation的 running/recovery-file/encoding/BOM/byte-roundtrip/atomic-replace/post-verify/rollback safety不变；installer-owned backup最多保留3份且必须先更新 `last-backup` 再 prune。

## 9. Validation status

0.3.87 当前方案 implementation batch 已完成。Final implementation SHA `8af8a172580922f8ac4f256da34d85807dc5ef92` 的适用 evidence：

```text
CI #1697 / run 34919368407: SUCCESS
Virtual qB Pages Source #785 / run 34919368408: SUCCESS
Virtual qB Pages #770 / run 34919376184: SUCCESS
materialization: SUCCESS
deploy: SUCCESS
live verification: core/services/mobile/locale/release-profile/preferences lanes all SUCCESS
```

本轮实现/验证覆盖 native Settings topology reconciliation、General source-layout + single-scroll owner、shared Detail table toolbar/layout、source-pinned local qB Peer flags，以及阻止这些真实 owner落地的 stale semantic gates。没有为了过 gate恢复旧 Speed owner、旧 docs固定字符串、外部 flag CDN或其它错误 runtime owner。

本轮 scope不包含：manual Current Stable Locale Matrix、Real qB Full Frozen Matrix、promotion、main movement、tag、Release或 `[candidate]`；这些没有被运行，也不能因为0.3.87日常开发验证完成就自动进入。

当前没有新的已批准产品 TODO。下一步开发等待用户提出新方案；AI 不得自行继续 Batch B、UI优化、promotion或其它未授权工作。

## 10. 文档维护

Current facts变更更新本文件；Settings/Locale/runtime-copy contract见 `docs/005`；architecture owner见 `docs/003`；installation/runtime package见 `docs/007`；当前未完成事项只维护 `docs/014`。禁止再创建 patch-version复盘/第二 handoff。
