# 002. 兼容与实现状态

> 本文件只记录**当前仍有效的产品兼容事实与最新验证状态**。历史排障过程见对应复盘文档。

`webui/**` 是正式产品；source catalog、real-qB、Virtual qB、browser 与 Pages 是发现/证明层，不替代正式产品语义。

## 1. 当前版本与分支状态

截至 2026-09-12，本轮 docs 复盘开始前：

```text
published latest Release: v0.3.61
repository/product VERSION: 0.3.71
validated product SHA: ba8ee49886fad159e76014514b5a0a4ba803bc29
dev:  ba8ee49886fad159e76014514b5a0a4ba803bc29
main: ba8ee49886fad159e76014514b5a0a4ba803bc29
v0.3.71 tag: absent
v0.3.71 GitHub Release: absent
qB support floor: 4.1.0 official stable
latest admitted stable: 5.2.3
Frozen stable profiles: 65
```

本次 docs-only 更新必须先进入 `dev`，因此提交后 `dev` 会领先 `main`。这属于正常开发状态，**不得为了“让分支看起来一致”直接修改 main**。

任何 commit（包括 docs）都会改变 exact SHA。旧 candidate/Pages evidence 只证明旧 SHA；未来准备 promotion/release 时，必须基于新的 final dev exact SHA 重新建立要求的 evidence。

## 2. 当前已完成的 P0/P1

### 2.1 Release exact identity

Linux/Windows stable installer 当前都执行：

```text
resolve Release tag metadata
-> resolve tag to exact commit SHA
-> pin asset URL to resolved tag
-> verify SHA256SUMS
-> require package VERSION == tag version
-> require package GIT_SHA == tag commit SHA
```

成功时 source identity 明确输出：

```text
Source: Release v<VERSION> at <40-char SHA> (checksum and Release identity verified)
```

指定版本不存在、tag/version/SHA 不一致或 checksum 不一致时全部 fail closed；不得 fallback 到 latest/dev。

### 2.2 Dev materialized-only distribution

Linux 和 Windows Dev 都必须消费 Virtual qB Pages 发布的 exact-SHA materialized payload，不允许 raw source fallback。

Dev identity 必须满足：

```text
current dev HEAD
== downloads/dev/GIT_SHA
== downloaded package GIT_SHA
```

Materialized payload 至少包含：

```text
private/data/qb-releases.json
private/data/qb-release-profiles/
private/data/qb-settings-native.txt
translations/webui_*.qm
```

### 2.3 Windows qB config safety

`installers/install.ps1` 的 `-configure` 已完成 fail-closed hardening：

```text
qBittorrent running -> refuse
non-empty qBittorrent_new.ini/recovery file -> refuse
zero/obviously invalid INI -> refuse
multiple config candidates -> refuse to guess
exactly one [Preferences] section required
managed WebUI keys must be unique and inside [Preferences]
encoding/BOM preserving read/write
byte-identical safety backup
temp write -> strict reread/invariant verify -> atomic replace
post-replace byte/text verify
failure rollback without overwriting unexpected concurrent changes
```

`tests/windows-config-encoding.ps1` 在 Windows PowerShell 5.1 contract 中覆盖 UTF-8 no-BOM/BOM、UTF-16 LE/BE 和上述负例。

### 2.4 Pages race / duplicate owner

Pages 现在只有：

```text
Virtual qB Pages Source
-> ref-bound workflow_dispatch
-> Virtual qB Pages
```

旧 default-branch `workflow_run` owner 已退役；final validated SHA 上 `event=workflow_run` 为 0。

Pages deployment 使用串行 concurrency，且 deploy 前重新确认 current branch HEAD == exact workflow SHA。旧 run 在 branch 前进后必须在 deploy 前 fail closed。

Live catalog readiness 等待稳定条件：

```text
catalogError || (catalogReady && !catalogBusy)
```

不通过删除 busy 断言或修改产品生命周期伪造通过。

## 3. 最新 exact-SHA candidate 证据

对 `ba8ee49886fad159e76014514b5a0a4ba803bc29`：

```text
CI #1349 / run 34661850911                  PASS
Virtual qB Pages #548 / run 34661856893     PASS
main Pages #549 / run 34664830791           PASS
```

CI 已证明：

```text
smoke PASS
Linux browser candidate PASS
Windows browser candidate PASS
all-stable upstream source audit PASS
12/12 locale/source enrich shards PASS
merged exact catalog PASS
Settings translation LKG PASS
full stable PRODUCT compatibility PASS
release candidate package PASS
release candidate deployment acceptance PASS
```

真实 candidate deployment acceptance：

```text
qB 4.6.7 PASS
qB 5.2.3 PASS
VERSION 0.3.71
GIT_SHA == ba8ee49886fad159e76014514b5a0a4ba803bc29
app/preferences + locale write/read/reload PASS
canonical Settings PASS
promotion/release rehearsal PASS
remoteWrites = 0
```

Candidate artifact 包含并校验：

```text
WeiG-qB-WebUI.zip
weigg-install.sh
weigg-install.ps1
SHA256SUMS
CANDIDATE_SHA
```

## 4. 当前不等于“可直接发布 v0.3.71”

重要：`ba8ee498...` 没有同 SHA 的 `Real qB Full Frozen Matrix`（G-FM）65/65 run。当前仓库曾把 main fast-forward 到该 SHA，但这次没有通过 `Promote validated dev to main` workflow 来强制校验 G-FM。

因此必须区分：

```text
candidate + Pages validated         YES
main currently contains that tree   YES
formal release gate complete        NO
v0.3.71 tag/release allowed now     NO
```

本次 docs commit 又会推进 `dev`，所以以后真正准备发布时，应以**新的 final dev exact SHA**重新执行正式流程；不要试图给旧 `ba8ee498...` 补一个 tag 来绕过新的 dev 状态。

## 5. 正式支持与 runtime owners

兼容状态统一为：

```text
NATIVE
NORMALIZED
DERIVED
READ_ONLY
UNAVAILABLE
UNKNOWN
```

危险写操作在 `UNKNOWN` 时必须 HTTP 前 fail closed。

Release profile resolution：

```text
EXACT
EQUIVALENT
INHERITED
FALLBACK
```

Inheritance 只用于 source-backed 的安全关系，不授权新的危险能力，也不能跨 hard translator/API boundary 盲继承。

Runtime catalog 采用 sharded 结构：

```text
private/data/qb-releases.json
private/data/qb-release-profiles/<sourceSha>.json
```

禁止恢复 65 full profiles 的 monolithic runtime catalog。

主要 owners：

```text
Exact release/source facts             W.ReleaseProfile
HTTP endpoint/transport invocation     W.QBClient
User-visible capability policy         W.CapabilityRegistry
Torrent status/filter semantics        W.TorrentSemantics
Torrent field/column provenance        W.TorrentFieldRegistry
Settings Preference semantics          W.SettingsSchema
Persisted language truth               qB app/preferences.locale
WeiG translation/presentation runtime  W.I18n
```

最终 authority 见 `DESIGN.md`。

## 6. 发布前仍需要的重型门禁

`Real qB Full Frozen Matrix` 是发布前手动门禁：

```text
65 exact stable versions
1 real qB version per job
fail-fast: false
max-parallel: 16
aggregate exact-SHA evidence
```

准备 release 时，同一个 final dev exact SHA 必须满足：

```text
candidate PASS
Pages exact-SHA PASS
G-FM 65/65 PASS
formal promotion gate PASS
main Pages exact reuse/live identity PASS
```

然后才允许创建 `v<VERSION>` tag，让 Release workflow 复用 exact candidate artifact。

## 7. 永久分支纪律

```text
所有修改 -> dev
验证 -> dev
修复红灯 -> dev
文档 -> dev
workflow -> dev
installer -> dev
```

`main` 不是开发分支。AI/维护者不得因为 main 与 dev 暂时相同，就直接在 main 改文件、创建修复 commit、cherry-pick、force push 或用 connector `update_ref` 绕过 promotion gate。

main 只接受**已验证 final dev exact SHA 的 fast-forward promotion**。

详细执行规则见：

```text
docs/006.发布与晋级流程.md
docs/014.AI协作与分支纪律.md
```

## 8. Truth hierarchy

发生冲突时优先级：

```text
qB official source / exact runtime identity
-> admitted release profile
-> canonical WeiG product owner
-> real-qB evidence
-> browser / Virtual qB / Pages evidence
-> docs/tests fixtures
```

Stale test、Simulator 或 docs 应跟随正确 source/runtime truth 更新；不得为旧测试结论降低产品安全边界。
