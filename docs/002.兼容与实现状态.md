# 002. 兼容与实现状态

> 本文件记录当前真实产品兼容状态。`webui/**` 是正式产品；source catalog、evolution audit、Virtual qB 和 browser fixtures 都是为了实现/证明正式产品兼容，不替代真实产品语义。

## 1. 正式支持范围与优先级

正式支持 floor：

```text
qBittorrent 4.1.0
-> 每一个 4.x official stable
-> 每一个 5.x official stable
-> 当前最新 official stable
-> 未来 5.x / 6.x / 7.x / x.x.x official stable
```

当前 source-derived stable matrix 已验证：

```text
qB 4.1.0 -> 5.2.3
65 个 official stable profiles
50 个 qB 4.x stable + 15 个 qB 5.x stable
当前 audited WebAPI ceiling: 2.15.1
upstream master WebAPI: 2.16.2
```

优先级固定为：

```text
P0 当前最新 stable 正确可用
P0 未来新 stable / 新 major 能自动进入发现、事实提取和产品验证
P1 qB 4.1.0 起所有历史 stable 保持兼容
P2 representative real-qB / promotion hardening
```

### qB 4.0.x

qB4.0.x / WebAPI v1 **不是当前实现、测试、CI 或阶段目标**。

只保留未来可选扩展原则：

```text
Legacy WebAPI v1 Adapter slot
-> canonical WeiG semantics
-> same UI
```

只有未来明确决定支持 qB4.0.x 时才实现。禁止为了这个低使用率旧版本牺牲当前/未来 stable 的开发优先级，也禁止把 v1/v2 分支散落到 UI、Settings、Torrent View。

## 2. 当前阶段状态

```text
Phase A — Source Facts Expansion                     DONE
Phase B — qB4.1+ Product Compatibility Core          DONE
Phase C — Full Stable Product Compatibility Matrix  DONE
Phase D — Future Stable Discovery / Automation       NEXT
Phase E — Torrent Fields / Columns provenance        PLANNED
Phase F — Settings + Search/RSS/Logs/Details         PLANNED
Phase G — Real-qB / Release Hardening                PLANNED
Legacy WebAPI v1 Adapter                             FUTURE OPTIONAL
main promotion                                       NOT STARTED
```

当前产品版本：

```text
VERSION: 0.3.58
```

`main` 未修改。

## 3. Phase A — Source Facts Expansion — DONE

Phase A 的目标是建立事实层，不改变正式 WebUI 行为。

检查点：

```text
base: 4178c5a4302c347367ae04d0dd71fe54909d2ffa
implementation: c3d184d1f39164467d9fde2cfabdfa22a315fae5
post-Phase-A docs / Phase-B base: 1de58781dd9179507b2c170a7657d6681c898201
VERSION: 0.3.57
```

每个 stable profile 现在可以 source-derived 提取/校验：

```text
stable tag
qB version / WebAPI version
source SHA
controller actions
Action required / optional parameters
Torrent info request parameters
Torrent info response fields
Torrent state strings
Torrent Properties fields
Tracker fields
File fields
WebSeed fields
native filters
Preference getter / setter / type provenance
release-to-release added / removed evolution
```

稳定版枚举不再限制 major=4/5，未来 official stable 只要 `>=4.1.0` 就能进入事实提取。

真实 upstream 扫描中已泛化处理：

- C++ `//` / `/* */` comment-aware body scanning；
- `checkParams(...)` / `requireParams(...)`；
- legacy `char[]`、Qt `_s`、Qt `_qs`；
- parser 无法理解新源码形态时 fail closed，而不是静默生成空 capability。

Phase A 未修改 `webui/**`。

## 4. Phase B — qB4.1+ Product Compatibility Core — DONE

最终 candidate：

```text
SHA: 84b86956924d79b1086bb10a915e45599f9d6685
branch: dev
VERSION: 0.3.58
main: untouched
```

最终 gate：

```text
smoke / npm test / JavaScript syntax                 PASS
Full qB Release compatibility audit (all stable)     PASS
Linux browser candidate                              PASS
Windows browser candidate                            PASS
release candidate packaging                          PASS
Full qB stable WebAPI evolution audit                PASS
conditional Linux semantic UI regression             SKIPPED by condition, not failure
```

### 4.1 Filters

产品 filter 现在按以下优先级解析：

```text
native upstream filter
-> normalized alias
-> reliable local derivation from source-proven Torrent fields/states
-> unavailable
```

qB4 的 `paused/resumed` 与 WeiG canonical `stopped/running` 继续统一；`stalled/checking/...` 在没有 native filter、但 source facts 足以可靠判断时使用已有 Torrent catalog 本地推导。

Native HTTP capability 不再有权隐藏一个可以可靠 local-derived 的产品 filter。

### 4.2 Category / Tags read 与 write 分离

现在正式拆成：

```text
Category/Tags read + facet + filter
!=
Category/Tags taxonomy endpoint / mutation actions
```

例如 qB4.1.0：

```text
torrents/info.category    source-proven -> Category facet 可用
torrents/info.tags        source-proven -> Tags facet 可用
native Tags taxonomy API  不存在         -> 不冒充 native list/write
Tags mutation actions     不存在         -> 写操作隐藏/拒绝
```

`SpatialRuntime` 的正式 UI owner 现在绑定：

```text
Category Select -> categoryFacet
Tags Select     -> tagFacet
```

而不是把 Tags facet 错误绑定到 native `tags` endpoint capability。

### 4.3 Torrent Actions

ReleaseProfile / Selection / QBClient 使用 exact source action facts。

已确认修复：

```text
Reannounce
Remove Tracker
```

在缺少 upstream action 的 release 上：

- UI 不暴露可点击动作；
- QBClient 再做一次 fail-closed；
- 不发送必然 404 的 HTTP 请求。

### 4.4 Performance

本地 derivation 复用既有批量 Torrent catalog；没有加入：

```text
N torrents -> N requests
```

Catalog 继续按批量 `torrents/info` 分页获取并多处复用。

## 5. Phase B 实际修改文件

从 Phase B base `1de58781dd9179507b2c170a7657d6681c898201` 到最终 candidate，共涉及以下正式范围：

```text
VERSION
webui/VERSION
package.json
package-lock.json

webui/private/data/capabilities.json
webui/private/scripts/release-profile.js
webui/private/scripts/torrent-semantics.js
webui/private/scripts/capabilities.js
webui/private/scripts/app.js
webui/private/scripts/selection.js
webui/private/scripts/qb-client.js
webui/private/scripts/spatial.js

tests/release-profile-contract.mjs
tests/capability-contract.mjs
tests/torrent-workspace-contract.mjs
tests/product-compatibility-contract.mjs
tests/browser-feature-parity.mjs
tests/browser-adaptive-ui.mjs
tests/browser-sidebar-capability-visual.mjs
```

Phase B 没有重写 Settings / Search / RSS / Logs 主逻辑，也没有实现 qB4.0 WebAPI v1。

## 6. 当前 compatibility owners

正式产品兼容链：

```text
qB upstream stable source
        |
        v
Source Extractors / generated Release Catalog
        |
        v
W.ReleaseProfile
        |
        +-> W.TorrentSemantics
        +-> W.CapabilityRegistry
        +-> W.SettingsSchema
        +-> W.QBClient
        |
        v
canonical WeiG product semantics
        |
        v
UI / presentation
```

UI 不应自己保存 qB patch 版本表。

## 7. 兼容规则

每项产品能力按顺序处理：

```text
1. NATIVE       upstream 直接支持
2. NORMALIZED   endpoint/名称/参数不同，由兼容层归一化
3. DERIVED      从已拥有且 source-proven 的数据可靠本地实现
4. READ_ONLY    能证明读取，不能证明写入
5. UNAVAILABLE  无法安全实现
6. UNKNOWN      未来未知 surface，尤其写操作 fail closed
```

禁止：

- presentation 中散落 qB version `if`；
- 因为 major 变成 6/7 就自动拒绝；
- 用版本号猜 source facts；
- 把 read capability 和 write capability 合并成一个 `true/false`；
- 点击后 404 才算“检测不支持”；
- 用 Simulator PASS 代替正式产品证明。

## 8. Phase C — Full Stable Product Compatibility Matrix — DONE

Phase C 把：

```text
每一个 official stable >= 4.1.0
```

从“结构/source audit target”升级为“正式产品 compatibility target”。

最终 candidate：

```text
base: 8a3a3eec3af567967621fa3ca28ecc9a241adba4
candidate: b14926b8d67b5919aa2c281762ed97c5cee46c21
branch: dev
VERSION: 0.3.58 unchanged
main: untouched
```

### 8.1 Formal product matrix

新增：

```text
tests/full-stable-product-compat.mjs
```

它直接消费 candidate 流程刚生成的 exact `qb-releases.json`，并对每一个 stable 执行正式产品 owners：

```text
W.ReleaseProfile
W.CapabilityRegistry
W.TorrentSemantics
W.SettingsSchema
W.QBClient
```

不是只比较 JSON，也不是只跑代表版本。

每个 stable 都验证：

```text
Filters
  native / normalized / reliable local derivation

Facets
  Tracker / Path / Category / Tags
  read 与 native taxonomy/write 分离

Torrent Actions
  exact source action / parameter provenance
  unsupported Reannounce / Remove Tracker 在 HTTP 前 fail closed

Torrent Fields
  当前 17 个正式 columns 所需字段

Settings
  Preference descriptor / getter / setter / write type provenance

Search / RSS / Logs
  capability 不允许无证据回退或历史倒退
```

### 8.2 实际 full-stable 结果

candidate `b14926...` 生成并验证：

```text
65 official stable releases
4.1.0 -> 5.2.3
50 qB 4.x + 15 qB 5.x
```

产品矩阵结果：

```text
65 / 65 stable product profiles                    PASS
36 releases 使用至少一个 reliable local filter derivation
11 releases 在 native Tags taxonomy 出现前已支持 Tags read/facet
latest 5.2.3 resolved Torrent actions: 20
latest 5.2.3 source-proven writable Preferences: 214 / 223
```

source catalog 同一轮统计：

```text
API actions: 6094
Action parameters: 9334
Torrent info fields: 3201
Torrent states: 1235
Torrent filters: 806
Properties fields: 2321
Tracker fields: 524
File fields: 496
Preference descriptors: 10759
Preference setter-typed: 10179
read/write type conflicts: 0
```

### 8.3 Phase C 最终 gate

```text
smoke / npm test                                  PASS
Representative fixture compatibility matrix      PASS
Generate exact all-stable release catalog         PASS
Audit all official stable from 4.1.0             PASS
Full stable PRODUCT compatibility matrix          PASS
Linux browser candidate                           PASS
Windows browser candidate                         PASS
release candidate packaging                       PASS
Full qB stable WebAPI evolution audit             PASS
conditional Linux semantic UI regression          SKIPPED by condition, not failure
```

release candidate 复用通过产品矩阵审计的 exact catalog artifact，不重新生成另一份 catalog。

### 8.4 Phase C 实际修改文件

从 `8a3a3eec3af567967621fa3ca28ecc9a241adba4` 到 `b14926b8d67b5919aa2c281762ed97c5cee46c21`：

```text
.github/workflows/ci.yml
tests/ci-contract.mjs
tests/compat-matrix.mjs
tests/full-stable-product-compat.mjs
tests/release-compat.mjs
```

Phase C **没有修改**：

```text
webui/**
tools/*
VERSION / webui/VERSION / package version
main
```

`release-compat.mjs` 的代表版本 gate 也移除了 `major>=5 ? ... : ...` 这类测试层产品假设，HTTP action/filter 期望改为 capability/source fact 驱动。

## 9. Phase D — Future Stable Discovery / Automation — NEXT

Phase D 重点服务“当前和未来 stable”，而不是旧 qB4.0：

```text
发现新的 official stable tag
-> 自动 source extract
-> 生成 exact ReleaseProfile
-> 跑 Full Stable Product Compatibility Matrix
-> 输出 added/removed upstream + product capability diff
-> 出现真实产品 gap 时 CI fail
```

目标不是为 qB6/qB7 预先写版本表，而是让新 stable/new major 自动进入同一条证明链。

Phase D 还应加入架构防回归：

```text
webui/private/scripts/**
```

禁止新增散落的 qB major/version 产品判断；允许版本知识存在于集中 ReleaseProfile / Adapter / exceptional manifest owner 内。

qB4.0.x 继续保持 `FUTURE OPTIONAL`，不进入 Phase D。

## 10. 真实证明边界

当前已经有：

```text
all-stable source/evolution audit
all-stable release structural audit
all-stable PRODUCT compatibility matrix
representative qB4/qB5 real Chrome product matrix on Linux/Windows
exact-SHA candidate package
```

这证明当前 source-derived 65 个 stable profile 都能通过正式产品 compatibility owners 的不变量验证。

后续仍需逐步增加 representative **real qB binary/server** evidence。Virtual qB / browser fixtures 是强测试层，但不单独等价于真实 qB binary PASS。

## 11. 阶段纪律

每完成一个大阶段：

1. STOP；
2. fresh-read `dev`；
3. 记录 before/after SHA；
4. 列出实际修改文件；
5. 列出 product semantics 变化；
6. 列出 CI/browser/source evidence；
7. 更新 `docs/002` 与 `docs/010`；
8. 说明下一阶段预计修改文件；
9. 再进入下一阶段。

`main` 只有在用户明确批准发布时才允许修改。
