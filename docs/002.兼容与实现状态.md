# 002. 兼容与实现状态

> 本文件只记录**当前仍有效的产品兼容事实与最新验证状态**。历史排障过程见对应复盘文档。`webui/**` 是正式产品；source catalog、real-qB、Virtual qB、browser 与 Pages 是发现/证明层，不替代正式产品语义。

## 1. 当前版本与分支状态

截至 2026-09-13：

```text
published latest Release: v0.3.61
repository/product VERSION: 0.3.76
main: ba8ee49886fad159e76014514b5a0a4ba803bc29
dev: active; every handoff/write must fresh-read refs/heads/dev
pre-0.3.76 dev baseline: b5ca6bbd85ba7f6b935ea53d4ceec4b50d906e21
qB support floor: 4.1.0 official stable
latest admitted stable: 5.2.3
Frozen stable profiles: 65
current-stable locale surface: 61
```

`b5ca6bbd...` 是本轮 0.3.76 开始前 fresh-read 的 dev exact baseline；它仍是 0.3.75。0.3.76 写入后必须以新的 final dev SHA 作为 code/cache/test/evidence identity，不能继续把该 baseline 当 current HEAD。

`dev` 领先 `main` 是正确状态。未经用户明确授权，不得移动 main、promotion、tag 或 GitHub Release；不得 force push。

`VERSION = 0.3.76` 不代表 `v0.3.76` 已发布。当前 GitHub Release 仍是 `v0.3.61`。

### 1.1 正式 compatibility scope anchors

正式兼容优先级始终是 `qB 4.1.0+ official stable -> current stable -> 未来 official stable / future new major`。Future stable/new-major compatibility 必须继续由 **source-derived / 上游源码生成** 的 exact release facts、bounded inheritance 与 fail-closed UNKNOWN 驱动，不能因为 major 更高就整体拒绝，也不能盲继承 current latest 的危险写语义。

`qB 4.0.x` **不是当前实现/CI scope**；未来可选扩展只能经用户明确扩大范围，并通过集中 Legacy Adapter 进入 canonical WeiG semantics，禁止把 WebAPI v1/v2 patch 分支散落到 UI/caller。

验证职责继续分开：`Linux browser candidate` 与 `Windows browser candidate` 提供代表性跨平台浏览器证据；`Full qB Release compatibility audit` / all stable upstream source audit 证明完整 admitted stable source/product compatibility；需要真实 runtime/hosting 的行为还必须由 real-qB evidence 证明。任何一层 PASS 都不能自动替代另一层。

## 2. 0.3.75 已建立的 source / recovery / write-gate 基线

0.3.75 已把 0.3.74 source groundwork 闭合到 build/product packaging：

```text
exact qB source / official TS
-> per-shard source + recovery evidence
-> deterministic merge
-> Settings/source LKG v2
-> native bundle / catalog materialization
-> bounded runtime profile + exact bridge shards
```

### 2.1 Deterministic native-QM recovery

`tools/qb-native-qm-recovery.mjs` 是 canonical recovery owner。候选译文 winner 固定为：occurrence count 最大；同票时 newest exact qB stable 更新者优先；再同票按 lexical/codepoint translation order。并行 shard merge 必须 order-independent / associative；malformed/conflicting evidence fail closed。

完整 official recovery translation bodies 是 build-only 证据，只用于 materialize `translations/webui_<locale>.qm`；不得重新进入 browser release profile JSON，也不得把 exact Settings shard 膨胀成全历史翻译数据库。

### 2.2 Settings/source LKG v2

LKG v2 继续绑定：

```text
exact qbVersion
exact sourceSha
exact torrentTableColumns
exact source/translation bridge facts
canonical deterministic recovery union hash
```

Candidate source shards/LKG/package chain仍消费 exact-SHA certified artifact；本轮没有改变 source parser 或 LKG owner。

### 2.3 0.3.75 write gates

Torrent File Priority 继续由 exact `torrentscontroller.h:filePrioAction` source ownership 决定；INHERITED/FALLBACK/UNKNOWN 或 source 未证明时 HTTP 前 fail closed。

Transfer normal/global rate-limit write 继续要求 `setDownloadLimitAction + setUploadLimitAction + toggleSpeedLimitsModeAction`；Alternative 模式继续要求 `appcontroller.h:setPreferencesAction + toggleSpeedLimitsModeAction`。`W.CapabilityRegistry` 的 strict `allActions` 与 `applyLimits()` preflight 保持不变。

## 3. 0.3.76 DataGrid 原生列正式消费

### 3.1 Source truth 不再停留在 evidence 层

此前 exact source/LKG 已经生成并保留 `torrentTableColumns`，字段包括：

```text
key
source order
caption / translation source
source defaultWidth
defaultVisible
dataProperties
```

0.3.76 不新增第二 parser，而是让正式产品 `W.TorrentFieldRegistry` 消费 `W.ReleaseProfile.torrentTableColumns`。对 exact/certified profile，这份 source schema 现在是 Desktop DataGrid column set/defaults 的 authoritative input；旧 17-field registry 保持 semantic/fallback owner，而不再限制 exact release 的原生列集合。

### 3.2 User override v2 / 非破坏迁移

`cfg.columns` 在 exact native schema 下升级为 full-schema user override：

```text
key
width
visible
layoutVersion: 2
```

迁移规则：

- 旧用户已有显示/隐藏、宽度与可识别顺序尽量保留；
- `state/status` 等通过已证明的 `dataProperties` 做 canonical mapping，而不是按版本猜；
- qB 新增 exact native column 自动按 source-relative 位置合入；
- 无用户 override 的新列使用 upstream `defaultVisible/defaultWidth`；
- 暂时不存在的 saved preference 不因 effective view 被删除；
- FALLBACK/UNKNOWN profile 不宣称 native column parity，继续安全回退到现有 legacy behavior。

Source-only native column 使用现有 torrentInfo object 的 source/dataProperty 值渲染，不再因为不在旧 17-field list 就只显示 `—`。这只是 presentation projection，不新增 API 请求。

### 3.3 Sort / interaction 边界

Field presence 不等于 server-sort proof。Source-only column 默认没有 sort；只有 direct canonical semantic 或 `dataProperties` 已映射到 canonical WeiG sort semantic 时才可复用该 sort key。

Columns interaction 仍由 `app.js -> openColumns()` 单一 owner 负责：

```text
checkbox visibility
↑ / ↓ reorder
Desktop drag reorder
Android 360 ms long-press drag reorder
```

Android 长按激活前移动超过阈值会取消 drag，普通纵向滚动继续由浏览器处理；没有 `MutationObserver`、monkey patch、post-render repair 或第二 layout owner。

## 4. 0.3.76 Transfer session 展示语义统一

### 4.1 数据 owner 不变

现有 `app.js` transfer cycle 仍通过同一个 `transfer/info` 请求取得：

```text
dl_info_data / up_info_data        session cumulative totals
dl_info_speed / up_info_speed      realtime rates
connection_status                  connection state
```

`W.TransferRuntime` 继续只保存 bounded realtime samples/history 与现有 metadata snapshot，不新增请求、timer、poll owner 或第二 state store。

### 4.2 Compact capsule = 累计；Dialog = 累计 + 实时

Canonical compact transfer capsule 改为：

```text
↓ 下载 <session downloaded>
↑ 上传 <session uploaded>
```

它不再显示 realtime KiB/s；`app.js -> loadTransfer()` 直接把 `dl_info_data/up_info_data` 写入现有 status nodes，避免异步覆盖/repair。

Canonical Transfer statistics dialog 同时显示 session totals 与 realtime rates：Desktop 宽布局为 `下载实时 | 下载累计 | 上传累计 | 上传实时`；窄屏/Mobile 为 2x2：第一行下载/上传累计，第二行下载/上传实时。Graph 仍复用 `W.TransferRuntime` 同一 bounded history；full graph legend 与 Drawer compact chart legend 可复用同一 session totals，但不创建第二 poll/history owner。

Global/alternative rate-limit write gate、单位选择与 bounded graph window/averaging行为不因此次展示调整而改变。

## 5. 当前验证证据

### 5.1 0.3.76 写入前本地 isolated validation

基于 fresh-read `b5ca6bbd...` 重建本批目标文件后，已通过：

```text
node --check webui/private/scripts/app.js                    PASS
node --check webui/private/scripts/torrent-fields.js         PASS
node --check webui/private/scripts/transfer.js               PASS
existing torrent-field-provenance contract                  PASS
new native-column-layout contract                           PASS
new transfer-session-layout contract                        PASS
package JSON parse                                          PASS
```

这证明本批 isolated syntax/owner/migration/layout contract 成立；**没有运行完整仓库 `npm test`，不能把它写成 PASS。**

### 5.2 旧 0.3.75 evidence 只能作为历史 baseline

0.3.75 baseline 曾有 normal smoke/Pages 等证据，但 exact-SHA rule 不允许这些证据自动证明 0.3.76 final SHA。0.3.76 commit 写入 `dev` 后，ordinary push 的 normal CI/Pages 状态必须重新读取；在结果产生前均为 pending/not established。

本批用户明确不要求手动 candidate，因此状态必须写成：

```text
0.3.76 local isolated contracts              PASS
0.3.76 final-SHA normal CI                    PENDING / read after push
0.3.76 CI candidate full chain                NOT TRIGGERED in this batch
0.3.76 manual Current Stable Locale Matrix    NOT RUN
0.3.76 manual Real qB Full Frozen Matrix      NOT RUN
0.3.76 formal promotion                       NOT RUN
v0.3.76 tag / Release                         NOT RUN
```

任何 ordinary push workflow 若按 classifier 自动 SKIP heavy/candidate job，只能写 SKIPPED，不能写 PASS。

## 6. Compatibility states / runtime owners

统一 capability/product modes：

```text
NATIVE
NORMALIZED / ADAPTER
DERIVED
READ_ONLY
UNAVAILABLE
UNKNOWN
```

Release profile resolution：

```text
EXACT
EQUIVALENT
bounded INHERITED
FALLBACK
```

主要 Current Owners 保持：

```text
Exact release/source facts             W.ReleaseProfile
HTTP endpoint/transport invocation     W.QBClient
User-visible capability policy         W.CapabilityRegistry
Settings Preference semantics          W.SettingsSchema
Torrent status/filter semantics        W.TorrentSemantics
Torrent field/column projection        W.TorrentFieldRegistry
Column settings interaction            app.js -> openColumns()
DataGrid sizing/resize                  W.DataGrid
Transfer poll publisher                 app.js transfer cycle
Transfer bounded history/panel          W.TransferRuntime / W.Transfer
Persisted language truth               qB app/preferences.locale
WeiG translation/presentation runtime  W.I18n
```

0.3.76 没有更换 Current Owner，只把既有 exact column facts 接入产品，并调整同一 Transfer telemetry 的 presentation semantics。最终 owner authority 见 `DESIGN.md`。

## 7. 当前尚未完成的正式 release-grade 门禁

若之后准备 promotion/release，同一个 **final dev exact SHA** 仍必须取得：

```text
normal CI PASS
CI candidate PASS
Dev Pages exact-SHA artifact/live verify PASS
manual Current Stable Locale Matrix PASS
  -> 当前 operational surface: qB 5.2.3, 61/61
manual Real qB Full Frozen Matrix PASS
  -> 当前 operational surface: 65/65
central release-compat-evidence verifier PASS
formal Promote validated dev to main PASS
main Pages exact reuse/live identity PASS
```

Candidate Full qB Release compatibility audit 仍不等于 65-version real-qB G-FM。旧 SHA evidence 不得冒充 final 0.3.76 SHA evidence。

## 8. 当前下一步

```text
1. 完成本批 detached tree/commit 并在 ref 更新前再次 fresh-read dev。
2. 仅当 current dev 仍等于本批 parent 时，force:false fast-forward dev。
3. 推送后读取 0.3.76 final SHA 的 ordinary normal CI / workflow 状态并按真实结果记录。
4. 本批不手动触发 candidate，不移动 main，不创建 tag/Release。
5. 若之后用户明确进入正式验证/发布阶段，再为同一 final SHA 取得 candidate、exact Pages、manual Locale、manual G-FM 与 central evidence。
6. 后续 docs/tests-only follow-up 保持 VERSION 0.3.76；只有新的 webui/** 产品变化才再次 bump patch VERSION。
```

## 9. Truth hierarchy

发生冲突时：

```text
qB official source / exact runtime identity
-> admitted release profile
-> canonical WeiG product owner
-> real-qB evidence where applicable
-> browser / Virtual qB / Pages evidence
-> docs/tests/fixtures
```

Stale test、Simulator、old checkpoint 或旧 docs 必须跟随正确 source/runtime truth 更新；不得为旧结论降低安全边界。
