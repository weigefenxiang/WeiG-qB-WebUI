# 002. 兼容与实现状态

Status: **current compatibility / implementation / evidence facts**

本文件只记录当前仍有效的产品兼容事实、已实现能力和验证边界；历史过程由 Git history / Actions / artifacts 保存。Current HEAD 与 Actions 必须 fresh-read。当前计划是否完成、是否存在下一步工作，只维护在 `docs/014.AI协作与分支纪律.md`。

## 1. 当前产品状态

```text
repository/product VERSION: 0.3.102
current product commit: 44d317f12b2232d8745bc76ec19abe8f57ee9737
latest validated ordinary-CI checkpoint: 3015670e7e44f53ed5c3e5f3ab2df7d559429fa5
ordinary CI run 35042852742: SUCCESS
latest full Virtual qB Pages checkpoint: 9705226612e210b7a720aad8d949a87d181a66fa
Virtual qB Pages run 35042417653: SUCCESS
Pages Source for 3015670e... run 35042852704: SUCCESS; Pages dispatch correctly SKIPPED because the change was ordinary-browser-test-only
main: ba8ee49886fad159e76014514b5a0a4ba803bc29
latest published Release/tag: v0.3.61
qB support floor: 4.1.0 official stable
latest admitted stable: 5.2.3
Frozen stable profiles: 65
```

`44d317f...` 是最近一次真正改变正式 `webui/**` 产品 bytes 的 0.3.102 product commit。其后的 `a324230...`、`c8dcf1d...`、`970522...`、`3015670...` 只修 tests/readiness，不改变产品 bytes，因此不 bump Product VERSION。Exact-SHA evidence 仍按各自适用范围区分：ordinary CI 的最终适用 checkpoint 是 `3015670...`，Virtual qB Pages 的最近完整适用 checkpoint 是 `970522...`。

0.3.102 已闭环上一轮三项 external failures：

- `browser-feedback.mjs` 不再寻找已经退休的 standalone `listen_port` DOM，而按 current compound SettingRow 选择 `listen_port_compound`。
- browser locale bootstrap 在需要判断 `locale` 写权限时，先 lazy-load Settings compatibility/write proof，再按 source-proven write gate决定是否写入；persisted truth 仍是 qB `app/preferences.locale`。
- Pages preference anchor 按 Settings layout contract验证 canonical row projection：Speed 的 `schedule_from_hour/min + schedule_to_hour/min` 由一个 `schedule_range` compound row表示，同时仍逐项证明四个 source Preferences存在且被该 row表示。

后续 final ordinary browser gate 还修正了一个 test readiness 假设：不再用固定 100ms 猜测 legacy tooltip metadata 已 scrub，而等待 canonical `PolishRuntime` 的实际 scrub 条件成立；最终 `title/data-tooltip` 零残留断言没有删除或放宽。

兼容优先级继续面向 **future official stable / future major**，不得靠 hard-coded major 拒绝未来版本。qB 4.0.x 不属于当前实现/CI范围；正式支持底线保持 qB 4.1.0 official stable。

## 2. Runtime compatibility truth

当前浏览器 compatibility owner 为 `W.CapabilityRegistry`。它消费 checked-in compact contracts，并结合 `W.QBClient` 检测的 qB/WebAPI identity解析 exact/equivalent/bounded inherited/fallback状态。`W.QBClient` 只拥有 transport 与运行实例版本检测，不重新拥有 capability/version policy。

运行时 compact contracts 是 **source-derived / 上游源码生成** 的兼容真值。正式 runtime 不包含旧 release-profile owner、`qb-releases.json` 或 per-release profile shards；qB official source、65-release catalog、完整 translation/source evidence只留在 tools/tests/CI/simulator，用来生成或审计 compact contracts。

## 3. Runtime assets

raw `webui/` 直接携带：

```text
private/data/capabilities.json
private/data/torrent-compat.json
private/data/detail-compat.json
private/data/settings-compat.json
private/data/source-actions.json
private/data/qb-settings-native.txt
translations/webui_*.qm
VERSION
```

必须不存在：`private/scripts/release-profile.js`、`private/data/qb-releases.json`、`private/data/qb-release-profiles/`、旧 Settings translation shards 与 full recovery runtime bodies。

历史上为解决 connector 大文件传输限制曾短暂使用 `.weigg-bootstrap/**` 分块 payload 与一次性 `dev-bootstrap-*.yml`；它们不是 runtime asset，也不是 product source。最终 product tree 已删除这些临时文件，后续不得重新引入，也不要把未被任何 ref/tree引用的 dangling blobs当待上传源码。

## 4. Settings / Locale

Preference current value/presence只来自 `app/preferences`。`W.SettingsSchema + settings-compat.json` 拥有 Preference semantic/type/setter descriptor；certified write gate来自当前 compatibility identity；`W.QBClient.setPreferences()` 后必须 `getPreferences()` verification reread。唯一 persisted language truth仍是 `app/preferences.locale`。

qB-owned copy走 `W.I18n` + `qb-settings-native.txt` + source-proven official QM/bridge；`4.5.0 -> 4.6.4` Alternative WebUI translation hard gap保持 exact bridge，不因 QM存在而伪装 native。

Settings compound controls（例如 Listen port + Random、schedule From/To）是**单个 canonical SettingRow 内的组合控件**。一个 DOM row可以合法表示多个 upstream Preferences；测试必须按 Settings layout contract验证 source Preference -> canonical row projection，不能强迫“一 Preference = 一 DOM row”。组合 row也不自动意味着顶层 SettingsGrid full-span；除非独立 layout owner明确证明，否则仍占标准 grid cell。

Locale bootstrap必须同时尊重 browser locale、qB persisted `app/preferences.locale`、source-proven writable proof与 `W.I18n` presentation边界；只改 DOM文字、W.I18n active locale或 localStorage都不能替代 qB persisted truth。

## 5. Torrent / Detail protected state

`W.TorrentSemantics`、`W.TorrentFieldRegistry`、`W.Selection/ActionRegistry`、`W.QbUiEvidence`、`W.SharedColumns`、`W.ColumnInteraction`、`W.VirtualList` 等既有唯一 owner不变。Files必须保持 qB response order与 exact source file index；synthetic folder != qB source file；file/folder Rename provenance独立；Tracker使用 exact URL；Peer flags本地 source-pinned；Copy browser-local；transport mutation只经 QBClient；checked Tag removal必须 exact `removeTagsAction`；unknown dangerous write零 HTTP。

0.3.100/0.3.101 已把 Torrent/Detail 的滚动期数据提交纪律收口到 canonical `W.VirtualList`：原生横/纵向滚动或 scrollbar drag期间允许 polling继续取得数据，但不得用新 snapshot替换当前 DOM；只保留 latest pending snapshot，交互 idle后一次提交。Detail Files展开/收起以用户点击 folder row的 viewport Y为 anchor，不能恢复 stale child anchor。

## 6. Evidence boundary

**Linux/Windows browser evidence** 证明真实浏览器产品行为；**Virtual qB Pages live evidence** 证明部署后 simulator + product + live browser链；**Full qB stable WebAPI evolution audit** 证明 all-stable上游源码/API演进。Ordinary CI、Virtual qB、Pages/browser、real-qB 与 release-grade matrix各自证明不同层级，不能互相冒充。

- `SKIPPED != PASS`。
- 但 classifier明确判定“不适用”时，不应为了制造绿灯而手工触发无关 heavy lane。
- Pages `build` / `deploy` SUCCESS不等于 Pages全链 SUCCESS；required live verify lanes必须全部闭环。
- 旧 SHA PASS不自动证明新 SHA；反之，仅 tests/docs-only change也不使未改变的 product bytes自动变成新 Product VERSION。
- Promotion/main/tag/Release仍需用户明确授权。
