# 002. qBittorrent 兼容与实现状态

更新时间：2026-09-01  
当前产品版本：**0.3.6 Final Candidate**

## 1. 支持边界与判定原则

WeiG qB WebUI 的兼容目标不是“只支持几个写死版本”，而是：

```text
qBittorrent 4.1.9.1
→ 当前稳定版 5.2.3
→ 当前 upstream / next
```

兼容判定遵循 **COMPAT-001**：优先检测 WebAPI、endpoint、Preference key、response field 与 capability，禁止仅凭 qB major version 开关功能。

截至 2026-09-01，官方稳定版仍属于 5.x；仓库 `master` WebAPI 已进入 2.16.x 的下一开发线。官方尚无 6.x 正式版。因此 6.x 只能作为 **synthetic forward-major sentinel**，用于验证前端没有 `major > 5 => unsupported` 一类硬编码，不能宣称“已支持 qB 6.x”。

## 2. Release-blocking 实机环境

仓库只定义 release-blocking 的版本/能力目标：

```text
Legacy LIVE target
qBittorrent 4.1.9.1
WebAPI 2.2.1

Modern LIVE target
qBittorrent 5.2.x
WebAPI 2.15.x
```

具体 URL、IP、凭据、容器名、Host path 与 qB-visible path 均由测试操作者在运行时提供，不写入开源仓库。

自动 API fixture、Chromium browser fixture、静态 CI、源码分析都不能替代真实实例回归。

## 3. Representative Compatibility Matrix

版本选择按 WebAPI/capability 边界，而不是只按 major version：

| 角色 | qBittorrent | WebAPI | Gate |
| --- | --- | --- | --- |
| Legacy floor | 4.1.9.1 | 2.2.1 | Fast + Release + LIVE |
| Early 4.x API | 4.2.5 | 2.5.1 | Release |
| Mature 4.x A | 4.3.9 | 2.8.2 | Fast + Release |
| Mature 4.x B | 4.4.5 | 2.8.5 | Release |
| Advanced Preferences expansion | 4.5.5 | 2.8.19 | Release |
| Late 4.x | 4.6.7 | 2.9.3 | Fast + Release |
| Major transition | 5.0.5 | 2.11.2 | Fast + Release |
| Mature 5.x | 5.1.2 | 2.11.4 | Release |
| LIVE 5.2 target | 5.2.0 | 2.15.1 | Release + LIVE |
| Current stable | 5.2.3 | 2.15.1 | Fast + Release |
| Upstream / next | master | 2.16.2 | Fast + Release |
| Future sentinel | 6.0.0-synthetic | 3.0.0-synthetic | Fast + Release sentinel |

Canonical source：`tests/fixtures/qb-compat-matrix.json`。

### Fast Gate

每次 `dev` push 的代表节点：

```text
4.1.9.1
4.3.9
4.6.7
5.0.5
5.2.3
master / upstream
6.0.0-synthetic sentinel
```

### Release Gate

发布前运行完整代表矩阵：

```text
4.1.9.1
4.2.5
4.3.9
4.4.5
4.5.5
4.6.7
5.0.5
5.1.2
5.2.0
5.2.3
master / upstream
6.0.0-synthetic sentinel
```

**FUTURE-001**：future-major sentinel 只能报告 `forward-compatibility sentinel PASS/FAIL`，不能报告正式版本支持。

## 4. FIXTURE-001：fixture 必须反映真实 API 差异

禁止只改变：

```text
qbVersion
webApiVersion
```

然后让每个版本返回完全相同的数据。各 fixture 应按该代际真实能力描述：

```text
Alternative WebUI Preferences
Logs
Tags
exact private flag
pause/resume
start/stop
structured torrents/add
Advanced Preferences / enums
Search / RSS
Files / Peers / WebSeeds
Torrent Creator
Cookie/API-key related capability
```

如果能力事实尚未验证，fixture 必须标为 unknown/capability-detected，不允许猜测为 supported。

## 5. v0.3.6 前端基础能力

当前 Final Candidate 已实现：

```text
Canonical Select / Listbox
FloatingLayer Portal + collision
Canonical FilterChip / CheckControl
AmbientMark 随机品牌动效
Logs newest-first
Global Display Time Zone
Torrent Detail context Back / Esc
Advanced Setting Unit / Enum / conversion layer
Mobile Adaptive System 2.0
Storage free-space telemetry
TorrentSemantics: Private OR PT
```

这些前端能力不依赖 qB major version。差异由 `QBClient` capability boundary、实际 Preference/response field presence 与 `TorrentSemantics` 处理。

## 6. Capability Matrix

| 能力 | 4.1.9.1 | Late 4.x | 5.0.x | 5.2.x / upstream | 实现方式 |
| --- | ---: | ---: | ---: | ---: | --- |
| Torrent Start/Stop semantic bridge | ✓ | ✓ | ✓ | ✓ | capability bridge |
| Global DL/UL limits | ✓ | ✓ | ✓ | ✓ | Native |
| Alternative speed mode | ✓ | ✓ | ✓ | ✓ | Native |
| Transfer/session stats | ✓ | ✓ | ✓ | ✓ | Native + WeiG UI |
| Logs main API | ✓ | ✓ | ✓ | ✓ | Native |
| Log timestamp normalization | ✓ | ✓ | ✓ | ✓ | QBClient |
| Logs newest-first | ✓ | ✓ | ✓ | ✓ | v0.3.6 frontend |
| Incremental log refresh | ✓ | ✓ | ✓ | ✓ | `last_known_id` |
| Display Time Zone | ✓ | ✓ | ✓ | ✓ | `Intl.DateTimeFormat` |
| Canonical custom Select | ✓ | ✓ | ✓ | ✓ | UI primitive |
| FloatingLayer collision | ✓ | ✓ | ✓ | ✓ | frontend portal |
| AmbientMark | ✓ | ✓ | ✓ | ✓ | frontend |
| Detail context Back / Esc | ✓ | ✓ | ✓ | ✓ | navigation context |
| Mobile Adaptive System 2.0 | ✓ | ✓ | ✓ | ✓ | frontend layout contract |
| Storage free-space | field gate | field gate | field gate | field gate | `sync/maindata` |
| Alternative WebUI enabled/path | field gate | field gate | field gate | field gate | actual Preferences |
| Git SHA deployment identity | ✓ | ✓ | ✓ | ✓ | installer metadata |
| Tags | capability | capability | capability | capability | Native gate |
| Exact Private flag | × | capability | ✓ | ✓ | Native field gate |
| Private/PT union | PT fallback | field OR PT | exact OR PT | exact OR PT | `TorrentSemantics` |

## 7. Private / PT 语义

`Private / PT` 不是版本号特判：

```text
exact private field available
  → exact private OR configured PT tracker match

exact private field unavailable
  → configured PT tracker match only
```

legacy 版本不得因为字段缺失而把所有 Torrent 猜成 private；modern 版本也不得因为 exact private field 存在就忽略 PT tracker rule。

## 8. FLOATING-001：统一浮动层

所有 Select / Dropdown / Action Menu / Context Menu / Popover / Tooltip / Timezone picker 最终必须归一到同一个 Floating Layer contract：

```text
trigger.getBoundingClientRect()
        ↓
#weigg-floating-layer (document.body)
        ↓
position: fixed
        ↓
flip / shift / max-height / internal scroll
```

浮层不允许留在 `overflow:hidden/auto` 的业务容器中，也不允许以不断提高 `z-index` 作为裁切问题的修复方式。

## 9. TIME-002：Display Time Zone

时区属于 WeiG 前端显示层：

```text
qB timestamp
→ normalized epoch
→ W.Time
→ Intl.DateTimeFormat(timeZone)
```

UTC offset 根据当前日期与 IANA zone 实时计算，必须支持 `+05:30`、`+05:45` 和 DST。当前选择保存在 `localStorage: weigg.timeZone`，不会调用 `setPreferences()`，不会修改 qBittorrent daemon、Docker 或宿主机系统时区。

## 10. SETTING-UNIT-001：Advanced Unit / Enum Schema

Advanced 页面不能长期展示无法解释的裸数字。经过官方源码/API 验证的字段应提供 unit、API unit、display unit、enum 与 special value。涉及单位转换的字段必须先完成跨版本 read → display → write round-trip；未经验证禁止猜单位或自动换算。

## 11. STORAGE-001

数据源：

```text
sync/maindata
→ server_state.free_space_on_disk
```

含义是 qBittorrent 默认保存路径所在文件系统的可用空间，不是通用 VPS `/` 剩余空间。格式使用 IEC 自动单位 `B/KiB/MiB/GiB/TiB` 与自适应小数，不设“固定 3 位有效数字”契约。

首次 full sync 后使用独立 incremental RID；低频刷新；partial update 不含字段时保留上一有效值；unsupported/error 从未取得有效值时隐藏；真实 `0 B` 保持可见。

## 12. Mobile Adaptive System 2.0

手机主要 contract：

```text
SCROLL-001    每个长页面只有一个 primary scroll owner
COMMAND-001   Torrent filter + count + actions 单行
FACET-001     all-state collapsed label: Tracker/Path/Category/Tag
VIEWPORT-001  Torrent list 消费剩余 viewport，可 focus/restore
MOBILE-CARD   ↓ / ↑ / ETA / Size 优先单行
TOOL-PAGE     Search/RSS/Logs 不制造第二页空白
SETTINGS      settings-content 自己滚动到最后一个配置
```

专项 viewport：

```text
320 × 568
360 × 800
390 × 844
430 × 932
```

专项 fixture 同时覆盖 legacy qB 4.1.9.1 与 modern qB 5.2.3。

## 13. Logs 状态

```text
qB >= 4.1.0 → logs capability
legacy milliseconds >= 1e12 → /1000
modern seconds               → unchanged
ID descending
retain newest 5000
last_known_id = max ID
```

日志 toolbar 不再持有独立时区状态；全局状态栏与 Settings 共享同一个 `W.Time`。

## 14. Canonical Primitive 状态

| 模块 | Canonical primitive | v0.3.6 状态 |
| --- | --- | --- |
| qB Settings | SettingCard | implemented |
| Alternative WebUI | SettingCard | implemented |
| Advanced enums | Select/Listbox | implemented |
| Torrent list | DataGrid + VirtualList | implemented |
| General Select | Select/Listbox | implemented |
| Floating surfaces | FloatingLayer | implemented |
| Logs | DataPanel + VirtualList | implemented |
| Logs filters | FilterChip | implemented |
| Logs Follow | CheckControl | implemented |
| Display Time Zone | Global status Select + Settings | implemented |
| Torrent state | StatusPill | implemented |
| Transfer dialogs | Modal | implemented |
| Brand | AmbientMark | implemented |
| Detail return | Context Back | implemented |
| Mobile route scrolling | Primary scroll owner | implemented |
| Private/PT | TorrentSemantics | implemented |

## 15. Browser Gate

完整 representative interaction gate：

```text
12 compatibility nodes
× 3 viewports
  390 × 844
  1366 × 768
  1920 × 1080
```

Dedicated mobile gate：

```text
qB 4.1.9.1 + qB 5.2.3
× 4 phone viewports
  320 × 568
  360 × 800
  390 × 844
  430 × 932
```

必须验证 canonical Select、FloatingLayer collision、Global Display Time Zone、Advanced units/enums、AmbientMark + Reduced Motion、无横向溢出、单主滚动区、Facet all-state 语义、Private/PT union、storage incremental retention，以及 Page 2 → Detail → Back/Esc → Page 2 + scroll。

Fixture PASS 不能冒充真实版本 LIVE PASS。

## 16. LIVE regression

真实 LIVE 环境由操作者显式提供，不存放在仓库。至少覆盖：

```text
qB 4.1.9.1 / WebAPI 2.2.1
qB 5.2.x   / WebAPI 2.15.x
```

测试内容包括 Logs、全局时区、Settings/Advanced、Torrent Detail 返回、Alternative WebUI、Search/RSS、Desktop/Mobile/i18n 与 browser console。

Production cache header 也只针对操作者传入的实际 URL 检查，例如：

```sh
curl -I "$QB_URL"
```

HTML 目标仍是 `Cache-Control: no-store`。

## 17. Release 判定

```text
Repository CI                       required PASS on final HEAD
Compatibility Matrix contract       required PASS
12 × 3 representative browser gate required PASS
Dedicated mobile 2.0 gate           required PASS
FloatingLayer contract              required PASS
Timezone contract                   required PASS
Advanced Unit/Enum contract         required PASS
Storage contract                    required PASS
Cache contract                      required PASS
4.1.9.1 LIVE regression             pending real execution
5.2.x LIVE regression               pending real execution
Production HTML header              pending operator-supplied URL verification
```

Fixture/CI PASS 不冒充实机 PASS；synthetic future-major sentinel 不冒充正式 qB 6.x 支持。
