# 002. 兼容与实现状态

Status: **current compatibility / implementation / evidence facts**


> **2026-09-18 全量复盘同步**：本文件已按当前 A–F 方案复核。mutable current status / blocker / TODO / next-AI handoff 只维护在 `docs/014.AI协作与分支纪律.md` 的 `STOP — COMPLETED ARCHIVE BELOW` 之前；本文件只维护本领域 authority/contract。具体 exact SHA、Actions 与运行结果在实际开发前必须 fresh-read，不以本注记代替 repository truth。

本文件只记录当前仍有效的产品兼容事实、已实现能力和验证边界；历史过程由 Git history / Actions / artifacts 保存。Current HEAD 与 Actions 必须 fresh-read。**当前未完成事项只维护在 `docs/014.AI协作与分支纪律.md`**；本文件不建立第二套 TODO / handoff authority。

## 1. 当前产品状态

当前正在执行 A–F Native Surface Projection。Product VERSION 0.3.117 已把 #1893 certified typed-member source proof 正式 materialize，并完成 A1 generic Control Graph 产品切换。A1 的最新自动验证已经闭环：`5e9b027...` 是 0.3.117 product commit，`1af301e8... / CI #1898` 首次完整越过串行 representative browser suite，`0195167d... / CI #1899` 又以 8-way semantic UI matrix 全部 SUCCESS；Pages Source #987 证明已发布 `5e9b027...` Virtual qB Pages payload 可代表当前 `0195167...`，因为之后只有 tests/CI/docs 变化且 public product bytes 未变。当前 A1 状态为 **🟠 IMPLEMENTED — WAITING HUMAN REVIEW**；B 不得开始，整体计划仍是 **IN PROGRESS**。

```text
normal development branch: dev
main: do not move
[candidate]: prohibited by current user instruction
product VERSION: 0.3.117
qB support floor: 4.1.0 official stable
latest admitted stable: 5.2.3
Frozen stable profiles: 65
current plan: IN PROGRESS — see docs/014
```

qB 4.0.x **不是当前实现、CI 或正式支持范围**；当前正式 floor 从 qB 4.1.0 official stable 开始。未来新的 stable / major 必须通过 source-derived admission评估，而不是按 major/version猜兼容性。

当前已复核的最新 source/tooling checkpoint 是 `209d36e2d1b6e3a7cd7e791e9e1e82ee6cfb0bab`。该 SHA 的 CI run **#1867 / 35346821058 = SUCCESS**：smoke SUCCESS；native surface source base SUCCESS；Preferences 16/16 exact-SHA shards全部 SUCCESS；同一SHA source gate无 blocker；相同 SHA 的 Virtual qB Pages Source **#955 / 35346821091 = SUCCESS**。本轮修复了真实 upstream extraction缺口：qB Advanced中的若干 numeric Preference使用 `input type=text`，现在 exact source `MiB/KiB` unit会在 descriptor + scale proof成立时保留；5.2.3 `torrent_file_size_limit=MiB`、`socket_receive_buffer_size=KiB` 已进入正式IR。A-base Preferences compact 为 **311581 bytes**，SHA256 `4693b9a538d243c6ea4665ebc86809f1d704cc7524677718cf8132764eaafbba`；Frozen `releaseSetSha256=67abde3a4649d1142d798d5d3c14eb53b1f1eaf7fba4cf14d5cdebeb03161888` 未变。此前 Virtual qB Pages #847 的唯一失败已定位为 browser contract直接调用 `isWritable()` 时漏传 current prefs/state；该 stale caller已修正。最终 validated product SHA `8fc3175...` 的 **CI #1869 / 35355486177 = SUCCESS**、**Virtual qB Pages Source #957 / 35355486052 = SUCCESS**、**Virtual qB Pages #849 / 35355502565 = SUCCESS**；18/18 browser verify lanes全部成功，其中 `services` lane已实测 English → `zh_CN` save/reread/reload/reopen路径。 这套证据现在只作为 **A-base foundation evidence**：人工复核随后发现 About stale version、缺失单位/后缀、Listening Port Random helper、Scheduler时间组合、Proxy动态依赖/nested controls 等结构问题，因此不能证明A最终完成。

### 1.1 当前执行优先级：formal product first

当前 foundation 已经足以进入 A 的正式产品切换，因此开发重心必须从“继续扩 Preferences tooling”转到 `webui/**`：

```text
PRIMARY
  A1 qB Options Structural Projection / Control Graph
  -> exact Preferences HTML/behavior -> generic structural IR
  -> settings-compat -> SettingsSchema -> generic structural templates
  -> preserve existing fail-closed write gate; do not restore manual key/version owners

SUPPORTING ONLY WHEN NEEDED
  tools/**
  tests/**
  source parsers / census
  simulator / CI
```

新增 tests/tools 工作必须绑定一个具体 product blocker、product contract 或已知回归。若只是让 source framework“更完整”但不解除当前 `webui/**` blocker，应延后。当前可按约 70–80% 产品实现、20–30% 必要验证/工具作为执行启发，但不把比例当永久规则。

## 2. qB-owned / WeiG-owned 边界

```text
qB-owned
  Status / native Torrent filters/facets
  Options / Preferences
  RSS feeds/articles/Downloader/rules
  Torrent Detail
  qB actions / fields / menus
  qB official source/context copy

WeiG-owned
  Path facet
  Tracker Private / PT extension
  WeiG WebUI settings
  About
  WeiG presentation/layout/interaction extensions
```

qB-owned 的存在性、顺序、语义由 exact qB source 决定；当前值由 WebAPI 决定；文案由 exact `{source, context}` + official qB translation 决定。WeiG-owned 使用 WeiG schema/i18n/UI。两层不得互相冒充 owner。

## 3. Runtime compatibility truth

浏览器总控制面是 `W.CapabilityRegistry + webui/private/data/capabilities.json`，但 `capabilities.json` 不是所有版本事实的大杂烩。完整 admitted release source truth属于 build/CI Frozen catalog/LKG；browser只按 domain lazy-load compact facts：

```text
capabilities.json      release identity / high-level gates / compatFiles
torrent-compat.json    Torrent filters/status/fields/columns; B still needs native facet manifest
detail-compat.json     Torrent Detail source projection
settings-compat.json   bounded exact-source Settings runtime envelope; lazy decode/identity gate
rss-compat.json        RSS Downloader native surface/rule facts
source-actions.json    exact action/parameter provenance
qB-owned copy artifact source/context + official locale route
```

`capabilities.json.compatFiles` 当前包含 torrent/detail/actions/settings/rss。正式 runtime不得重新加载65-release full catalog。

危险 write继续 fine-grained fail closed：certified exact/equivalent identity、source action/endpoint、required parameter、payload semantics缺一不可；Settings进一步要求 current runtime key、exact Preference setter/type、`projection.safeWrite` 与 dependency gate同时成立。UNKNOWN/future不允许用 major/nearest-version猜写能力。

## 4. A / A1 — Options / Preferences 当前事实

### A-base foundation完成；A1实现与自动验证完成，等待第二轮人工复核

A-base已经完成单一Current Owner、bounded source-native runtime与危险write fail-closed gate；这些实现继续保留，不回退manual Settings owner。第一轮真实人工复核发现字段级 IR 缺少 qB source-owned control composition/behavior；A1 已据此引入 generic exact-source Control Graph、structural templates 与 typed-member proof，并完成 0.3.117 product cutover。当前不再有已知自动化 blocker，下一步必须由用户做第二轮真实 Options 人工复核。

已确认A1问题：

- source-direct About读取 `webui/private/weigg-install.json` 的 stale `0.3.7`，与canonical `0.3.109`不一致；
- Advanced若干 source-owned display adornment未显示，例如 `KiB / % / MiB / ms`；
- Listening Port源码中的 `Port input + Random button` 被摊平成单Preference row；
- Alternative-rate scheduler源码中的 `From HH:MM / To HH:MM / When` 被拆成多个同名数字行；
- Proxy Server nested fieldsets与 `Type`驱动的Host/Port/Auth/BitTorrent/peer/RSS/general-purpose enablement未完整投影；
- 同类风险扩展到 Connection Limits的enable/sentinel语义、I2P gated cluster及其它inline/nested Options结构。

根因不是某几个key漏写，而是当前 source IR以 `Preference -> row` 为中心。A1要求 exact qB source生成通用Control Graph：

```text
tab
-> fieldset / nested fieldset
-> row / cluster / source order
-> source/local labels + notes
-> preference controls
-> helper controls / source-owned buttons
-> display adornments
-> small auditable behavior predicate graph
-> descriptor + value projection
```

Renderer只能消费generic graph templates；禁止为 `listen_port`、scheduler、Proxy、I2P、Connection Limits建立手写key表、patch-version分支或第二renderer。

Display adornment与value projection必须拆分：source证明 `KiB/%/MiB/ms` 可以决定显示后缀，但不能因此自动推出API值需要scale。写入仍沿现有 `certified identity + source action + current runtime key + exact setter/type + projection.safeWrite + dependency + raw type` 门禁。

`settings-compat.json` 仍是唯一 Settings runtime domain入口。#1884 / `a52a99cb...` 已建立 legacy/modern Random 的 generic Control Graph；随后 0.3.113–0.3.116 依次修复 literal label、qB source-ref fallback、source-disabled input semantics，并由 #1890/#1891/#1892 保留为 regression evidence。最新 `d13744bc...` 以 generic local declared type + exact upstream member declaration 证明 `proxyConf.authEnabled`，CI **#1893 / 35384659152 = SUCCESS**，Preferences 16/16 exact-source shards与 source admission 全部通过；`proxy_auth_enabled` 现在具有 source-proven boolean read/write type、`typeAgreement=EXACT` 与 safe-write evidence，没有 Proxy/key 特判，也没有放宽 fail-closed。0.3.117 在 product commit `5e9b027...` 正式 materialize #1893 aggregate：Control Graph raw **483825 bytes** / SHA256 `c4f09e00498f6b45f1d3f3760d8732e8cc95e155111eb12c2017f73ef4032652`，deflate **43649 bytes** / SHA256 `06f0ef5c0bff20d5c1d59f65a7278aa48aaab7999cd51515cd1a60eb11dd41dd`，generated Settings manifest Git blob `4418668587ef35b122dc8069717a1bc00ad30018`。Frozen 65-release set与 `releaseSetSha256=67abde3a4649d1142d798d5d3c14eb53b1f1eaf7fba4cf14d5cdebeb03161888` 保持不变；source catalog identity更新为 `e7426f38b03044f78e35cb3396d019d699b865d7aa30797d8d44dfaa5f8ed0bf`，并与 compact capability control plane 同步绑定。`1af301e8... / CI #1898` 已让完整串行 representative browser suite SUCCESS；`0195167d... / CI #1899` 的 8 个 Linux semantic UI matrix jobs 全部 SUCCESS；Pages Source #987 确认 `5e9b027...` 的 successful Virtual qB Pages #861（build/deploy + 18 verify 全绿）可代表 current dev，因为后续无 Pages-relevant product bytes 变化。A1 已进入人工复核。

A1完整执行计划、validation与停止条件只维护在 `docs/014` active区。当前已经进入 `🟠 IMPLEMENTED — WAITING HUMAN REVIEW`；只有用户明确第二轮真实人工复核 PASS 后，才能进入 `🔵 HUMAN REVIEWED — WAITING FULL VERSION VALIDATION` 并运行 required admitted-version full validation。A/A1达到 `✅ VERIFIED DONE` 前不得进入B。

## 5. B / E — Torrent Sidebar 与 Detail 当前事实

Status filter semantics已走 `W.CapabilityRegistry + W.TorrentSemantics`；Torrent Detail已有成熟的 `torrentDetailUi() -> detail-compat.json -> W.QbUiEvidence -> generic Detail runtime` 链，E不另造第二套 Detail系统。

B仍未完成：`spatial.js` 目前固定声明 category/tag/tracker/savePath四个 facet，LibraryController也仍按固定 state/setter知道这些维度。目标是 source-derived native facet inventory/order/semantics -> generic state/projection；所有 qB-native facets按 exact source顺序出现，WeiG-owned Path永远最后。`All`只是 no-filter option，不应成为额外持久 facet标题。

E剩余工作是 source/copy/action completeness audit并补真正缺口，不能因为A/B/C/F重构而复制 renderer、polling、columns或 Detail state owner。

## 6. C — qB-owned copy 当前事实

`W.I18n` 已存在 exact-release official translation主链，`qb-settings-native.txt` 仍是当前 Settings-focused runtime copy registry；但 Settings外层、RSS、Sidebar等 surface仍存在手写英文/中文 fallback。

目标规则：qB-owned copy只以 exact upstream `{source, context}` 为身份；目标 locale从 exact release official translation facts解析；官方目标翻译不存在时只安全回退 upstream English source，不维护 WeiG英文→中文第二数据库。WeiG-owned Path、Private/PT、WeiG WebUI、About等继续 WeiG i18n。

若将 `qb-settings-native.txt` 泛化为 domain-neutral qB-owned copy artifact，必须原子替换文件、loader、callers、tests与 distribution contract；禁止新旧两个 copy registry并存。

## 7. D — RSS 当前事实

D的真实 rule-fidelity bug已修入当前产品：

- qB 4.3.0+ `smartFilter` 由 source manifest渲染/写回。
- qB 4.6+/5.x 使用 `torrentParams.stopped`，不再错误依赖 `torrentParams.paused`。
- historical `addPaused/createSubfolder` 与 modern `stopped/content_layout` 的 default/null语义由 source options保留，避免 checkbox破坏 null/default。
- `rss.js` 以 source-returned完整 rule object作为 draft base，只更新 manifest映射字段，未知字段保留。
- `rss-compat.json` 已成为正式 lazy-load runtime domain。
- native-surface source contract已经覆盖 all admitted stable；当前 latest exact SHA的最终 run状态仍需 fresh-read Actions确认。

D仍不能宣告整体完成：还需要 C/F 的 qB-owned official copy、independent RSS inventory/census、common catalog identity integration，以及 feeds/articles/read-state/Downloader actions final audit。不得恢复手写 rule-field owner。

## 8. F — Source admission / compact runtime 当前事实

已完成的 F-foundation：

```text
fixed Settings native-tab whitelist retired
common catalogIdentity helper exists
capabilities/torrent/detail/actions/settings compact compiler outputs share catalogIdentity
Preferences independent census exists and is wired into full-stable source contract
Preferences keyed native IR carries the same Frozen catalog identity
Preferences full source admission now uses one base + 16 bounded shards + strict exact-SHA aggregate
```

仍未完成：

```text
A1 structural projection 已在 0.3.117 完成并建立新自动证据；当前等待用户第二轮真实人工复核，PASS 后才能做 required admitted-version full validation
rss-compat generation is not yet unified into the same compact-runtime/common identity closure
runtime/distribution does not yet reject every mixed/stale domain set by common identity
independent census is not yet universal for B/C/D/E/F domains
```

最终 F要求所有 compact domains与 qB-owned copy都来自同一个 Frozen catalog并携带/验证同一 identity；browser只加载 bounded current-domain facts，不加载 full history。

## 9. 验证边界

阶段内部只跑与当前 product change直接相关的 unit/source/focused change-point tests；不要把“继续增加测试数量”当成阶段进度。阶段完整收口后最多一次 admitted-stable offline/Virtual full matrix。阶段 65 expected set必须直接来自 Frozen catalog，并校验 `executed==expected`、missing/duplicate/unexpected为空。

Source-driven domain completion必须满足：

```text
upstream inventory
= mapped facts + explicit reviewed exclusions
unaccounted = 0
duplicates = 0
```

Preferences字段级source census、formal Settings唯一browser owner与A1 structural control/accounting均已建立自动证据，覆盖helper/adornment/composition/behavior。当前仍缺用户第二轮真实人工复核与其后的 required full-version validation；两者闭环前 A 不得完成。A–F全部完成并冻结 final dev exact SHA后，才按 `docs/011` 运行一次昂贵 real-qB Full Frozen Matrix。若 real-qB后又产生新的最终产品 commit，则旧 SHA evidence不能证明新 SHA。
