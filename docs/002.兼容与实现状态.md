# 002. 兼容与实现状态

> 本文件记录当前真实产品兼容状态。`webui/**` 是正式产品；source catalog、evolution audit、Virtual qB、browser fixtures 是发现/证明层，不替代产品语义。

## 1. 当前正式状态

```text
branch: dev
Phase F approved base:
ca9ffaf221f3a18c4b9c9dbd7f5281e89009d6b9
VERSION: 0.3.59
main: untouched
```

阶段：

```text
Phase A — Source Facts Expansion                                DONE
Phase B — qB4.1+ Product Compatibility Core                     DONE
Phase C — Full Stable Product Compatibility Matrix             DONE
Phase D — Incremental Future Stable Admission / Last Known Good DONE
Phase E — Torrent Fields / Columns provenance                   DONE
Phase F — Settings + Tool/Detail Surface Provenance             APPROVED / IN PROGRESS
Phase G — Real-qB / Release Hardening                           PLANNED
Legacy WebAPI v1 Adapter                                        FUTURE OPTIONAL
main promotion                                                  NOT STARTED
```

Phase F docs-start 只定义 approved scope。产品实现必须等 docs-only smoke 通过后重新 fresh-read `dev`。

## 2. 正式支持范围 / Frozen LKG

```text
qB 4.1.0 -> 5.2.3
65 official stable profiles
50 qB4 + 15 qB5
current audited WebAPI ceiling: 2.15.1
Frozen catalog:
tests/fixtures/qb-release-catalog.lkg.json
SHA-256:
8b91742ac8eee276b7994c84445c681913430f220325a10afc8c931c89645ddb
manifest: tools/data/qb-stable-lkg.json
```

Frozen catalog 保持一个完整文件，不分片、不因发现新 stable 重写历史。

qB4.0.x / WebAPI v1：

```text
FUTURE OPTIONAL
not implemented
not in CI/full-stable matrix
```

## 3. 当前正式 product owners

```text
Exact release/source facts             W.ReleaseProfile
HTTP endpoint/transport invocation     W.QBClient
User-visible capability policy         W.CapabilityRegistry
Torrent status/filter semantics        W.TorrentSemantics
Torrent field/column provenance        W.TorrentFieldRegistry
Settings Preference semantics          W.SettingsSchema
Torrent query/page/sort state          W.LibraryController / W.AppState
Selection/actions                      W.Selection / ActionRegistry
Torrent row/card renderer              W.Components
DataGrid layout/resize/persistence     W.DataGrid
Logs runtime                           W.Logs
RSS query/presentation                 W.RSS / current UI caller
Settings presentation                  W.SettingsRenderer
```

`DESIGN.md` 是 Current Owner 最终 authority。Phase F 不允许为了兼容再造第二套通用 Capability owner。

## 4. Compatibility states

```text
NATIVE
NORMALIZED
DERIVED
READ_ONLY
UNAVAILABLE
UNKNOWN
```

优先级：

```text
NATIVE
-> NORMALIZED / ADAPTER
-> reliable old-API composition
-> reliable DERIVED
-> READ_ONLY when applicable
-> UNAVAILABLE
```

future `UNKNOWN` 只关闭无法证明的具体 capability；dangerous write 必须在 HTTP 前 fail closed。

禁止：

```text
version-only final truth
higher-version auto inheritance
read/write coarse boolean
404/501 after click as feature detection
N->N compatibility requests
presentation self-probe
second state / duplicate polling
Simulator PASS as product proof
```

## 5. 已完成阶段关键事实

### Phase A — Source Facts — DONE

```text
implementation:
c3d184d1f39164467d9fde2cfabdfa22a315fae5
VERSION 0.3.57
```

建立 stable tag、qB/WebAPI/source SHA、API actions/parameters、Torrent fields/states/filters、Details response fields、Preference getter/setter/type provenance、release evolution facts。

### Phase B — Product Compatibility Core — DONE

```text
candidate:
84b86956924d79b1086bb10a915e45599f9d6685
VERSION 0.3.58
```

完成 qB4 paused/resumed -> stopped/running、reliable derived filters、Category/Tags read/write 分离、Torrent action exact source gate、unsupported dangerous actions HTTP 前 fail closed。

### Phase C — Full Stable Product Matrix — DONE

```text
candidate:
b14926b8d67b5919aa2c281762ed97c5cee46c21
65 / 65 stable PASS
VERSION 0.3.58
```

正式 matrix 逐 stable 执行 runtime product owners，不只是 JSON presence compare。

### Phase D — Incremental Stable Admission — DONE

```text
validated implementation:
8d2f9d162af9357328879206d71ab82714916ccc
VERSION 0.3.58
```

Tag-first / no-new-stable immediate exit / new-profile-only source extraction / frozen prefix protection / `PENDING_REVIEW` fail-closed 已建立。

### Phase E — Torrent Fields / Columns provenance — DONE

```text
docs-start / implementation base:
97170de50dfd79c953cd93f64f6b5807147436eb

final validated implementation:
6443cc35f570e8f1ed7a7e078aa15efa1618ce25

Phase E docs closure / Phase F reviewed base:
ca9ffaf221f3a18c4b9c9dbd7f5281e89009d6b9
VERSION 0.3.59
```

Current owner：

```text
webui/private/scripts/torrent-fields.js
-> W.TorrentFieldRegistry
```

当前正式 17 fields：

```text
name size progress dlspeed upspeed eta state ratio tracker
category tags num_seeds num_leechs save_path added_on completion_on priority
```

Frozen 65 stable：

```text
17 / 17 fields = NATIVE on every profile
65 / 65 profiles PASS
```

Saved preference 与 effective availability 分离；Desktop/Mobile 共用 semantic owner；field presence 不证明 server-side sort semantics。

Phase E exact candidate：

```text
SHA: 6443cc35f570e8f1ed7a7e078aa15efa1618ce25
CI run: 34105803400
smoke PASS
Full qB Release compatibility audit PASS
Full Stable PRODUCT Matrix PASS
Linux browser PASS
Windows browser PASS
release candidate package PASS
```

Virtual qB Pages：

```text
source run 34105803300 PASS
downstream 34105813928 PASS
build/deploy/verify PASS
exact source 6443cc35f570e8f1ed7a7e078aa15efa1618ce25
```

Pages 是 auxiliary evidence，不替代 Phase G real-qB validation。

## 6. Phase F pre-start audit — 当前已证明的事实

### 6.1 Settings

现有 `W.SettingsSchema` 已按 exact Preference descriptors 决定 surface/type/unit/enum/editability；`settings.js` 保存链为：

```text
writableDraft
-> validateBeforeSave
-> QBClient.setPreferences
-> QBClient.getPreferences verification reread
```

Phase F 需要补全 endpoint-level read/write provenance：

```text
app/preferences read action
app/setPreferences write action
+
per-Preference setter/type provenance
```

当前 frozen 65 stable 两个 app actions 均 source-proven；future UNKNOWN 不得靠版本号猜可写。

### 6.2 RSS — 当前存在历史兼容错误

Source-derived catalog 与 upstream qB `release-4.1.0/rsscontroller.h` 均证明 qB 4.1.0 已有：

```text
addFolder
addFeed
removeItem
moveItem
items
setRule
renameRule
removeRule
rules
```

因此当前 `RSS requires WebAPI >= 2.1.0` 会错误隐藏 qB 4.1.0–4.1.2。

Formal truth：

```text
RSS core source actions: qB 4.1.0 -> 5.2.3, 65 / 65
```

Virtual qB/Pages 中“qB4.1.0 rss/items 应 404”的旧 expectation 是 stale evidence，Phase F 必须修 test/simulator，不允许为了旧绿灯保留错误产品行为。

### 6.3 Search

Frozen catalog audit：

```text
search/plugins
search/start
search/status
search/results
search/stop
```

核心 Search actions 从 **qB 4.1.4** 起 source-proven。Phase F 应以 exact action truth 决定 route/read/job actions，而不是最终依赖 WebAPI `>=2.1.1` heuristic。

### 6.4 Logs

```text
log/main
log/peers
```

从 formal support floor qB **4.1.0** 起 source-proven，65/65。Phase F 要把 route/read capability 绑定 exact actions；现有 polling cadence 不因兼容层再复制。

### 6.5 Torrent Details / writes

Frozen 65 source facts：

```text
properties                 65 / 65
files                      65 / 65
filePrio                   65 / 65
trackers                   65 / 65
addTrackers                65 / 65
webseeds                   65 / 65
torrentPeers               65 / 65
removeTrackers             60 / 65, from qB 4.1.5
editTracker                60 / 65, from qB 4.1.5
banPeers                   54 / 65, from qB 4.2.0
```

Catalog 已包含 Properties/Tracker/File/WebSeed response-field facts。当前 Overview 主要字段在 65/65 都存在；Tracker 的部分统计字段从 qB 4.1.5 才出现，应按 field provenance 隐藏/展示，不伪造。

`webseeds().catch(() => [])` 一类兼容行为需要退休：unsupported/error 与真实空列表必须区分。

### 6.6 Action parameter evolution

`editTracker` 已有 exact source parameter facts：

```text
qB 4.1.5 -> 5.1.x:
hash + origUrl + newUrl required

qB 5.2.x:
hash + url required
newUrl + tier optional
```

Phase F 应让 QBClient 消费 `ReleaseProfile.actionDescriptor()/apiActionParameters`，不能继续按 WebAPI threshold 猜参数名。

## 7. Phase F scope / implementation order

```text
F1 exact surface/action/parameter/response-field provenance substrate
F2 Settings endpoint read/write provenance
F3 Search / RSS / Logs granular capabilities
F4 Details / Files / Peers / Trackers / WebSeeds / Categories / Tags / Creator
F5 product diff + frozen matrix + browser + exact candidate
```

第一版预计现有 source facts 足够，**不修改 source parser/stable discovery/evolution tooling**。若实施证明 facts 不足，再升级风险等级并运行 heavy source/evolution audit。

## 8. Phase F non-goals

```text
no Settings rewrite
no large UI redesign
no qB4.0.x/WebAPI v1
no manual compatibility mode
no second capability owner
no N->N requests
no extra high-cost polling
no caller self-probe
no Phase G real-qB hardening yet
```

第一次正式 `webui/**` 修改才 bump `0.3.59 -> 0.3.60`；本 docs-start 保持 0.3.59。

## 9. Phase F DoD

```text
- touched surfaces use exact source truth for final capability
- read/write/action/parameter/response-field separated
- unknown dangerous writes fail closed before HTTP
- qB4.1.0 RSS corrected
- stale simulator/browser expectations corrected, not product weakened
- unsupported/error is not fake empty data
- frozen 65 formal product matrix PASS
- future-major synthetic UNKNOWN/no-HTTP PASS
- relevant Linux + Windows browser PASS
- final [candidate]: source catalog + all-stable audit + Product Matrix + package PASS
- docs Phase F DONE + STOP before Phase G
```

## 10. CI boundary

Runtime compatibility changes：

```text
smoke/npm/syntax
+ direct contracts
+ frozen LKG 65-profile product regression
+ relevant browser
```

Final candidate：

```text
exact generated catalog
-> all-stable source audit
-> Full Stable PRODUCT Matrix
-> Linux Chrome
-> Windows Chrome
-> exact-SHA candidate package
```

Source parser/discovery/evolution 未变时，不人为触发独立 heavy evolution audit。

## 11. Git / proof boundary

```text
fresh-read dev
-> candidate only parent = exact HEAD
-> exact expected diff
-> validate
-> fresh-read dev immediately before ref update
-> moved => discard/rebuild
-> force:false only
```

Repo-wide claims 只认 exact SHA + complete relevant tree/blob/file。Windows CRLF working-tree bytes 不能代替 Git blob identity。

## 12. Main boundary

```text
main untouched
no promotion/tag/release without explicit user approval
no force push
```
