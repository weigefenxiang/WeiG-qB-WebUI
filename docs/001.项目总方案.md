# 001. 项目总方案

Status: **current project authority / stable project overview**

WeiG qB WebUI 是 qBittorrent Alternative WebUI。`webui/**` 是唯一正式产品；source catalog、真实 qB harness、Virtual qB、browser/Pages 与 CI 都是发现、验证和发布设施，不能反过来定义产品语义。

本文件只维护长期目标、正式边界、authority 与当前高层状态。**Git history 是版本历史档案；docs 不再按 WeiG 版本号新增“复盘/下一 AI 交接”文件。** 已完成工作形成长期规则时写回对应 authority；当前未完成事项只维护在 `docs/014.AI协作与分支纪律.md`。

## 1. 长期目标

同一份正式 `webui/**` 应覆盖：

```text
qBittorrent 4.1.0 official stable
-> all admitted official stable releases
-> current latest official stable
-> future official stable / future major
```

兼容策略优先级：

```text
NATIVE
-> NORMALIZED / ADAPTER
-> reliable old-API composition
-> reliable DERIVED
-> READ_ONLY when applicable
-> UNAVAILABLE
-> UNKNOWN when facts are not provable
```

未来或未收录版本不能因为版本号更高就自动继承 current latest 全部语义，也不能按 major 整体拒绝。`UNKNOWN` 只关闭无法证明的具体能力；危险写操作必须在 HTTP 前 fail closed。

qB 4.0.x / WebAPI v1 不在当前正式支持范围。若未来用户明确扩大范围，只允许通过集中 Legacy Adapter 映射到 canonical WeiG semantics，禁止在 UI/caller 散落 v1/v2 分支。

### 1.1 Universal Portable 是正式产品形态

正式 WeiG WebUI 必须长期保持：

```text
同一份 materialized WeiG-qB-WebUI/
-> 可复制到任意受支持 qB 主机
-> qB 指向该 Alternative WebUI 目录即可运行
-> 正常运行不依赖 tools/**、simulator/**、CI、GitHub 或在线服务
-> 离线时仍保留已声明的正式产品能力
```

完整 upstream source、历史 release catalog、Settings/translation LKG、recovery bodies 与验证数据可以很大，但它们属于 **build/source/evidence**。最终用户 runtime 只应携带浏览器和 qB WebApplication 真正消费的最小 runtime IR/asset，不得把整套 build-time knowledge 原样复制给每个用户。

未来可以提供 targeted installer/当前实例裁剪作为附加优化，但不能取代 Universal Portable 官方产品，也不能让“复制一个目录直接使用”失效。

## 2. 当前基线

截至 2026-09-14，可核实的产品/发布状态：

```text
latest published WeiG Release: v0.3.61
repository/product VERSION: 0.3.85
main: stable promoted baseline; do not move without explicit authorization
dev: active development; always fresh-read refs/heads/dev
qB support floor: 4.1.0 official stable
latest admitted stable: 5.2.3
Frozen stable profiles: 65
current-stable locale surface: 61
```

这里的 65 / 61 是当前 operational facts，不是永久常量；未来由 exact LKG 自动演进。

`VERSION = 0.3.85` 不等于 `v0.3.85` 已发布。当前正式 GitHub Release 仍为 `v0.3.61`。

当前已经完成的主要产品基础包括：exact source/release profiles、deterministic translation recovery + Settings/source LKG、source-driven main Torrent columns、Transfer/Locale 单一 owner、installer exact identity/config safety、主 Torrent 列表横向滚动期间 polling repaint coalescing，以及 **Torrent Detail 原版语义收口**：source-generated tabs/General/tables/context actions、exact Tracker URL、Peer flag、首行 toolbar、shared columns 与单一横向 scroll owner。更细当前事实见 `docs/002`。

用户已经明确选定下一项产品方向：**Runtime Distribution / Settings / Translation 轻量化收口**。它目前只是批准的下一阶段方案，尚未写入 `webui/**`，不能误报为已实现。完整执行边界、阶段与验收条件只在 `docs/005` 与 `docs/014` 维护。

## 3. 文档 authority

```text
docs/001   项目目标、正式边界、全局原则
docs/002   当前兼容 / 实现 / 验证事实
docs/003   模块架构与 Current Owner 边界
docs/004   UI / cache 通用契约
docs/005   Settings / Locale / qB-owned 翻译与共享交互
docs/006   CI / candidate / promotion / release 流程
docs/007   安装、升级、Dev distribution、Windows config、回滚
docs/008   Torrent workspace / main-detail ownership
docs/009   Virtual qB Lab 验证边界
docs/010   真实 qB 产品兼容策略
docs/011   全版本真实 qB 发布前验证规则
docs/014   唯一 AI 交接 / 当前未完成事项
docs/015   长期开发硬规则
DESIGN.md  Current Owner / UI / design system final authority
```

不再维护：

```text
按 WeiG VERSION 命名的复盘文件
按 VERSION 命名的下一 AI 交接文件
HANDOFF-v2 / ROADMAP-v2 / FINAL-PLAN 等第二 authority
为已完成阶段保留重复 current-state 副本
```

真正需要保留的事故教训、source hard boundary 或安全契约，应合并回其长期 owner 文档；历史实现过程由 Git commit / Actions / artifacts 审计。

## 4. Truth hierarchy

行为真值优先级：

```text
qB official source + exact admitted profile
-> canonical WeiG runtime owner
-> real qB evidence where runtime/hosting behavior requires it
-> browser / Virtual qB / Pages evidence
-> docs / tests / fixtures
```

旧 prompt、stale test、Simulator 或旧 docs 与已确认 source/runtime truth 冲突时，应更新旧内容，不能为了旧断言恢复错误 owner、错误 capability 或不安全写行为。

Source parser 遇到未知 upstream 结构必须 fail closed，并读取 exact source；禁止 guessed mapping、fake empty data 或 presentation self-probe。

## 5. Current Owner 与产品实现原则

一个 semantic purpose 只能有一个 Current Owner。正式 owner 切换必须 atomic cutover：

```text
new owner proven
+ callers switched
+ tests/contracts updated
+ docs/DESIGN updated when needed
+ superseded owner/state/event/polling/helper retired
```

禁止：

```text
old/new 双 active
MutationObserver repair
monkey patch
post-render repair
第二 polling/state/layout/language owner
feature-local 重造 canonical UI 工具
```

Desktop/Mobile 是同一业务 state 的 adaptive presentation，不是两套应用。

## 6. 分支与 exact-SHA 纪律

长期 canonical branches：

```text
dev   normal development / fix / docs / validation
main  validated promotion result
```

所有修改先进入 `dev`。每个 mutation batch 前 fresh-read 当前 `dev`；更新 ref 前再次 fresh-read 做 race check；只允许 safe fast-forward，禁止 force push。

未经用户明确授权不得：

```text
移动 main
formal promotion
tag
GitHub Release
手动触发 Candidate
手动触发 Current Stable Locale Matrix
手动触发 Real qB Full Frozen Matrix
主动补跑 pending Settings/source release evidence
```

Exact Git SHA 是 code、CI、Pages、candidate、matrix、promotion 与 Release evidence 的身份。旧 SHA 的 PASS 不自动证明新 SHA；`SKIPPED != PASS`。

## 7. Locale / translation 原则

唯一 persisted language truth：

```text
qB app/preferences.locale
```

qB-owned wording采用 native-first：

```text
exact qB source/context
-> source-bound translator behavior
-> QBT_TR / qB native translator
-> deterministic official QM recovery where source-proven
-> exact official-TS bridge only for proven native gap
```

WebAPI负责 qB 数据/API truth，但当前没有“返回 qB 内置 translation dictionary/QM”的通用 WebAPI。Alternative WebUI 的 native translation 行为必须按 exact qB source证明；不能假设它会自动借用 built-in WebUI 的 translation resource。

`4.5.0 -> 4.6.4` Alternative WebUI document-translation gap 是 source-proven hard boundary，必须保留 exact bridge。完整 recovery translation bodies只能属于 build/source evidence；下一阶段目标是把 runtime bridge/QM 缩到 WeiG 真正使用的 qB-owned refs，而不是继续携带 all-release/all-locale Settings translation database。

WeiG 自有功能文案由 WeiG 自己负责；qB upstream 已拥有并有 exact source/context 的文案不得再维护一套手写中英文映射。

## 8. Distribution / Windows 安全原则

Dev installer只消费 Pages materialized distribution，不允许 raw-source fallback。Stable installer只消费 immutable GitHub Release identity与checksum。

Materialized distribution必须满足 Universal Portable 原则，同时保持 runtime 最小化：

```text
build/source evidence may be large
!=
end-user runtime should carry the same large evidence
```

`qb-settings` 全量历史 Settings/translation shard 被明确列为下一阶段 **runtime-removal target**；它的 source/LKG 证据仍可保留在 tools/CI/artifacts，用于生成、审计和验证。`qb-release-profiles` 当前仍承担 exact source/capability/write safety，不得在同一轮为减体积而直接删除或降级其 provenance；后续若优化必须是独立、source-proven 的 runtime IR 收口。

Windows qB config 外部修改必须：strict decode/round-trip、byte-identical backup、temp write、invariant reread、target-unchanged check、safe replace、post-write verify；禁止 PowerShell whole-file `Get-Content/Set-Content` 转码路径。

Installer backup retention、Dev distribution classifier、Windows config细节统一由 `docs/007` 维护，不再拆成独立历史事故/Windows Dev 文档。

## 9. 当前开发入口

下一位 AI 不需要阅读一串历史版本复盘。固定入口是：

```text
docs/015
-> docs/014
-> docs/001 / 002 / 003
-> 与任务有关的专项 authority
-> DESIGN.md
-> fresh-read repository truth
```

当前已经有用户明确批准的下一方案：Runtime Distribution / Settings / Translation 轻量化收口。下一位 AI 必须严格按 `docs/014` 的 scope 与阶段继续，先做 source-first / Current Owner / compatibility-impact fresh audit，再在 `dev` 实施；不得顺手扩大到 promotion、release、其它 UI 重构或未经批准的 `qb-release-profiles` 重构。
