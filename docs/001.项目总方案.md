# 001. 项目总方案

Status: **current project authority / stable project overview**

WeiG qB WebUI 是 qBittorrent Alternative WebUI。`webui/**` 是唯一正式产品，并且从 0.3.99 起本身就是可直接复制运行的 self-contained runtime；tools、source catalog、Virtual qB、CI 与 Pages 只负责生成、审计、验证与发布证据，不能反向成为浏览器运行依赖。

本文件只维护长期目标、正式边界、authority 与当前高层状态。**Git history 是版本历史档案**；docs 不再按 WeiG 版本号新增复盘/下一 AI 交接文件。当前计划状态、未完成事项与 next-AI handoff 只维护在 `docs/014.AI协作与分支纪律.md`。

## 1. 长期目标与兼容原则

```text
qBittorrent 4.1.0 official stable
-> all admitted official stable releases
-> current latest official stable
-> future official stable / future major
```

未知能力不得靠版本号猜；危险 write 必须在 HTTP 前 fail closed。一个 semantic purpose 只有一个 Current Owner；禁止 old/new 双 active、MutationObserver repair、monkey patch、post-render repair、第二 polling/state/layout/language owner。

## 2. 当前基线

截至 2026-09-16，本轮已验证基线：

```text
repository/product VERSION: 0.3.102
current product commit: 44d317f12b2232d8745bc76ec19abe8f57ee9737
validated ordinary-CI checkpoint: 3015670e7e44f53ed5c3e5f3ab2df7d559429fa5
ordinary CI run 35042852742: SUCCESS
latest full Virtual qB Pages checkpoint: 9705226612e210b7a720aad8d949a87d181a66fa
Virtual qB Pages run 35042417653: SUCCESS
latest published WeiG Release/tag: v0.3.61
main: ba8ee49886fad159e76014514b5a0a4ba803bc29; do not move without explicit authorization
dev: normal development branch; current HEAD must always be fresh-read
qB support floor: 4.1.0 official stable
latest admitted stable: 5.2.3
Frozen stable profiles: 65
current approved plan: COMPLETED / WAITING_FOR_USER_DIRECTION
```

`VERSION = 0.3.102` 不代表 `v0.3.102` 已发布；正式 Release 仍为 `v0.3.61`。Product commit、validated test/evidence checkpoint、docs-only current HEAD 是不同概念：SHA 只作已验证/已观察 checkpoint，接手时必须 fresh-read current `dev` 与 Actions。

0.3.102 已闭环上一轮 external deterministic failures：Locale bootstrap 在判断 `app/preferences.locale` 可写性前会先加载 Settings compatibility/write proof；Settings/browser tests 已按 current compound SettingRow 与 semantic readiness 验证，不再依赖退休 DOM 或固定等待时间。正式产品架构没有因此新增第二 language/layout/scroll owner。详细事实见 `docs/002` 与 `docs/014`。

当前用户批准的这一轮方案已经实现并完成适用 external validation；没有已知未闭环的 current deterministic failure。**在用户提出新的明确方案前，不主动开始 0.3.103、不把未来方向变成自动 TODO。**

## 3. 当前正式架构

```text
qB WebAPI
   ↓
W.QBClient
   ↓
W.CapabilityRegistry ← capabilities.json / torrent-compat.json / detail-compat.json / source-actions.json
   ↓
product semantic owners
   ↓
WeiG UI

Settings Preference semantics ← settings-compat.json + W.SettingsSchema
qB-owned copy                ← qb-settings-native.txt + minimal official QM

qB official source / release catalog
   ↓
tools / CI / simulator evidence
   ↓
validate compact runtime contracts
```

Browser runtime 已退休旧 release-profile owner、`private/data/qb-releases.json` 与 `private/data/qb-release-profiles/`。65-release catalog 继续作为离线 source/CI/simulator evidence，不进入 dev 正式浏览器 runtime。

0.3.100–0.3.102 坚持同一架构：Torrent/Detail 复用 canonical `W.VirtualList`，Settings 由 `W.SettingsSchema + W.SettingsRenderer` 消费实时 `app/preferences` truth，`W.CapabilityRegistry` 是 runtime compatibility/current release facts owner。交互修复不得通过新建第二 scroll/poll/layout/language owner实现。

## 4. Universal Portable

raw `webui/` 与 materialized distribution 都必须自包含：正常运行不依赖 Node、tools、simulator、CI、GitHub 或在线翻译服务。Distribution 只负责 exact `GIT_SHA`、ZIP/checksum、manifest/installer 等发布身份，不再为 dev 生成 per-release browser profiles。

## 5. Authority

`docs/002` 维护 current facts；`docs/003` 维护架构 owner；`docs/004` 维护 UI/cache contract；`docs/005` 维护 Settings/Locale/i18n；`docs/006` 维护 CI/candidate/promotion/release；`docs/007` 维护安装/distribution；`docs/008` 维护 Torrent workspace；`docs/009` 维护 Virtual qB；`docs/010` 维护兼容策略；`docs/011` 维护 full real-qB release gate；`docs/webapi-evolution-ledger.md` 是 WebAPI evolution evidence index；`docs/014` 是唯一 mutable handoff/TODO authority；`docs/015` 是长期硬规则；`DESIGN.md` 是 Current Owner/UI/design final authority。

下一位 AI 接手时应先阅读 **全部 `docs/*`**，再读 `DESIGN.md`，随后 fresh-read branches/VERSION/Release/Actions；不得只凭本文件或旧聊天继续开发。

## 6. 分支纪律

所有开发先进入 `dev`。每个 write batch 前 fresh-read `dev`，更新 ref 前再次 race-check，只允许 fast-forward，禁止 force push。未经用户明确授权不得移动 `main`、使用 `[candidate]`、promotion/tag/Release 或手动 release-grade matrix。

等待用户方向有两种不同状态，必须区分：

```text
PAUSED / NOT COMPLETED       -> 仍有已知未闭环工作，但用户要求暂停
COMPLETED / WAITING          -> 已批准方案已闭环，没有 current TODO，等待用户提出新方案
```

当前属于第二种。下一位 AI 不得因为历史失败、未来方向或“顺便优化”自行开新任务；用户提出新方案后，才在 `dev` 上开始新的 coherent batch。