# 001. 项目总方案

Status: **current project authority / AI handoff entry**

WeiG qB WebUI 是 qBittorrent Alternative WebUI。**`webui/**` 是唯一正式产品**；source catalog、真实 qB harness、Virtual qB、browser/Pages 与 CI 都是发现、验证和发布工具，不能反过来定义产品语义。

本文档不再维护 Phase A–G 等历史阶段。长期维护只记录当前目标、正式边界、执行流程与不可破坏的规则。

## 1. 长期目标

同一份正式 `webui/**` 应覆盖：

```text
qB 4.1.0
-> all admitted official stable releases
-> current latest official stable
-> future official stable / future major
```

兼容策略优先级：

```text
NATIVE
-> NORMALIZED / ADAPTER
-> reliable old-API composition
-> reliable DERIVED
-> READ_ONLY when applicable
-> UNAVAILABLE
```

对未来或未收录版本，无法证明的具体能力标记为 `UNKNOWN`。`UNKNOWN` 只关闭无法证明的能力，尤其危险写操作必须在 HTTP 前 fail closed；不能因为 major/version 更高就自动继承当前 latest 的全部语义，也不能按 major 整体拒绝。

## 2. 当前基线

截至 2026-09-11：

```text
latest stable WeiG release on main: v0.3.61
main product VERSION: 0.3.61
current dev product VERSION: 0.3.71
pre-doc-recap dev product SHA: c1823a4f7a721b34eab7db1855f30844ca43b43e
support floor: qBittorrent 4.1.0
Frozen LKG: qB 4.1.0 -> 5.2.3
stable profiles: 65
Frozen catalog: tests/fixtures/qb-release-catalog.lkg.json
manifest: tools/data/qb-stable-lkg.json
```

`dev` 的 0.3.71 尚未 promotion 到 `main`。本文档更新本身属于 docs-only commit；因此提交后 `dev` HEAD 会高于上述 product SHA。任何后续 candidate/evidence 必须以届时 final exact SHA 为准。

Frozen catalog 是已验收历史事实。新 stable 的 admission 可以扩展它，但不能无理由重写已验证历史。

qB 4.0.x / WebAPI v1 当前不在正式支持范围。如未来实现，只允许集中式 legacy adapter 进入 canonical WeiG semantics，禁止把 v1/v2 分支散落到 UI caller。

## 3. Authority

```text
docs/001   项目目标、正式边界、全局规则
docs/002   当前兼容事实与实现状态
docs/003   模块架构与 Current Owner
docs/004   UI / cache contract
docs/005   统一交互、Settings 与 Locale 当前契约
docs/006   CI / promotion / release 流程
docs/007   安装、升级、回滚、手动部署
docs/008   Torrent 工作区与状态所有权
docs/008.Windows开发版安装   Windows Dev 安装与配置安全
docs/009   Virtual qB Lab 辅助验证规则
docs/010   真实 qB 产品兼容原则
docs/011   全版本真实 qB 验证规则
docs/012   qB 原生翻译 / i18n 复盘
docs/013   2026-09-11 Windows 配置编码事故复盘与未完成项
DESIGN.md  Current Owner / UI / design system final authority
```

行为真值优先级：

```text
qB official source + exact admitted profile
-> canonical WeiG runtime owner
-> real qB evidence
-> browser / Virtual qB / Pages evidence
-> documentation
```

如果测试、Simulator 或旧文档与已确认 source/runtime truth 冲突，应修测试/Simulator/文档，不得为了旧断言保留错误产品行为。

## 4. 分支规则

长期只保留：

```text
main   stable / promoted branch
dev    normal development branch
```

普通开发在 `dev`。`main` 只接受已经验证的 exact SHA 的 safe fast-forward promotion。

禁止：

```text
force push main
merge commit 代替 exact-SHA promotion
在 main 单独修改产品再发布
未经用户明确授权移动 tag / GitHub Release
长期保留 fix/tmp/phase 工作分支
```

本轮 0.3.71 排障明确要求：**只动 `dev`，不动 `main`、tag、GitHub Release。**

## 5. Exact-SHA 原则

Git SHA 是以下证据的共同 identity：

```text
source checkout
CI / candidate
real-qB matrix
Pages site artifact
promotion
release package
release tag
```

**任何新 commit 都产生新的 candidate identity。旧 SHA 的 candidate/G-FM/Pages evidence 不自动证明新 SHA。**

纯 docs commit 不要求机械跑重型 G-FM/candidate，但一旦准备 promotion/release，必须在当时 final `dev` exact SHA 重新取得发布所需证据。

## 6. 兼容实现规则

正式兼容必须由 machine-readable source facts 与 canonical owners 驱动。

禁止：

```text
业务层手写 patch-version compatibility table
major>=5 之类作为最终产品行为 oracle
404/501 after click 作为 capability owner
manual compatibility mode
fake empty data 掩盖 unsupported/error
N items -> N compatibility requests
presentation self-probe
第二套 capability owner
第二套业务状态 / duplicate polling
runtime monkey patch / MutationObserver bridge 修复架构问题
```

允许 version 信息参与 source profile 选择、诊断和 evidence；最终 feature truth 仍来自 exact actions / parameters / fields / Preference provenance 等事实。

### 6.1 Release profile resolution

当前 runtime resolution：

```text
EXACT       exact admitted official stable
EQUIVALENT  canonical suffix equivalence, e.g. 5.2.3-r1 -> 5.2.3
INHERITED   same major.minor nearest lower certified profile when source/runtime contract permits
FALLBACK    no safe relation
```

目标允许例如：

```text
5.2.4   -> temporarily resolve from 5.2.3
5.2.3.1 -> resolve from 5.2.3
```

但不是“nearest wins”。不能跨 hard source boundary 盲继承；危险写操作仍必须 source/range proven。

### 6.2 Runtime catalog 必须 sharded

真实 qB 4.6.7 已证明接近 10 MiB 的单体 runtime `qb-releases.json` 不是可接受设计。当前生产 runtime 使用：

```text
private/data/qb-releases.json                     # compact index
private/data/qb-release-profiles/<sourceSha>.json # exact profile shard
```

只加载当前 profile shard；load failure 必须暴露具体错误，不能吞成空 catalog/fallback。完整 65-profile source evidence 仍可存在于构建/审计层，不应重新塞回真实 qB runtime 单文件。

### 6.3 Language / translation 当前原则

Persisted language truth：

```text
qB app/preferences.locale
```

qB-owned wording 采用 native-first：

```text
qB official source/context
-> QBT_TR
-> qB translator
-> official minimal QM
```

WeiG-only wording继续由 `W.I18n` 拥有。

全 65 stable translator audit 已完成；`4.5.0 -> 4.6.4` 因 upstream Alternative WebUI 不执行 document translation，保留 exact official-TS compatibility bridge。详细见 `docs/012.qB原生翻译与国际化复盘.md`。

## 7. Windows 配置安全规则

2026-09-11 真实 Windows 事故证明：外部安装器不能把整个 `qBittorrent.ini` 当普通文本“读出再 Set-Content 写回”。PowerShell 5.1 的 UTF-8 no-BOM / ANSI 行为曾造成中文、日文配置全局 mojibake。

当前 `dev` 已完成 encoding-preserving read/write + PowerShell 5.1 regression，但安全链还必须继续补齐：

```text
qB running + -configure -> fail closed
non-empty qBittorrent_new.ini -> fail closed
zero/invalid config -> fail closed
temp write -> strict reread -> atomic replace
backup + restore on failure
portable/custom/multi-config ambiguity -> do not guess
```

在这些 guardrails 完成前，不得把当前 Windows installer 描述为“配置修改完全安全”。完整复盘见 `docs/013.Windows配置编码事故复盘.md`。

Windows Dev bootstrap authority：

```text
https://weigefenxiang.github.io/WeiG-qB-WebUI/downloads/dev/install.ps1
```

不要使用 `main/installers/install.ps1 -dev` 取得开发版 raw source。

## 8. 当前验证状态

针对 product SHA `c1823a4f7a721b34eab7db1855f30844ca43b43e`：

```text
smoke                               PASS
Windows browser candidate           PASS
Linux browser candidate             PASS
Full qB Release compatibility audit PASS
release candidate packaging         PASS
candidate deployment acceptance     FAIL
```

candidate deployment 当前确定性失败：

```text
Candidate qB public root did not expose exact build SHA.
```

Pages 同 SHA：

```text
build  PASS
deploy PASS
verify FAIL
```

Pages verify 当前是 5000-torrent 场景的 wait race：`catalogReady=true` 时 `catalogBusy` 尚在短暂 true。应修 acceptance 等待 `catalogReady && !catalogBusy`，不要修改产品 lifecycle 迎合测试。

因此当前 0.3.71 **不是 final green release candidate**。

## 9. 发布规则摘要

完整 authority 见 `docs/006.发布与晋级流程.md`。长期顺序：

```text
finish changes on dev
-> freeze final dev exact SHA
-> normal CI PASS
-> manually run Real qB Full Frozen Matrix, exact SHA, 65/65 PASS
-> run CI candidate on the same exact SHA
-> ensure exact-SHA Pages dev artifact exists and live verify PASS
-> Promote validated dev to main, fast-forward only
-> verify main Pages reuses same exact dev artifact
-> tag current main exact SHA as v<VERSION>
-> Release workflow reuses validated candidate artifact
```

G-FM 是发布前重型门禁，不跟随普通 dev push 自动运行；当前并发上限固定为 16。

## 10. Version 规则

只有正式 `webui/**` 产品行为变化才需要考虑产品版本 bump。纯 docs/tests/tools/workflow/Simulator 维护不应机械 bump 产品 VERSION。

正式 release tag 必须满足：

```text
tag == v<VERSION>
tag SHA == current main SHA == validated release candidate SHA
```

Release workflow 不得重新构建产品；应发布已验证 candidate 中的：

```text
WeiG-qB-WebUI.zip
SHA256SUMS
```

## 11. 维护纪律

修改 workflow、验证 contract、release choreography 时必须同步检查依赖。删除旧 workflow 时，必须同步搜索并退役引用该 workflow 的 tests/contracts/docs。

任何 gate 变红时先按日志分类根因，不降低断言、不放宽 exact-SHA identity、不用重跑掩盖确定性 bug。

当前下一优先级顺序：

```text
1. Windows installer fail-closed / atomic config write hardening
2. candidate real-qB exact build-SHA exposure failure
3. Pages catalogReady/catalogBusy acceptance wait race
4. same final SHA rerun candidate + Pages + required real-qB acceptance
5. only when preparing release: G-FM 65/65 -> promotion -> tag/release
```
