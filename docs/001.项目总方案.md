# 001. WeiG qB WebUI 项目总方案

Status: **v0.2 integrated baseline**  
Primary repository: `weigefenxiang/WeiG-qB-WebUI`  
Compatibility floor: **qBittorrent 4.1.9**  
Current product version: **0.2.0**

## 0. 启动原则

新 AI、新开发者或新会话不得根据聊天记忆猜项目。修改前至少：

```text
1. 读取本文件
2. UI/视觉任务读取根目录 DESIGN.md
3. 查看 README.md 与 VERSION
4. 查看真实 Git main / HEAD / CI
5. 搜索已有 Component / API Client / Capability / VirtualList / Installer
6. 明确 4.1.9、5.x、Desktop、Mobile、性能和回归影响
7. Reuse → Extend → New
8. 修改后跑 CI，并同步必要文档
```

## 1. 产品定位

WeiG qB WebUI 是 qBittorrent Alternate WebUI，目标不是单纯换皮，而是同时做到：

- Nebula Noir 高品质视觉；
- 原生 qB 核心管理能力完整；
- 参考 VueTorrent 的功能覆盖和移动端思路，但保持独立视觉/架构；
- qB 4.1.9 强兼容，并延伸到当前 5.x；
- capability-based progressive enhancement；
- 20 / 50 / 100 / 200 服务端分页；
- 数千 Torrent 时 DOM 保持窗口化；
- Desktop 可配置 DataGrid；
- Mobile 独立 Torrent Card + Action Sheet；
- Tracker/Announce 隐私脱敏；
- GitHub 一键部署、备份和回滚；
- 永不出现无返回路径的死路。

## 2. 兼容等级

```text
Tier 0 / Release blocking
qBittorrent 4.1.9.x

Tier 1
4.2.x
4.3.x
4.4.x
4.5.x
4.6.x

Tier 1 / modern API family
5.0.x
5.1.x
5.2.x

Future
newer 5.x / 6.x → Capability Compatibility Mode
```

### 实机实验室

```text
Lab A
qBittorrent 4.1.9.1
WebAPI 2.2.1
container: qbittorrent
host config root: /root/qbittorrent/config

Lab B
qBittorrent 5.2.0
container: q3
host config root: /root/qbittorrent3/config
```

Lab A 和 Lab B 均是 v0.2 稳定化阶段的 Release Blocking 环境。静态 CI 成功不能替代真实 API 回归。

## 3. 核心硬规则

### COMPAT-001

4.1.9 核心回归失败，禁止发布。

### COMPAT-002

Feature/UI 不直接判断 qB 主版本。版本/WebAPI 信息只用于生成 Capability Profile。

### UI-001

所有视觉修改以根目录 `DESIGN.md` 为唯一权威。

### TYPO-001

所有文字必须属于 Semantic Typography Role；Feature 不得自行硬编码字号。

### TYPO-002

整体阅读尺寸不足时通过全局 `--font-scale-offset` 一次增加 2–3px，不逐组件打补丁。

### TYPO-003

字体大小与 UI Density 独立。

### NAV-001 — No Dead-End Navigation

非首页必须有 Back。错误状态保留 Back/Home/Reload。

### MOBILE-001

Mobile 是一等平台，不是 Desktop Table 缩小版。

### PERF-001

Torrent API 默认 `PAGE_SIZE=50`，允许 20 / 50 / 100 / 200。

### PERF-002

```text
Data Count != DOM Count
```

200 个当前页模型不代表 200 个挂载 DOM Row。

### PERF-003

Torrent、Files、Peers、Trackers、Logs、Search Results 等大型列表使用 Virtual Window + overscan。

### PRIVACY-001

任何 Tracker/Announce URL 在显示、筛选 key、UI 本地状态中必须先脱敏 query/fragment credential/passkey/token。

### DEPLOY-001

自动安装/升级必须可备份、可验证、可回滚；多 qB Docker 实例不得猜目标。

## 4. 总体数据流

```text
qB WebAPI
↓
API Client
↓
Version/WebAPI Detection
↓
Capability Profile
↓
Normalizer / Stable Data Model
↓
Application State
↓
Feature Controller
↓
Canonical Components
↓
Desktop / Mobile Composition
```

UI 不散落调用 `/api/v2/*`。

## 5. Compatibility Layer

启动探测：

```text
GET app/version
GET app/webapiVersion
+ known historical exceptions
+ endpoint fallback when needed
```

关键桥接：

```text
4.x pause/resume   ↔ 5.x stop/start
4.x paused filter  ↔ 5.x stopped filter
```

Capability 示例：

```text
rss
search
trackerEdit
tags
peerBan
renameFile
renameFolder
fileIndexes
privateFlag
logs
webseeds
preferences
```

业务只查询 Capability，不关心 endpoint 名字如何演进。

## 6. Torrent 分页与 LargeList

用户可以选择：

```text
20
50 (default)
100
200
```

普通列表优先直接使用 qB：

```text
filter
category
tag
sort
reverse
limit
offset
```

需要全库名称/Tracker/SavePath/PT 过滤时，在后台以有限批次建立 Data Catalog；Catalog 仅保存数据，不映射为隐藏 DOM。

VirtualList 只渲染 viewport + overscan。

目标：

```text
Desktop Torrent DOM 通常约 20–60，prefer <100
Mobile Torrent DOM prefer <=50
```

主界面只显示产品信息，例如总数/页码；DOM/Cache/Render 等进入 Settings → Performance。

## 7. Desktop DataGrid

DataGrid v2 提供：

```text
column resize
sort
show/hide
order
persisted widths/order
reset defaults
```

默认列：

```text
名称
大小
进度
下载
上传
ETA
状态
```

扩展列使用统一 schema，例如 Ratio、Tracker、Category 等。Capability 不支持的业务字段不得假装可用。

## 8. Mobile

Mobile 使用：

```text
Top Bar
Drawer
Torrent Cards
More / Action Sheet
Detail Tabs
Touch controls
```

不在手机上提供微小桌面列宽拖动手柄。

默认 Card 信息：

```text
Name
Status
Progress
Download
Upload
ETA
Size
```

显示字段由 WeiG Settings 配置。

## 9. Tracker / PT / Private

Normalizer：

```text
https://tracker.m-team.cc/announce?credential=SECRET
→
https://tracker.m-team.cc/announce
```

显示和筛选不得泄漏 query secret。

新版 qB 如提供 private flag，使用 API 精确 Private 筛选；旧版无此能力时使用用户配置 PT Tracker domain rule，并明确这是 heuristic。

## 10. Torrent 功能

v0.2 统一 Client 已覆盖或为 Capability 准备：

```text
Add Torrent / Magnet / URL
Start / Resume
Pause / Stop
Force Start
Delete / Delete data
Recheck
Reannounce
Rename
Set Location
Category
Tags
Sequential Download
First/Last Piece Priority
Auto Torrent Management
Queue Top / Bottom
Per-Torrent DL/UL Limit
```

UI 入口通过 Toolbar + More/Action Sheet 暴露，手机版同样可达。

## 11. Torrent Detail

```text
Overview
Files
Trackers
Peers
HTTP Sources
```

大量子列表继续窗口化。

Files 支持 qB 当前版本提供的文件优先级；Trackers 使用隐私安全显示并按 Capability 开放添加/编辑/删除；Peers 在支持时提供 Ban；HTTP Sources 兼容失败时安全降级为空状态。

## 12. Settings Center

### WeiG WebUI

```text
Theme: System / Dark / Light
Font: Standard / Large(+2px) / XLarge(+3px)
Density: Compact / Standard / Comfortable
Starfield: Off / Subtle / Full
Motion
Page Size: 20 / 50 / 100 / 200
Refresh: 1 / 2 / 5 / 10s
Column layout
PT Tracker rules
Performance diagnostics
```

### qBittorrent Preferences

按当前 `/app/preferences` 真正返回的 key 显示，而不是维护一份只适用于最新 qB 的静态表。

主分组：

```text
Downloads
Connection
Speed
BitTorrent
Web UI
Advanced
```

Advanced 提供其余当前版本返回的 Preferences，并可搜索。

## 13. Search / RSS / Logs

入口 capability-aware：

- Search API 不支持 → 入口隐藏；
- RSS 不支持 → 入口隐藏；
- Logs 不支持 → 入口隐藏。

支持时使用 qB 原生 API，并保持 LargeList / Error / Back 规则。

## 14. Refresh Engine

```text
Visible Torrent dynamic data: default ~2s
Transfer info: ~2s
Static Preferences: on demand
Detail lists: view/tab active时
Background browser tab: ~10s or lower priority
```

失败不得毫秒级死循环。

## 15. Typography / Visual

全部细节见 `DESIGN.md`。必须遵守：

```text
Semantic Typography
Global Font Offset
Independent Density
Nebula Noir
CSS Starfield
Surface hierarchy
Nebula Flow
Reduced Motion
Tracker privacy
Mobile cards
DataGrid
```

## 16. Installer

Linux installer：

```text
--list-containers
--container=NAME
--config-root=/host/config/root
--dir=/container/or/local/path
--configure
--rollback
```

多 qB Docker 实例必须显式选目标。识别 `/config` bind mount 后，在宿主机真实目录部署，而向 qB 输出容器可见的 Root Folder。

必须排除 Docker overlay/rootfs 模板配置。

安装成功前验证：

```text
public/login.html
private/index.html
```

Windows 保持对应的安装/备份/回滚能力。

## 17. 测试门槛

Static CI：

```text
v0.2 UI invariants
JavaScript syntax
Compatibility tokens
Typography/VirtualList/DataGrid tokens
No external runtime assets
Installer safety
Linux shell syntax
Windows PowerShell syntax
```

Real regression：

```text
Lab A 4.1.9.1
Lab B 5.2.0
```

重点：Login、分页、筛选、Tracker 脱敏、添加、开始/停止、删除、More Actions、Detail、Files、Trackers、Peers、Settings、Mobile、Back、UTF-8、大库滚动。

## 18. Release Gate

```text
DESIGN PASS
TYPOGRAPHY PASS
4.1.9.1 PASS
5.2.0 PASS
PAGINATION PASS
VIRTUAL DOM PASS
TRACKER PRIVACY PASS
MOBILE PASS
TORRENT ACTIONS PASS
DETAIL PASS
SETTINGS PASS
NAVIGATION PASS
UTF-8 PASS
INSTALL / ROLLBACK PASS
CI PASS
```

## 19. v0.2 一语概括

> 完整 qB 管理底座 + 4.1.9→5.x Capability 兼容 + 20/50/100/200 服务端分页 + Virtual DOM + 可配置 DataGrid + Tracker/PT 隐私筛选 + Settings Center + Mobile 独立交互 + Semantic Typography + Nebula Noir v2 + 4.1.9.1/5.2.0 双实机回归。
