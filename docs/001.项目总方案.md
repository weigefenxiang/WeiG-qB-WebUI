# 001. 项目总方案

Status: **current product plan / AI handoff entry**

WeiG qB WebUI 是 qBittorrent Alternative WebUI。**`webui/**` 是唯一正式产品；正式 support floor 为 qBittorrent 4.1.0。** Source catalog、WebAPI evolution ledger、Virtual qB、Endpoint/Transport Contract 都是辅助发现与验证工具，不是产品本身。

Authority：

```text
docs/001 + docs/010     product goal / support scope / phase priority
docs/002                current compatibility facts
DESIGN.md               Current Owner / UI / design system
docs/003                module architecture
docs/006                release / CI / validation tiers
docs/009                Virtual qB auxiliary tooling only
exact-SHA runtime + upstream/source/real-qB evidence = final behavioral truth
```

## 1. 长期目标

同一份 `webui/**` 应覆盖：

```text
qB 4.1.0
-> all verified 4.x official stable
-> all verified 5.x official stable
-> current latest official stable
-> future stable / qB6.x / qB7.x / future x.x.x
```

兼容解析优先级：

```text
NATIVE
-> NORMALIZED / ADAPTER
-> reliable old-API composition
-> reliable DERIVED
-> READ_ONLY when applicable
-> UNAVAILABLE
```

future `UNKNOWN` 只关闭无法证明的具体 capability，尤其危险写操作；不能因为 major/version 变大就整体拒绝，也不能因为版本更高就自动继承 current latest semantics。

禁止：

```text
fake data
N items -> N compatibility requests
high-cost compatibility polling
presentation self-probe
second business state / duplicate owner
runtime bridge / monkey patch repair
manual compatibility mode
```

## 2. 当前正式范围

```text
branch: dev
Phase F final validated candidate:
7ae5493ab409af81abe25e579245160c26bfc7e3
VERSION: 0.3.60
main: untouched
```

Frozen historical facts：

```text
qB 4.1.0 -> 5.2.3
65 official stable profiles
50 qB4 + 15 qB5
file: tests/fixtures/qb-release-catalog.lkg.json
SHA-256:
8b91742ac8eee276b7994c84445c681913430f220325a10afc8c931c89645ddb
```

Frozen catalog 必须保持**一个完整文件**，禁止分片/重写 verified history。

qB4.0.x / WebAPI v1：

```text
FUTURE OPTIONAL
not implemented
not in current CI/full-stable matrix
```

未来如明确批准，只允许集中 `Legacy WebAPI v1 Adapter -> canonical WeiG semantics`，禁止把 v1/v2 判断散落到 UI caller。

## 3. 阶段状态

```text
Phase A — Source Facts Expansion                                DONE
Phase B — qB4.1+ Product Compatibility Core                     DONE
Phase C — Full Stable Product Compatibility Matrix             DONE
Phase D — Incremental Future Stable Admission / Last Known Good DONE
Phase E — Torrent Fields / Columns provenance                   DONE
Phase F — Settings + Tool/Detail Surface Provenance             DONE
Phase G — Real-qB / Release Hardening                           PLANNED / STOP
Legacy WebAPI v1 Adapter                                        FUTURE OPTIONAL
main promotion                                                  NOT STARTED
```

Phase F 已完成 canonical docs closure；**在新的用户 checkpoint / 明确批准之前 STOP，不自动开始 Phase G。**

## 4. 已完成阶段关键 checkpoint

```text
Phase A implementation:
c3d184d1f39164467d9fde2cfabdfa22a315fae5
VERSION 0.3.57

Phase B validated candidate:
84b86956924d79b1086bb10a915e45599f9d6685
VERSION 0.3.58

Phase C validated candidate:
b14926b8d67b5919aa2c281762ed97c5cee46c21
VERSION 0.3.58

Phase D validated implementation:
8d2f9d162af9357328879206d71ab82714916ccc
VERSION 0.3.58

Phase E docs-start / implementation base:
97170de50dfd79c953cd93f64f6b5807147436eb

Phase E final validated implementation:
6443cc35f570e8f1ed7a7e078aa15efa1618ce25
VERSION 0.3.59

Phase E docs closure / Phase F reviewed base:
ca9ffaf221f3a18c4b9c9dbd7f5281e89009d6b9

Phase F final validated candidate:
7ae5493ab409af81abe25e579245160c26bfc7e3
VERSION 0.3.60
CI run 34125875685
Virtual qB Pages Source run 34125875723
```

Phase F final candidate evidence：

```text
smoke PASS
Virtual qB Pages Source PASS
Generate exact stable source catalog PASS
Audit every official stable >= qB 4.1.0 PASS
Full Stable PRODUCT Compatibility Matrix PASS
Linux Hosted Chrome browser candidate PASS
Windows Hosted Chrome browser candidate PASS
exact-SHA reusable release candidate package PASS
```

## 5. Phase F — Settings + Tool/Detail Surface Provenance — DONE

### 5.1 目标

已覆盖：

```text
Settings
Search
RSS
Logs
Torrent Details
Files
Peers
Trackers
WebSeeds
Categories / Tags
Metadata / Torrent Creator
```

目标链：

```text
exact ReleaseProfile source facts
-> existing canonical owner / CapabilityRegistry
-> read / write / action / parameter / response-field provenance
-> QBClient defense-in-depth fail closed
-> current UI owner
```

### 5.2 非目标与实施偏差记录

```text
不重写 Settings UI/SettingsSchema
不大改 UI/视觉设计
不新增手动 compatibility mode
不实现 qB4.0.x/WebAPI v1
不新增 N->N compatibility requests
不新增第二 capability owner
不新增 duplicate polling / second state
不提前做 Phase G real-qB release hardening
```

Phase F 开始时预计现有 parser facts 足够；实施 Torrent Creator 时发现 `KEY_SOURCE_PATH / KEY_TASK_ID` 等字符串常量无法被旧 action-parameter parser 证明，因此按 source-truth-first 原则补了一处受限 parser hardening，而不是在 runtime 猜参数。

Phase F 第一次正式 `webui/**` 改动已完成 patch bump：

```text
0.3.59 -> 0.3.60
```

并同步：

```text
VERSION
webui/VERSION
package.json.version
package-lock.json root/package version
```

## 6. Phase F 关键 source truth

### 6.1 Settings

```text
W.SettingsSchema = Preference semantic owner
W.ReleaseProfile = exact getter/setter/type descriptors
W.QBClient = app/preferences + app/setPreferences transport
settings.js = presentation caller
```

写入需要两层证明：

```text
surface endpoint writable
AND
per-Preference setter/type writable
```

### 6.2 RSS 历史修正

qB **4.1.0** 已有 core RSS actions，因此此前按 `WebAPI >= 2.1.0` 才启用 RSS 的 expectation 会错误隐藏 qB 4.1.0–4.1.2。

Phase F 已修产品与 Virtual qB/Pages stale expectation，对齐 upstream。

### 6.3 Search / Logs

```text
Search core actions: source-proven from qB 4.1.4 onward
Logs main/peers: source-proven from qB 4.1.0 onward
```

Search/RSS/Logs route capability 已逐 surface/action 使用 exact action truth，不再以 qB/WebAPI version heuristic 作为最终 capability truth。

### 6.4 Torrent Details / writes

```text
properties                 65 / 65
files                      65 / 65
filePrio                   65 / 65
trackers                   65 / 65
addTrackers                65 / 65
webseeds                   65 / 65
torrentPeers               65 / 65
removeTrackers             60 / 65, from qB 4.1.5
editTracker                60 / 65, from qB 4.1.5
banPeers                   54 / 65, from qB 4.2.0
```

`unsupported/error -> fake empty` 已退休；UNAVAILABLE/UNKNOWN、真实空、HTTP/runtime error 三态分离。

### 6.5 Action parameter evolution

`editTracker`：

```text
qB 4.1.5 -> 5.1.x:
required hash + origUrl + newUrl

qB 5.2.x:
required hash + url
optional newUrl + tier
```

QBClient 已由 exact `actionDescriptor()/apiActionParameters` 驱动 form，future unknown parameter contract HTTP 前 fail closed。

### 6.6 Parser hardening 复盘

Phase F 实际修改：

```text
tools/qb-action-surface-parsers.mjs
commit a9df16dc988661b688171a5abbfc8f2a1a8633e1
```

只解析唯一、直接字符串常量；未知表达式/冲突常量不推断。该改动有 direct parser contract、真实 upstream compatibility audit，final candidate 又重新生成 exact stable catalog并执行 all-stable source/product audit。Frozen LKG verified history 未重写。

## 7. Phase F implementation order — COMPLETED

```text
F1 Surface provenance substrate
F2 Settings
F3 Search / RSS / Logs
F4 Torrent Details + Categories/Tags + Creator
F5 Product diff + full validation
```

没有创建 dormant 新 owner 或长期双实现；Current Owner identity 保持单一。

## 8. “65/65 compatible”的正式产品解释

**同一份 `weig-qb-webui/webui` 可以面向已验证 qB 4.1.0 -> 5.2.3 official stable 自动适配；但这不等于所有版本拥有全部后期功能。**

```text
该版本有 capability     -> 正常开放
该版本没有 capability   -> 正确隐藏/禁用/降级
future/unknown 未证明    -> UNKNOWN
危险 write UNKNOWN      -> HTTP 前 fail closed
```

代表性边界：

```text
Settings preferences/setPreferences endpoint    qB 4.1.0+
RSS core                                        qB 4.1.0+
Logs main/peers                                 qB 4.1.0+
Properties/Files/Trackers/Peers/WebSeeds        qB 4.1.0+
filePrio/addTrackers                            qB 4.1.0+
Search core                                     qB 4.1.4+
remove/edit Tracker                             qB 4.1.5+
Peer Ban                                        qB 4.2.0+
Tags capability                                 qB 4.2.0+ source truth
Torrent Creator                                 qB 5.0.0+
```

Settings endpoint 可用也不代表每个 Preference 都 writable；仍逐 Preference 按 setter/type provenance 决定。

Phase F 已证明 source/profile/product/browser 这一层。**“放到任何设备都能用”的真实设备/真实 qB binary/server 端到端证据属于 Phase G。**

## 9. Phase F Definition of Done — SATISFIED

```text
1. touched user-visible surfaces 不再靠 version-only guess 做 final capability truth
2. read/write/action/parameter/response-field provenance 分离
3. dangerous/unknown writes HTTP 前 fail closed
4. Settings 保留 getter/setter/type discipline
5. qB 4.1.0 RSS truth 修正
6. unsupported/error 不伪装为空数据
7. current 65 frozen stable 全部通过 formal product owners
8. future-major synthetic UNKNOWN/no-HTTP PASS
9. 无 N->N compatibility request / extra high-cost polling / second state
10. Linux + Windows browser PASS
11. exact candidate catalog + all-stable source audit + Full Product Matrix + package PASS
12. canonical docs closure + STOP before Phase G
```

Validated candidate：`7ae5493ab409af81abe25e579245160c26bfc7e3`。

## 10. Phase G — Real-qB / Release Hardening — PLANNED

Phase G 不重复 Phase F，而是补上真实 qB / 真实部署证据。

至少覆盖：

```text
G1 representative real-qB matrix
   - qB 4.1.0 minimum
   - modern qB4 representative
   - qB5 representative
   - latest official stable resolved at start

G2 real semantic flows
   - auth/session
   - Settings read/save/reread
   - Search/RSS/Logs where supported
   - Details reads/writes where supported
   - Categories/Tags
   - Torrent add/core writes
   - Torrent Creator on qB5 where supported

G3 deployment/operator hardening
   - Alternative WebUI path/config
   - Linux/container/VPS
   - Windows
   - cache/auth/cookie/proxy/HTTPS/base-path where applicable
   - upgrade/rollback/backup/recovery

G4 promotion-time frozen evidence
   - exact dev SHA
   - real-qB evidence manifest
   - frozen/source/browser regressions after fixes
   - release candidate identity
   - known risks/operator notes
```

Phase G PASS 后也必须 STOP；不能自动 promotion/tag/release。

## 11. Phase G 之后

当前 roadmap 没有自动 Phase H。后续均为新的用户 checkpoint：

```text
dev -> main promotion        explicit approval
optional tag/release         explicit approval
future stable admission      Phase D incremental contract
future qB6/qB7               exact source admission + granular UNKNOWN
Legacy WebAPI v1 Adapter     FUTURE OPTIONAL
Simulator fidelity           P3
```

## 12. Git / proof discipline

```text
fresh-read dev exact HEAD
-> candidate only parent = fresh HEAD
-> exact compare expected paths
-> validate candidate
-> fresh-read dev immediately before ref update
-> if moved: candidate invalid, rebuild
-> force:false safe fast-forward only
```

Repo-wide ownership/absence/uniqueness 只认 target exact SHA + complete tree/blob/file。Search/snippet 只能 discovery；working-tree CRLF-transformed bytes 不能证明 Git blob identity。

## 13. Main boundary

未经用户明确授权：

```text
no main write
no promotion
no tag
no release
no force push
```

## 14. 下一位 AI 阅读顺序

```text
001 项目总方案
-> 010 真实 qB 产品兼容路线
-> 002 兼容与实现状态
-> DESIGN.md
-> 003 项目架构
-> 006 发布与晋级流程
-> 009 Virtual qB Lab
```

接手后第一件事必须 fresh-read GitHub `dev` exact SHA；聊天记录、旧 SHA、旧 CI 不能替代 current repository truth。Phase F 已结束；除非用户明确批准新的 checkpoint，**STOP，不自动开始 Phase G。**
