# Frozen source evidence has a byte-level SHA-256 identity and must checkout identically on every OS.
tests/fixtures/qb-release-catalog.lkg.json text eol=lf
# qb-settings-native @@PROFILE keeps a semantic final empty TSV field; preserve its trailing tab.
webui/private/data/qb-settings-native.txt -whitespace
